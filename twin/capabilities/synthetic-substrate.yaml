# The checklist measures the CODE's realisation of a resolved decision, not the decision itself.
capability: synthetic-substrate
owning_ticket: '12'
criteria:
  - index: 1
    text: "The real/synthetic seam defined, with a consistency rule between spine and substrate."
    checked: true
    evidence: "twin/spine.py — the spine is the org's own real, dated `signal` documents (Spine.from_overlay reads twin/model.py's Overlay.signals directly, no second authored format), and the consistency rule is enforced rather than declared: reconcile() refuses, naming what is missing, if the substrate does not carry every spine fact knowable by a checkpoint (anchor()), verbatim; reconcile_at_every_checkpoint() runs that check once per distinct spine date. diff_against_spine() demonstrates the free-running half — everything not anchored — stays wide on the real Carillion fixture, so a planted signal sits among dozens of non-plant decoys rather than standing alone as the diff's sole residual (the failure decision ticket 12 Q3 names as 'actively dangerous'), while an over-anchored negative control does expose the plant as the sole residual, proving the guard measures a real property."
    ticked_by: build ticket 50
  - index: 2
    text: "A fidelity target + a stated list of what would make the substrate an unfair test."
    checked: true
    evidence: "twin/substrate_eval.py — five declared, targeted dimensions (signal_to_noise, plant_difficulty, spine_consistency, reporting_asymmetry, mundanity), each a real value computed from a batch's own content, not a manual eyeball: evaluate_fidelity() and TARGETS give every dimension a target band, and UNFAIR_TEST_CONDITIONS states what would make the substrate an unfair test, matching decision ticket 12's own list — over-anchored noise (caught by build ticket 50's diff_against_spine), silent drift, a trivially findable plant, a falsely balanced positive/negative split, and volume too thin to bury a plant — each demonstrated failing its own dimension on a real, deliberately-constructed batch (tests/test_substrate_eval.py), not merely asserted plausible. tune() is the supported tuning loop AC asks for: a balanced 50/50 template mix genuinely misses the reporting-asymmetry target, and the loop converges over more than one iteration to a batch that clears every band, negativity bias included (decision ticket 12 Q3c, spec story 60 — reporting asymmetry as measured and negativity bias as produced are the same property)."
    ticked_by: build ticket 51
  - index: 3
    text: "The planting protocol (strength, lead time, burial, distribution of difficulty)."
    checked: true
    evidence: "twin/planter.py's Plant.strength (a declared unit-interval float, read from twin/plant-horizons.yaml beside the horizon and reason it already carries, enforced with the identical 'every plant must carry one' discipline — plant() refuses a recipe whose planted signals are not every one covered by a strength, tests/test_planter.py::test_plant_refuses_a_planted_signal_with_no_declared_strength) closes the one field this criterion had no home for; lead time (actionability_horizon) and burial (plant_difficulty) were already ticked by build tickets 73 and 51. Distribution of difficulty is twin/substrate_eval.py's new plant_difficulty_spread(): the max-minus-min across a batch's own per-plant difficulty scores, added to TARGETS/evaluate_fidelity/UNFAIR_TEST_CONDITIONS beside the existing mean-based plant_difficulty rather than replacing it — a batch where every plant sits at the identical difficulty now genuinely fails a real, constructed negative control (tests/test_substrate_eval.py::test_a_uniform_difficulty_batch_fails_plant_difficulty_spread, using UNCAMOUFLAGED_PLANTED_SIGNALS, spread == 0.0) while the camouflaged PLANTED_SIGNALS batch and the real committed Netflix substrate both show a genuine spread (0.333 and 0.6 respectively, test_the_tuned_batchs_plants_show_a_genuine_difficulty_spread, tests/test_netflix.py::test_every_fidelity_dimension_lands_inside_its_band)."
    ticked_by: build ticket 87
  - index: 4
    text: "A blind/adversarial separation mechanism between planter and detector."
    checked: true
    evidence: "twin/planter.py, twin/detector.py, twin/scorer.py — a real, enforced adversarial split, not a promise: the planter (twin/planter.py) is the only module that ever reads substrate_generator.generate()'s plants field, and hands the detector only PlantedWorld.public (the batch with plants stripped); the detector (twin/detector.py) imports nothing naming planter (checked by AST scan of its real source, tests/test_detector.py::test_detector_module_imports_nothing_naming_planter) and is behaviourally blind, not merely unwired — detect() returns byte-identical output whether or not a decoy plants key is spliced into its input (test_detect_is_indifferent_to_a_spliced_in_ground_truth_key), so ground truth cannot leak back in even if a caller tried; the scorer (twin/scorer.py) reads both as two independent arguments (ground_truth, detections — never a merged object). The limit decision ticket 12 Q2 names is recorded, not papered over: every ScoreResult carries SHARED_PRIOR_LIMITATION verbatim (planter and detector share model priors; a synthetic result is never evidence the twin anticipates the world). Every plant also carries an actionability horizon (Plant.actionability_horizon): a detection on or before it scores TIMELY_SCORE, one after scores LATE_SCORE — near-zero, not zero — with the horizon named in the reason string, and a missed plant scores MISSED_SCORE (tests/test_scorer.py; harness guard planter_detector_scorer_are_structurally_separated_and_late_detection_scores_near_zero)."
    ticked_by: build ticket 52
  - index: 5
    text: "Generation method + reproducibility/versioning decision."
    checked: true
    evidence: "twin/substrate.py — a versioned recipe format (twin.substrate-recipe/v1: prompts, seed, model version, planted-signal schedule) plus a seed regenerates a toy substrate byte-for-byte; the content-hash reference form (twin/blob.py, build ticket 01) is exercised for real against non-empty generated bytes; and the authored-or-derived spike is answered and recorded — regenerated substrate is AUTHORED, because a live generator cannot promise identical_pins_identical_bytes, demonstrated by a stand-in generator that genuinely does not reproduce from an identical recipe."
    ticked_by: build ticket 48
  - index: 6
    text: "Anti-contamination measures."
    checked: true
    evidence: "twin/substrate_eval.py's KNOWN_REAL_ENTITIES (the org roster already committed across the backtest/flagship fixtures — Carillion, Enron, Wirecard, NMC Health, Kodak, Netflix, Intel, Maersk, AstraZeneca, Sanofi, Royal Mail — plus three real, publicly-named people tied to those same events) is a small, known-real blocklist, checked by contamination_hits()/contamination() as a sixth declared dimension in TARGETS/evaluate_fidelity — target band (0.0, 0.0), zero tolerance. Distinct from and not confused with twin/scoring.py's Enron-as-control discount (that mechanism prices memorisation on the real-history backtest suite; this one scans this module's own synthetic batches, confirmed by grep — no other contamination check touches twin/substrate.py/substrate_eval.py output). The scan runs on free-running content only, skipping lines identical to spine.anchor()'s own inserted facts (which legitimately name the real subject, e.g. Carillion, by design), proven directly: tests/test_substrate_eval.py::test_contamination_ignores_the_anchored_spine_facts constructs the real Carillion batch, confirms an anchored fact literally naming 'Carillion' is present, and asserts contamination stays at 0.0 despite 'Carillion' being on the blocklist. The real committed Netflix substrate scores contamination == 0.0 (tests/test_netflix.py::test_every_fidelity_dimension_lands_inside_its_band)."
    ticked_by: build ticket 87
  - index: 7
    text: "Ethics/non-identification check."
    checked: true
    evidence: "twin/substrate_eval.py's refuse_if_contaminated() — sharing contamination_hits()'s own blocklist machinery with AC 6, per this ticket's own instruction — is the harder gate: not a target band a batch can still be reported as failing while shipping anyway, but a raised ContaminationError, wired into twin/substrate_report.report() before a substrate batch's report is committed as an artefact. Fires on a planted real-name collision, not merely runs clean: tests/test_substrate_eval.py::test_refuse_if_contaminated_fires_on_a_planted_real_name_collision plants 'Markus Braun' (real, publicly identifiable — Wirecard's former CEO) into a constructed batch's free-running content and asserts ContaminationError is raised naming that entity; test_refuse_if_contaminated_is_silent_on_clean_content proves the same call does not raise on genuine content. No prior code checked generated substrate content against real, identifiable people or organisations at all (confirmed: only a prose line in decision ticket 12 named the requirement before this ticket)."
    ticked_by: build ticket 87

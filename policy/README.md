# policy

Moved to its own repo (PRD §5.1 — "each top-level directory is its own repo in the
reference org"): **[github.com/policy-as-versioned-flux/policy](https://github.com/policy-as-versioned-flux/policy)**.

History up to the split is preserved there (`git subtree split --prefix=policy`).
This directory is a pointer only — see the linked repo for the actual source,
tags, and CI.

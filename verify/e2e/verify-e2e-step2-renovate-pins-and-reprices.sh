#!/usr/bin/env bash
# NORTH-STAR §4 step 2: renovate pins and re-prices. Reads SKIP until ticket 25 lands (ticket 52, the harness).
source "$(dirname "${BASH_SOURCE[0]}")/lib.sh"
step 2 "renovate pins and re-prices"
skip "step 2 not built yet, owned by ticket 25"

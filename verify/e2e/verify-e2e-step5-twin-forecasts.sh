#!/usr/bin/env bash
# NORTH-STAR §4 step 5: twin forecasts. Reads SKIP until ticket 49 lands (ticket 52, the harness).
source "$(dirname "${BASH_SOURCE[0]}")/lib.sh"
step 5 "twin forecasts"
skip "step 5 not built yet, owned by ticket 49"

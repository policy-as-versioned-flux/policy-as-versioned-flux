#!/usr/bin/env bash
# NORTH-STAR §4 step 3: price crosses band and PR opens. Reads SKIP until ticket 26 lands (ticket 52, the harness).
source "$(dirname "${BASH_SOURCE[0]}")/lib.sh"
step 3 "price crosses band and PR opens"
skip "step 3 not built yet, owned by ticket 26"

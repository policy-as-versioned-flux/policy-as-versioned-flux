#!/usr/bin/env bash
# NORTH-STAR §4 step 4: flux reconciles cage. Reads SKIP until ticket 40 lands (ticket 52, the harness).
source "$(dirname "${BASH_SOURCE[0]}")/lib.sh"
step 4 "flux reconciles cage"
skip "step 4 not built yet, owned by ticket 40"

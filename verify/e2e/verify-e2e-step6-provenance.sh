#!/usr/bin/env bash
# NORTH-STAR §4 step 6: provenance. Reads SKIP until ticket 32 lands (ticket 52, the harness).
source "$(dirname "${BASH_SOURCE[0]}")/lib.sh"
step 6 "provenance"
skip "step 6 not built yet, owned by ticket 32"

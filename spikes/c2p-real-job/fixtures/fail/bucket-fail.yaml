# Verbatim from pavf-policy/tests/require-s3-bucket-encryption/bucket-fail.yaml
# rule: [] -> no encryption algorithm set -> require-s3-bucket-encryption FAILS -> sc-28 not-satisfied
apiVersion: s3.aws.m.upbound.io/v1beta1
kind: BucketServerSideEncryptionConfiguration
metadata:
  name: bucket-fail
  namespace: default
  labels:
    mycompany.com/policy-version: "2.2.0"
spec:
  forProvider:
    region: us-west-1
    bucketSelector:
      matchLabels:
        testing.upbound.io/example-name: mybucket
    rule: []

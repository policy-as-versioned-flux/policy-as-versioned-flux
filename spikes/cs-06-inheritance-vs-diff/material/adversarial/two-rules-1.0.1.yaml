# PROTOTYPE fixture — spike cs-06, adversarial case 2 (the NEW side). Same two
# rules as two-rules-1.0.0.yaml, swapped. The true delta is empty.
apiVersion: policies.kyverno.io/v1alpha1
kind: ValidatingPolicy
metadata:
  name: two-rules-1-0-1
  labels:
    policy-as-versioned.dev/policy: two-rules
    policy-as-versioned.dev/policy-version: "1.0.1"
spec:
  validationActions: [Audit]
  matchConstraints:
    resourceRules:
      - apiGroups: [""]
        apiVersions: ["v1"]
        operations: ["CREATE", "UPDATE"]
        resources: ["pods"]
  matchConditions:
    - name: only-this-policy-version
      expression: >-
        object.metadata.?labels['policy-as-versioned.dev/policy-version'].orValue('') == '1.0.1'
  validations:
    - expression: >-
        has(object.metadata.labels) && 'owner' in object.metadata.labels
      message: "an owner label is required"
    - expression: >-
        object.spec.?securityContext.?runAsNonRoot.orValue(false) == true
      message: "runAsNonRoot must be true"

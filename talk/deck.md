---
marp: true
title: "The eco-system, read from its own truth surface"
description: "Seven steps, each carrying the grade its own check gave, in the check's own words."
theme: default
paginate: true
footer: "generated by talk/build_deck.py · run=235 · hub=6bfb7dd · 2026-09-10T16:46Z"
style: |
  section { font-size: 24px; }
  section h1 { font-size: 44px; }
  section h2 { font-size: 32px; }
  pre { font-size: 13px; line-height: 1.35; }
  code { font-size: 13px; }
  img { max-height: 62vh; }
---

<!-- GENERATED FILE. Do not hand edit: talk/build_deck.py overwrites it from
     the named run's captures and talk/narration.json, and talk/verify-demo.sh
     grades it against that run, not the file you edited. Prose: talk/narration.json. -->
<!-- deck run=235 hub=6bfb7dd source=recorded -->

# The eco-system, read from its own truth surface

### Seven steps, each carrying the grade its own check gave, in the check's own words.

built 2026-09-10T16:46Z from the captures the truth surface committed for run **235**, a scheduled run recorded 2026-09-10T10:30Z at hub `6bfb7dd`. Each beat quotes that run's capture for its own check, and carries the grade that capture gave.

the run this deck describes, as the truth surface recorded it in `talk/truth.log`:

```text
TRUTH 2026-09-10T10:30Z run=235 hub=6bfb7dd enact=development units=[driftwood=a181725@main feeds=c3c654a@main ico=abcb3a8@main insurer=61fba9d@main ludlow=2c2d740@main nist=f83126f@main platform=5b88f1d@policy/v5.0.0 tuppence=bd15aae@main] pass=80 [observed=25 self=42 simulated=4 meta=9] fail=8 skip=29 [never=10 waits=19] excluded=8 total=125 ceiling=106
```

measured: 80 passes are observed 25 + self 42 + simulated 4 + meta 9, against a ceiling of 106 of 125 (8 excluded, 11 can never pass on this runner); fail 8, skip 29 (never 10, waits 19)

<!--
Everything on the slides that follow was read out of one run of one command. Not a screenshot I took on a good day, not a diagram of how it ought to work: the actual output of the checks, quoted, with the verdict the run gave. Where a step could not be looked at, the slide says so and prints the reason the check itself gave. I would rather show you a red cell than a green one I cannot stand behind.
-->

---

## How to read this deck

Every figure on a numbered slide is read out of `talk/captures/`, one file per check, written by the truth surface on the run this deck names. Nothing is retyped, and a slide that is not a numbered step carries no figure at all — there would be no capture behind it.

Three outcomes, and only three: **observed true**, **could not look**, **observed false**. A check that could not look prints its own reason, in its own words — never a reason the deck made up on its behalf.

A step with no capture in the run renders as **no check yet, owned by ticket NN**, and only where the check has genuinely not been written. A check that exists and produced no output is a missing observation, and the deck is refused.

`talk/build_deck.py` writes this file. `talk/verify-demo.sh` runs those refusals over the **committed** file as well as over a fresh rebuild: a status that disagrees with the run, a cited capture that is missing, a headline quoted from another commit, or a figure that is not a figure in the capture behind it, and the deck does not ship.

<!--
One habit runs through the whole deck. I never type a number onto a slide. The generator reads it out of the capture the check wrote, and a second script refuses the deck if a figure appears that the capture does not contain. That is not a discipline anyone can keep by hand, which is exactly why it is a script.
-->

---

## Who publishes what, to whom

![w:900](diagrams/estate.png)

Pre-rendered from `talk/diagrams/estate.mmd`, drawn on the twenty-fifth of August. Two parties have joined the eco-system since it was drawn and are not on it: the intelligence publisher and the insurer. It is a picture of the dependency direction, not a read of the truth surface.

<!--
Regulators publish controls and penalties. A platform publishes implementations. Adopter organisations compose what they inherit into their own signed artefact. Nothing here is a tenant of anything else: each party lives in its own organisation, ships on its own cadence, and is consumed only through a pinned, signed dependency. Two more parties have joined since this was drawn, so treat it as the shape, not the census.
-->

---

<!-- aside name=continuous-refresh status=FAIL cited=yes script=verify/schedules/verify-schedules.sh capture=talk/captures/verify_schedules_verify-schedules.out wanted=talk/captures/verify_schedules_verify-schedules.out -->

## Continuous refresh: what runs on a clock, and when each last fired

**observed false** — 4 schedule/cage check(s) observed false: feeds/fetch.yml insurer/fetch.yml ludlow tuppence

```text
$ bash verify/schedules/verify-schedules.sh
PASS: driftwood/propose-tier.yml: last scheduled run 23h ago (success)
PASS: driftwood/renovate-run.yml: last scheduled run 23h ago (success)
PASS: driftwood/twin-sweep.yml: last scheduled run 22h ago (success)
FAIL: feeds/fetch.yml: last scheduled run 2h ago concluded 'failure' -- a clock whose run dies or is
  cancelled records no observation -- ticket 85 owns it (the cage read the `__pycache__` its own python
  wrote, because the pre-switch clean carried no -x; fixed here 2026-09-04, and the red stands until the
  clock next ticks), and that ticket reads 'resolved': either the fix has not reached a scheduled run
  yet, or this red is a new one wearing an old ticket's name
PASS: ico/fetch.yml: last scheduled run 2h ago (success)
FAIL: insurer/fetch.yml: last scheduled run 1h ago concluded 'failure' -- a clock whose run dies or is
  cancelled records no observation -- ticket 77 owns it (the requote job prices against `<adopter>
  exposure v1.1.0`, and no adopter's v1.1.0 tree carries an exposure section, so the job refuses; ticket
  77 item 2 cuts the releases), and that ticket reads 'resolved': either the fix has not reached a
  scheduled run yet, or this red is a new one wearing an old ticket's name
```

<!--
Continuous refresh is the part people expect to be a person with a calendar reminder. It is not. Every party in this eco-system carries its own clock, in its own repository, on its own cadence, and each one appends observations and never declarations. This slide is the register of those clocks and the last time each of them actually ran — including, at the bottom of the list, the clock that produced this deck. Note the red one. A clock whose run dies records nothing, and a clock that records nothing is indistinguishable from a clock that was never wired up, which is why it is graded rather than assumed.
-->

---

<!-- beat step=1 status=PASS cited=yes script=verify/e2e/verify-e2e-step1-regulator-publishes.sh capture=talk/captures/verify_e2e_verify-e2e-step1-regulator-publishes.out -->

## 1 · A regulator publishes a new penalty schema version

**observed true** — ico's newest penalty-schema envelope validates and its tag is on the real remote

```text
$ bash verify/e2e/verify-e2e-step1-regulator-publishes.sh
E2E step 1 regulator publishes
PASS: ico/penalty-schema/v4/feed.json: envelope valid (feed/penalty-schema 4.0.0)
PASS: ico/penalty-schema/v4/feed.json: payload vs penalty-schema/payload.schema.v4.json
PASS: ico/penalty-schema/v4/feed.json: bump.yaml bump=none
PASS: ico/penalty-schema@4.0.0: tag v4.0.0 on ico, and its tree carries the penalty-schema this pin is
  used for
```

<!--
The regulator publishes a machine-readable penalty schema, under the same feed envelope everything else in the eco-system publishes under. The envelope validates, the payload validates against its own schema, and the version bump was computed rather than declared. The signature is the honest half: a signed tag can only be cut by the release workflow on the real remote, after a human merges. Until that happens this step says it is waiting, and names the tag it is waiting for.
-->

---

<!-- beat step=2 status=PASS cited=yes script=verify/e2e/verify-e2e-step2-renovate-pins-and-reprices.sh capture=talk/captures/verify_e2e_verify-e2e-step2-renovate-pins-and-reprices.out -->

## 2 · Renovate raises the pin; the composition re-prices

**observed true** — a SYNTHETIC pin bump (penalty-schema v3 -> v4, made here on a throwaway copy of driftwood, not driftwood's own merged history) re-prices driftwood's prices[] through composition (1,787,177.08 -> 607,314.15), offline, with no repo touched; the one bump that really was merged (driftwood #20, threat-register v1 -> v2) moved the recorded pin and no money

<!--
A bot raises the pin in one adopter and a human merges it. What happens next is the point: the adopter's own composition re-prices its exposure against its own size, its own customers, its own regulators. Three adopters take the same feed and get three different answers, and for two of them the new payload prices them exactly as the old one did. The slide says so, in the check's own words, rather than pretending every bump moves money.
-->

---

## What did the re-pricing

![w:900](diagrams/compose.png)

Pre-rendered from `talk/diagrams/compose.mmd`, drawn on the twenty-fifth of August. The refusals shown are the ones composition implemented then.

<!--
One seam. The adopter's declaration goes in, its parents go in at resolved commits, and out come the rendered files, an advisory header, the prices, and a short list of refusals. The prices are recomputed every run and never applied by the machine: they are an input to a reviewed pull request.
-->

---

<!-- aside name=monte-carlo status=PASS cited=yes script=.estate-clone/platform/fair/verify-fair-tail.sh capture=talk/captures/.estate-clone_platform_fair_verify-fair-tail.out wanted=talk/captures/.estate-clone_platform_fair_verify-fair-tail.out -->

## The pound behind the re-pricing is a simulation, and it names its tail

**observed true** — fair.py names its tail, the lognormal-GPD splice prices heavier than the bounded PERT at the same shock, and a malformed severity spec refuses.

```text
$ bash .estate-clone/platform/fair/verify-fair-tail.sh
==> 1. the engine's own assertions (tail names, heavy > bounded, sums refuse)
ok  ALE=19559 VaR95=30948 TVaR=34087 carried=34958 | Deny buys 19439 (99% effective)
ok  multi-source: combined ALE=73579 (~= 19559+54139) TVaR=128447 > naive-independent TVaR=118711
ok  tail: lognormal-gpd VaR95=58953 vs bounded-pert VaR95=38910 at the same ALE (24186); malformed specs
  refuse
ok  sum_prices: one perspective and one currency sums, every mix refuses
==> 2. the CLI reports the tail, and a spliced tail out-prices a bounded one
  ok   driftwood: unversioned image ships cart  tail=bounded-pert VaR95=30948
  ok   driftwood: cart PII with the twin's heav tail=lognormal-gpd VaR95=58953
  ok   same shock, heavier tail: VaR95 58953 > 30948
```

<!--
Every price you have seen so far comes out of this. It is a seeded Monte Carlo over a loss distribution, run by the platform, and the thing worth watching is not the amount: it is that the engine names its tail. The same shock, priced with a spliced heavy tail rather than a bounded one, buys a materially larger annual loss at the ninety-fifth percentile. A model that hides which tail it used can be argued with forever. This one prints it, refuses a malformed severity spec rather than defaulting it, and refuses to sum across perspectives or currencies at all.
-->

---

<!-- beat step=3 status=PASS cited=yes script=verify/e2e/verify-e2e-step3-price-crosses-band-pr-opens.sh capture=talk/captures/verify_e2e_verify-e2e-step3-price-crosses-band-pr-opens.out -->

## 3 · The price crosses a band, the cage tier moves, a proposal opens

**observed true** — a SYNTHETIC residual placed either side of driftwood's own signed appetite band of 40,000 GBP (20,000.00 -> 58,269.23 GBP, not driftwood's real priced position) selects baseline -> restricted through driftwood's own selection-policy package 1.1.0 (which platform/graded/cage.py agrees with), and BOTH tighten-only outcomes are graded on throwaway copies (ADR-0022): against driftwood's own declaration (driftwood declares isolated and the priced line selects restricted, which does not tighten it) the proposer HOLDS, and against a SYNTHETIC copy this step rewrote to baseline it LANDS -- the landed one a --dry-run pull request declaring posture.acme.io/tier: restricted on gitops/apps/namespace.yaml, driftwood's governed Namespace, beside an untouched pod manifest; the estate does not loosen a declaration to match a price, and driftwood's real repository is byte-identical after both (python half; the tier landing in force is step 4)

```text
$ bash verify/e2e/verify-e2e-step3-price-crosses-band-pr-opens.sh
E2E step 3 price crosses band and PR opens
    driftwood band 40,000 GBP (its own party.yaml), selection policy 1.1.0
      (driftwood/selection-policy/VERSION)
    residual 20,000.00 -> tier baseline; 58,269.23 (crosses the band) -> tier restricted
    HOLD  [as declared, declared isolated]: pull_request, held=tighten-only on line ico-feed; the priced
      line selects restricted and the party folds to restricted, which does not tighten it: strictest
      priced line is 'restricted' across ['ico/feed/penalty-schema']; the Namespace declares 'isolated';
      held -- the proposer only tightens
    LAND  [SYNTHETIC declaration, declared baseline]: pull_request, landed=dry-run on
      gitops/apps/namespace.yaml; the diff declares posture.acme.io/tier: "restricted" and keeps
      policy-as-versioned.dev/governed
    both legs: no branch beyond the copy's own main, nothing written in either copy,
      gitops/apps/namespace.yaml and deploy/pod.yaml byte-identical in driftwood's real repository,
      nothing opened
```

<!--
The adopter published an appetite band. A residual crosses it, and the tier the workload runs under moves up a rung, through the adopter's own versioned selection policy. Then the machine stops. It opens a pull request against the governed namespace declaration and waits for a human. Nothing timed ever changes a verdict on its own, and the pod is not touched: the label on the pod is an output of the namespace declaration, not a thing anyone edits.
-->

---

<!-- beat step=4 status=SKIP cited=yes script=verify/e2e/verify-e2e-step4-flux-reconciles-cage.sh capture=talk/captures/verify_e2e_verify-e2e-step4-flux-reconciles-cage.out -->

## 4 · Flux reconciles the new cage spec onto the cluster

**could not look** — no cluster on this runner, and the lane sample cannot stand in: a fact could not be looked at, and a fact not looked at is never a pass

```text
$ bash verify/e2e/verify-e2e-step4-flux-reconciles-cage.sh
E2E step 4 flux reconciles the new cage spec onto the cluster
   five-fact sample 2026-09-09T11:27:02Z on cluster dsample-34345475988 (run 34345475988), 3 sources
     driftwood-composed   true  fact_1_ready_at_the_pin_on_the_real_remote: Ready at the pinned tag and
       commit, from the publisher's real remote
     driftwood-composed   ?     fact_2_tag_signature_verified_at_the_source_boundary: nothing checked a
       signature at this source boundary: the GitRepository declares no spec.verify (Flux speaks OpenPGP
       and SSH only) and carries no verdict from the identity-pinned gitsign-verifying controller
       (ticket 41). Not looked at, and a thing not looked at is never a pass
     driftwood-composed   true  fact_3_last_applied_revision_equals_the_pinned_commit: every
       Kustomization consuming driftwood-composed applied eacae33ca3a1
     driftwood-composed   true  fact_4_rendered_objects_byte_equal_to_an_offline_render: all 16 rendered
       objects are live and equal to the offline render
     driftwood-composed   FALSE fact_5_every_rendered_object_is_in_the_flux_inventory: 15 of 16 rendered
       objects are in no Flux inventory (absent from the cluster, or live but put there by something
       other than Flux)
     nist                 true  fact_1_ready_at_the_pin_on_the_real_remote: Ready at the pinned tag and
       commit, from the publisher's real remote
     nist                 true  fact_2_tag_signature_verified_at_the_source_boundary: the gitsign tag
       v1.1.0 was verified at the source boundary against nist's own pinned identity
       ^https://github\.com/policy-as-versioned-nist/nist/\.github/workflows/cut-release\.yml@refs/heads/(main|release/[0-9]+\.[0-9]+\.x)$
     nist                 true  fact_3_last_applied_revision_equals_the_pinned_commit: nist is a
       verified source only (ticket 16 Q5): no Kustomization reconciles it, so the revision in force is
       the parent sha the composed set was built from. HEADER 33a05df1f524 vs pin 33a05df1f524. ONE
       LINK, NOT A SECOND PROOF: this reads composed/HEADER.yaml, the same composed set whose own fact 3
       is the applied revision above -- the three fact-3 lines in this sample are one chain of custody,
       not three independent observations of a revision in force (ecosystem ticket 76)
     nist                 true  fact_4_rendered_objects_byte_equal_to_an_offline_render: all 16 rendered
       objects are live and equal to the offline render
     nist                 FALSE fact_5_every_rendered_object_is_in_the_flux_inventory: 15 of 16 rendered
       objects are in no Flux inventory (absent from the cluster, or live but put there by something
       other than Flux)
     platform             true  fact_1_ready_at_the_pin_on_the_real_remote: Ready at the pinned tag and
       commit, from the publisher's real remote
     platform             true  fact_2_tag_signature_verified_at_the_source_boundary: the gitsign tag
       v2.0.1 was verified at the source boundary against platform's own pinned identity
       ^https://github\.com/policy-as-versioned-platform/platform/\.github/workflows/cut-release\.yml@refs/heads/(main|release/[0-9]+\.[0-9]+\.x)$
     platform             true  fact_3_last_applied_revision_equals_the_pinned_commit: platform is a
       verified source only (ticket 16 Q5): no Kustomization reconciles it, so the revision in force is
       the parent sha the composed set was built from. HEADER 533dccb0a823 vs pin 533dccb0a823. ONE
       LINK, NOT A SECOND PROOF: this reads composed/HEADER.yaml, the same composed set whose own fact 3
       is the applied revision above -- the three fact-3 lines in this sample are one chain of custody,
       not three independent observations of a revision in force (ecosystem ticket 76)
     platform             true  fact_4_rendered_objects_byte_equal_to_an_offline_render: all 16 rendered
       objects are live and equal to the offline render
     platform             FALSE fact_5_every_rendered_object_is_in_the_flux_inventory: 15 of 16 rendered
       objects are in no Flux inventory (absent from the cluster, or live but put there by something
       other than Flux)
     fact 3 is ONE CHAIN, not 3 independent proofs: 1 applied revision(s) observed on the cluster, and
       nist, platform (2 verified-source-only publisher(s)) read back through composed/HEADER.yaml to
       that same composed set
     FALSIFIER NOT LOOKED AT: cluster_side_verification_passes_a_tag_that_identity_pinned_ci_rejects on
       platform -- platform's own release.yml is not in this checkout, so the identity it pins cannot be
       read here
     FALSIFIER NOT LOOKED AT: cluster_side_verification_passes_a_tag_that_identity_pinned_ci_rejects on
       nist -- nist's own release.yml is not in this checkout, so the identity it pins cannot be read
       here
   SKIP: a fact could not be looked at, and a fact not looked at is never a pass
```

<!--
This is the step where the price becomes a constraint on a running workload, and it is the step whose green I will not read to you off my own laptop: a number produced on the presenter's machine is a second clock, and a second clock is how a demo drifts away from the thing it claims to describe. So a pass here is filled in by the scheduled run and by nothing else. What you are reading is not that rule biting, though. It is the check's own verdict, in its own words: Flux really does reconcile driftwood at its pinned revision, and two named things are still missing before the cage can be called in force. The slide prints both, because they are the honest state of it. One more thing about this step, added on 2026-09-10, because it is the sentence this whole estate turns on. The claim is that there is no gate: a workload that does not fit its cage is not denied, it runs on a tighter rung, and the bottom rung runs and reaches nothing. On every run you can cite, that claim is proven OFFLINE ONLY -- the cage's decision logic, against the estate's own pinned engine, with no pod, no API server and no network in it. The instrument that watches a real pod be admitted and then fail to reach anything needs a cluster this runner has never had, so it has recorded could-not-look every time. I have watched it hold on a laptop and I am not allowed to quote that, and I am not going to. What changed today is that the adopters' own scheduled lane now takes two more facts on the cluster it already builds: the workload the cage puts on its bottom rung is admitted and running, and it reaches neither the API server nor the internet while a control workload beside it reaches both. They are pre-registered, they are graded in the same sample as the other five, and at the time of writing neither has been scored on a scheduled run. When one is, this slide will say which way it came out. ADR-0028 is where that decision and the road not taken are written down.
-->

---

## The twin's loop

![w:900](diagrams/twin-loop.png)

Pre-rendered from `talk/diagrams/twin-loop.mmd`, drawn on the twenty-fifth of August.

<!--
A dated signal binds to a component. One scenario runs, at a declared time, under a regime that has to be named because there is no default. Out comes a bundle of forecasts, always plural, one per rival world model. They are scored under proper scoring rules against what actually happened. The design signs that score card with an agent identity attesting the absence of a human; that is the actor half of attestation, and it is shelved for this build (ticket 90), so nothing on this run observed it. What this run observed is the artefact half: the signature on the artefact, and what it says it does and does not assert.
-->

---

<!-- beat step=5 status=SKIP cited=yes script=verify/e2e/verify-e2e-step5-twin-forecasts.sh capture=talk/captures/verify_e2e_verify-e2e-step5-twin-forecasts.out -->

## 5 · The twin plays a dated signal forward and is scored

**could not look** — the twin did not play a dated signal forward for every adopter: driftwood/twin/verify-twin-sweep-moved.sh could not look: SKIP: the moved path has not fired live yet: observations/twin-sweep.jsonl carries no line with "moved": true (the step is proven offline above; the live firing is the half only the clock can supply, once the overlay or a pin moves under it); ludlow/verify-twin-overlay.sh could not look: SKIP: the emitter could not look: CANNOT LOOK: ludlow's twin cannot price a forward-intel payload today, and the instruments it is missing are named rather than defaulted (ADR-0020): perspective 'ludlow' declares 'plan-administration-fees' as its cash flow and puts no priced valuation on it (grade 3, outside the pricing threshold of 2), because party.yaml publishes no signed size: for the amount to derive from; the one causal edge to 'plan-administration-fees' (member-records-loss-cuts-plan-administration-fees) is graded 3, outside the ladder's path admission threshold of 2, so no impact may enter this perspective's pound through it. The overlay, the vendored world layer and the six standing scenarios are all present and load; what is absent is a figure and an evidenced path, and inventing either is the one thing this estate refuses.; ludlow/twin/verify-twin-scenarios.sh could not look: SKIP: the overlay with its six scenarios loads, and the emitter could not price it: CANNOT LOOK: ludlow's twin cannot price a forward-intel payload today, and the instruments it is missing are named rather than defaulted (ADR-0020): perspective 'ludlow' declares 'plan-administration-fees' as its cash flow and puts no priced valuation on it (grade 3, outside the pricing threshold of 2), because party.yaml publishes no signed size: for the amount to derive from; the one causal edge; ludlow/twin/verify-twin-sweep-moved.sh is not in this estate; ludlow has no .github/workflows/twin-sweep.yml, so nothing sweeps this overlay on a schedule; tuppence/verify-twin-overlay.sh could not look: SKIP: the emitter could not look: CANNOT LOOK: tuppence's twin cannot price a forward-intel payload today, and the instruments it is missing are named rather than defaulted (ADR-0020): perspective 'tuppence' declares 'payment-fee-income' as its cash flow and puts no priced valuation on it (grade 3, outside the pricing threshold of 2), because party.yaml publishes no signed size: for the amount to derive from; the one causal edge to 'payment-fee-income' (customer-records-loss-cuts-payment-fee-income) is graded 3, outside the ladder's path admission threshold of 2, so no impact may enter this perspective's pound through it. The overlay, the vendored world layer and the six standing scenarios are all present and load; what is absent is a figure and an evidenced path, and inventing either is the one thing this estate refuses.; tuppence/twin/verify-twin-scenarios.sh could not look: SKIP: the overlay with its six scenarios loads, and the emitter could not price it: CANNOT LOOK: tuppence's twin cannot price a forward-intel payload today, and the instruments it is missing are named rather than defaulted (ADR-0020): perspective 'tuppence' declares 'payment-fee-income' as its cash flow and puts no priced valuation on it (grade 3, outside the pricing threshold of 2), because party.yaml publishes no signed size: for the amount to derive from; the one causal edge t; tuppence/twin/verify-twin-sweep-moved.sh is not in this estate; tuppence has no .github/workflows/twin-sweep.yml, so nothing sweeps this overlay on a schedule

```text
$ bash verify/e2e/verify-e2e-step5-twin-forecasts.sh
E2E step 5 twin forecasts
  ok  driftwood/twin/orgs/driftwood/meta.yaml (the adopter's own overlay)
  ok  driftwood/twin/world/ (the world layer, vendored)
  ok  driftwood/twin/emit-forward-intel.py (the forward-intel emitter)
  ok  driftwood: 6 standing scenarios in driftwood/twin/orgs/driftwood/scenarios
  ok  driftwood/twin/signals.yaml (the pin -> standing-scenario table)
  ok  driftwood/verify-twin-overlay.sh: PASS: driftwood's twin emits one forward-intel feed from its own
    overlay, labelled and unpicked
  ok  driftwood/twin/verify-twin-scenarios.sh: PASS: driftwood carries its six standing scenarios, two
    new classes in all three places, and no fine amount in the twin
  ok  driftwood's scheduled sweep (`5 7 * * *`) has appended 5 dated observation(s); the most recent
    swept_at is 2026-09-09T12:17:34Z
  ok  ludlow/twin/orgs/ludlow/meta.yaml (the adopter's own overlay)
  ok  ludlow/twin/world/ (the world layer, vendored)
  ok  ludlow/twin/emit-forward-intel.py (the forward-intel emitter)
  ok  ludlow: 6 standing scenarios in ludlow/twin/orgs/ludlow/scenarios
  ok  ludlow/twin/signals.yaml (the pin -> standing-scenario table)
  ok  tuppence/twin/orgs/tuppence/meta.yaml (the adopter's own overlay)
  ok  tuppence/twin/world/ (the world layer, vendored)
  ok  tuppence/twin/emit-forward-intel.py (the forward-intel emitter)
  ok  tuppence: 6 standing scenarios in tuppence/twin/orgs/tuppence/scenarios
  ok  tuppence/twin/signals.yaml (the pin -> standing-scenario table)
  ok  the twin's own evals scored the artefacts: PASS: 7 harness-mechanism metrics (the label
    twin/evolution_judge.py declares for the corpus behind these scores, and the one this run asserted
    the harness spent; harness-mechanism means each heuristic is scored against the corpus it was fitted
    on, so the number grades the harness, not the twin's judgement -- a held-out corpus is not built
    yet), exactly the set twin/skill-thresholds.yaml declares, at their thresholds and none fallen,
    three real-firm beats, identical bytes on this architecture, and every published feed envelope
    binding to one dated signal
```

<!--
The twin sits above the adopter, not inside the platform. It has the adopter's own overlay, a set of standing scenarios, and a lookup from a pinned feed version to the dated signal that justifies it. It publishes forward intelligence as a feed the platform consumes, on the same envelope as everything else. And its own skill is graded by the truth surface, which is the part that matters: a forecaster that is never scored is decoration. The demo runs on driftwood's twin. Not a preference: it is the only one of the three adopters whose overlay can price a forward-intel payload today, and the capture on this slide is where the other two say so themselves, naming the instrument each is missing rather than defaulting it.
-->

---

<!-- aside name=priced-cage status=SKIP cited=yes script=verify/pound-seam/verify-pound-seam.sh capture=talk/captures/verify_pound-seam_verify-pound-seam.out wanted=talk/captures/verify_pound-seam_verify-pound-seam.out -->

## driftwood's cage has a price, and two independent engines pick the same rung

**could not look** — driftwood serves a total of 3,704,381.74 GBP carrying no `ordinal` and no `aggregate`: composition.py writes both, and this adopter was composed under platform v2.0.1, which predates them. It waits on the owner's next signed platform tag and on this adopter's pin moving to it; nothing here may be re-rendered from an untagged branch (eco-system ticket 79 items 9 and 10)

```text
$ bash verify/pound-seam/verify-pound-seam.sh
PASS: driftwood: appetite 40,000 GBP, signed on its own party.yaml
PASS: driftwood: the twin entry names the reduction set its residuals came from
  (platform-cage-tiers@1.0.0)
PASS: driftwood: platform's tier table and driftwood's own graded curve still pick the same rung as
  cheapest on the same shock (isolated); they differ by up to 171,600 on the rungs themselves, which is
  why the entry names which set priced it
PASS: driftwood: one twin edge, policy 1.1.0, curve sha256:70e5e, tail bounded-pert, perspective
  driftwood in GBP
```

<!--
This is what a priced cage looks like, and it is driftwood's, because driftwood is the one adopter of the three whose twin can price a scenario today — the other two publish no signed size, so their own emitters refuse rather than invent a figure, and you will have seen that said in their own words on the previous slide. Read the middle line. The platform's tier table and driftwood's own graded curve are two separate implementations, and they pick the same rung as cheapest on the same shock, while disagreeing about what the other rungs cost. The agreement that matters is the decision, not the arithmetic.
-->

---

<!-- aside name=local-clock status=SKIP cited=yes script=verify/local-clock/verify-local-clock.sh capture=talk/captures/verify_local-clock_verify-local-clock.out wanted=talk/captures/verify_local-clock_verify-local-clock.out -->

## Where the twin's model call actually runs

**could not look** — no .local-clock/last-run.json on this machine -- the local clock has not run here, or this is not the owner's machine

```text
$ bash verify/local-clock/verify-local-clock.sh
PASS: offline, with stub-claude.sh and stub-gh.sh standing in over a throwaway adopter and a throwaway
  bare origin (a fixture, not the clock having run) -- the proposal is cut from origin/main with local
  main 1 behind, is one commit, unsigned, authored and committed as the clock although the config signs
  as the owner, --push lands it on the origin with main unmoved and asks gh for that PR, --push with gh
  logged out refuses before any model call, a signed or person-authored proposal, a two-commit branch
  hiding either behind a clean tip, a git replace standing a clean double before a signed commit, a hook
  or fsmonitor or remote written into the unit's config (the hook never ran, evil.git got nothing), an
  amended base or merge-shaped commit, a trailer naming a person, a tag the child made and a nested
  clock are each refused, a rehearsal is stamped, marked, counted by the scan and refused by the
  validator, a declaration, unfinished work, a file that is not a *.claim.yaml or a claim without the
  headless mark is refused with the model's PR body deleted, and a nothing run or a dry run leaves no
  worktree or branch
    PASS: talk/local-clock.sh exists and talk/local-clock.README.md names its 7 flags exactly
    SKIP: no .local-clock/last-run.json on this machine -- the local clock has not run here, or this is
      not the owner's machine
    PASS: no committed envelope, claim, observation or capture carries injected: true on HEAD of 9
      repositories or origin/main of 9 (oldest last updated 0h ago); 0 live local-clock branch(es) carry
      none, 0 rehearsal branch(es) may
```

<!--
One honest caveat about the twin, said out loud rather than buried. No pod anywhere in this eco-system makes a model call, and no schedule makes one either. The call runs from a script on the owner's own machine, under a guard, and what it may do is bounded by a fixture that proves the refusals rather than describing them: the clock may open a proposal and it may not sign one, may not author as a person, may not tag, and may not start another clock. What you are looking at here is that fixture passing and the marker itself absent, because the machine that grades this deck is not the machine the clock runs on. That is a could-not-look, and it is the honest word for it.
-->

---

<!-- beat step=6 status=PASS cited=yes script=verify/e2e/verify-e2e-step6-provenance.sh capture=talk/captures/verify_e2e_verify-e2e-step6-provenance.out -->

## 6 · Provenance: every step verifiable in the transparency log

**observed true** — every artefact the slice publishes reaches a signed tag or is honestly queued; of the 18 lines the 8 release-shipping units declare in their own party.yaml publishes[], 13 newest tags matched a real Fulcio cert subject with its Rekor entry validated and 5 lines have no tag of their own shape yet

```text
$ bash verify/e2e/verify-e2e-step6-provenance.sh
E2E step 6 provenance
  ok   84 published artefacts resolve to a tag on the publisher's real remote; 3 honestly queued for
    cut-release.yml
  ok   8 release workflows pin an anchored, own-repo identity regexp at the Actions OIDC issuer
  ok   driftwood/exposure newest tag v1.1.0: real cert subject matches the anchored regexp, Rekor entry
    validated
  ok   driftwood/forward-intel newest tag v1.1.0: real cert subject matches the anchored regexp, Rekor
    entry validated
  ok   feeds/cve has no tag yet: looked for 'cve/vX.Y.Z' and 'vX.Y.Z' among 3 real tags -- queued for
    cut-release.yml
  ok   feeds/eol has no tag yet: looked for 'eol/vX.Y.Z' and 'vX.Y.Z' among 3 real tags -- queued for
    cut-release.yml
  ok   feeds/fx has no tag yet: looked for 'fx/vX.Y.Z' and 'vX.Y.Z' among 3 real tags -- queued for
    cut-release.yml
  ok   feeds/market-moves has no tag yet: looked for 'market-moves/vX.Y.Z' and 'vX.Y.Z' among 3 real
    tags -- queued for cut-release.yml
  ok   feeds/news has no tag yet: looked for 'news/vX.Y.Z' and 'vX.Y.Z' among 3 real tags -- queued for
    cut-release.yml
  ok   feeds/threat-register newest tag threat-register/v3.0.0: real cert subject matches the anchored
    regexp, Rekor entry validated
  ok   ico/penalty-schema newest tag v4.0.0: real cert subject matches the anchored regexp, Rekor entry
    validated
  ok   insurer/quote-driftwood newest tag v1.0.0: real cert subject matches the anchored regexp, Rekor
    entry validated
  ok   insurer/quote-ludlow newest tag v1.0.0: real cert subject matches the anchored regexp, Rekor
    entry validated
  ok   insurer/quote-tuppence newest tag v1.0.0: real cert subject matches the anchored regexp, Rekor
    entry validated
  ok   ludlow/exposure newest tag v1.1.0: real cert subject matches the anchored regexp, Rekor entry
    validated
  ok   nist/sp-800-53 newest tag v1.1.0: real cert subject matches the anchored regexp, Rekor entry
    validated
  ok   platform/currency-controller newest tag v2.0.1: real cert subject matches the anchored regexp,
    Rekor entry validated
  ok   platform/identity-substrate newest tag v2.0.1: real cert subject matches the anchored regexp,
    Rekor entry validated
  ok   platform/policy newest tag policy/v5.0.0: real cert subject matches the anchored regexp, Rekor
    entry validated
  ok   tuppence/exposure newest tag v1.1.0: real cert subject matches the anchored regexp, Rekor entry
    validated
```

<!--
Every artefact the slice publishes reaches a signed tag, or is honestly queued for the workflow that will cut one. Each release workflow pins an anchored identity regular expression at the Actions issuer, so a signature from the wrong repository does not satisfy it. And where a tag exists, the certificate subject was matched against that regular expression and the transparency log entry validated, live. The units with no tag yet are named, not omitted.
-->

---

<!-- beat step=7 status=PASS cited=yes script=verify/e2e/verify-e2e-step7-honesty.sh capture=talk/captures/verify_e2e_verify-e2e-step7-honesty.out -->

## 7 · Honesty: one command reports pass, fail or could-not-look

**observed true** — steps 1-6 each report one honest verdict (verdicts: PASS PASS PASS SKIP SKIP PASS PASS)

```text
$ bash verify/e2e/verify-e2e-step7-honesty.sh
E2E step 7 honesty
  #   step (NORTH-STAR 4)        verdict   why
  --- -------------------------- --------- ---
  1   regulator publishes        PASS      ico's newest penalty-schema envelope validates and its tag is
    on the real remote
  2   renovate pins and reprices PASS      a SYNTHETIC pin bump (penalty-schema v3 -> v4, made here on a
    throwaway copy of driftwood, no...
  3   price crosses band pr open PASS      a SYNTHETIC residual placed either side of driftwood's own
    signed appetite band of 40,000 GBP...
  4   flux reconciles cage       SKIP      no cluster on this runner, and the lane sample cannot stand
    in: a fact could not be looked at...
  5   twin forecasts             SKIP      the twin did not play a dated signal forward for every
    adopter: driftwood/twin/verify-twin-sw...
  6   provenance                 PASS      every artefact the slice publishes reaches a signed tag or is
    honestly queued; of the 18 line...
  7   honesty                    PASS      steps 1-6 each report one honest verdict
```

<!--
This is the whole demonstration in one table, written by a different script from the ones it reads. Every step reports one verdict. Where a step could not be looked at, the reason travels with it. This slide is also what refuses this deck: if a beat's status ever disagreed with this table, the check would fail, and the deck would not be shown.
-->

---

## Honesty over green

A check that passes because it could not look is worse than one that fails.

The red and amber cells in this deck are the roadmap. They are generated, they move when the eco-system moves, and no one can quietly retype them.

What the headline counts, and what it does not: the truth surface grades the estate's own verify scripts. It does not run the twin's python test suite, which carries one standing red of its own — the drift window is open and its newest sample is weeks old, a recorded gap, not a surprise. A count is only honest with its scope beside it.

The recording of this talk is a by-product, and it is a screen recording of a human driving these slides — to be published as a release asset on the hub, with no audio track kept and no synthetic narration anywhere in the pipeline. The deliverable is the run.

<!--
If you take one thing away, take this. A green result that could not look at the thing it grades is the most expensive kind of lie a governance programme can tell itself, because it survives audit. Everything here is built to make that particular lie hard to tell: three outcomes rather than two, the reason travelling with the refusal, and the slides generated from the run rather than from my memory of it. And one caveat I will say out loud rather than bury: the number counts the estate's verify scripts, not the twin's own test suite, which has a standing red about a drift window nobody has sampled recently. It is recorded, it is not fixed, and it is not inside the headline.
-->

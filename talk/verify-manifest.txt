# What each script in the gate measures (eco-system ticket 83). Read by talk/verify-all.sh
# through talk/truth_manifest.py, which documents the format; the short form:
#
#   path | class | skip        # a trailing comment is the place for a borderline note
#
#   class  estate-observation  the verdict turns on the CONTENT of another party's artefact or
#                              on live state outside the script's own repository
#          self-proof          a party grading its own code, fixtures or artefacts (using another
#                              party's schema or engine as the RULER is still self-proof)
#          simulation          synthetic or throwaway material, said so in the verdict
#          meta                grades other checks or the record, not the estate
#   skip   -                   no could-not-look expected; one that happens FAILS the gate
#          never: <regex>      cannot pass on the scheduled runner as built (no cluster, no
#                              cross-org credential); outside the CEILING
#          waits: <regex>      the estate's own state has not arrived; inside the ceiling
#          the regex is matched case-insensitively against the script's last line on exit 3;
#          it runs to the end of the line, so alternation with '|' is fine, and '#' cannot appear
#
# The judge sees the WHOLE last line. talk/verify-all.sh cuts it at 160 characters for the grade
# table only (fixed 2026-09-04: run 70 graded a correctly declared could-not-look FAIL because the
# runner's absolute path pushed the declared phrase past the cut). A pattern may therefore sit
# anywhere in the line.
#
# KNOWN LIMIT, ticket 83 decision 3. A pattern that matches an AGGREGATE could-not-look line
# ("3 of 9 checks could not be looked at") declares the shape of the sentence and not the reason
# for it, so a NEW underlying reason still hides under the old declaration. Four lines are in that
# state: insurer/verify-insurer-quote.sh, platform/verify-publisher-gate.sh,
# verify/e2e/verify-e2e-step6-provenance.sh and verify/feed-contract/verify-untagged-pin-is-priced.sh.
# Narrowing them needs each script to name its own reason on its last line, which is a change to
# four scripts in four repositories and is not this ticket's.
#
# Every script talk/verify-all.sh discovers must be here or in talk/verify-exclusions.txt; every
# line here must name a script that exists. The TRUTH line prints the split of passes by class,
# the split of skips by kind, and ceiling = total - excluded - never.
#
# THE SUBSTRATE THIS FILE IS DERIVED FROM (delegated, ADR-0025, 2026-09-04). Every row is
# derived from what the script prints ON THE SCHEDULED RUNNER: no cluster, no docker daemon
# worth the name, no cross-org credential. A machine with live KinD clusters is a DIFFERENT
# substrate, and six `never:` rows pass there instead of skipping, which the gate grades
# `FAIL manifest: ... passed but the manifest says it can never pass`. Those are artefacts of
# the substrate, not of the estate, and they are named in .scratch/ecosystem/REPAIR-2026-09-04.md.
# A runner qualifier that turns a stale-never judgement into a NOTE when the run is off the
# runner was considered and NOT built here: it changes what `ceiling` means, which is the one
# number NORTH-STAR publishes, so it is its own ticket, not a line in a repair.
# The kind rule that follows from this: when a script's could-not-look ON THE RUNNER is a
# missing substrate, its row is `never:`, and the pattern also names the reasons the same tail
# prints where the substrate IS there, so the row is true in both places and a new reason still
# cannot hide. When the runner's reason is the estate's own state, the row is `waits:`.
#
# The classification rule (delegated, ADR-0025, 2026-09-04): a pass is an observation of the
# estate only when its verdict turns on another party's artefact or live external state; a
# script whose live tail needs a cluster is classed by what its PASS would rest on, so the
# cluster tails are estate-observation with `never:`. A line for a script listed in
# talk/verify-exclusions.txt is kept and inert: it records what that script would have measured,
# so the day it re-enters the gate its class is already decided, not re-argued. The runner's
# substrate reason today is
# "kind cluster 'driftwood' is not listed by kind get clusters" (platform/lib.sh) or "does not
# exist" (tuppence). Ticket 86 (lane branch) edits the `never:` line it lifts;
# ticket 91 un-retires the currency controller and edits its line. Ticket 56 lifted its own
# (2026-09-04): the schedules credential is no longer the ceiling, the ruleset is.

# --- driftwood (adopter) -----------------------------------------------------------------
.estate-clone/driftwood/scripts/verify-identity-regexp.sh | self-proof | -
.estate-clone/driftwood/twin/verify-twin-scenarios.sh | self-proof | -
.estate-clone/driftwood/twin/verify-twin-sweep-moved.sh | self-proof | waits: no observations/twin-sweep\.jsonl exists yet   # ticket 72 landed this script with `-`, which says "no could-not-look is expected"; it has one, and always has: the offline half is proven in the run, and the live half is a firing only the clock can supply once the overlay or a pin moves under it
.estate-clone/driftwood/verify-reconcile.sh | self-proof | waits: no local kind cluster .* and the lane sample cannot stand in   # decision 13: on the runner the only PASS it can reach grades the adopter's OWN committed drift/samples.jsonl, so it is self-proof there. The estate-observation path is the live one below need_substrate, where facts 1-3 read the publishers' real remotes; it needs a kind cluster and never runs on the scheduled runner
.estate-clone/driftwood/verify-twin-overlay.sh | self-proof | -

# --- feeds (intelligence provider) -------------------------------------------------------
.estate-clone/feeds/verify-feeds.sh | self-proof | -   # validates its own feeds against platform's envelope schema: the schema is the ruler
.estate-clone/feeds/verify-market-and-news.sh | self-proof | -
.estate-clone/feeds/verify-news-headline-skill.sh | self-proof | -

# --- ico (regulator) ---------------------------------------------------------------------
.estate-clone/ico/.github/scripts/verify-declared-bump.sh | self-proof | -
.estate-clone/ico/verify-certificate-identity-regexp.sh | self-proof | -
.estate-clone/ico/verify-penalty-feed.sh | estate-observation | -   # part 8 resolves the nist pin as a real tag on nist's real remote

# --- insurer (risk carrier) --------------------------------------------------------------
.estate-clone/insurer/verify-insurer-party.sh | self-proof | -
.estate-clone/insurer/verify-insurer-quote.sh | self-proof | waits: [0-9]+ of [0-9]+ checks could not be looked at   # prices its own quotes through platform's fair.py: the engine is the ruler

# --- ludlow (adopter) --------------------------------------------------------------------
.estate-clone/ludlow/verify-adopter-gate.sh | simulation | -   # throwaway platform and ludlow repositories built in the run
.estate-clone/ludlow/twin/verify-twin-scenarios.sh | self-proof | waits: the emitter could not price it   # ticket 64. Six standing scenarios, four committed classes and the signal lookup all grade offline; the declared could-not-look is check 9, which has no priced valuation to re-derive because this party publishes no signed `size:`
.estate-clone/ludlow/verify-twin-overlay.sh | self-proof | waits: cannot price a forward-intel payload today   # ticket 64. The overlay, the vendored world layer, the ladder checked against platform's own published one and four planted refusals all grade offline; the declared could-not-look is the emitter refusing, by name, for the two instruments this party does not have (no signed `size:`, and one grade-3 edge to the declared cash flow). It goes away when the owner signs a size and a grade-1-or-2 edge replaces the anchor
.estate-clone/ludlow/verify-certificate-identity-regexp.sh | self-proof | -
.estate-clone/ludlow/verify-reconcile.sh | self-proof | waits: no local kind cluster .* and the lane sample cannot stand in   # decision 13, as driftwood's: the runner path grades ludlow's own committed lane sample; the live cluster path is the estate observation and does not run here

# --- nist (regulator) --------------------------------------------------------------------
.estate-clone/nist/.github/scripts/verify-declared-bump.sh | self-proof | -
.estate-clone/nist/scripts/verify-catalog.sh | self-proof | -
.estate-clone/nist/scripts/verify-cert-identity-regexp.sh | self-proof | -

# --- platform ----------------------------------------------------------------------------
.estate-clone/platform/access/verify-access.sh | estate-observation | never: kind cluster 'driftwood' is not listed   # identity plane, EXCLUDED by ticket 90 (see talk/verify-exclusions.txt); this line records what it would have measured, and is inert until the identity lane runs it
.estate-clone/platform/break-glass/verify-break-glass.sh | self-proof | -
.estate-clone/platform/compose/verify-composition.sh | estate-observation | waits: not on the real remote until cut-release.yml cuts   # composes real driftwood against its five real pinned parents
.estate-clone/platform/computed-semver/verify-cage-engine.sh | self-proof | -
.estate-clone/platform/computed-semver/verify-comparison-window.sh | self-proof | -
.estate-clone/platform/computed-semver/verify-corpus-generator.sh | self-proof | -
.estate-clone/platform/computed-semver/verify-coverage.sh | self-proof | -
.estate-clone/platform/computed-semver/verify-gate.sh | self-proof | -
.estate-clone/platform/computed-semver/verify-generator-standing-check.sh | self-proof | -
.estate-clone/platform/computed-semver/verify-pairing.sh | self-proof | -
.estate-clone/platform/computed-semver/verify-rederive-bumps.sh | self-proof | -
.estate-clone/platform/computed-semver/verify-release-integrity.sh | self-proof | -
.estate-clone/platform/computed-semver/verify-witness-set.sh | self-proof | -
.estate-clone/platform/currency-controller/verify-currency.sh | estate-observation | never: kind cluster 'driftwood' is not listed   # ticket 91 un-retires the controller and owns this line
.estate-clone/platform/distribution/verify-coexistence.sh | self-proof | waits: coexistence needs two RELEASED versions to show side by side|docker is not reachable|is not listed by kind get clusters|cannot list Flux kustomizations|Flux has no Kustomization|Flux not Ready on kind-|policy-versions ResourceSet not reconciled on   # re-derived 2026-09-04: ticket 63 added 5.0.0 to versions.yaml, so "declares one version" stopped being what this script says. Two are declared now and only 4.0.0 is CUT, so the reason moved from declaration to release; that branch runs before any substrate check, so it is the line on the runner today. CORRECTED 2026-09-04 (second pass): cutting the second tag does NOT lift this row, and ticket 84 is not what cuts it -- ticket 63 declared 5.0.0 and the signed tag is the owner's cut-release.yml dispatch. Ticket 84's own obligation is separate: it supplies the THIRD declared line and raises this script's threshold from two to three, which rewrites this reason string and keeps the row `waits`, because the `n_versions -lt` branch fires again on an estate-state reason. With two CUT versions the `n_versions -lt 2` branch stops firing and the script falls straight through to `substrate_ok`, so on the runner the reason becomes a missing substrate and the live tail can print its own two as well -- which is why the pattern names all of them now, and is true before and after the cut. So long as the threshold is two, what the signed cut of 5.0.0 must ALSO do, in the same commit as the tag, is flip this row to `estate-observation | never:`, because from that day the runner's reason is substrate, not the estate's own state, and the kind rule above then makes it a `never` outside the ceiling. It is not flipped today: today's reason IS the estate's own state, so `never` would be false now and would shrink the published ceiling on a false ground
.estate-clone/platform/distribution/verify-declared-versions-admit.sh | estate-observation | never: kind cluster 'driftwood' is not listed
.estate-clone/platform/distribution/verify-governed-namespace-guard.sh | self-proof | -
.estate-clone/platform/distribution/verify-infra-declaration.sh | estate-observation | -   # reads driftwood's, ludlow's and tuppence's composed policies
.estate-clone/platform/distribution/verify-orphan-guard.sh | self-proof | -
.estate-clone/platform/distribution/verify-render-version-tree.sh | self-proof | -
.estate-clone/platform/distribution/verify-retirement.sh | estate-observation | never: docker is not reachable|is not listed by kind get clusters|cannot list Flux kustomizations|Flux has no Kustomization|Flux not Ready on kind-|absence is not evidence of pruning|retirement not applied there   # re-derived 2026-09-04: ticket 63's second declared version retired the offline refusal ("a retirement would leave an empty allow-list") that used to fire first, so this script now falls through to its LIVE tail, which reads Flux Kustomizations on a cluster. That makes it estate-observation (live state outside the repo). The alternation names each reason the tail can actually print -- the five substrate reasons platform/lib.sh sets, and the two the tail itself writes when the substrate IS there -- so the row is true on the runner and on a machine with clusters, and a NEW reason still cannot hide in it. STRONGER REASON for `never`, found 2026-09-04 (second pass): it is not that the scheduled runner has no cluster. EVERY branch of this script's live tail calls live_tail_skip -- substrate missing, policy-vN present, policy-vN absent -- and pass_line() exits 3 whenever any tail skipped, so this script CANNOT reach exit 0 on any machine, cluster or no cluster, until it grows a branch that observes a pruning it watched happen. That also dates the arithmetic: the old `waits: declares one version` row was never true either, because a `waits` says the script passes the day the estate's state arrives and this one would not have. The ceiling has been one too generous since ticket 83 first classified this script (2026-09-04, commit 0406a5d), not since ticket 63 pushed it past its offline refusal
.estate-clone/platform/engine/verify-engine.sh | estate-observation | never: kind cluster 'driftwood' is not listed
.estate-clone/platform/eud/verify-eud.sh | estate-observation | never: kind cluster 'driftwood' is not listed   # identity plane, EXCLUDED by ticket 90 (see talk/verify-exclusions.txt); this line records what it would have measured, and is inert until the identity lane runs it
.estate-clone/platform/fair/verify-fair-tail.sh | self-proof | -
.estate-clone/platform/feeds/verify-feeds.sh | self-proof | -
.estate-clone/platform/graded/verify-graded.sh | estate-observation | never: kind cluster 'driftwood' is not listed   # ticket 86's lane branch owns this line
.estate-clone/platform/honesty/verify-honesty.sh | self-proof | -
.estate-clone/platform/identity/verify-federation.sh | estate-observation | never: kind cluster 'driftwood' is not listed   # identity plane, EXCLUDED by ticket 90 (see talk/verify-exclusions.txt); this line records what it would have measured, and is inert until the identity lane runs it
.estate-clone/platform/identity/verify-identity.sh | estate-observation | never: kind cluster 'driftwood' is not listed   # identity plane, EXCLUDED by ticket 90 (see talk/verify-exclusions.txt); this line records what it would have measured, and is inert until the identity lane runs it
.estate-clone/platform/oscal/verify-claims.sh | estate-observation | -   # every claimed control id resolves in nist's real pinned catalogue
.estate-clone/platform/oscal/verify-upflow.sh | simulation | -   # self-generated PolicyReports
.estate-clone/platform/party/verify-party-artefact.sh | self-proof | -
.estate-clone/platform/policy/verify-conditional.sh | estate-observation | never: docker is not reachable|is not listed by kind get clusters|cannot list Flux kustomizations|Flux has no Kustomization|Flux not Ready on kind-|still claim policy-version 2\.0\.1|ValidatingPolicy not installed on   # re-derived 2026-09-04: the old reason (the conditional branch "lives only in require-nonroot-2-0-1", a retired line) stopped being true when ticket 63 declared 5.0.0, which carries the branch. That branch was the FIRST test in the script, and with it gone the script falls through to the substrate check and then to its live admit/deny tail -- so it is estate-observation and `never` on a runner with no cluster. The alternation names the five substrate reasons platform/lib.sh sets and the two the tail writes once the substrate is there (fixtures still labelled 2.0.1; the policy not installed). Relabelling tests/conditional/resources.yaml to 5.0.0 is a platform enactment push, not this repair's
.estate-clone/platform/posture/verify-posture-projection.sh | estate-observation | never: kind cluster 'driftwood' is not listed   # identity plane, EXCLUDED by ticket 90 (see talk/verify-exclusions.txt); this line records what it would have measured, and is inert until the identity lane runs it
.estate-clone/platform/risk/verify-risk-tuned.sh | self-proof | -
.estate-clone/platform/shift-left/verify-shift-left.sh | self-proof | waits: declares one major line   # the owner's dispatch of the 5.0.0 tag lifts it with a second declared line
.estate-clone/platform/shift-left/verify-tier-binding.sh | self-proof | -   # platform planting cases against its own rule; the hub's verify/tier-binding runs it over the real adopters
.estate-clone/platform/tcor/verify-tcor.sh | self-proof | -
.estate-clone/platform/verify-certificate-identity-regexp.sh | self-proof | -
.estate-clone/platform/verify-cut-release-tags.sh | simulation | -   # throwaway repositories and a local remote
.estate-clone/platform/verify-first-gate-determined-release.sh | self-proof | waits: waiting for the owner to let cut-release\.yml cut
.estate-clone/platform/verify-publisher-gate.sh | self-proof | waits: part\(s\).* could not look
.estate-clone/platform/verify-source-verification.sh | estate-observation | never: kind cluster 'driftwood' is not listed   # artefact side (the gitsign source controller, ticket 41): stays in the gate under ticket 90
.estate-clone/platform/wardley/verify-wardley.sh | self-proof | -
.estate-clone/platform/wargamer/verify-wargamer.sh | self-proof | -

# --- tuppence (adopter) ------------------------------------------------------------------
.estate-clone/tuppence/reset/verify-reach-secrets.sh | estate-observation | never: kind cluster 'driftwood' does not exist   # identity plane, EXCLUDED by ticket 90 (see talk/verify-exclusions.txt); this line records what it would have measured, and is inert until the identity lane runs it
.estate-clone/tuppence/scripts/verify-adopter-gate.sh | simulation | -   # throwaway repositories built in the run
.estate-clone/tuppence/scripts/verify-identity-regexp.sh | self-proof | -
.estate-clone/tuppence/twin/verify-twin-scenarios.sh | self-proof | waits: the emitter could not price it   # ticket 64, the same shape as ludlow's line above
.estate-clone/tuppence/verify-twin-overlay.sh | self-proof | waits: cannot price a forward-intel payload today   # ticket 64, the same shape as ludlow's line above
.estate-clone/tuppence/verify-reconcile.sh | self-proof | waits: no local kind cluster .* and the lane sample cannot stand in   # decision 13, as driftwood's: the runner path grades tuppence's own committed lane sample; the live cluster path is the estate observation and does not run here

# --- hub: the cross-party proofs under verify/ -------------------------------------------
verify/adr-supersession/verify-adr-supersession.sh | meta | -   # ticket 39: grades the record, deliberately reads no Python
verify/branch-refs/verify-branch-refs.sh | estate-observation | waits: has cut no tag at all, so there is no signed version to pin to|carries no clone of|decided on the runner and not written in this file   # tickets 62 and 77: reads every unit's own workflows and every publisher's real tags. The declared could-not-look is driftwood's twin-sweep.yml consuming the HUB, which has cut no tag for it to pin to; ticket 64 cuts it, and the day it exists this line's skip goes away. The second alternative is the script's OTHER could-not-look, added 2026-09-04 because it was reachable and undeclared: a publisher this checkout has no clone of cannot be asked which tags it signed. The third, added 2026-09-04 round 3, is a `repository:` no literal matrix decides -- none in the estate today, and it is a named skip rather than the silence it used to be. LIMITS, three: the check reads .github/workflows/*.yml and grades actions/checkout@ steps only -- a .yaml workflow, a composite action or a `git clone` in a run: block is invisible to it -- and a templated `repository:` is graded only where the job's own matrix is a literal list of values carrying no `include:` and no `exclude:`, because either of those changes the value set the lists alone describe (none of the three exists in the estate today; branch_refs.py's docstring records the check). All 52 cross-organisation checkout lines in the eight units are graded, insurer/fetch.yml's templated one once per adopter in its matrix. The wrapper's own two could-not-looks (no interpreter, no estate clone) are deliberately undeclared, as on verify-untagged-pin-is-priced.sh: a runner that has lost its venv or its clone should go red, not shrug
verify/demo/verify-demo.sh | meta | waits: recording commit this checkout cannot reach   # grades the deck against the run it names
verify/disclaimer/verify-disclaimer.sh | estate-observation | -   # reads nine repositories' party.yaml, README and NOTICE
verify/e2e/verify-e2e-step1-regulator-publishes.sh | estate-observation | waits: waiting for tag|queued for cut-release|not yet a tag
verify/e2e/verify-e2e-step2-renovate-pins-and-reprices.sh | simulation | waits: no pin bump available on disk moves|no adopter pins a priceable feed   # a SYNTHETIC bump on a throwaway copy of an adopter; the real merged bump is verify/renovate
verify/e2e/verify-e2e-step3-price-crosses-band-pr-opens.sh | simulation | waits: no adopter publishes a selection-policy package   # a SYNTHETIC residual either side of the adopter's real band
verify/e2e/verify-e2e-step4-flux-reconciles-cage.sh | estate-observation | waits: no cluster on this runner, and the lane sample cannot stand in|reconciles .* at its pinned revision .* only:
verify/e2e/verify-e2e-step5-twin-forecasts.sh | estate-observation | waits: did not play a dated signal forward for every adopter|evals could not look   # ticket 64: the adopter list is derived from the party artefacts claiming the adopter role, not hardcoded to driftwood, and the step now requires a dated `swept_at` in each adopter's own observations/twin-sweep.jsonl. Both halves of the widened skip are the estate's own state -- two overlays waiting on the owner's push, and a scheduled sweep that has fired four times and never yet appended an observation -- so it stays `waits:`
verify/e2e/verify-e2e-step6-provenance.sh | estate-observation | waits: waiting for tag|could not (be )?look   # real Fulcio certificates and Rekor entries
verify/e2e/verify-e2e-step7-honesty.sh | meta | -
verify/every-green/verify-every-green.sh | meta | -   # ticket 76: grades the SHAPE of every discovered script's greens; this manifest grades what each green RESTS ON and whether a could-not-look was expected. Neither reruns the other's question
verify/feed-contract/verify-feed-contract.sh | estate-observation | waits: waiting for tag|does not carry the tag object|could not reach https://github\.com/|payload_schema is a URL|feed\.json yet   # every adopter pin resolves on the publisher's real remote, and since ticket 77 its TREE is read too. The second alternative is that tree read's own could-not-look, declared 2026-09-04 because it is reachable: a tag can exist on the publisher's remote while this checkout lacks the tag object, and reading nothing is never a pass. Round 3, same day: feed_contract.py can print SIX distinct could-not-look reasons and this row declared two, so a network blip ("could not reach ...") or an offline payload schema ("payload_schema is a URL ...") read as red. All six are named now -- "waiting for tag" covers both the tag that is not cut yet and the section that is on the publisher's branch, and the last alternative is a declared feed whose directory exists with no v*/feed.json in it yet. The wrapper's own missing-interpreter could-not-look stays undeclared on purpose, as on verify-untagged-pin-is-priced.sh
verify/feed-contract/verify-untagged-pin-is-priced.sh | estate-observation | waits: could not reach|could not look at the signature|no publishes\[\] record|untagged \(   # ticket 69: pin state read from the publisher's real remote. A missing interpreter is deliberately NOT declared here: if the runner loses its venv the gate should go red, not shrug
verify/local-clock/verify-local-clock.sh | meta | never: no \.local-clock/last-run\.json on this machine   # ticket 92: grades the owner's local clock; the marker only exists on the owner's machine, never on the runner
verify/misuse/verify-misuse.sh | estate-observation | waits: could-not-look by name
verify/party/verify-party.sh | estate-observation | -
verify/pound-seam/verify-pound-seam.sh | estate-observation | -
verify/priced-holes/verify-priced-holes.sh | estate-observation | waits: composed/evidence\.json carries no deltas\[\]   # its honest could-not-look, declared 2026-09-04: no adopter's composed evidence carries deltas[] until it is re-composed, and re-composing an adopter is an enactment push the owner makes. `-` said none was expected, which was never true
verify/proportionality/verify-proportionality.sh | estate-observation | never: kind-driftwood is not reachable   # each institution's own signed band; the live tail wants the adopter's rendered cage on a cluster
verify/provenance/verify-provenance.sh | simulation | never: no rekor-cli here|kind-driftwood is not reachable   # a narrative chain over committed fixtures; the live tails want rekor-cli and a cluster, neither of which the runner is built with
verify/provenance/verify-release-evidence-reaches-main.sh | estate-observation | waits: adopter pin\(s\) could not be graded
verify/record/verify-record-states-the-purpose.sh | meta | -
verify/renovate/verify-renovate-merged-feed-pr.sh | estate-observation | waits: no merged Renovate feed-pin PR exists yet
verify/sampler-wait-order/verify-sampler-wait-order.sh | estate-observation | -   # reads the three adopters' checked-out workflows
verify/schedules/verify-lane.sh | estate-observation | -
verify/schedules/verify-schedules.sh | estate-observation | never: no observation-lane ruleset|github unreachable   # ticket 56 (2026-09-04) lifted the old `never: GitHub unreachable`. The gate step still carries no credential and now does not need one: truth.yml's `clocks` job hands the live facts in through CLOCK_VERDICT, so "did each clock run inside its period" grades for real on a citable run. What can NEVER pass is the other live question -- a push ruleset is private/internal-only and all nine repos are public (ADR-0024 point 3, amended) -- so the ruleset reason is the new ceiling. The unreachable alternative stays for a local run with neither a verdict file nor `gh`.
verify/tier-binding/verify-tier-binding.sh | estate-observation | waits: tier_binding\.py is not in this platform checkout|no party in this estate has both a composed|run \./clone-estate\.sh first   # ticket 78: reads every party's own committed Namespace and evidence
verify/truth-line/verify-truth-line.sh | meta | -   # ticket 83: grades this manifest and the arithmetic behind the split, never the estate
verify/twin-evals/verify-twin-evals.sh | self-proof | -
verify/twin-per-adopter/verify-twin-per-adopter.sh | estate-observation | waits: carry no twin overlay and are named rather than omitted|claims the adopter role, so there is no adopter   # ticket 64: reads every party artefact in the estate for the adopter role, then each adopter's own overlay tree. estate-observation because the verdict turns on other parties' artefacts. The first declared could-not-look is the state this check was built to make visible -- an adopter that claims the role and carries no overlay -- and it closes the day the tuppence and ludlow branches merge. The second, added 2026-09-05 after review, is the module's other reachable reason: an estate in which no party claims the adopter role at all. The wrapper's own two (no interpreter, no estate clone) are deliberately UNDECLARED, the same call verify-branch-refs.sh and verify-untagged-pin-is-priced.sh already record: a runner that has lost its venv or its clone should go red, not shrug

# 12 — The graph schema and validation

**What to build:** Formalises the minimal schema the skeleton seeded. The organisation as a typed knowledge graph:
components, edges, overlays, and the Wardley value-chain spine.

**Special-category data has no schema slot.** Article 9 compliance is an impossibility of
representation, not a validation rule that could later be relaxed. People's structural edges are in
the graph; behavioural observations live in a gated overlay.

Do not start before ticket 07 lands — the skeleton's schema is the seed, and racing it produces two
schemas.

**Blocked by:** 07

**Status:** ready-for-agent

**Reading list:** Decision tickets 07, 15. Spec stories 1, 10, 11.

- [ ] Typed schema for components, typed edges, and overlay membership, validated on write.
- [ ] `no_special_category_slot` goes live — there is no representation for special-category data, demonstrated by a failing attempt to author it.
- [ ] Structural edges and behavioural observations are in different units with different gating.
- [ ] The **graph fixture** is generated by this ticket's code in CI (not committed once), so downstream tracks test against a live contract rather than a fossil.
- [ ] Extends the invariant suite; never weakens it. Any invariant change names the invariant and cites the authorising decision ticket.
- [ ] Declares its depth grade as a **computed checklist** against the owning decision ticket's acceptance criteria — `full` is derived from the checklist, never asserted.

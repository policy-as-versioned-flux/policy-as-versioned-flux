# Review 2026-08-31 — everything built, graded against the ambition

Method: one ultracode workflow, 55 agents. Six auditors surveyed the estate against
NORTH-STAR §2 to §7. Every non-minor finding got one adversarial skeptic. 38 shortfalls
confirmed, 10 claims refuted, 17 minors recorded. Live reads were read-only per the
2026-08-27 authorisation. Tickets 54 to 67 chart the remediation; six existing tickets
got dated update comments (53, 17, 38, 48, 20, 37).

---

# Everything built, graded against the ambition — review of 2026-08-31

Citable base: `TRUTH 2026-08-31T17:22Z run=13 hub=eba3569 ... pass=53 fail=7 skip=21 excluded=2 total=83` (talk/truth.log). All shortfalls below were adversarially verified. Local runs are diagnostic only.

## Scorecard

| Dimension | Grade | One line |
|---|---|---|
| Truth surface | RED | The one citable number carries instrument faults printed as observed-false; no TRUTH line in 11 runs has fail=0, and five of run 13's seven reds have no owner. |
| Demo steps 1–7 | AMBER | All seven steps are graded inside the gate with honest verdict lines; no step has completed for real, and steps 1, 4 and 6 cannot be graded in CI today for self-inflicted reasons. |
| Seven principles | AMBER | The mechanisms exist and mostly prove themselves; principles 1, 4, 5 and 6 each carry one live, unowned gap (no conditional instance, one declared version, dead publisher clocks, red signature spine in CI). |
| Participants | AMBER | Six of eight units publish, sign and verify for real; feeds and insurer are mechanically unrunnable (zero registered workflows, zero tags), and the twin covers one adopter of three. |
| Process / map | AMBER | The tracker, ADRs and recorded decisions are real and binding; the live map cites a health number the truth surface never recorded and still says "Nothing is red" while seven checks are red. |
| Defect classes | AMBER | Every previously caught class stays closed under re-test; four new classes are live and unowned: toolchain skew, SIGPIPE-as-red, timeout-without-evidence, and the `--git-dir` sibling of the fixed `-C` hole. |

## What is genuinely done and proven

- One command, on a clock, in CI, is real. `talk/verify-all.sh` discovers 83 scripts by glob, grades three outcomes by exit code, prints SKIP reasons, and writes one dated, gitsign-signed TRUTH line daily inside a working observation cage. A red run still records its line. The exclusions file is self-checking and both exclusions are justified.
- The hub clock fires. truth.yml ran on schedule 2026-08-28 through 31. The number and its date are recorded on every run.
- The signature spine exists end to end. nist v1.0.0/v1.1.0, ico v3.0.0, platform policy/v4.0.0 and implementations v2.0.0/v2.0.1 are annotated tags cut and gitsign-signed by CI release workflows. All three adopters pin platform 2.0.1@533dccb with composed trees re-rendered.
- The £ seam computes for real. Step 2's re-price moves tuppence 222,574 → 326,139 GBP through composition; tier selection runs through driftwood's own signed selection-policy; twin evals hold seven skill metrics at threshold.
- The estate refuses dishonesty by construction. No exemption ledger exists anywhere; coverage-exclusions.yaml is empty and add-only-declared-holes; the governed-namespace guard denies claim-less pods; the five-fact sampler refuses hand-taken samples by run id, author and signature.
- The retirement of 2.0.0/2.0.1/3.0.0 used the estate's own mechanism with reasons recorded in versions.yaml.
- All five §7 supersessions carry dated banners; the deck generates 253 lines from 81 captures and its phrase and figure checks work.
- The previously caught defect classes are clean under re-probe: the tuppence scenario E no-op is gone, enact_guard's `-C` and leading-`cd` holes are closed with failing-before tests, cut-release-push.sh now pushes branch and tags in one atomic push, and every feeds/insurer selfcheck's refusal was proved to bite.
- Grading honesty improved the same day it wobbled: "a clock too new to have fired is a could-not-look" (eba3569) and the verify-demo race fix (2ade57f) both landed with reasons.

## Confirmed shortfalls, ranked

### Critical

**C1. The gate's kyverno pin does not match the estate, so the sole declared policy version fails its own checks and the gate can never go green.**
truth.yml line 72 pins kyverno CLI v1.19.0 — a URL fix (db47f88), not a version decision — while the estate is authored against 1.18.2 (BUILD-BRIEF.md:81). Under 1.19.0 the cage-tier 4.0.0 CEL does not compile (`expected type 'string' but found 'dyn'` at `posture.acme.io/tier: variables.tier`). This one root cause reds verify-graded, verify-shift-left and verify-render-version-tree in runs 12 and 13 — three of the seven fails. Proven by A/B: verify-graded passes rc=0 under 1.18.2 and reproduces run 13's exact fail under the pinned 1.19.0 binary. The composed v4.0.0 every adopter pins carries the same expression, so the policy would not load on a ≥1.19 cluster. No ticket, GAPS row, handoff line or exclusion owns the skew, and talk/truth.log has no fail=0 line in all 11 runs.
Ambition: NORTH-STAR §3 principles 2 and 4, §4 step 4, §5; map Destination.
Remedy: fix the CEL (e.g. `string(variables.tier)`) or pin 1.18.2 with a written reason; re-verify graded/shift-left/render-version-tree under the CI toolchain. Owning ticket: none → new ticket 54.

### Major

**M1. feeds and insurer are mechanically unrunnable and nobody noticed.** GitHub registers zero workflows in both repos (0 runs ever) despite files on the default branch `ecosystem/thin-slice` with Actions enabled; the daily fetch crons have silently missed every slot and cut-release cannot be dispatched — the only route to a first signed tag. Both repos ship zero tags, all feeds bump.yaml say `none`, four adopter subscriptions wait at could-not-look, and driftwood pins the unsigned quote-driftwood v1 against its own party.yaml rule. HANDOFF says "that content is already live" — it is not. Ambition: §2 publisher/insurer rows, §3 principle 5, §4 step 1. Owning ticket: none → new tickets 57 and 58 (pin-untagged decision).

**M2. The signature spine is red in the citable run and the red is misclassified.** verify-source-verification rejects the real signed policy/v3.0.0 fixture with "unable to get local issuer certificate" — a CI trust-chain environment failure (the same bytes verify locally against only the pinned roots) — and verify_gitsign.py wraps every nonzero openssl rc as REJECTED with no could-not-look branch, so an environmental error prints as observed-false. Resolved ticket 41's "ACCEPTS the real tag" is now false on the truth runner. Ambition: §3 principle 6, §4 steps 1/6, §5 three-outcomes rule. Owning ticket: none → new ticket 55.

**M3. verify-publisher-gate structurally cannot finish inside the gate and its timeout leaves no evidence.** Killed at verify-all.sh's default 300s in runs 12 and 13 (truth.yml sets no VERIFY_TIMEOUT); its buffered parts leave a header-only capture, so the red is undiagnosable from the truth surface. The handoff's recorded reason (waiting on policy/v4.0.0) is stale — the tag exists and the check still dies on the clock. Owning ticket: none → new ticket 55.

**M4. verify-corpus-generator's red is a SIGPIPE artifact.** `ls | head -1` under `set -o pipefail` dies on EPIPE in CI; a healthy generator is graded observed-false in both citable runs. One-line fix. Owning ticket: none → new ticket 55.

**M5. Four checks SKIP forever because the clock never installs jsonschema.** truth.yml installs pyyaml only, so verify-e2e-step1, step6, feed-contract and driftwood's twin-overlay hard-skip in every CI run — a could-not-look the observer causes in its own environment. Step 1's fact is done (ico v3.0.0 cut and pinned; passes locally with the venv), yet the citable lane never reaches the tag check, and the handoff's unlock story is stale. Owning ticket: none → new ticket 54.

**M6. verify-schedules' live half can never look from inside the citable run.** The gate step deliberately carries no GitHub credential, so "did each clock run inside its period" SKIPs on every CI run — structurally, even after the unit crons fire. The token withholding is a recorded security decision; its consequence is not, and ticket 28 stands resolved on an assertion the gate cannot make. The surface cannot see whether its off-board observers are alive. Owning ticket: none → new ticket 56.

**M7. Even after the adopter clocks fire, the citable run cannot convert their observations.** verify-reconcile calls need_substrate before its cluster-free five-fact sample grading, so it SKIPs in CI before reading drift/samples.jsonl; verify-e2e-step4 has no sample-reading path. The handoff's "what merging unlocks" is mechanically wrong for the gate. Ticket 40's resolved Answer also cites a four-of-five-facts observation no citable record supports. Owning ticket: none → new ticket 60.

**M8. Step 2 has never happened for real, and the feed pin cannot arrive by Renovate at all.** Every Renovate PR in all three adopters is closed/autoclosed; every landed bump was a hand-made PR. The customManager over inherits[] that ticket 22 deferred to ticket 21 was never written, so threat-register v2 sits unconsumed while all adopters pin v1; and a Renovate PR cannot go green because party.yaml and composed/ must move in the same commit. The step-2 gate check passes on a disclosed offline simulation. Owning ticket: none → new ticket 61.

**M9. Step 3's real mechanism is red on its only real triggers.** propose-tier failed both times it fired (2026-08-25 and 2026-08-31, "no parent tree provided"); the fix merged 17:05Z but no trigger has exercised it; no proposal PR has ever opened; the £ crossed a band only in the SYNTHETIC dry-run. Owning ticket: none → new ticket 60.

**M10. The feed parents are consumed unpinned in every adopter's CI.** ico is checked out at `ref: main` even though signed v3.0.0 exists; feeds and insurer at `ref: ecosystem/thin-slice`; no Flux pin exists for either feed parent; the compensating verify-feed-contract SKIPped in run 13. §2's "pinned, signed dependency" holds for platform and nist only. Owning ticket: none → new ticket 62.

**M11. The four architectural gaps from HANDOFF §"What merging does NOT fix" are orphans.** (1) One declared version leaves coexistence, retirement and shift-left without a subject — and gap 1 already grades red. (2) The conditional-rule arm of principle 1 has no living instance (root-if-attested lived only in retired 2.0.1). (3) Federation has no peer: no second SPIRE trust domain anywhere, and party/schema.json refuses the three decided fields. (4) The observation-lane server-side cage cannot be enforced on public repos and ticket 28's ruleset plan was falsified after resolution. Every ticket touching them is resolved; NORTH-STAR §6 records no decision covering them. Owning ticket: none → new ticket 58 (grilling), then builds.

**M12. The ratified unlabelled-default flip to isolated is unbuilt and unowned.** All 9 served cage-tier bodies still default an ungoverned Namespace to baseline; the stated precondition (infra declared, entitled, asserted green) is fully met; REGRILL answer 28 binds the owner to the strictest-cage default; no open ticket carries the flip. Owning ticket: none → new ticket 63.

**M13. The live map cites a number the truth surface never recorded, and its health claim is false.** map.md's "65 pass, 0 fail, 16 could-not-look ... Nothing is red" appears on no TRUTH line; the newest line says 53/7/21, and at least five reds are unowned. The map breaks the rule it states two paragraphs later. Owning ticket: none → new ticket 67.

**M14. Two §5 bullets have no implementation.** "A fall is a blocking event": nothing compares a TRUTH line to its predecessor, hub main has no protection, and the permanently red workflow saturates the only signal — run 13 fell 54→53 and nothing fired. "Ticket Status: is derived from a named check": 16 of 53 tickets free-type `Status: open`; GAPS 2.9 names the gap with no owner. Owning ticket: none → new ticket 59.

**M15. "One twin per adopter" exists only for driftwood, yet ticket 29 is resolved claiming all three.** tuppence and ludlow carry zero twin files on any branch; no record scopes this down; the gate hardcodes driftwood and cannot see the gap. Owning ticket: none → new ticket 64.

**M16. The twin's release is untagged and unsigned, and driftwood's forward-intel feed is outside every tag.** The hub has zero tags and no release workflow; PIN.yaml says `tag_cut: false`; ticket 29's tag-cut scope closed without the tag; forward-intel sits on main with `bump: none`, unsigned by driftwood's own rule, and no check goes amber naming it. Owning ticket: none → new ticket 64.

**M17. enact_guard admits a push via `--git-dir` — the fixed `-C` hole's sibling.** `git --git-dir=<enactment>/.git push origin main` resolves against the caller's cwd, matches the self-push carve-out, and is ADMITTED (reproduced in-session); `GIT_DIR=` in the environment is the same class. Owning ticket: none → new ticket 65.

**M18. The demo check's on-clock premise names a workflow step that does not exist.** verify-demo grades "committed deck matches this run" on the scheduled run, but truth.yml has no deck-build step and its cage refuses talk/deck.md — so the check reds every scheduled run whose grades moved, and will red again on run 14. Owning ticket: none → new ticket 66.

### Minor (polish, listed for the record)

Silent-PASS-on-missing-kyverno in render-version-tree; render-version-tree's red carries no reason; cosign unpinned on the clock; deck grades stale for steps 2/3/5; step 5 grades file presence not the scheduled forecast; ico's bump.yaml still says `major` post-release; ticket 53 fixed but open and the handoff banner behind reality; platform still ships its own feeds tree; unit observation-lane allow-lists whitelist hub-only paths; map fog list stale; two NORTH-STAR copies disagree on the reversals; ticket 17 partly moot; a pass-to-skip fall would go green; open tickets 38/48 carry stale scope; five-facts falsifier 1 can silently not fire. All folded into tickets 54, 55, 59, 64, 66, 67 and the ticket updates below.

## Refuted claims (do not re-raise)

- "The eco-system has never run as a chain" — the observe-only harness and offline steps are recorded design; the real chain reached signed tags and merged bumps; the break it cites was fixed 42 minutes later and ticket 53 owns the residue.
- "No scheduled clock has ever fired in any org" — schedule fires from the default branch; the clocks landed on main 2026-08-31 and no slot has passed; the hub clock fires daily; eba3569 grades the wait honestly.
- "Ticket 53 is the single blocker and the map hides it" — the push script is fixed, the bundle is on main, v2.0.1 is cut and pinned, and the three adopter PRs merged the same day.
- "Only pods have ladder cages" — the twin, human-access and device gaps are exactly open tickets 30, 27 and recorded GAPS rows; the enact leak was fixed with failing-before tests.
- "Holes are refused, not priced" — that build is open ticket 38's verbatim scope, an owned open ticket, not an orphan.
- "The observation lane is conventional, not enforced" — the public-repo limitation is the project's own amended record (ADR-0024, rulesets README), graded SKIP honestly. (Ownership of the decision remains in new ticket 58.)
- "Forecast scoring machinery does not exist" — twin/scoring.py and twin/forecast_book.py implement it; the scorer party is owned open ticket 46; the grep was scoped to a documented partial vendoring.
- "No live forecast is scored" — the mechanism is built and gate-checked; the remaining decisions are open tickets 51 and 46's own text.
- "17 of 22 decisions rest on a bare agree with no route to ratification" — the provisional convention is a recorded process rule with an explicit reopen-with-a-reason path; no ambition document requires conversion.
- "Three publisher fetch clocks observe themselves and nobody owns converting them" — a recorded, deliberate scope (ADR-0024, ticket 10 D2); the surface downgrades them to SKIP honestly.

## Remediation plan — order of attack

The destination sentence is: run end to end once, on a clock, three orgs consuming. The route, in order: **54** (the gate can go green) → **55** (every red is real or explained) → **57** (feeds/insurer can run and sign) → **61** (step 2 lands by Renovate once) → **60** (steps 3–4 observed on the clock, converted into the number) → **56** (the surface sees its own clocks) → **58** (the owner decides the four gaps + the untagged-pin question) → **62, 63** (pins and the flip, behind 57/58) → **59, 64, 65, 66, 67** (the §5 machinery, the twin widening and tags, the guard hole, the deck check, the record). Ticket details and updates to existing tickets follow in the structured plan.

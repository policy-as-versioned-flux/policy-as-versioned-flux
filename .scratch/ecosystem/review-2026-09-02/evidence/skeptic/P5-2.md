# Skeptic pass on P5-2 — NOT REFUTED (corrected)

Re-derived 2026-09-02 from GitHub and the unit clones. Confidence: high.

## What I re-ran

### 1. The PR population is complete and contains no proposal

```
gh pr list --repo policy-as-versioned-{driftwood,tuppence,ludlow}/<repo> --state all --limit 100 \
  --json number,title,headRefName,author,state,mergedAt
```
- driftwood 22, tuppence 14, ludlow 12 = **48**. Numbering is contiguous 1..22 / 1..14 / 1..12
  (checked), so this is the complete history — no proposal PR was ever opened and lost.
- Filtering titles and head refs for `retune|war-?gamer|tier` returns exactly three rows, all the
  same human merge:
  ```
  driftwood #6 | policy-composition/tickets-09-16-wip | policy-composition: party artefact, governed namespace, propose-tier workflow (tickets 11, 17)
  tuppence #5 | policy-composition/tickets-09-16-wip | (same title)
  ludlow   #4 | policy-composition/tickets-09-16-wip | (same title)
  ```
  `gh pr view` on each: `author=chrisns`, `mergedBy=chrisns`, merged 2026-08-25T13:30Z. The
  auditor's attribution is exact.

### 2. No `wargamer/retune-*` ref exists anywhere

The proposer's branch name is `f"wargamer/retune-{slug}"` — `platform/wargamer/wargamer.py:195` and
`:227`. `gh api repos/policy-as-versioned-<org>/<repo>/branches` for driftwood, tuppence, ludlow and
platform lists 11/8/7/11 branches; none begins `wargamer/`.

### 3. Ticket 60's quote is verbatim

`.scratch/ecosystem/issues/60-the-scheduled-observations-land-in-the-citable-number-a.md:28`:
"the proposer returned `[]` — no band crossed, no proposal PR, ledger derived from 0
closed-unmerged proposal PRs." Line ~86 records the same for the 12:01Z scheduled firing.

### 4. Ownership is real

`.scratch/ecosystem/issues/74-step-3-happens-once-for-real.md` — `Status: open`, `Blocked by: none`,
"Done = one proposal PR opened by the clock on a real crossing, merged by a human". Matches.

## Corrections the finding needs

**(a) The arm runs; it has never fired — and on two of three adopters it never even runs.**
`gh run list --workflow propose-tier.yml`:
- driftwood: `33505428933` schedule **success** 2026-09-01T12:01Z; `33626099181` schedule
  **success** 2026-09-02T11:43Z (after the citable run 21 at 10:11Z). The 11:43Z log ends
  `[]` — a second `[]`, previously unrecorded on any ticket.
- tuppence: `33515059588` (09-01) and `33633036907` (09-02), both schedule **failure**.
- ludlow: `33518603510` (09-01) and `33638327430` (09-02), both schedule **failure**.
- `gh run view 33633036907 --log-failed`: `##[error]A branch or tag with the name
  'ecosystem/thin-slice' could not be found` at the feeds checkout. Source:
  `tuppence/.github/workflows/propose-tier.yml:182,189` and `ludlow/...:185,192` pin
  `ref: ecosystem/thin-slice` for feeds and insurer — a branch deleted from those repos. Steps 12
  and 13 (re-compose, run the proposer) are `skipped`.
  Ticket 74's Notes already say this, deferring to ticket 62.

So "has never run" is loose. Precise: the proposer runs daily on driftwood and has twice
correctly proposed nothing; on tuppence and ludlow it has never reached the proposer step at all.

**(b) It is stronger than the finding says: no machine-opened PR of any kind has ever been merged
in an adopter repo.** Of the 48, 24 are merged and **all 24 are authored by `chrisns`**; the 13
bot-authored (`app/renovate`) PRs are **all CLOSED, 0 merged** (tallied in one pass over the three
repos). Even driftwood #20, the one feed-pin bump that landed, was human-opened — its body:
"Renovate created the branch but its PR-creation call errored (prBlockedBy: Error), so this PR was
opened manually from the unmodified branch head ea1c8db." So the "**proposals open**, a human
merges" half has never happened for *any* proposer, not only for the tier proposer.

**(c) The fit-impact sentence overstates two of the three downstream items.**
- The **rejection ledger** is not untested on real input: driftwood run `33626099181` logged
  `note: ledger derived from 0 closed-unmerged proposal PR(s) on
  policy-as-versioned-driftwood/driftwood; 0 key(s) carry weight; half-life 30d, suppress at 0.5
  (rejection-decay.yaml 1.0.0).` The live GitHub-derived query path ran and reported honestly. What
  is unexercised is the **decay/suppress arithmetic against a real rejection** (only the offline
  selfcheck, `tier_pr.py:638-642`, exercises it).
- The **human-merge attestation** does not exist to be untested. `grep -rniE
  "attest.{0,40}(human|merge)|(human|merge).{0,40}attest"` over all eight unit clones returns only
  device/WebAuthn hits; the only "human merges" strings are prose (`tier_pr.py:19`,
  `propose-tier.yml:32`, `twin-sweep.yml:6,19`, `shift-left.yml:183`). It is unbuilt, not untested.
- The **proposal's signature** is genuinely untested and also unwired — that is P5-3's territory,
  which I did not re-derive here.

## Verdict

The claim as stated survives on primary evidence. Corrected wording below.

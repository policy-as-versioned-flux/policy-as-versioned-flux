# Skeptic re-derivation: EQ-04 — NOT REFUTED (confirmed, with corrections)

## What I re-ran (primary sources only)

1. `gh api ".../actions/workflows/twin.yml/runs?per_page=1" --jq .total_count` -> `25`
   `gh run list --workflow=twin.yml --limit 100 --json ...` -> 25 rows, `Counter({'failure': 25})`,
   oldest 2026-08-16T09:02:11Z, newest 2026-09-02T09:36:29Z (id 33615039125, head 7b92990).
   So: all 25 runs the API knows of are red, not merely "the pulled window".

2. `gh run view 33615039125 --json jobs` -> 8 jobs:
   tests=failure, invariants=failure, typecheck=failure;
   demo, reproduce-elsewhere, determinism x3 = success. Head SHA 7b92990 == TRUTH run 21 hub commit.

3. `gh run view 33615039125 --log-failed`:
   - typecheck: `twin/feed_signal.py:232: error: Unused "type: ignore" comment  [unused-ignore]`
     / `Found 1 error in 1 file (checked 159 source files)`
   - invariants: ` 44  FAIL  flux_coverage_floor_is_still_reachable ... **This guard staying red is
     the finding, not a defect in it** — see build ticket 70's finding 1`; `RESULT: 70 passed, 1 failed`
   - tests: `FAILED tests/test_invariant_suite.py::test_the_suite_is_green` on the same invariant 44;
     `1 failed, 1550 passed`. Same root cause as invariants — auditor's reading is correct.

4. Local repro, hub checkout at 7b92990:
   `.venv/bin/python -m mypy --version` -> `mypy 1.14.1`
   `.venv/bin/python -m mypy twin tests conftest.py --ignore-missing-imports --warn-unused-ignores`
   -> identical single error.

5. The one-line-fix claim, verified by experiment (copy of twin/ tests/ conftest.py into a
   scratchpad probe dir, single line edited there, repo untouched):
   line 232 is `            signal_for(quote, **missing)  # type: ignore[arg-type]`
   with the comment removed: `Success: no issues found in 159 source files`.
   Nothing else needed. One line.

6. Not a toolchain-drift instrument fault: `.github/workflows/twin.yml:51` pins
   `'mypy==1.14.1'` in every job's pip install, and there is no mypy.ini / setup.cfg /
   pyproject.toml in the hub (only pytest.ini). CLI flags are the whole config.

## Corrections / sharpenings the auditor did not have

- **The typecheck red is dateable to a boundary, and it is 10 runs, not 25.** Per-run job
  conclusions: typecheck was `success` on 33306564420 (2026-08-30T10:28, 6e495ec),
  33250632297, 33194645405, 33140590262 (2026-08-28T04:01, first run after the job split), and
  `failure` on every run from 33370552516 (2026-08-31T07:55, head 27d7d5c) to 33615039125.
  Before 2026-08-28 the workflow was not split into named jobs, so no typecheck row exists.
- **The offending line arrived with the file.** `git log --diff-filter=A -- twin/feed_signal.py`
  -> only commit `0046da3 2026-08-29 06:26:07 +0100 ecosystem ticket 50: the twin's feed-signal
  lookup and the headline skill`. `git cat-file -e 6e495ec:twin/feed_signal.py` -> absent at the
  last green typecheck run; `git merge-base --is-ancestor 0046da3 6e495ec` -> not an ancestor.
  So the file (and its dead ignore) reached the CI branch between 2026-08-30 and 2026-08-31.
- **"Owned by nobody" is verified, not asserted.** `grep -rn "Found 1 error|Unused \"type:
  ignore\"|unused-ignore" --include="*.md" .scratch/ docs/ talk/` -> no match. `grep -rn
  "feed_signal|typecheck" .scratch/ecosystem/ docs/` -> only two incidental mentions
  (`issues/11-...:30` listing the job names, `issues/03-the-truth-surface.md:35` recording the
  first split run as "typecheck ... green"). No ticket, guard or capture carries the red.
- **Disambiguate ticket 70.** The owning ticket is the *build* ticket
  `.scratch/twin/build/70-full-system-coherence-confirmatory.md` (finding 1 at :19), not the
  ecosystem ticket `.scratch/ecosystem/issues/70-the-observation-lane-...md`. Both exist.
- **"Sole pytest/mypy gate" survives a wider sweep than the auditor ran.** No `pytest|mypy|
  unittest` string in any workflow of the eight unit repos (fresh clones), in the hub's
  `truth.yml`, in `talk/verify-all.sh` / `verify-demo.sh`, in any of the 12 hub `verify/`
  scripts, nor in the legacy org repos' workflows I could read
  (c2p-collector/verify.yml, fleet/pr-gate.yml, policy/release.yml — all clean;
  governance-agent, handbook-generator, pr-gate-action return 404 for
  `.github/workflows`, so they have none). Also no Python test file exists in any unit repo
  (`find ... -name "test_*.py" -o -name conftest.py` -> nothing).
- **M14 quote is real:** `.scratch/ecosystem/REVIEW-2026-08-31.md:75` — "the permanently red
  workflow saturates the only signal". Ticket 59 `Status: open` (:4); its clause (a) is exactly
  the fall-blocking mechanism. Build ticket 70 and ecosystem ticket 70 are both open.

## Verdict
Not refuted. Every quoted string in the finding reproduced verbatim from the primary source.
The one claim that needed narrowing is chronological, and narrowing it strengthens rather than
weakens the point: the typecheck red is 2 days and 10 runs old, arrived alone, and sat inside a
workflow that had already been red for 15 straight runs — which is precisely the saturation
M14 named.

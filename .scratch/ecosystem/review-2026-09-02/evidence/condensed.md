# thesis-fidelity

VERDICT: Partly fit, and unevenly so. The *mechanism* half of the thesis — policy as a signed, semver, pinned dependency, composed from parents and distributed by Flux — is genuinely built, machine-checked on the citable run (verify-feed-contract PASSes 24 pin assertions against real remotes), observed live in an ephemeral KinD cluster on the adopters' clock, and in one respect exceeds the thesis: semver is computed from measured verdict movement and a weaker declaration is refused, which the 2022 post never asked for. The *doctrinal* half is where fitness breaks. (a) The one requirement the thesis calls non-negotiable — at least three coexisting versions with a retirement window — stands at ONE declared version; coexistence, retirement and shift-left all SKIP on run 21 from that single root cause; the gate's own bar is two, not three; the owned remedy (tickets 58 Q1 / 63) also reaches only two; and the retirement window has no mechanism at all — the DECIDED replacement (price a stale pin by the EOL ramp, open a retirement PR) is unimplemented in both halves, so the only retirement the estate ever performed was a flag day. (b) The mea culpa's locked-door half is demonstrated by verify/proportionality — the best single piece of thesis fidelity in the estate, deriving Audit-vs-Deny from each party's own signed £ band — but its control, encrypt-at-rest, exists ONLY in the hub's harness, in no unit repo, under no tag, pinned by nobody; and no policy anywhere in the estate governs access control, data protection or key management, the three the mea culpa names. Every Deny that ships is a meta-rule about the policy-version claim. (c) The human-governance layer's carrier is absent: three accepted, unsuperseded ADRs and CONTEXT.md require created/lastReviewed/rationale on every policy version, and there are zero such annotations, zero rationale.md, zero lastReviewed across all eight units — nothing is dated, reviewed on a cadence, or removed if undefended. (d) The last mile is behind the 2022 org: zero "handbook" hits in any unit. What would make it fit: declare a second and third policy line so the crux beat has a subject; implement the DECIDED supersede-pricing and the retirement proposal so a window exists; publish encrypt-at-rest (or another real data-protection control) as a versioned member of platform's line so the locked-door half is a dependency like everything else; land the ADR-0007 metadata (or amend ADR-0007 to say the £ replaced it); and add a customManager over policy/v* so a policy version can arrive as a Renovate PR at all.

STRENGTHS:
- The dependency graph is real and machine-checked against the real remotes on the citable run: talk/captures/verify_feed-contract_verify-feed-contract.out (run 21) ends PASS after 24 individual pin assertions across eight repos, e.g. 'PASS: driftwood pins feeds/feed/threat-register@v2: tag threat-reg
- Semver is COMPUTED, not merely declared — an advance beyond the thesis. platform/computed-semver/ (7,524 lines of .py/.sh, the largest module in platform) derives the bump from measured verdict movement over a generated corpus; versions.yaml:79 carries a reviewed 'bump: "major"' field and cut-releas
- The mea culpa's proportionality split is turned into a computation rather than an opinion. verify/proportionality/verify-proportionality.sh PASSes on run 21: 'risk_bought £21,107 | driftwood band £40,000 -> Audit | ludlow band £5,000 -> Deny ... ok committed policies match the £-derived render'. The
- 'Informed debate through pull requests rather than exemption requests' (research/03:79-88) is enforced structurally. grep -rn 'exemption' across all eight units returns 20 hits and every one is a negation or a reference to a deleted mechanism (compose/composition.py:36 'not an exemption: it is a DEC
- 'Usable' and 'testable' are exercised on every adopter PR, not asserted. driftwood/.github/workflows/shift-left.yml reads the platform pin under review, checks platform out AT that tag, verifies the commit against the pin, runs the pinned kyverno CLI via platform/shift-left/ci-check.py against drift
- The updatable loop completed once for real and is graded from the PR record rather than simulated. verify-renovate-merged-feed-pr PASSes on run 21: 'driftwood #20: Renovate raised threat-register v1 -> v2, Chris Nesbitt-Smith merged it, and party.yaml and composed/ moved together'. Independently con
- Live, cross-org distribution fidelity IS observed on a clock and imported into the citable number. driftwood/.github/workflows/drift-sample.yml:127 runs 'kind create cluster --name ${name} --wait 120s' on a fresh ephemeral cluster, installs checksum-pinned kyverno/flux-operator/flux, reconciles from
- The estate refuses to fake a subject it no longer has. With one declared version, verify-coexistence.sh:35-45 explicitly declines to loop a one-element array ('looping a one-element array to claim coexistence would be the false pass this project forbids. Do not invent a second version to keep the be
- A real, original human-governance mechanism exists that the 2022 work had no equivalent of: platform/wargamer/rejection_ledger.py derives proposal suppression from closed, unmerged PRs with a half-life ('suppress while sum(0.5 ** (age_days / h)) >= reject_suppress'), keyed <org>/<kind>/<slug>, refus

## [critical] TF-01 The estate cannot demonstrate multi-version coexistence at all today, and both the gate's threshold and the only planned remedy settle for a bar below the one the thesis and CONTEXT publish
CLAIM: The thesis's one explicitly non-negotiable requirement — a runtime serving ≥3 semver policy versions concurrently (research/03-blogs-thesis.md:40, :172, :207; restated in CONTEXT.md:153-155 as "(≥3) ... *The crux of the original implementation*", never relaxed by any ADR or owner decision) — is met at one. platform/distribution/versions.yaml:77 holds the array's sole element, `{ version: "4.0.0", tag: "policy/v4.0.0", commit: "64635dfd...", bump: "major" }`, and it is the estate's only versions.yaml; driftwood's composed artefact carries only policies/v4.0.0.

At one declared version, four beats go SKIP on run 21 rather than proving anything live: coexistence (verify-coexistence.sh:36, thres
EVIDENCE: platform/distribution/versions.yaml:79 is the sole array element: `- { version: "4.0.0", tag: "policy/v4.0.0", commit: "64635df...", bump: "major" }`. platform/distribution/verify-coexistence.sh:35 reads `if [ "$n_versions" -lt 2 ]; then ... live_tail_skip`. Run 21 captures (git show origin/main:talk/captures/...): `.estate-clone_platform_distribution_verify-coexistence.out` ends `SKIP ... declares one version (4.0.0); coexistence needs two declared versions to show side by side, and retirement 
OWNER: owned-by-open-ticket issues/58 (resolved, PROVISIONAL) -> issues/63 (open)
REMEDY: Either declare three lines (ticket 63's isolated flip plus one more, e.g. re-issuing a Priority-triple-repaired 3.x line) and raise verify-coexistence.sh's threshold from 2 to 3 so the check matches CONTEXT.md; or the owner amends CONTEXT.md:153-155 and NORTH-STAR to say two is the estate's bar and records why the thesis's three was dropped.

## [critical] TF-02 Policy cannot be retired the way a dependency is: one version runs, falling behind costs nothing and nothing proposes moving — the thesis's transition window is a regression against the 2022 org that had it working
CLAIM: The thesis's stated reason for multi-version coexistence is a transition window (research/03-blogs-thesis.md:40-41; CONTEXT.md:153-155). ADR-0010's consumer-side `sunset:` was retired in favour of ticket 13 Answer item 5 (`.scratch/ecosystem/issues/13-lift-or-retire-the-original-mechanisms.md:83`, DECIDED, therefore binding): "a pin behind a newer published version is priced by the existing EOL ramp ... the adopter's scheduled proposer reads pins against the feed and opens a retirement PR". Neither half is built. The pricing half is not merely unbuilt but architecturally blocked where it would live: `eol_ramp`/`eol_scenario` (platform/feeds/to_fair_scenario.py:74,92) have no caller outside t
EVIDENCE: grep -rn 'sunset' across all eight unit clones returns two prose comments only (platform/feeds/to_fair_scenario.py:17, platform/feeds/README.md:70) — no field, no code. The EOL ramp exists (platform/feeds/to_fair_scenario.py:74 `def eol_ramp`) but platform/feeds/eol/v2/eol-feed.json's `components` are python-3.9, ubuntu-20.04, istio-1.18, kyverno-1.10 — no policy version is an entry. platform/compose/composition.py:1952-1967 (`compute_prices`) iterates only edges whose kind is in FEED_KINDS; dri
OWNER: duplicate-of-0831-finding-still-open REVIEW-2026-08-31.md M11 (retirement clause)
REMEDY: Smallest honest fix: add the platform policy line to the eol feed (or an equivalent `supersedes: {version, published}` on the newer release element) and extend compute_prices to price a parent pin behind the newest published version; then teach the adopter's scheduled proposer to emit a retirement-kind proposal on the key rejection_ledger already reserves. Alternatively the owner records that the 

## [major] TF-03 The £ never selects the enforcement action in any shipped artefact: proportionality is proven only in a hub bench rig, while every Deny an adopter actually pins was hand-authored upstream
CLAIM: encrypt-at-rest — the only control for which the £-to-enforcement-action render produces an actual Kyverno policy body — exists solely as hub-side fixtures under verify/proportionality/ and appears in zero unit repositories: it is in no party's publishes[] (platform publishes only distribution/policies and identity), in no composed/ tree, under no signed tag (the hub carries no tags at all, and no platform tag tree contains a file matching 'encrypt'), is not distributed by Flux and is pinned by no adopter. verify-proportionality.sh:59 proves it by re-checking hub-committed renders (`render.py --check`), and it passes in run 21.

Two corrections to the surrounding framing. (1) It does not car
EVIDENCE: Files: verify/proportionality/control/encrypt-at-rest.tmpl.yaml, verify/proportionality/policies/encrypt-at-rest-{driftwood,ludlow}.yaml, verify/proportionality/scenarios/encrypt-at-rest.json, verify/proportionality/tests/encrypt-at-rest/. `grep -rn 'encrypt-at-rest'` across scratchpad/units/{platform,nist,ico,driftwood,tuppence,ludlow,feeds,insurer} returns zero hits. verify-proportionality.sh:59 checks the committed fixtures with `python3 "$HERE/render.py" --check` — i.e. the policies are hub-
OWNER: new TF-03
REMEDY: Publish encrypt-at-rest as a real member of platform's version tree (it already has a template, a scenario and kyverno tests), let composition render it per adopter from each party's own appetite band, and have verify-proportionality read the composed artefacts instead of hub-side fixtures. If the owner prefers it stay a harness demonstration, say so in ADR form so a reader is not misled by the ca

## [critical] TF-05 The estate's replacement for the thesis's locked door — that the bottom rung runs and reaches nothing — has never been observed by the only surface the project permits itself to cite, so "a platform enacts priced cages" is an unciteable claim with no ticket that would make it citable
CLAIM: The beat "the bottom rung runs and reaches nothing" — the estate's replacement for the mea culpa's locked door — has never been graded PASS on any of the 18 TRUTH runs recorded in talk/truth.log, and cannot be while the citable gate has no cluster. .estate-clone/platform/graded/verify-graded.sh has never exited 0: it FAILed on runs 9, 12 and 13 (offline core, the kyverno-1.19 instrument fault of ticket 54) and has exited 3 SKIP from run 15 through run 21, verified on run 21 from the workflow log itself (gh run view 33616685427 --log: "SKIP  SKIP: offline proof holds; live tail could not look: kind cluster 'driftwood' is not listed"). The cause is structural: no version of .github/workflows/t
EVIDENCE: talk/captures/.estate-clone_platform_graded_verify-graded.out on origin/main ends `SKIP: offline proof holds; live tail could not look: kind cluster 'driftwood' is not listed by kind get clusters — the Namespace declares the tier and the pod wears it; the cage only tightens; the bottom rung runs and reaches nothing; TCoR booked`. `git log --format='%H %ci' origin/main -- talk/captures/.estate-clone_platform_graded_verify-graded.out` shows the file last changed at f3b87c7, 2026-08-31 20:41 — byte
OWNER: new none
REMEDY: Either extend the adopters' existing ephemeral-cluster lane (drift-sample.yml already builds one correctly) with two more facts — 'a pod at the isolated rung is admitted' and 'it reaches nothing' — so cage behaviour enters the citable number the same way distribution fidelity already does; or record in ADR form that cage behaviour is proven only offline and say so on the deck.

OWNER QUESTIONS:
- Is '≥3 coexisting versions with a retirement window' still a requirement you hold the estate to, or did the 2026-08-29 retirement quietly retire the requirement along with the three versions? If it still holds, what is the second and third line — and if it does not, what replaces the transition wind | CHANGES: If three: tickets 63 and 58 need a sibling ticket for the third line, verify-coexistence.sh's threshold moves from 2 to 3, and TF-01/TF-11's shortfalls become owned. If two: CONTEXT.md:153-155 and the
- Ticket 13 item 5 is one of only five DECIDED items in the tracker and says a pin behind a newer published version is priced by the EOL ramp and that the adopter's proposer opens a retirement PR. Neither exists in code. Do you still want it built, or did the decision lapse? | CHANGES: If built: a new build ticket lands two changes — a policy-line entry (or a supersedes field) that compute_prices can read, and a retirement-kind proposal on the key rejection_ledger already reserves —
- The mea culpa names access control, data protection and cryptographic key management as the things that belong behind a locked door, and the estate ships no policy in any of the three; the one control that demonstrates the split (encrypt-at-rest) lives only in the hub's verification harness. Should  | CHANGES: If published: platform's version tree gains a member, composition renders it per adopter from each party's own appetite band, verify-proportionality reads composed artefacts instead of fixtures, and t
- ADR-0007, ADR-0006 and ADR-0001 all require every policy version to carry created/lastReviewed/rationale as annotations plus a versioned rationale.md, and none of it exists anywhere. Did the £ replace the review metadata — a price re-derived on a clock instead of a review date — or is the metadata s | CHANGES: If the £ replaced it: ADR-0007 gains a dated addendum, CONTEXT.md:127-128's definition is removed, and TF-06 becomes a documentation fix. If still owed: a build ticket adds the annotations to the gene
- The twin and its test suite are 53,301 lines — 54% of the estate's executable code — against ~14,600 (15%) implementing the versioned-dependency thesis, and 23% of tickets versus 35% for pricing/twin/insurance/feeds. Is the thesis still the subject of this estate, or is it now one participant in an  | CHANGES: If the thesis is still the subject: the dependency-thesis tickets (TF-01, TF-02, TF-08) outrank the twin's open tickets in the remediation order, and the PRD's framing stands. If the eco-system is the
- No citable run has ever observed a policy denying or caging anything, because .github/workflows/truth.yml creates no cluster and verify-graded has SKIPped on every scheduled run since 2026-08-31. The adopters' drift-sample lane does build a real ephemeral KinD cluster, but its five facts are all dis | CHANGES: If yes: two facts are added to drift/five-facts.py (a pod at the isolated rung is admitted; it reaches nothing), the doctrinal claim enters the citable number the same way distribution fidelity alread

# participants

VERDICT: Half the §2 clause is genuinely built and the other half is not. "Each participant lives in its own GitHub organisation" and "signs its own artefacts" hold: eight of nine rows are single-repo orgs (the ninth, the twin, has no org at all — `gh api orgs/policy-as-versioned-twin` is a 404), and all 24 tags across the eight units verify against Rekor under gitsign, each identity-pinned to that repo's own cut-release.yml OIDC subject, with no exception. "Ships on its own cadence" half-holds: nine clocks exist and are registered, but feeds' and insurer's have failed every scheduled run they have ever had, tuppence's and ludlow's propose-tier fail at checkout every day, driftwood's twin-sweep fails every day, and nist's succeeds while writing null. "Consumed only through a pinned, signed dependency" does not hold: exactly two of the five parent edges (platform, nist) carry a real tag+commit Flux pin — and those two are exactly correct, which shows the pattern is understood — while ico, feeds and insurer are consumed with no pin at all, at a moving `main` in driftwood and at a branch that no longer exists in tuppence and ludlow; three publisher release gates load the platform's default branch as their instrument; and the truth surface itself clones every unit's default branch rather than its now-existing signed tag. "No participant reaches into another" fails in one structural place: driftwood's twin-sweep runs inside `hub/.estate-clone/driftwood` because the twin has no tag to pin. The sharpest single defect is not coupling but provenance: the insurer's gitsign-verified tag v1.0.0 publishes quotes asserting they were priced against `driftwood/tuppence/ludlow exposure v1.1.0`, and no adopter's v1.1.0 tag contains an `exposure` section at all — two of the three digests match the adopters' unsigned working trees and driftwood's matches nothing, while the insurer's own clock has REFUSED on all three legs on both of its runs. To be fit, this dimension needs, in order: a signed adopter tag that actually carries the exposure section (or a commit-level pin) so the insurer's claim becomes true; ticket 62's twelve stale refs closed; ico/feeds/insurer given real tag+commit pins including a Flux source; the publisher release gates and clone-estate.sh moved onto tags; and a twin tag so driftwood can stop being a tenant of the hub's directory layout.

STRENGTHS:
- Signing is real and I re-derived it: 24 of 24 tags across all eight unit repos return `Good signature from [.../cut-release.yml@refs/heads/main]` and `Validated Rekor entry: true` under `git -c gpg.format=x509 -c gpg.x509.program=gitsign tag -v`. No unsigned tag, no bad signature, anywhere. This is 
- Eight of the nine §2 rows are their own single-repo GitHub organisation, confirmed live: `gh api orgs/policy-as-versioned-<u>` returns repos=1 for platform, nist, ico, driftwood, tuppence, ludlow, feeds and insurer (only `policy-as-versioned-twin` 404s).
- Every pin that exists is exactly correct. I resolved each against the real tag: platform v2.0.1 -> 533dccb0a823001b396fd60ab08014bf75065a37 (matches all three adopters' gitops/platform/platform-pin.yaml); nist v1.1.0 -> 33a05df1f5241bca6ffbc1c69a70075cdb7a5819 (matches all three gotk-sync-nist.yaml)
- One participant genuinely carries a real external datum: nist's catalogue is the verbatim NIST SP 800-53 rev 5.2.0 OSCAL file; `scripts/verify_catalog.py` re-derives 1196 controls across 20 groups and the on-disk sha256 matches CATALOG_VERSION.json exactly.
- Refusal-over-guessing is implemented in participants' own code and observable under live load: insurer/pricing/quote.py exits `REFUSED: missing instrument: ... carries no `exposure` section -- there is no signed exposure to attach a layer to` (gh run view 33615860064) rather than emitting a number, 
- The estate records its own defects in-tree with dates: insurer/party.yaml documents the fabricated-v1.2.0 laundering found 2026-08-29; tuppence's and ludlow's gotk-sync.yaml explain why `./apps` never becomes Ready; driftwood/gitops/apps/pod.yaml records the runAsNonRoot-on-nginx kubelet defect and 
- The worst live coupling breach is already owned and accurately diagnosed: ticket 62's 2026-09-01 comment independently names the same twelve `ecosystem/thin-slice` checkouts my own grep counted, and correctly notes the feeds half is now unblocked because feeds cut threat-register/v1.0.0 and v2.0.0.

## [critical] P1 The signature stops being evidence: a gitsign-signed quote and a signed adopter balance sheet both attest a priced exposure that exists at no signed version — the second shipment of the same laundering class
CLAIM: The insurer has never priced a quote from a signed exposure; its signed tag v1.0.0 asserts a provenance that has never existed.

Every published insurer quote names `<adopter> exposure v1.1.0` plus an `exposure_sha256` as its provenance (insurer party.yaml:51-53; quote/{driftwood,tuppence,ludlow}/v1/feed.json). No adopter's v1.1.0 tag tree contains an `exposure` section — the string does not occur anywhere in the tuppence or ludlow v1.1.0 trees, and in driftwood's only as prose in drift/verdict.yaml:8. Nor could it: v1.1.0 was cut on 2026-08-25 18:06 and the exposure section first landed on driftwood's main on 2026-08-29 02:19. The adopters' v1.1.0 party.yaml also carries no `publishes:` key
EVIDENCE: insurer/party.yaml:51-53 pins `{party: driftwood|tuppence|ludlow, kind: feed, name: exposure, version: "v1.1.0"}`. `git -C <adopter> show v1.1.0:composed/HEADER.yaml` yields top-level keys `policy-as-versioned.dev/composed, parents, baseline, governed-namespaces, holes, selected-controls, ungoverned-namespaces` and no `exposure`, for all three; each adopter has only tags v1.0.0 and v1.1.0. `git -C insurer show v1.0.0:quote/driftwood/v1/feed.json` carries `priced_against ... {"party":"driftwood",
OWNER: new none
REMEDY: Cut a signed adopter tag whose tree carries the exposure section (or move the pin from a tag to a commit), then re-run the requote so a quote is produced from something signed. Independently: make quote.py refuse to emit a `priced_against` entry naming a tag whose tree lacks the named section, and add a gate check that resolves every `priced_against` pin against the tag it names. Add a dated corre

## [major] P2 Two of the three named adopters cannot consume their publishers at all: a deleted branch ref kills compose, propose, shift-left and release on the clock, so §2's "ships on its own cadence" and §4's steps 2-3 hold for driftwood alone
CLAIM: Twelve cross-participant checkouts in tuppence and ludlow name `ecosystem/thin-slice`, a branch that exists on neither feeds nor insurer since ticket 57 renamed their default branch on 2026-09-01, so two of the three adopters cannot compose, cannot run their scheduled tier proposal, and cannot cut a release. The twelve sites are {tuppence, ludlow} x {propose-tier.yml, shift-left.yml, cut-release.yml} x {feeds, insurer} (tuppence propose-tier.yml:182,189, shift-left.yml:360,367, cut-release.yml:97,104; ludlow 185,192 / 354,361 / 97,104), and they are present on both repos' live main (tuppence 4cc958a, ludlow 880868c, both 2026-09-02). Observed live: tuppence propose-tier run 33633036907 (sche
EVIDENCE: grep of `ref: ecosystem/thin-slice` gives 12 sites: {tuppence,ludlow} x {propose-tier.yml, cut-release.yml, shift-left.yml} x {feeds, insurer} (e.g. tuppence/.github/workflows/propose-tier.yml:182,189; shift-left.yml:360,367; cut-release.yml:97,104). `gh api repos/policy-as-versioned-feeds/feeds/branches --jq '.[].name'` -> fetch/cve-2.0.1, fetch/eol-2.0.1, fetch/fx-1.1.0, fetch/threat-register-2.0.1, main, observations; insurer -> main, observations. Live: tuppence propose-tier run 33633036907 
OWNER: owned-by-open-ticket ticket 62
REMEDY: Re-point all twelve refs to the tag+commit party.yaml declares (feeds threat-register/v2.0.0 is available now; insurer v1.0.0 is available now), and add the branch-ref refusal check ticket 62 already specifies so a composing job cannot regress to a branch ref.

## [major] P3 The silent-change failure the thesis exists to prevent is live on three of five parent edges: driftwood's daily price is derived from unpinned, unsigned branch tips while its signed party.yaml declares pinned versions
CLAIM: Three of driftwood's five parent edges — ico, feeds and insurer — are consumed by ref, not by pin. There is no Flux GitRepository for any of the three in any adopter (PIN_FILES holds only nist/controls and platform/implementations), and party.yaml records only a bare floating-major version string ("v3", "v2", "v1") with no commit. That version string is not inert: verify-feed-contract resolves it to a real tag on the publisher's remote and PASSED for all three in run 21 (ico v3.0.0, feeds threat-register/v2.0.0, insurer v1.0.0), and step 6 verified those tags' gitsign certs and Rekor entries. What is missing is any binding of the consumed bytes to that verified tag: driftwood checks all thre
EVIDENCE: `grep -rn 'kind: GitRepository' {driftwood,tuppence,ludlow}/gitops` yields sources only for self, platform and nist — no ico, feeds or insurer object anywhere. party.yaml records only `version: "v3"` (ico) and `version: "v2"`/`"v1"` (feeds) — no tag, no commit. driftwood consumes all three at a moving default branch in nine places: propose-tier.yml:129,144,151; cut-release.yml:83,97,104; shift-left.yml:343,363,370, each `ref: main`. ico has a signed v3.0.0 (gitsign-verified) that driftwood names
OWNER: owned-by-open-ticket ticket 62
REMEDY: Move the three checkouts to the tag+commit party.yaml declares, add an ico Flux GitRepository alongside the nist one (same gitsign-identity annotation pattern), and land ticket 62's branch-ref refusal so this cannot silently return.

## [major] P4 A publisher's signature attests a release gate nobody can reconstruct: ico, insurer and feeds decide what they may assert using whatever platform main said that day
CLAIM: Three publisher release gates load the platform from its moving default branch, so what a publisher may assert depends on unpinned platform main.

ico's, insurer's and feeds' release.yml each check out policy-as-versioned-platform/platform with no `ref:` (ico/.github/workflows/release.yml:59-62, insurer/.github/workflows/release.yml:60-63, feeds/.github/workflows/release.yml:50-53), which under actions/checkout means that repository's moving default branch. Those checkouts supply the code and schema the gates decide on: ico/verify-penalty-feed.sh:21 sets `fair="$here/../platform/fair/fair.py"`, hard-fails at :29 if it is missing, executes it at :55-56 and :71-72, and asserts on its numeric o
EVIDENCE: ico/.github/workflows/release.yml:60-62, insurer/.github/workflows/release.yml:61-63 and feeds/.github/workflows/release.yml:50-53 each specify `repository: policy-as-versioned-platform/platform` with a `path:` and no `ref:`. The gates depend on that tree: ico/verify-penalty-feed.sh:21 `fair="$here/../platform/fair/fair.py"`; feeds/verify-feeds.sh:25-29 resolves `platform/feeds/schema.json` (the ADR-0019 envelope every feed is validated against); insurer/verify-insurer-party.sh:83 `checker="${PL
OWNER: new none
REMEDY: Give each publisher's release.yml the same `ref:` its own cut-release.yml or gitops platform pin already names (feeds already has v2.0.1 to copy), and extend ticket 62's branch-ref refusal check to publisher workflows, not only adopter ones.

## [major] P5 The only citable instrument grades each participant's unsigned moving tip, not the signed version its own Flux sources pin — so a green says the working branches work, not that the released eco-system does
CLAIM: The truth surface grades the estate from each unit's *default branch*, not from the signed tags the estate's own release discipline produces, and the decision to pin was deferred on a premise that has since expired.

`talk/verify-all.sh:32` unconditionally runs `clone-estate.sh`, whose line 57 clones each unit with no ref (`git clone --quiet "https://github.com/$org/$u" "$dir"`), and whose lines 37-39 still say "No signed tag exists yet (ticket 09/12: known, accepted partial state) ... Once a signed v1.0.0 lands, pin it here (--branch v1.0.0)". That premise is false: six units cut their first signed tag on 2026-08-21 — the same day the comment was authored (c9d0f20) — and feeds and insurer o
EVIDENCE: /Users/cns/httpdocs/controlplane/policy-as-versioned-flux/clone-estate.sh:37-39 reads '# No signed tag exists yet (ticket 09/12: known, accepted partial state) so # this clones the default branch. Once a signed v1.0.0 lands, pin it here # (--branch v1.0.0) so the offline harness matches what Flux actually runs.' and line 57 is `git clone --quiet "https://github.com/$org/$u" "$dir"` with no --branch. All 24 tags across the eight units verify (gitsign, Rekor). The TRUTH run=21 line's units=[...] a
OWNER: new none
REMEDY: Either pin clone-estate.sh to each unit's newest signed tag (the script's own stated plan) and record the tag in the TRUTH line beside the SHA, or state explicitly in NORTH-STAR §5 that the surface grades default-branch state and add a second, tag-pinned lane for the signed claim.

## [major] P6 The twin is unattestable: an unsigned, untagged artefact sets a £1.9m priced line and the cage tier in driftwood's composed evidence, so "signed by an agent identity" and "every actor is attestable" are not true of the one participant that produces forecasts
CLAIM: The twin is the only participant that exists and has no GitHub organisation, no release tag and no agent signature, and it is consumed by verbatim copy from inside another participant's checkout. Specifics, all re-verified: `gh api orgs/policy-as-versioned-twin` returns 404 while all eight unit orgs resolve with repos=1; the hub carries zero tags on three independent reads (`git tag -l`, `git ls-remote --tags origin`, `gh api .../tags` -> 0) while every unit repo carries 1 to 11; driftwood/twin/world holds exactly the 30 files of the hub's `twin.fixtures.LIBRARY_WORLD_FILES` with 0 byte differences, under PIN.yaml `tag_cut: false`; and no signed tag anywhere covers a twin artefact — driftwoo
EVIDENCE: `gh api orgs/policy-as-versioned-twin` -> 404 Not Found (every other participant org resolves with repos=1). `git tag -l` in the hub returns nothing. driftwood/twin/PIN.yaml: `twin_version: 0.1.0`, `twin_tag: twin/v0.1.0`, `tag_cut: false`. driftwood/twin/VENDORED.md: 'Files | 30', 'Copied | byte-for-byte, verbatim, no edits', staged to `world_ref: c2d07330a778ed547b60cfbb87217bcf9813181f`. driftwood/.github/workflows/twin-sweep.yml:64-71: `working-directory: hub/.estate-clone/driftwood`, first 
OWNER: owned-by-open-ticket ticket 64
REMEDY: Cut twin/v0.1.0 with a hub release workflow so driftwood can pin it the way it pins platform, then either give the twin its own org/repo or record in NORTH-STAR §2 that the twin ships from the hub by design. Re-point twin-sweep so it does not require the hub's clone layout.

## [major] P7 The twin's clock can only ever say "nothing moved": §4 step 5's forward-intelligence proposal lane has never run and cannot, and ticket 72 can close green without it ever running
CLAIM: driftwood's twin sweep can never propose: the `moved=true` branch is unreachable dead code under GitHub's default `bash -e` shell — a real, structural estate defect that the owner already found, diagnosed and ticketed a day before this review.

twin-sweep.yml:88 opens the sweep step with `set -uo pipefail`, which does not clear `-e`; GitHub runs the step as `shell: /usr/bin/bash -e {0}` (observed in both run logs). `twin/emit-forward-intel.py --check` returns 1 as its *moved* signal (emit-forward-intel.py:316-318), so any genuine forecast movement aborts the step at line 90, before `rc=$?` at line 91 — making the `moved=true` arm (line 95) and the propose-PR step gated on it (line 103) struc
EVIDENCE: driftwood/.github/workflows/twin-sweep.yml sweep step body: `set -uo pipefail` then `python3 twin/emit-forward-intel.py --check` / `rc=$?` / `if [ "${rc}" -eq 0 ] ... elif [ "${rc}" -eq 1 ]; then echo "moved=true" ... else echo "::error::the sweep could not render the overlay at all (exit ${rc})"`. The live log of run 33627910027 (2026-09-02T12:04:23Z) records `shell: /usr/bin/bash -e {0}`, and its last two lines are `FAIL: twin/forward-intel/v1/feed.json is not what the overlay renders` then `#
OWNER: owned-by-open-ticket ticket 72
REMEDY: Add `set +e` (or `|| rc=$?` / `shell: bash` without -e) around the emit-forward-intel.py call so rc is actually captured, and add a check that the moved path has fired at least once — a branch that has never executed on any run is not proven.

## [major] P9 Principle 5's clock is registered on nine rows and productive on none of the three that matter most — the intelligence publisher has never once refreshed a feed, and the regulator's green clock writes a dated falsehood about its own signed catalogue
CLAIM: Three of the nine registered clocks produce nothing usable, and the citable run cannot see any of it.

(a) feeds' clock has failed both scheduled runs it has ever had (33488014777 2026-09-01, 33605912295 2026-09-02). Eleven of its twelve matrix legs failed; one leg, `fetch (market-moves)` on 2026-09-01, succeeded and wrote the only observation feeds owns (commit 63c7904, payload_sha256 71d4c8ff…, published_version 1.0.0). Five of the six feeds declared in party.yaml have never recorded an observation. The cause is a branch-switch bug in the clock's own cage, not the lane rules: `.github/workflows/fetch.yml:120` cleans without `-x`, then line 123 checks out the orphan `observations` branch, w
EVIDENCE: `gh run list --repo policy-as-versioned-feeds/feeds`: fetch/schedule failure 2026-09-01T08:37Z (33488014777) and 2026-09-02T07:54Z (33605912295) — the only two. feeds' observations branch (`git ls-tree -r origin/observations`) carries only observations/README.md and observations/market-moves.jsonl, so five of six feeds have never recorded an observation. `gh run view 33615860064 --repo policy-as-versioned-insurer/insurer --json jobs`: fetch success, requote(driftwood|tuppence|ludlow) all failure
OWNER: owned-by-open-ticket ticket 57 (feeds/insurer clock) + new for nist null-observation leg
REMEDY: Point nist's fetch reader at catalog/ + CATALOG_VERSION.json so it observes the artefact it actually publishes (or record explicitly that a controls catalogue has no observable feed version). Fix the __pycache__ leak into the observation lane once, shared across hub/feeds/insurer. The insurer clock unblocks with P1.

OWNER QUESTIONS:
- Is the insurer's row meant to prove that a quote can be priced from a *signed, tagged* exposure, or only that the layer arithmetic and the refusal discipline are correct? Concretely: must an adopter cut a tag whose tree carries the exposure section before the row counts as built? | CHANGES: Either P1 becomes critical work (cut an adopter tag carrying exposure, re-quote, and add a gate check resolving every priced_against pin against the tag it names) plus a dated correction on ticket 36,
- Should the truth surface grade the estate at each unit's signed tag, or at its default branch? clone-estate.sh's own comment promises the former 'once a signed v1.0.0 lands', and all eight have landed. | CHANGES: If tags: P5 becomes a build ticket, the TRUTH line gains a tag field beside each SHA, and several currently-green checks may move because they would grade different bytes. If default branch: §5 needs 
- Does the twin have to become its own organisation with its own signed tag, or is shipping from the hub and being vendored into adopters the intended shape? | CHANGES: If its own org: ticket 64 grows a repo-split and driftwood's twin/ becomes a pinned dependency like platform's. If hub-shipped by design: §2's twin row and the 'own GitHub organisation' sentence need 
- For the intelligence-publisher row, is 'consumes public sources on a clock' a requirement, or is a committed-fixture clock with real cited grounding the accepted permanent shape? | CHANGES: If real ingestion is required: at least one feed needs a live urllib fetch with a could-not-look outcome, and news' resolvability check needs implementing to match its own prose. If fixtures are the a
- Is nine separate organisations meant to demonstrate separation of *artefacts* (own signing identity, own clock, own tags) or separation of *authority* (a participant can refuse another's change)? | CHANGES: If artefact-level is the ambition: §2 gains a sentence saying so and P15 closes as recorded scope. If authority-level: this becomes a build question about private repos or org-level required workflows
- When a participant's pin becomes unusable — a deleted branch, a tag whose tree lacks the section, a moving `main` — should the consuming clock go red, or should it degrade to a priced hole and keep running? | CHANGES: A single answer would let one check cover all of P2, P3, P4 and P1 (refuse a branch ref in any composing job; resolve every declared pin against the tag it names; price what cannot be resolved). Witho

# principles

VERDICT: Against §3 as written the estate is fit in the two principles that have engines (3, One £; 4, versioned dependency) and in the honesty machinery of 6; partly fit in 5 and 7; not yet fit in principle 1's conditional arm or in principle 2 beyond version-claiming pods. The pattern is consistent and is the finding: wherever the estate built an ENGINE it built it to an unusually high standard and instrumented its own drift (computed-semver cut a real gate-determined tag with a signed evidence file that names its own coverage gaps; pound_seam refuses a sum that crosses a perspective and cross-checks two selection engines at every band boundary; the five-fact sampler pre-registered its falsifiers before sample one and refuses hand-taken samples by run id and signature; twenty scheduled jobs are structurally proven unable to stage a declaration). Wherever the estate needed a SUBJECT for that engine, the subject is missing and the number still goes green because the check honestly SKIPs: one declared policy version so coexistence, retirement and shift-left have nothing to compare; no workload reconciled by Flux anywhere, so "the workload keeps running, caged tighter" has never been observed; no conditional-policy instance alive; no proposal PR ever opened; humans, devices and the twin not on the ladder at all. To become fit, in order: give principle 2 a Flux-reconciled workload and settle the two live Deny policies against ADR-0022's "one refusal"; cut the second policy line so principle 4's coexistence and patchability arms have a subject and principle 1's conditional branch comes back; wire gitsign into the proposer before step 3 fires so principle 5's enactment leaves an attestable trace; and close the two "green that could not look" holes in the five-fact falsifier and the step-6 tag glob so principle 6's own rule holds in its own instruments.

STRENGTHS:
- policy/v4.0.0 is a real, gate-determined release: platform/computed-semver/evidence/4.0.0.json records bump {declared: major, computed: major}, outcome passed, counts {old 153, new 152, union 164}, corpus_checksum sha256:ae12308f64efd, wall_clock 88.78s, 28 named movement entries and an explicit lim
- The signature spine is real across four separate orgs and I re-derived it independently: platform policy/v4.0.0 (tlog 2664927495), driftwood v1.1.0 (tlog 2585920387), ico v3.0.0 (tlog 2664928228), feeds threat-register/v2.0.0 (tlog 2673492733) — every one 'Good signature from .../<org>/<repo>/.githu
- The cage is tighten-only in fact, not only in prose, in the served copy every adopter composes from (platform/distribution/policies/v4.0.0/cage-tier.yaml): readOnlyRootFilesystem and runAsNonRoot are ORed with what the container declared, cpu/memory ceilings are a MIN via quantity().isLessThan(), pr
- The bottom rung is a running cage, proven by a real TCP connection rather than a YAML read: platform/graded/verify-graded.sh:480-505 execs `nc -w 4 -z <apiserver ClusterIP> 443` and `nc ... 1.1.1.1 80` from the isolated pod and fails if either succeeds after 60s, asserts the baseline pod still reach
- One £ is enforced and green on the citable run: verify_pound-seam PASS — 'every price names its perspective, currency, source and per-customer share; no sum crosses either'; verify_proportionality PASS — 'same control, same £ (risk_bought £21107) — Audit in driftwood, Deny in ludlow'. platform/risk/
- Two independent tier-selection implementations are cross-checked to the band boundary and pass: pound_seam check 9 runs driftwood's own published selection-policy package and platform/graded/cage.py over the same residuals at each boundary and with every rung as a floor and refuses any disagreement;
- The clock cage is structural and PASSes for all twenty scheduled jobs: run 21's verify_schedules capture prints, per job, 'caged — no shell step in this job stages a declaration or mints a signed artefact'; OBSERVATION_LANE is declared once as data in each workflow (truth.yml:38; driftwood drift-sam
- The Flux falsification test as re-scoped by REGRILL 1 exists, is pre-registered before sample one (driftwood/drift/window.yaml five_fact_sample, declared_on 2026-08-28, three named falsifiers, coverage floor 0.9), refuses rehearsals ('a sample taken by hand ... is a REHEARSAL and is never cited'), a
- A forecast has been scored against reality and the red survived: tests/test_royal_mail_beat.py docstring records Brier 0.9025, 'worse than a coin flip', and test_the_score_lands_poor_and_says_so asserts worst brier > 0.25 AND adjusted_brier > 0.25 ('the discount rescued a forecast it should not'). B
- No exemption mechanism exists anywhere: grep -rni 'PolicyException|exemption' across all eight unit clones returns 19 non-markdown hits, every one a negation or a reference to the deleted render-exemption.py. platform/computed-semver/coverage-exclusions.yaml is empty (declared_holes: []) and its hea
- Honesty of reporting is graded separately from outcome and works: run 21's verify-e2e-step7-honesty is PASS with '(verdicts: PASS PASS PASS FAIL PASS PASS PASS)' — a red step 4 and a green step 7 in the same tally is correct, not a contradiction. The truth clock itself reddened honestly on run 21: g

## [major] P6-1 The falsification test is not honest about itself: the adopter's cross-org signature falsifier can never run for platform or nist, and the sampler records that inability as "did not fire", so the next green sample will claim a falsifier passed that nobody ran
CLAIM: In driftwood's five-fact sampler, falsifier 2 (`cluster_side_verification_passes_a_tag_that_identity_pinned_ci_rejects`) records `fired: false` — "did not fire" — for every source whose CI-side check was never run: `gitsign_verifies()` is called only when `src["party"] == "driftwood"`, the else branch returns `ok = None`, and `(ok is False)` collapses that to `False` (drift/five-facts.py:524-528). The grader's only could-not-look branch tests `state.get("fired") is None` (:683), so those records are silently skipped — contradicting the comment three lines above (:674-676, "a falsifier nobody ran is exactly the state that must never ride along inside a PASS"), the tri-state contract in the fu
EVIDENCE: driftwood/drift/five-facts.py:522-528: `ok, why = gitsign_verifies(...) if src["party"] == "driftwood" else (None, f"{src['party']}'s own release.yml is not in this checkout...")` then `f2 = {..., "fired": (ok is False), "ci_accepts": ok, ...}` — `ok is False` is False when ok is None. five-facts.py:683 grades could-not-look only on `state.get("fired") is None`. The comment at :674-677 reads 'a falsifier nobody ran is exactly the state that must never ride along inside a PASS'. Confirmed live in
OWNER: new none
REMEDY: One line: `"fired": (False if ok is False else (True if ok is True else None))` — i.e. carry None through when ok is None. Then grade()'s existing could-not-look branch converts the sample to SKIP with a reason, as intended.

## [major] P6-2 The truth surface's provenance line does not measure the artefacts the eco-system actually ships: it declares a signed publisher unsigned and verifies the platform's abandoned tag line instead of the policy line the demonstration runs on
CLAIM: verify-e2e-step6-provenance.sh:88 selects each unit's tag with `git tag -l 'v*.*.*'`, a pattern anchored on a leading `v` that cannot match a namespaced tag. feeds' only tags are threat-register/v1.0.0 and threat-register/v2.0.0, so the selector finds nothing, :89 files feeds under `queued`, and TRUTH run 21's citable capture states "ok no signed tag yet, honestly queued for cut-release.yml: feeds" and passes with "7 of 8". That statement is false: threat-register/v2.0.0 is gitsign-signed at tlog index 2673492733 with "Good signature from [.../policy-as-versioned-feeds/feeds/.github/workflows/cut-release.yml@refs/heads/main]" and "Validated Rekor entry: true", on commit 69c89b0 — the exact f
EVIDENCE: verify/e2e/verify-e2e-step6-provenance.sh:87: `tag="$(git -C "$ESTATE/$u" tag -l 'v*.*.*' 2>/dev/null | sort -V | tail -1)"`; :89 `[ -n "$tag" ] || { queued+=("$u"); continue; }`. Run 21 capture: `ok no signed tag yet, honestly queued for cut-release.yml: feeds` and `PASS: ... 7 of 8 anchored identity regexps matched`. But in the feeds clone `git tag -l` returns threat-register/v1.0.0 and threat-register/v2.0.0, and I verified the latter: `git -c gpg.format=x509 -c gpg.x509.program=gitsign tag -
OWNER: new none
REMEDY: Enumerate each unit's tags through the same parser cut-release uses (or glob '*v[0-9]*.[0-9]*.[0-9]*' and sort by the semver suffix) so namespaced and policy-line tags are both reachable, and verify the newest tag per published line rather than one tag per repo.

## [major] P5-3 Step 3 — the beat that exists to prove an agent proposer is attestable — will land an unsigned commit and be self-certified as signed, so the first real proposal proves nothing about who proposed it
CLAIM: Reversal 16 ("sign the proposal commit with the workflow Actions identity; add it to the expected-identity regexp", REGRILL-ANSWERS.md:79, confirmed binding :87) is unbuilt in both halves. propose-tier.yml on all three adopters installs no gitsign and sets no signing config — its proposer step (driftwood propose-tier.yml:184-185) sets only user.email/user.name — while twin-sweep.yml:77-83,116,211-213 and drift-sample.yml:107-109,251-253 in the same repo do pin and configure gitsign for their observation commits. tier_pr.py:350 commits with no -S and no gpgsign config, then pushes over git (:351), so GitHub's own web-flow signing cannot apply. No expected-identity regexp in the estate names p
EVIDENCE: driftwood/.github/workflows/propose-tier.yml: grep for gitsign|gpg|sign returns exactly one hit, a prose comment at line 29 about a stale signed size. Its proposer step sets only `git config user.email/user.name`. platform/wargamer/tier_pr.py:350 is `_git("commit", "-q", "-m", p["title"], cwd=adopter_dir)` with no -S and no gpgsign config anywhere in the file. driftwood/.github/scripts/adopter-gate.py:75-78 pins only `.github/workflows/cut-release\.yml@refs/heads/(main|release/...)`. platform/wa
OWNER: new GAPS.md:29 (row 1.9); REGRILL-ANSWERS.md:79 (reversal 16)
REMEDY: Add the pinned-gitsign install and `git -c gpg.format=x509 -c gpg.x509.program=gitsign` config to propose-tier.yml (copy twin-sweep.yml's block verbatim), add propose-tier.yml to each adopter's expected-identity regexp with a verify check, and delete the literal "signed": True from wargamer.py:200,232 so the selfcheck stops asserting an unobserved attribute. Do this before ticket 74 fires.

## [major] P4-2 The cage release was never distributed: what Flux actually puts on an adopter cluster is a retired policy line, and the step-4 instrument cannot tell which line it is
CLAIM: All three adopters' composed ResourceSets reconcile policy versions 2.0.0, 2.0.1 and 3.0.0 — the three lines platform retired on 2026-08-29 — while the cage release 4.0.0 exists only at each adopter's HEAD, outside every tag any cluster reads (driftwood, tuppence and ludlow each have only v1.0.0 and v1.1.0; v1.1.0 carries composed/policies up to 3.0.0). Evidence: composed-set.yaml:85-87 (driftwood; :87-89 in tuppence and ludlow), and the run-21 sample at driftwood 6cf0671 recording last_applied_revisions = {composed-v2-0-0, composed-v2-0-1, composed-v3-0-0}. Sharper still: `python3 scripts/render_composed.py --list` in driftwood's HEAD worktree returns ONE object (the orphan guard alone), ag
EVIDENCE: driftwood/gitops/composed/composed-set.yaml:85-87 declares `- { version: "2.0.0" }`, `"2.0.1"`, `"3.0.0"`, with the in-file comment at :88-92: '4.0.0 -- the cage release (ticket 26) -- is NOT here yet on purpose. This array is reconciled from the tree at tag v1.1.0, and v1.1.0 carries composed/policies up to 3.0.0 only.' Run 21's sample confirms live: `last_applied_revisions: {composed-v2-0-0, composed-v2-0-1, composed-v3-0-0}`. `ls composed/policies/` at HEAD returns only v4.0.0. The retirement
OWNER: new none
REMEDY: Cut a driftwood tag whose tree carries composed/policies/v4.0.0, and in the same reviewed PR add the `{ version: "4.0.0" }` element and bump the driftwood-composed pin — the exact sequence the file's own comment prescribes. Then the five-fact sample proves the current line.

## [major] P3-1 Driftwood's own published response curve is inert: its largest signed price line is the platform's self-declared cage-efficacy table wearing the adopter's perspective label
CLAIM: The *residual ladder* on driftwood's twin price entry — and therefore the rung it selects and the appetite sentence it records — is computed from platform/graded/cage.py's self-declared reduction table (0.30/0.70/0.92/0.98), not from driftwood's own published graded response curve (0.05/0.30/0.65/0.90 at costs £8k/£20k/£42k/£95k, each carrying an evidence_grade and a written basis). The entry's headline amount (£1,897,646.11) is NOT the platform's: it is fair.py's annualisation of driftwood's own lm triple [192000, 384000, 800000] with a frequency borrowed and named from feeds' threat-register (platform/fair/fair.py references no cage table). The material consequence is not the £171,600 the 
EVIDENCE: driftwood/composed/evidence.json prices[3]: {source: twin, kind: twin, perspective: driftwood, currency: GBP, amount: 1897646.1130078766, residual_basis: 'platform-cage-tiers@1.0.0', policy_version: 1.0.0, tail: bounded-pert}. driftwood/twin/orgs/driftwood/responses/*.yaml declare mode reductions 0.05/0.30/0.65/0.90 at costs 8000/20000/42000/95000; platform/graded/cage.py:82-111 TIERS declare 0.30/0.70/0.92/0.98 at 500/2000/6000/15000. Run 21's verify_pound-seam capture measures the gap: 'platfo
OWNER: new none
REMEDY: Either price the twin entry from the adopter's own published curve (the artefact already carries it) and keep the platform table only as a fallback that names itself, or record an owner decision that the platform's calibration is the shared unit and the adopter's curve is evidence only. Either way, open a ticket — today it is disclosed and unowned.

## [major] P2-4 A second live refusal contradicts "there is no gate", and the glossary plus a green truth-surface check both certify a fix that never shipped
CLAIM: The estate ships three Deny-mode ValidatingPolicies where ADR-0022 blesses one. `policy-version-orphan-guard` carries `validationActions: [Deny]` in the platform ResourceSet (platform/distribution/versions.yaml:117), in its offline twin (render-orphan-guard.py:68) and in all three adopters' composed artefacts (driftwood|tuppence|ludlow composed/orphan-guard.yaml:13 and gitops/composed/composed-set.yaml), and `posture-trust-boundary` carries it too (platform/posture/policies/posture-trust-boundary.yaml:23, plus every served version dir at :15) — alongside the one refusal ADR-0022:41 blesses, `governed-namespace-requires-claim`. The estate's own gate grades the orphan refusal as correct: verif
EVIDENCE: platform/distribution/versions.yaml:117 `validationActions: [Deny]` on policy-version-orphan-guard, message 'policy-version not in the platform-declared version array (orphan): no ResourceSet element declares it, so it cannot run.' platform/distribution/verify-orphan-guard.sh:45-49 makes denial the pass condition: 'orphan pod (9.9.9) was NOT denied — a version outside the array could run'. Run 21 capture: 'PASS: only versions the array declares can run; the allow-list is the array.' CONTEXT.md:2
OWNER: new none
REMEDY: Either convert the orphan-guard to a MutatingPolicy that renders isolated for an undeclared claim (what CONTEXT.md already says), inverting verify-orphan-guard.sh's pass condition; or amend ADR-0022 with a dated note admitting a second permitted refusal and its reason, and correct CONTEXT.md. This is an owner decision, not an implementation detail.

## [major] P2-6 The cage — the thesis's most distinctive claim — is proven only by presenter-run evidence that the north star's own citation rule forbids any document from using
CLAIM: The cage's behaviour has never been observed on a citable run. Every live claim about the cage behaving — a pod admitted and Running in a caged Namespace, a forged tier label clobbered, an untiered governed Namespace falling closed to isolated, the isolated pod failing to connect to the API server ClusterIP:443 and 1.1.1.1:80, the baseline pod still reaching, a running isolated pod accepting an UPDATE — exists only in verify-graded.sh's live tail (units/platform/graded/verify-graded.sh:283-542), which has never produced a green line on any TRUTH run: the script graded FAIL on every recorded run from 4 to 13 (runs 4-11 on `cage-netpol generate matrix failed`, runs 12-13 on the kyverno 1.19 CE
EVIDENCE: Run 21 capture .estate-clone_platform_graded_verify-graded.out ends 'SKIP (live tail): kind cluster driftwood is not listed by kind get clusters' / 'SKIP: offline proof holds; live tail could not look'. Nine other platform scripts SKIP for the identical reason on run 21 (access, currency-controller, engine, eud, federation, identity, posture-projection, verify-source-verification, declared-versions-admit), plus tuppence/reset/verify-reach-secrets. REVIEW-2026-08-31 C1 records the same script FAI
OWNER: new none — no existing ticket/ADR/REGRILL covers verify-graded.sh's live-tail cluste
REMEDY: Decide whether the citable run gets a substrate (an ephemeral KinD in the truth workflow, as drift-sample.yml already does for the adopters) or whether the cage's live tail is permanently a could-not-look and the estate says so in the record. Today it is neither decided nor named.

## [major] P1-1 The estate's only live answer to a would-be exemption is the priced cage; no served policy today offers the conditional rule anyone can meet
CLAIM: No currently-declared policy version carries a conditional-allowance branch, so principle 1's "conditional rule anyone can meet" arm has no *served* instance. The only such branch the estate ever released — `variables.nonroot || (variables.attested && variables.hardened)` in `distribution/policies/v2.0.1/require-nonroot.yaml:56`, shipped under signed tag policy/v2.0.1 on 2026-08-24 — was retired on 2026-08-29 when versions.yaml was cut to a single element (`versions.yaml:77`, 4.0.0 only); 4.0.0 and all three adopters' composed sets carry a flat non-root + read-only-fs rule with no widening branch, and no other served policy in any of the eight repos contains one. The beat still proves the ru
EVIDENCE: platform/policy/verify-conditional.sh:73 emits, and run 21's capture records verbatim: 'SKIP (live tail): the root-attested conditional branch this beat proves lives only in require-nonroot-2-0-1, and 2.0.1 is retired (not in distribution/versions.yaml, 2026-08-29); the only currently-declared version (4.0.0) replaced it with a flat non-root+read-only-fs rule that has no root-if-attested branch, so no currently-declared version's live install can observe this claim'. distribution/versions.yaml:7
OWNER: owned-by-open-ticket ticket 63 (open, unblocked; decision recorded in ticket 58)
REMEDY: Ticket 63 as written: cut 5.0.0 with the root-if-attested branch restored and relabel platform/policy/tests/conditional/resources.yaml to the new version (verify-conditional.sh:76 already refuses to trust a stale-fixture tail).

## [major] P4-1 The estate serves one policy line, so the thesis' "non-negotiable" multi-version runtime — coexistence, a retirement window, and the ±1 shift-left window — is unproven at runtime on every citable run
CLAIM: Only one policy line is declared, so coexistence, retirement and the ±1 shift-left window have no multi-version subject, and all three checks are graded could-not-look on the citable run. distribution/versions.yaml:77 declares exactly one element (version 4.0.0, tag policy/v4.0.0, commit 64635df…, bump major) in platform at 46cd775, the tree TRUTH run 21 graded. Run 21's own CI log (gh run view 33616685427 --log) grades .estate-clone/platform/distribution/verify-coexistence.sh, .../verify-retirement.sh and .estate-clone/platform/shift-left/verify-shift-left.sh SKIP, each naming the one-version fact as its reason; verify-all.sh maps exit 3 to SKIP, and all three scripts genuinely exit 3.

Two
EVIDENCE: platform/distribution/versions.yaml has one array element (line 77: version 4.0.0, tag policy/v4.0.0, commit 64635df..., bump major). Run 21 captures: verify-coexistence 'SKIP: ... distribution/versions.yaml declares one version (4.0.0)'; verify-retirement 'SKIP: distribution/versions.yaml declares one version (4.0.0), so a retirement would leave an empty allow-list'; verify-shift-left 'SKIP: distribution/versions.yaml declares one major line (4.0.0), so a target has no ±1 neighbour'. map.md rec
OWNER: owned-by-open-ticket ticket 63 (open, blocked-by 58 which is resolved-provisional)
REMEDY: Cut 5.0.0 (ticket 63) so a second line exists; then decide separately whether a patch to a still-declared older line is ever demonstrated, since retirement has so far been the estate's answer to every defect.

## [major] P5-1 Principle 5's clock is evidenced only as a committed cron line: the citable number would not move if every schedule in the eco-system stopped firing
CLAIM: On every run whose capture is in git — runs 9 through 21, all twelve recorded — the live half of verify-schedules ("did each clock run inside its own period") records one SKIP per required clock and has never once graded. On run 21 that is 14 SKIP lines covering all 13 required clocks (driftwood propose-tier/renovate-run/twin-sweep, ludlow propose-tier/renovate-run, tuppence propose-tier/renovate-run, feeds/ico/insurer/nist/platform fetch, hub truth), every one reading "GitHub unreachable (Command ['gh','auth','status'] returned non-zero exit status 1.)". The cause is that the gate step carries no GitHub credential — deliberately, for a reason recorded at truth.yml:59-64,92-98: talk/verify-a
EVIDENCE: Run 21's verify_schedules capture carries twelve lines of the form 'SKIP: <unit>/<workflow>: GitHub unreachable (Command [gh, auth, status] returned non-zero exit status 1.) -- cannot look at whether this clock ran inside its period', covering driftwood propose-tier/renovate-run/twin-sweep, ludlow propose-tier/renovate-run, tuppence propose-tier/renovate-run, feeds fetch, ico fetch, insurer fetch, nist fetch, platform fetch and hub truth. The capture's last non-empty line is one of these SKIPs, 
OWNER: owned-by-open-ticket ticket 56
REMEDY: Ticket 56's scope. The read is cross-org, so it needs either a read-only token scoped to the nine repos, or each unit publishing its own last-run observation into its lane for the hub to read without a credential.

## [major] P5-2 The £ has never moved a cage: the priced-intelligence loop has never closed into an enacted change, so the demonstration's pivot beat is unproven on real input
CLAIM: No proposal PR has ever opened, so NORTH-STAR §4 step 3 has never fired — and no machine-opened PR of any kind has ever been merged in an adopter repo. Across the complete PR history of the three adopters (48 PRs; numbering contiguous 1..22 / 1..14 / 1..12) there is not one `wargamer/retune-*` branch, and `gh api .../branches` confirms no such ref exists on driftwood, tuppence, ludlow or platform (the proposer's prefix is `wargamer/retune-{slug}`, wargamer.py:195,227). The only tier-related PRs are the three human "policy-composition: … propose-tier workflow" merges (driftwood #6, tuppence #5, ludlow #4), all authored and merged by chrisns on 2026-08-25. Stronger still: all 24 merged PRs are
EVIDENCE: `gh pr list --repo policy-as-versioned-{driftwood,tuppence,ludlow}/<repo> --state all --limit 60` returns 22, 14 and 12 PRs respectively; filtering for 'war-gamer', 'retune' or 'tier' in the title returns only the three human 'policy-composition: party artefact, governed namespace, propose-tier workflow' merges (driftwood #6, tuppence #5, ludlow #4), all authored and merged by chrisns. Ticket 60's own 2026-09-01 comment: 'the proposer returned [] — no band crossed, no proposal PR'.
OWNER: owned-by-open-ticket ticket 74
REMEDY: Ticket 74's scope, with P5-3's signing fix landed first so the first real proposal is attestable.

## [minor] P3-2 The heavy-tail seam ADR-0021 designed is unguarded and unexercised: no live £ figure crosses it yet, so the drift is latent, but the estate applies its own two-engines rule inconsistently
CLAIM: The lognormal-GPD severity model is implemented twice — platform/fair/severity.py (80 lines: a Monte-Carlo sampler plus a JSON-spec validator) and twin/severity.py (215 lines: closed-form survival/VaR/TVaR/loss-exceedance-curve) — and nothing anywhere compares them. I verified they sample the same distribution today (400k draws each at the estate's own heavy-tail parameters; tail mass and quantiles agree to Monte-Carlo error), so the duplication is real. Their contracts have already diverged, unguarded: platform refuses xi < 0 (severity.py:62-63) where twin explicitly supports it (twin/severity.py:87-89), and platform samples xi >= 1 where twin refuses TVaR as undefined (twin/severity.py:169
EVIDENCE: platform/fair/severity.py (80 lines) and /Users/cns/httpdocs/controlplane/policy-as-versioned-flux/twin/severity.py (215 lines) both implement `S(x) = S_lognormal(u) * (1 + xi*(x-u)/beta)^(-1/xi)`; `diff` reports DIFFERENT. platform/fair/severity.py's header explains the duplication: 'It lives here, not at twin/severity.py, because twin/ is a python package in the HUB repo and fair.py runs inside the platform repo ... The spec crosses as JSON; the sampler lives beside the engine that consumes it
OWNER: new none
REMEDY: Add a check that runs both severity modules over the same lm spec with the same seed and refuses a divergence beyond a declared tolerance — the same shape as pound_seam check 9. Cheap, and it makes the deliberate duplication safe.

## [major] P4-3 The computed release number is measured over a corpus the owner ruled too thin: three-way interactions are never exercised, so a bump can be under-computed and still pass
CLAIM: REGRILL 6 (P050) is a recorded owner override — "Run the full set. Accept the runtime cost", listed under "Where the owner overrode the recommendation" (REGRILL-ANSWERS.md:12, :51) — and it is unimplemented and unowned. The shipped release, policy/v4.0.0 (tag on origin, the only live element in distribution/versions.yaml:77), records pairwise-only coverage in its signed evidence: coverage.pairwise_gap says "no three-way interaction was built", generated-corpus/manifest.yaml:12 says combination: pairwise over five axes, and the generator's own selfcheck asserts "never a full cross" — printed again in run 21's capture today. No three-way or sampled-cross mode exists in computed-semver. The nam
EVIDENCE: REGRILL-ANSWERS.md row 6 (P050): 'Pairwise vs full combination coverage | Run the full set. Accept the runtime cost.' — listed again under 'Where the owner overrode the recommendation'. platform/computed-semver/evidence/4.0.0.json coverage.pairwise_gap: 'axes were combined pairwise (predicate-expression x version-pin, predicate-expression x tier-label, and every pair among version-pin, tier-label, namespace and workload-declares), so no three-way interaction was built'. Ticket 18 (resolved) stat
OWNER: new tickets 18 & 21 (resolved, both silent on the build)
REMEDY: Either build the full cross in corpus_generator and re-run the next release's gate against it (the sizing is already recorded), or put the pairwise gap back to the owner as a decision to reverse re-grill 6 with a reason. Do not leave a binding override sitting inside a resolved ticket.

## [major] P6-3 The twin is the one participant an adopter consumes without a pin or a signature: the estate's largest £ enters driftwood's evidence as a local file read, so the signed adopter artefact vouches for the copy, not the publisher
CLAIM: driftwood/twin/forward-intel/v1/feed.json — proven to be the source of the GBP 1,897,646.11 twin entry in driftwood's composed evidence (its payload.curve hashes to prices[3].curve_hash sha256:70e5e9f6..., recomputed with composition.py:1685-1687's own algorithm) — exists in no git tag. `git tag --contains` on its commit f75509a is empty, and `git ls-remote --tags` on the real remote shows driftwood has only v1.0.0 and v1.1.0, neither of which contains any part of twin/. Under ADR-0019 point 2 ("The signature is the gitsign tag. No in-band signature field") the feed is unsigned by the estate's only signing mechanism, and the envelope carries no signature field of its own. Nothing in the gate
EVIDENCE: `git ls-tree -r --name-only v1.1.0 | grep -c twin/forward-intel` -> 0. `git log --format=%H -1 -- twin/forward-intel/v1/feed.json | xargs git tag --contains` -> empty. driftwood/composed/evidence.json prices[3] carries {source: twin, amount: 1897646.1130078766, curve_hash: sha256:70e5e9f6...}. The envelope itself carries no signature field (kind, name, version, published_by, published_at, payload_schema, payload only). Run 21's verify-feed-contract capture passes it three times on schema grounds
OWNER: owned-by-open-ticket ticket 64 (open); REVIEW-2026-08-31.md M16
REMEDY: Ticket 64's scope: cut the twin release tag and the adopter's forward-intel feed under a signed tag, and make verify-feed-contract go amber (not silently PASS) for a published feed that resolves to no tag.

## [major] P7-1 "Proven in force across an org boundary" is true at a declared ceiling for the two publishers — but the citable capture prints it as three independent per-publisher proofs
CLAIM: The cross-org proof is a chain, and at run 21 the chain joins two different platform revisions. For the two publishers, fact 3 is a comparison of two files in the sampler's own checkout — composed/HEADER.yaml's parent sha against gitops/*-pin.yaml's commit — with no cluster read at all (five-facts.py:469-479; header_parents() and sources() both read the working tree). On the sampled ephemeral cluster no Kustomization consumes either publisher source, by deliberate design (drift-sample.yml:161-167 applies platform-pin.yaml only, "never -k gitops/platform/"); driftwood's tree does carry an opt-in Kustomization over GitRepository/platform (gitops/platform/platform-distribution.yaml:5-16), and n
EVIDENCE: Run 21's driftwood sample, verbatim: 'platform true fact_3_last_applied_revision_equals_the_pinned_commit: platform is a verified source only (ticket 16 Q5): no Kustomization reconciles it, so the revision in force is the parent sha the composed set was built from. HEADER 533dccb0a823 vs pin 533dccb0a823' — identical text for nist. drift/window.yaml's fact_3 claim explicitly writes this exception into the pre-registration. The choice traces to ticket 16 Q5, recorded 'provisional on a bare agree'
OWNER: owned-by-open-ticket ticket 16 Q5 (resolved, PROVISIONAL)
REMEDY: Either put the chain-vs-direct question to the owner as a decision on the wording of REGRILL 1, or add a Kustomization that reconciles a small platform-owned object directly so fact 3 becomes an observation for at least one publisher. Do not leave the strongest claim resting on a provisional bare agree.

OWNER QUESTIONS:
- Does 'everything is always caged' bind the permanently unversioned population — kube-system, Kyverno's own pods, Flux's controllers, cert-manager, and any COTS workload an adopter runs — or does the cage apply only to workloads that claim a policy version? | CHANGES: If bound: the COTS shim (GAPS 3.14, 'decided four times, built zero times') becomes a build, ticket 63's unlabelled-default flip is necessary but not sufficient, and cage-tier needs a second matchCond
- Is the orphan-guard's Deny the doctrine's second permitted refusal, or must it become a fail-closed cage to isolated as CONTEXT.md already says it is? | CHANGES: If it must cage: versions.yaml's guard becomes a MutatingPolicy, verify-orphan-guard.sh's pass condition inverts, all three adopters' composed orphan-guards re-render, and the engine computes a bump f
- Whose numbers price an adopter's twin residual — the platform's calibration table, or the adopter's own published response curve? | CHANGES: If the adopter's curve wins: composition re-prices every twin entry, cage.py's table becomes a fallback that must name itself, and the £ figures in every deck and capture move. If the platform's table
- Must a demonstration of the eco-system include a running governed workload reconciled by Flux, or is the composed policy set proven in force sufficient? | CHANGES: If required: a one-line path fix on driftwood plus a pod in tuppence and ludlow's gitops/apps, and the five-fact sample's fact 4/5 set grows to include a workload — after which step 4 can be observed 
- Must the proposal commit be gitsign-signed before step 3 is allowed to happen for real, or may the first real proposal land unsigned and be signed later? | CHANGES: If required first: ticket 74 gains a blocker, propose-tier.yml gets twin-sweep.yml's gitsign block, and each adopter's identity regexp grows a second workflow. If not: the estate's first real enactmen
- Is re-grill 6's 'run the full combination set, accept the runtime cost' still binding for the next release, given the shipped 4.0.0 evidence records pairwise coverage with the gap disclosed? | CHANGES: If binding: corpus_generator builds the full cross, the release wall clock goes from 89 seconds to tens of minutes, and 5.0.0's evidence is the first to carry it. If reversed: the reversal needs recor

# demo-steps

VERDICT: Not fit yet for the purpose the map's Destination states, and the gap is honestly visible in the estate's own number rather than hidden by it. Of the seven §4 steps, two are real end to end (1: ico v3.0.0 signed and resolved on the real remote; 6: seven tags Rekor-verified by the real gitsign binary on the citable run), one is real once but only half (2: the pin moved by a real bot commit merged by a human in driftwood PR #20, but old_price == new_price so the money did not), one is real for one adopter and blocked by an instrument fault (4: driftwood's scheduled lane sample has facts 1/3/4/5 true and fact 2 false only because the in-cluster verifier rejects a tag that gitsign+Rekor verify fine), two have never happened at all (3: no proposal PR, no wargamer/* branch, today's real proposer returned []; and 5's "on its schedule": driftwood twin-sweep has fired twice and failed twice), and one is a reporting roll-up that correctly refuses to grade the others' results (7). The map's destination sentence — "run end to end once, on a clock, with driftwood, tuppence and ludlow consuming" — fails on three independent counts and is today not even a gradeable proposition, because verify-schedules cannot reach GitHub from the hub runner and every "did this clock run" question SKIPs. What IS fit is the instrument: in the seven steps I traced, the gate never turned an absence into a pass, step 4 has never once graded PASS, and the two false sentences I found (feeds' tags, step 2's word "merged") both understate or mis-frame rather than invent. Four things would make this dimension fit: push round 3 of the sampler wait-order fix so tuppence and ludlow reconcile at all (no owning ticket); land ticket 73 so driftwood's fifth fact turns; make one residual really cross a band (ticket 74); and fix the twin's re-render so its clock succeeds and step 5 asserts it (tickets 72, 64). Three of the four are charted; the first is not, and it is nearest to done.

STRENGTHS:
- Step 6 is genuinely, cryptographically real on the citable run: run-21 capture shows seven tags (driftwood v1.1.0, ico v3.0.0, insurer v1.0.0, ludlow v1.1.0, nist v1.1.0, platform v2.0.1, tuppence v1.1.0) verified by the real gitsign binary against real Fulcio certs with 'Validated Rekor entry: true
- Step 1's remote check is a real network call, not a local read: verify/feed-contract/feed_contract.py:86 runs `git ls-remote --tags https://github.com/policy-as-versioned-<party>/<party>`, caches per run, and returns None (→ SKIP) rather than a pass when unreachable.
- Step 2's real-world event happened once and is graded from the real PR record, not a simulation: verify/renovate/verify-renovate-merged-feed-pr.sh PASSes on run 21 citing driftwood #20 — commit ea1c8db authored by github-actions[bot], moving party.yaml + composed/HEADER.yaml + composed/evidence.json
- The lane-sample grader is built to refuse: drift/five-facts.py:627-654 returns FAIL if drift/window.yaml no longer declares all three pre-registered falsifiers, SKIPs on a sample older than the freshness bound, and SKIPs via sample_provenance() on a hand-typed or unsigned sample — closing a document
- Step 3's PASS line was corrected against its own reader and now names itself: the dated 2026-08-29 note at step3_band.py:262-269 replaced wording that implied driftwood's real priced position had crossed its band, and the shipped line reads 'a SYNTHETIC residual placed either side of driftwood's own
- The proposer correctly proposes nothing: driftwood's real scheduled propose-tier run 33626099181 (2026-09-02T11:43Z, conclusion success) printed `note: ledger derived from 0 closed-unmerged proposal PR(s)` then `[]`. A machine that stays quiet when no band is crossed is worth as much as one that fir
- Step 7's grading bites and proves it every run: verify-e2e-step7-honesty.sh:36-70 plants a hedged PASS, an exit-code/last-line mismatch, a non-conforming step, a missing step and a green whose transcript confesses mid-run, requires all four dishonest ones caught while an honest SKIP and FAIL pass th
- A red step 4 and a green step 7 in the same TRUTH line are both correct and not a contradiction: step 7 grades reporting, not results, and says so in its header, the README and its own verdict text — a genuinely well-drawn seam that stops one fact moving the number twice.
- The deck check caught the deck: run 21's verify/demo/verify-demo.sh FAIL is the mechanism working — the committed talk/deck.md records beats 1-6 as SKIP while a rebuild from run-21's own captures gives PASS,PASS,PASS,FAIL,PASS,PASS. The estate's one presentation artefact under-claims, and the gate s
- Step 4's substrate handling never turns absence into a pass: verify-e2e-step4-flux-reconciles-cage.sh:63-73 checks kubectl, kind, docker, the named cluster and the Flux CRD before any live read, and falls to grade_lane_sample (which itself can only PASS/SKIP/FAIL on a signed, fresh, run-id-bearing s

## [major] DS-F1 The honesty rule "a green that could not look is a red" is broken by the estate's own gate, on the one beat whose distinguishing words are "on its schedule" — and that clock has now failed both times it fired
CLAIM: Step 5 reads PASS on the citable run while both twin clocks are red and nothing on that run looked at either.

verify/e2e/verify-e2e-step5-twin-forecasts.sh grades on five file-presence checks, a scenario file count, and a delegate (verify/twin-evals/verify-twin-evals.sh) that is deliberately time-frozen (rss.run("1970-01-01T00:00:00Z"), signal_for(..., tag="placeholder-tag", commit="0"*40)). It never reads a run, a firing or a schedule. (It does read dates — twin/feed_signal.py:159-165 takes each envelope's event date or published_at — so the precise gap is the scheduled firing, not "a date".) Its run-21 capture ends PASS.

Meanwhile driftwood/twin-sweep.yml, the estate's only scheduled for
EVIDENCE: Script: verify/e2e/verify-e2e-step5-twin-forecasts.sh checks five paths with `want` then runs verify/twin-evals/verify-twin-evals.sh; no reference to a run id, timestamp or workflow anywhere in the file. Run 21 capture (origin/main:talk/captures/verify_e2e_verify-e2e-step5-twin-forecasts.out) ends 'PASS: the twin's overlay, feed, 6 scenarios and signal lookup are all present, and its own evals scored them'. Clock: `gh run list --repo policy-as-versioned-driftwood/driftwood --workflow twin-sweep.
OWNER: owned-by-open-ticket ticket 64 (open)
REMEDY: Two lines in step 5: after the artefact checks, require (a) the committed forward-intel render to be reproducible from the overlay and (b) a dated sweep observation to exist; exit 3 naming ticket 72 until then, rather than exit 0.

## [critical] DS-F2 The one joint the thesis is actually about has never fired: no crossed band has ever opened a reviewed proposal PR, so "the eco-system ran end to end once" cannot be claimed
CLAIM: Step 3 has never happened. No `wargamer/retune-*` branch (the proposer's branch shape, platform/wargamer/wargamer.py:195,227) and no proposal pull request exists in any state on driftwood, tuppence or ludlow. Driftwood's proposer is fully operational and has run successfully four times — twice on its 06:47 clock and twice on PR events, including 3 seconds after the real Renovate feed bump PR #20 merged — and returned `[]` every time; today's scheduled run 33626099181 (2026-09-02T11:43Z, success) re-composed real prices at `--as-of 2026-09-02` showing `old_tier: isolated`, `proposed_tier: isolated`, `changed: false`, so the empty result is a genuine "no residual crossed a band", not a no-evid
EVIDENCE: `gh api repos/policy-as-versioned-{driftwood,tuppence,ludlow}/{driftwood,tuppence,ludlow}/branches --jq '.[].name'` — no wargamer/* branch on any of the three. `gh pr list --state all` on all three: no proposal PR. Driftwood's real scheduled run 33626099181 (2026-09-02T11:43Z, conclusion success) log: 'note: ledger derived from 0 closed-unmerged proposal PR(s) ...' then '[]'. tuppence propose-tier scheduled runs 33633036907 and 33515059588 = failure; ludlow 33518603510 = failure. The e2e probe i
OWNER: owned-by-open-ticket ticket 74 (lineage: REVIEW-2026-08-31 M9 -> ticket 60 -> ticket 74)
REMEDY: Ticket 74 as written. The concrete trigger: a real feed bump that pushes a caged residual across driftwood's £40,000 band (the step-3 probe shows the flip between £20,000 and £58,269), arriving by Renovate as step 2 already can, then a human merge.

## [major] DS-F3 The truth surface — the only record any document may cite — asserts a merged re-price that never happened, and the one bump that did merge moved no money, so step 2's second clause reads green on the board and is unmet in the estate
CLAIM: The re-price half of NORTH-STAR step 2 has never happened for real, and the e2e step-2 verdict line misdescribes an offline probe as a merged event.

(a) Exactly one Renovate PR has ever been merged anywhere in the estate — driftwood #20, head `renovate/feeds-threat-register-2.x`, single commit `ea1c8db` by `github-actions[bot]`, merged by a human 2026-09-01T08:57:50Z (checked across all eight units; the other seven have zero). It moved no money: the whole merge is 6 changed lines across 3 files, every one a version string, with `old_price` and `new_price` both `19558.549772440045`. That is an estate fact, not a composition defect — feeds `threat-register/v2` changes only the fintech scenari
EVIDENCE: Real bump: `git show ea1c8db -- composed/evidence.json` in driftwood → `"old_price": 19558.549772440045` / `"new_price": 19558.549772440045`; only version strings changed. e2e PASS (origin/main:talk/captures/verify_e2e_verify-e2e-step2-renovate-pins-and-reprices.out, last line): 'PASS: a merged pin bump (threat-register v1 -> v2) re-prices tuppence's prices[] through composition (222,574.31 -> 326,139.13), offline, with no repo touched'. tuppence's Renovate PRs #1, #2, #4, #10 are all CLOSED unm
OWNER: new none
REMEDY: Change step 2's PASS to the step-3 shape — 'a SYNTHETIC pin bump (no such PR has been merged on tuppence) ...' — and record, in ticket 74 or a successor, that no real bump has yet moved the £.

## [major] DS-F4 Step 6 has never Rekor-verified the intelligence publisher's tags and states in its PASS that it has none — the estate's one real cross-org bump consumes exactly that unverified artefact, and a not-looked is printed as an ok
CLAIM: Step 6 part 3 selects each unit's tag with `git tag -l 'v*.*.*'` (verify/e2e/verify-e2e-step6-provenance.sh:88), a pattern whose anchored literal leading `v` excludes feeds' per-feed scheme (`*/v*.*.*`, feeds .github/workflows/release.yml:24). Feeds therefore falls into `queued` (:89) and the script states, falsely, "no signed tag yet … feeds" (:104) and "feeds have no signed tag yet" in the PASS line (:119) — in the same run in which verify-feed-contract names threat-register/v1.0.0 and threat-register/v2.0.0 on feeds and explicitly defers their signature check to step 6 ("existence, not signature -- step 6 checks the signature"). Both tags are real annotated tags that I verified myself wit
EVIDENCE: Script: verify/e2e/verify-e2e-step6-provenance.sh part 3, `tag="$(git -C "$ESTATE/$u" tag -l 'v*.*.*' 2>/dev/null | sort -V | tail -1)"`; empty → `queued+=("$u")`. Real tags: `gh api repos/policy-as-versioned-feeds/feeds/tags --jq '.[].name'` → threat-register/v2.0.0, threat-register/v1.0.0. Glob check on the fresh clone: `git -C .../units/feeds tag -l 'v*.*.*' | wc -l` → 0. Run 21 capture: 'ok   no signed tag yet, honestly queued for cut-release.yml: feeds' and 'PASS: ... and feeds have no sign
OWNER: new DS-F4
REMEDY: Read each unit's tag shape rather than assuming vX.Y.Z — feed_contract.py already has tag_forms(entry, version) — or at minimum fall back to `git tag -l '*/v*.*.*'` and verify the newest per feed. One-line fix; removing the false sentence is the important part.

## [major] DS-F6 Flux has been proven to put policy in force on one adopter, not three, and the fix for the other two exists only in a local clone with no ticket owning it
CLAIM: tuppence and ludlow have never been **observed** with their composed set in force, and the fix has no owning ticket. Every five-fact sample either has ever appended — 9 records each, across 3 runs each, including the `tuppence-composed` / `ludlow-composed` sources — records fact_4 and fact_5 false. Ticket 60 (`Status: resolved`) attributes this to a sampler wait-ordering race and says a "round 3" fix is "committed and patched" on branch `ticket-60-wait-order`; that branch exists on no adopter remote (`gh api repos/.../branches` lists only `ticket-60-grade-the-lane-sample` and `ticket-60-sampler-waits` on all three), living only in the hub's local `.estate-clone/` (50fa2da / 6dcba87 / a396c3b
EVIDENCE: tuppence origin/main:drift/samples.jsonl newest record (run 33632084845, scheduled, 2026-09-02T12:49Z, on tuppence HEAD after PR #14 merged): fact_4 False — '12 of 16 rendered objects are absent from the cluster and 0 are live but unequal to the offline render'; fact_5 False — '16 of 16 rendered objects are in no Flux inventory'. Same shape in every earlier tuppence and ludlow record. Ticket 60's closing note: 'Round 3 (branch ticket-60-wait-order, one commit per unit, order mechanically asserte
OWNER: new ticket 60 (resolved, post-resolution comment only, no successor ticket)
REMEDY: Open a ticket for round 3 (or reopen 60) and get the three unit commits pushed and merged — enact_guard correctly refuses the push, so this needs the owner. Then confirm on the next scheduled sample rather than a dispatch.

OWNER QUESTIONS:
- What does 'end to end once, on a clock' have to mean before you would call it done — must a single scheduled run of the whole fleet produce all seven steps green in one TRUTH line, or is it enough that each step has been observed on its own clock at some point, and the honesty command reports the se | CHANGES: If you want one synchronised pass, a chaining or correlation mechanism has to be built (an id carried from the regulator tag through the bump PR, the proposal, the reconcile and the forecast) — that i
- The one real merged Renovate bump (driftwood #20) raised the pin but left the price identical. Does step 2 count as done, or does 'the composition re-prices' require the £ to have actually moved on a real merge? | CHANGES: If the £ must move, step 2 reopens, the e2e step-2 PASS line needs the step-3 SYNTHETIC treatment, and a real price-moving feed bump becomes a prerequisite of ticket 74 rather than a separate concern.
- Round 3 of the sampler wait-order fix is committed but unpushed, and enact_guard is doing exactly what it was built to do. Do you want to push it, and do you want an open ticket cut for work that is blocked only on your push? | CHANGES: With the push, three of run 21's seven fails have a route to green and 'three adopters consuming' becomes reachable. Without it, the destination is blocked on something no ticket owns and no review wi
- Step 5's script grades artefact presence, not the schedule, and its clock has failed both times it fired. Is the intended fix to make step 5 assert a dated sweep observation (ticket 64), or should the twin's schedule be treated as out of scope for the demonstration until the twin has its own signed  | CHANGES: Either step 5 gets hardened now (ticket 64) and goes SKIP until twin-sweep succeeds — moving the TRUTH line by one and making the deck's beat 5 honest — or step 5's sentence in NORTH-STAR §4 is amende
- When a proposal PR finally opens (ticket 74), who reviews and merges it, and against what standard — is your merge the demonstration, or does the estate need a second reviewer for the proposal to mean what §3 principle 5 says it means? | CHANGES: If a self-merge is the standard, ticket 74 can complete alone and step 3 goes green. If review is the standard, a second identity or a ruleset has to exist before the proposal PR is opened, which puts
- Should a check ever be allowed to say 'no signed tag yet' about a publisher whose tags it simply could not match — or should every 'queued / not yet' verdict be required to prove the absence rather than infer it from a failed lookup? | CHANGES: A rule that 'absence must be observed, not inferred' would add a small assertion to step 6 and to any other check with a queued/not-yet branch, and would make the honesty hedge list catch this class (

# truth-surface

VERDICT: Fit in mechanism, unfit in framing. The mechanism is real and rare: glob discovery of 84 scripts, three graded outcomes by exit code, a self-checking exclusions file, a pinned toolchain, a working observation cage (21 CI runs, zero cage-step failures), and one dated number committed to git under a verifiable Sigstore identity (`git -c gpg.format=x509 -c gpg.x509.program=gitsign log -1 --show-signature a209496` → "Good signature from …/truth.yml@refs/heads/main, Validated Rekor entry: true"). Nothing about it is fake, and nearly every serious limit I found was already written down in the estate's own hand. It is unfit as a health measure for three compounding reasons. (1) The denominator is a fiction: 12 of 84 scripts can never exit 0 on the scheduled runner — 11 demand a persistent kind cluster named driftwood that truth.yml never creates, and verify-schedules demands cross-org GitHub auth the gate step deliberately withholds — and 5 more are dark because versions.yaml declares one version, so `pass=57 … total=84` is measured against a structural ceiling of 70 and a today-reachable 65 that nothing states or checks. (2) The composition is not what the framing implies: platform contributes 29 of 57 passes and ~26 of those are self-proofs; on my classification only 20 of 57 passes rest on a fact spanning more than one party or reaching live external state, 5 are simulations on synthetic material, and zero of 84 scripts read a live Kubernetes API — the only cluster evidence in run 21 is three JSONL lines recorded 13 hours earlier. (3) The surface cannot see its own instrument failing: seven scripts exit 0 after printing their own "could not look" (demonstrated), six real eco-system clocks are failing today while the gate reports one SKIP, a fall in the number blocks nothing, and four TRUTH lines (runs 1, 2, 3, 14) have gone missing unnoticed. Six cheap changes would make it fit: exit 3 not 0 in the seven scripts; publish the ceiling beside the number; give verify-schedules a token or its own job; pin the flux install; move the hub cron after the adopters' sample lanes; build ticket 59's two §5 bullets. None is architecture.

STRENGTHS:
- One command, on a clock, in CI, writing one dated, SIGNED, committed number. Verified: `git -c gpg.format=x509 -c gpg.x509.program=gitsign log -1 --show-signature a209496` → 'gitsign: Good signature from [https://github.com/policy-as-versioned-flux/policy-as-versioned-flux/.github/workflows/truth.ym
- The exclusions mechanism is sound and cannot rot: talk/verify-all.sh:39-45 fails the gate both when a listed path has no reason AND when a listed path no longer exists. Both entries are provably executed by their parents in the same run — ico/verify-penalty-feed's capture shows 'Signature Verified S
- The observation cage works in practice. Across all 21 truth.yml CI runs, not one failed at the cage step: `gh run view <id> --json jobs` shows the failing step is 'fail if the gate failed' on runs 5-13 and 15-21, the tool install on run 14, and 'record the TRUTH line' on runs 2-3. No run has ever fa
- gitsign, kyverno and cosign are each pinned by version AND sha256 in .github/workflows/truth.yml:39-49, and the kyverno 1.18.2 pin carries its own written reason citing the 1.19.0 accident that reddened three checks for two runs.
- SKIP reasons are specific and actionable, never boilerplate — e.g. 'distribution/versions.yaml declares one version (4.0.0); coexistence needs two declared versions to show si…' and 'cs-16's cut-in-the-middle shape needs at least three declared versions and distribution/versions.yaml declares [4.0.0
- The reds are real estate faults, not instrument noise. Six of run 21's seven reds trace to four distinct estate defects with named owning tickets (73 cert-skew, 62/74 ludlow+tuppence objects absent, 72 twin feed re-render); only verify-demo is an instrument fault.
- The five-fact grader refuses to be fooled: drift/five-facts.py's grade() will not credit a sample unless its run field is a real GitHub Actions run id and the appending commit was authored and signed by the lane's own identity — closing a documented 2026-08-29 incident where three hand-typed lines g
- The estate documents its instrument's limits against its own interest, in its own words: units/driftwood/.github/rulesets/README.md 'Amended 2026-08-28: this leg is NOT in force, and cannot be until the repos are private'; verify-e2e-step3's capture 'a SYNTHETIC residual … not driftwood's real price
- The 2026-08-31 review's remediation verifiably landed. C1 (kyverno pin), M2 (signature spine — source-verification now prints 'it ACCEPTS the real signed tag'), M3 (VERIFY_TIMEOUT 900, publisher-gate completes in 358s), M4 (corpus SIGPIPE, now a glob with the reason in a comment), M5 (jsonschema==4.
- The cross-party contract layer IS genuinely observed: verify/feed-contract resolves 22 real cross-party pins against 8 real remotes plus ~60 envelope validations; verify/e2e/step6 verifies 7 real Fulcio cert subjects with Rekor entries validated; verify/party checks all 8 parties' own signed party.y

## [critical] TS-C1 The only instrument any document is allowed to cite can turn "could not look" into a green — including provenance's end-to-end PASS — so the project's one honesty claim is not true of the honesty machine itself
CLAIM: Nine verify scripts convert absence into a pass. Seven print a SKIP line and exit 0 — verify-cage-engine.sh:12-15, verify-comparison-window.sh:11-14, verify-gate.sh:11-14, verify-rederive-bumps.sh:9-12, verify-generator-standing-check.sh:18-21 (whole script), plus verify-corpus-generator.sh:34-37 (step 3 only) and verify-witness-set.sh:68-71 (step 5, falling through to PASS at :117) — all in platform/computed-semver/. Two more, verify/proportionality/verify-proportionality.sh (:71-76 kyverno body proof, :94 live tail) and verify/provenance/verify-provenance.sh (:47-49 openssl, :66-72 Rekor, :80-90 SPIRE), have no exit-3 path at all and degrade to printed notes before asserting PASS; provenan
EVIDENCE: Demonstrated live, read-only: `cd units/platform/computed-semver && env PATH=/usr/bin:/bin bash verify-cage-engine.sh; echo EXIT=$?` → `SKIP: kyverno CLI not found -- cage_engine's Track 1 (ValidatingPolicy admission) needs it` / `EXIT=0`; and with kyverno present `EXIT=0`. Both branches exit 0. Source: units/platform/computed-semver/verify-cage-engine.sh:12-15. Five siblings carry the identical whole-script form (`grep -n "command -v kyverno" -A4 verify-*.sh`): verify-comparison-window.sh:11-14
OWNER: new ticket 55 (partial, unmerged) / no ticket for the remaining seven
REMEDY: Change `exit 0` to `exit 3` in the six computed-semver scripts and make witness-set step 5 / corpus-generator step 3 propagate a script-level SKIP; add an exit-3 path to verify-proportionality and verify-provenance for their live tails. Ten one-line changes; the pattern is already written correctly in distribution/verify-render-version-tree.sh, which cites the 2026-08-25 incident this class came f

## [major] TS-M1 The deck that renders the demonstration is built from a local run at a superseded commit, so §4's story is shown to viewers as six SKIPs against a run that graded five PASS — and the red that says so is the one red no estate change can clear
CLAIM: verify-demo has FAILed on the last eight graded scheduled runs (13, 15-21; run 14 never reached the gate), and no hub-side mechanism keeps it green: talk/deck.md is outside truth.yml:38's OBSERVATION_LANE and the cage (truth.yml:126-144) fails the run on anything outside it, and no workflow builds the deck (`grep -rn "build_deck\|deck" .github/workflows/` → nothing; truth.yml's only gate step is `bash talk/verify-all.sh`) — contradicting talk/verify-demo.sh:31 "The committed deck is built by the scheduled workflow AFTER verify-all.sh finishes", and :44 "CI renders it on the scheduled run" (DECK_RENDER appears nowhere under .github/). It is not red by construction: runs 10-12 PASSed, because 
EVIDENCE: Grade history, read from the capture at each `truth: record run N` commit (`git show <sha>:talk/captures/verify_demo_verify-demo.out | tail -1`): runs 10, 11, 12 PASS; runs 13, 15, 16, 17, 18, 19, 20, 21 all `FAIL: talk/deck.md has been hand edited or is stale; run python3 talk/build_deck.py`. The run-21 capture shows the committed deck's beats 1-6 all `status=SKIP` against a rebuild of `PASS,PASS,PASS,FAIL,PASS,PASS`. Cause 1: .github/workflows/truth.yml:38 `OBSERVATION_LANE: "talk/truth.log dr
OWNER: owned-by-open-ticket ticket 66 (open); REVIEW-2026-08-31 M18
REMEDY: Either add a deck-build step to truth.yml after the gate and add talk/deck.md to OBSERVATION_LANE (a declaration the clock may write, which is a real ADR-0024 decision, not a config tweak), or make the two scheduled-only checks compare the committed deck against the captures at ITS OWN commit rather than this run's. Correct verify-demo.sh:33-36's header either way — it currently describes machiner

## [major] TS-M2 Principle 5 is untrue today — feeds do not refresh, the twin does not sweep, proposals do not open — and the one citable surface records it as a single SKIP
CLAIM: verify-schedules can never PASS inside the citable run, its server-side-ruleset half emits no line at all, and five real clocks are red today unseen by it.

Never PASS, for two independent reasons: (a) 13 distinct liveness questions SKIP on every CI run because the gate step carries no GitHub credential — .github/workflows/truth.yml:65-66 checks out `persist-credentials: false`, :92-98 gives the gate only `{VERIFY_TIMEOUT: '900'}`, and GH_TOKEN reaches only the cage step at :109-110, so `gh auth status` fails and schedules.py:507-512 sets live=False; (b) three unconditional D2 SKIPs (ico/nist/platform fetch.yml carry no `gh pr create`; schedules.py:538-546) would force exit 3 even with a wor
EVIDENCE: verify/schedules/verify-schedules.sh:38-48: any per-clock SKIP makes schedules.py return 3 and the wrapper exits 3. The run-21 capture (talk/captures/verify_schedules_verify-schedules.out) carries 14 lines of the form "SKIP: <unit>/<workflow>: GitHub unreachable (Command ['gh','auth','status'] returned non-zero exit status 1.) -- cannot look at whether this clock ran inside its period". The ruleset check at verify/schedules/schedules.py:561 is guarded `if live and os.path.isdir(...)`, so with li
OWNER: owned-by-open-ticket ticket-56
REMEDY: Either (a) move verify-schedules into a second job in truth.yml that carries a narrowly-scoped cross-org read token and report its verdict into the same TRUTH line, or (b) have each unit's own clock publish a liveness observation into its own observation lane which the hub then reads offline. Independently and cheaply: change schedules.py:561 so the ruleset question emits a SKIP with its reason wh

## [major] TS-M4 The honesty instrument cannot tell a new break from a standing one, and the tracker has already gone false about the estate (NORTH-STAR §5, principle 6)
CLAIM: Of §5's last two bullets, one and a half are unbuilt. Bullet 5's first sentence IS built — `talk/truth.log` on origin/main holds 18 dated, run-numbered, SHA-stamped lines, and run 21 was recorded even though the run concluded `failure`. Its second sentence, "A fall is a blocking event", is unbuilt: `grep -rn "truth.log" talk/ verify/ .github/` returns 21 hits in 10 files, all of them writers (`.github/workflows/truth.yml:99-102`), OBSERVATION_LANE allow-list declarations, prose, or `talk/build_deck.py`, which selects only the line matching this commit (`build_deck.py:137-154`) and asserts only that a quoted line exists and is same-commit (`:391-400`). No code anywhere in the hub or in the ei
EVIDENCE: `grep -rn "truth.log" talk/ verify/ .github/` returns only writers (truth.yml:5,38 and the record step) and talk/build_deck.py, which quotes the newest line. No consumer compares two lines. Empirically: run 17 → 18 fell from pass=59 fail=1 to pass=57 fail=3 (talk/truth.log lines 14-15) and both runs' workflow conclusion is `failure` for the same reason as every other run — the signal is saturated (17 of 21 CI runs fail at the step 'fail if the gate failed'). For derived Status: `git -C units/pla
OWNER: duplicate-of-0831-finding-still-open ticket 59 (open); REVIEW-2026-08-31.md M14
REMEDY: Build ticket 59 as written: (a) truth.yml diffs the new TRUTH line against the previous and raises a distinct failure on any fall in pass or rise in fail, with a committed-reason escape hatch mirroring the exclusions file; (b) map each resolved ticket to its named gate check and flag Status from the scheduled run. Immediately and for free: correct ticket 55's Status and Answer, since its work is m

## [major] TS-M6 The only citable measure of "what works" cannot tell a loosely coupled eco-system apart from one party testing itself: 20 of 57 passes cross a party boundary, and the composition is nowhere labelled
CLAIM: Of the 57 passes on TRUTH run 21, platform owns 29 (platform's rows are 29 PASS / 15 SKIP / 1 EXCLUDED, 45 of the 84 discovered scripts) and 24 of those 29 are the platform grading its own modules against its own fixtures — the remaining five being three cross-party observations (compose/verify-composition against real driftwood, oscal/verify-claims against nist's real catalogue, distribution/verify-infra-declaration against driftwood/ludlow/tuppence's composed policies) and two simulations (verify-cut-release-tags, oscal/verify-upflow). On a per-script classification — where a pass counts as an eco-system observation only if its verdict turns on an artefact published by a party other than t
EVIDENCE: Grades by owning repo, re-derived from the run-21 log: platform 29 PASS / 15 SKIP / 1 EXCLUDED (45 of 84 rows); hub 13 PASS / 1 SKIP / 2 FAIL; driftwood 1/0/3; feeds 3; ico 3 (+1 excluded); insurer 1 PASS 1 SKIP; ludlow 2 PASS 1 FAIL; nist 3; tuppence 2 PASS 1 SKIP 1 FAIL. Classification is per-script, from reading all 82 run-21 captures. Self-proofs include all 10 computed-semver scripts, fair, tcor, honesty, wardley, wargamer, risk, party-artefact, break-glass, the four cert-identity-regexp sc
OWNER: new none
REMEDY: Tag each script in a committed manifest with its class (estate-observation / self-proof / simulation) and emit the split in the TRUTH line, e.g. `pass=57 [observed=20 self=31 simulated=5 meta=1]`. The classification is cheap because most scripts already say which they are in their own headers.

## [major] TS-M8 Step 5 of the demonstration can grade green off artefacts two other checks in the same run prove wrong: the e2e harness never consults the owning repo's verdict on the artefact it names
CLAIM: In TRUTH run 21, E2E step 5 reports PASS and prints `ok driftwood/twin/forward-intel/v1/feed.json (the feed the estate consumes)` while, in the same run, `.estate-clone/driftwood/verify-twin-overlay.sh` and `.estate-clone/driftwood/twin/verify-twin-scenarios.sh` both FAIL because that exact file is not what the overlay renders (its `derived_from` still names threat-register version 1; party.yaml has pinned v2 since 2026-08-28, and signals.yaml still carries only a v1 row). Step 5's `ok` on that path is a bare `[ -e ]` presence test (verify/e2e/verify-e2e-step5-twin-forecasts.sh:16-20, 26); step 5 as a whole is not presence-only — it also runs verify/twin-evals/verify-twin-evals.sh and grades
EVIDENCE: verify_e2e_verify-e2e-step5-twin-forecasts.out: 'ok driftwood/twin/forward-intel/v1/feed.json (the feed the estate consumes)' … 'PASS: the twin's overlay, feed, 6 scenarios and signal lookup are all present, and its own evals scored them'. Same run: .estate-clone_driftwood_verify-twin-overlay.out → 'FAIL: emit-forward-intel.py --check: FAIL: twin/forward-intel/v1/feed.json is not what the overlay renders' / 'TOTAL: could not read the feed; nothing further was observed'; .estate-clone_driftwood_t
OWNER: owned-by-open-ticket ticket-64 (open, misattributed as 66 in the finding) + ticket-72 (open)
REMEDY: Make step 5 consume the twin-overlay and twin-scenarios verdicts (the same way step 7 reads other steps' captures) so a red on the artefact reds the beat, instead of asserting presence.

OWNER QUESTIONS:
- What is the truth surface FOR — a daily regression alarm for you, or the citable evidence a skeptical audience is shown? The two want opposite denominators and opposite failure modes. | CHANGES: If it is an alarm: build ticket 59's fall-checker, publish a ceiling line, and stop treating fail=0 as the done bar. If it is evidence: split the TRUTH line into observed / self-proved / simulated (TS
- Is a persistent Kubernetes cluster in scope for the daily clock, or never? Eleven scripts hard-code a kind cluster named `driftwood` that truth.yml has never created and contains no code to create. | CHANGES: If in scope: truth.yml gains a KinD bring-up (minutes of runtime) and the ceiling rises to ~81. If never: the eleven should move to a separately-graded live suite invoked by talk/up.sh, and the daily 
- Is "at least three policy versions coexisting" still a required property of this estate, or did the retirement to a single 4.0.0 line supersede it? | CHANGES: If required: re-declaring a second and third line turns five SKIPs into real observations and gives the thesis's headline claim green evidence for the first time. If superseded: those five need an exc
- Should the hub's clock move to fire AFTER the adopters' drift-sample lanes, so step 4 grades today's cluster rather than yesterday's? | CHANGES: A one-line cron change collapses the feedback loop on tickets 72/73/74 from a day to hours, and makes a fix landing in the morning visible the same evening. If you prefer the current ordering, the dec
- What counts as "green"? fail=0 alone, or fail=0 AND every SKIP either owned by a ticket or listed with a reason? | CHANGES: It determines whether TS-C2's ceiling line is cosmetic or load-bearing, and whether the SKIP list needs the same reason-or-fail discipline the exclusions file already has. It also settles what ticket 
- Should a unit repo be able to move one of its own checks from red to green between two TRUTH lines? clone-estate.sh clones unpinned default branches, and its own comment says to pin once signed tags exist — which they now all do. | CHANGES: Pinning to signed tags makes the number reproducible and matches what Flux actually runs, at the cost of the surface lagging merged fixes by a release. Not pinning is defensible but the stale comment 

# pound-engine

VERDICT: Against what NORTH-STAR.md:32 actually asks — "one £ currency, proportionate to the org … so a pay rise, a hardening control, a cage tier, an insurance transfer and a strategic play are comparable" — the engine is close to fit and is the greenest surface in the estate: every £ verify script (fair-tail, tcor, risk-tuned, composition, pound-seam, honesty, penalty-feed, insurer-party, party) PASSes on TRUTH run 21, and none of run 21's seven FAILs is in this dimension. I reproduced three published numbers exactly by hand from signed artefacts (driftwood's uk-gdpr exposure £1,787,177.0751717847 to 2 ulp, the insurer's £113,403.30 premium exactly, the twin's lm triple byte-for-byte). The perspective/currency partition is watertight and enforced in code, not documentation; the hole breakdown is a strict partition refused unless the weights sum to 1.0; the multi-source correlation model in fair.py is actuarially correct and proved by its own selfcheck. Where it is not fit is that the estate publishes these figures with no qualifier, and a second party has priced a real insurance layer off them. On the estate's own numbers and its own engine, the expected loss to the 3m-xs-40k layer the insurer wrote for driftwood is £2,470,722 against a £113,403 premium — an implied loss ratio of 2,179% — so driftwood's signed exposure and the insurer's signed quote cannot both be believed. Underneath that, the frequency that annualises everything is an editorial constant asserting ~2.17 ICO monetary penalties a year against one £86m retailer and cannot draw a loss-free year at all; the magnitudes rest on n=2 published fines rescaled by a ratio that is neither the decided rule (ticket 07 §1's min()) nor the statute (Art 83(4)'s "whichever is higher"); one loss-magnitude table is a platform-held fixture of exactly the class ADR-0021 retired; and the estate's own back-test instrument, which runs and reports "under-prices, 40% VaR95 exceedance" for driftwood and "over-prices, ratio 0.127" for ludlow, is wired to nothing and grades a different number from the one that gets published. What would make it fit is small and mostly editorial: state on the artefact what the number is (an ordinal, auditable, traceable comparison instrument, not an expected annual loss), publish the insurer's implied loss ratio against the exposure it prices and let it be red, get size onto tuppence and ludlow so "proportionate to the org" is true of more than one adopter of three, aggregate the retained residuals against the one band instead of spending it once per line, and wire the calibration verdict to something.

STRENGTHS:
- Three published numbers reproduce exactly by hand from signed inputs. driftwood's uk-gdpr exposure: I re-implemented pert/simulate from their docstrings (not by importing them) off units/ico/penalty-schema/v3/feed.json (rate 0.02, cap_gbp 8,700,000, examples 7,552,800 and 275,000) and units/driftwoo
- The perspective/currency partition is enforced in code, not by discipline. fair.py:67-93 (sum_prices) raises on any mix of perspective or currency; composition.py:1704-1711 refuses a forward-intel feed pricing another party's balance sheet; insurer/pricing/quote.py refuses a quote insuring another a
- premium is structurally excluded from exposure. composition.py:1875-1879: EXPOSURE_KINDS = ('feed', 'twin'), with the reason stated -- 'folding it in would make the premium an input to the formula that computes it'. That is the right call, enforced by a tuple rather than by care.
- The hole breakdown is a strict partition, refused otherwise, and its keys are real. composition.py:1467-1472 raises Refused if published control_weights do not sum to 1.0 (abs_tol 1e-9); ico/verify-penalty-feed.sh:170-206 independently re-checks sum, duplicates, range and regime/violation-type exist
- The multi-source correlation model is actuarially correct and proved. fair.py:106-131 draws one frequency per year and sums magnitudes WITHIN each event across obligation sources, rather than simulating each regime independently; the selfcheck at fair.py:300-310 constructs the naive independent sum 
- Two selection engines are cross-checked at the band boundary and a disagreement is a hard FAIL. verify/pound-seam/pound_seam.py:566-596 runs the adopter's own selection_policy.py and platform/graded/cage.py:select_tier over 60 constructed cases (each rung's exact band boundary +/- 1e-6, every rung t
- Missing-instrument discipline is coherent and asymmetric in the right direction. A missing or stale size widens to the publisher's statutory cap and never refuses (composition.py:1477-1506); a publisher declaring no currency, or an FX rate absent for the date, refuses and emits nothing (composition.
- Nothing in the pound path is red on the citable run. From `gh run view 33616685427 --log`: platform/fair/verify-fair-tail.sh PASS, platform/tcor/verify-tcor.sh PASS, platform/risk/verify-risk-tuned.sh PASS, platform/compose/verify-composition.sh PASS, verify/pound-seam PASS, platform/honesty/verify-
- The self-referential pricing cycle was found and cut, correctly. driftwood/twin/emit-forward-intel.py:230-239 records that the insurer's quote -- priced off the exposure whose largest line derives from this very feed -- used to be named in derived_from, 'and it made the premium and the forecast mutu
- The ponytails are honest, dated and name their own upgrade path: quote.py:31-38 on rate-on-line vs E[layer loss], composition.py:1894-1901 on sum-of-means vs an aggregate distribution, cage.py:71-79 on the table being 'the PLATFORM's, self-declared, evidenced by nothing but this comment'. Almost non

## [major] PE-01 Principle 3's named comparable fails at its only instance: the estate's one insurance transfer is priced at 1/22 of the risk it takes, so the £ cannot yet be used to choose between a transfer, a control and a cage tier — and the gate reports it green
CLAIM: The insurer's premium for driftwood is 1/22.9 of the expected loss on the layer it writes, and this is readable straight off the quote's own published fields, not only from a simulation. `units/insurer/quote/driftwood/v1/feed.json` publishes `layer = 2,592,075.49` and `premium = 113,403.30`; premium/layer = 0.04375 = rate 0.035 × (1 + load 0.25) exactly (`quote.py:191-192`, `terms/driftwood.yaml:24-25`). Because `layer` is computed off `exposure.total` (`quote.py:189`), which is a sum of annualised expected losses — I reproduced all three driftwood exposure lines from `platform/fair/fair.py` to within 2e-10 of their signed amounts — the layer IS an expected-annual-loss quantity. Simulating t
EVIDENCE: Ran offline in units/platform/fair: `fair.simulate((1,2,4),(54367.816091954024,773782.5287356322,1791836.6896551724),n=200000,seed=42)` for the ico line, `((2,4,9),(1000,4000,9000))` for threat, `((2,4,9),(192000.0,384000.0,800000.0))` for twin; summed year by year; insured = 0.4*ico + threat + twin (terms/driftwood.yaml excludes pl-2+ra-3 = 60% of the ico line); layer = min(max(x-40000,0),3e6). Output: mean summed annual loss 3,696,478 (signed total 3,704,381.737952101 in units/driftwood/compos
OWNER: new ticket 14 (resolved, formula 1.0.0 ratified as interim) / ticket 37 (open, does 
REMEDY: Compute the implied loss ratio (E[layer loss] / premium) inside verify-insurer-quote.sh and print it on every run, red above some declared band. It costs one function and forces the owner to say which of the two numbers is the wrong one, rather than publishing both.

## [major] PE-02 The regulator publishes the fine but the platform invents how often it lands — so the estate's "annualised" £ is not an annual loss, and no participant owns the number that would make it one
CLAIM: driftwood's published 1,787,177 GBP uk-gdpr exposure is the annual-loss expectation of a fair.py simulation whose frequency is an editorial constant, DEFAULT_WARN_LEF = (1, 2, 4) at units/ico/schema/to_fair_scenario.py:38, asserting a mean 2.165 ICO monetary penalty events per year against one 86m-turnover retailer. The schema publishes no frequency and says so (:19-22); the whole headline is exactly linear in the constant (halving the triple halves the ALE to 0.500x; doubling gives 2.006x), and the ALE reproduces to the last digit offline (1787177.0751717845 vs evidence.json's old_price 1787177.0751717845). The schema can cite only five real UK GDPR monetary penalty notices in total across 
EVIDENCE: units/ico/schema/to_fair_scenario.py:38 `DEFAULT_WARN_LEF = (1, 2, 4)   # plausible regulatory-incident frequency, events/yr`; :19-22 'lef is NOT in the schema ... ponytail: a flat editorial per-regime warn-LEF'. units/platform/fair/fair.py:133 `freqs = [max(0, round(f)) for f in pert(*lef, n, rng)]`. My re-derivation of the 10,000 frequency draws for lef=(1,2,4) seed 42: {1: 1188, 2: 6055, 3: 2674, 4: 83} -- mean 2.1652/yr, minimum 1, zero years with no event. The schema itself (units/ico/penal
OWNER: new none
REMEDY: Either require the publisher to ship an lef with a cited base rate and a stated denominator (events per controller-year), and refuse as a missing instrument otherwise; or rename the output on the artefact so it stops claiming to be annualised. A third, cheap option: make frequency a counting distribution (Poisson with the PERT as the rate prior) so a loss-free year is at least possible.

## [major] PE-05 The appetite is not an org budget: nothing sums what driftwood retains, so every line reads "inside the band" while the org carries 2.18x it — and each new priced parent raises the retained total with every line still green
CLAIM: Cage tier selection runs independently per prices[] entry against the whole appetite band: composition.py:1934-1981 passes the same scalar `tolerance` to price_parent (once per feed edge, 1964-1967) and to price_twin (1977), and cage.py:182-187 select_tier — plus driftwood's own selection-policy/selection_policy.py:88 — compares ONE line's caged residual to the full band. Driftwood's three exposure lines each genuinely satisfy that test against 40,000 GBP (units/driftwood/party.yaml appetite.tolerance) while the residuals they retain sum to 87,387.44 GBP: ico isolated 1,787,177.075 x 0.02 = 35,743.54; threat-register baseline 19,558.550 x 0.70 = 13,690.98; twin isolated 37,952.92 as recorded
EVIDENCE: composition.py:1934-1981 (compute_prices) calls price_parent/price_twin once per edge, each passing the same bare `tolerance`; cage.py:171-181 select_tier compares one line's caged_residual to the full band. From units/driftwood/composed/evidence.json and cage.TIERS reduce values: ico proposed_tier 'isolated' -> 1,787,177.075 x 0.02 = 35,743.54; threat-register proposed_tier 'baseline' -> 19,558.550 x 0.70 = 13,690.98; twin proposed_tier 'isolated' -> residuals.isolated = 37,952.92 recorded on t
OWNER: resolved-ticket-but-still-true ticket 14 (resolved, Q2 decision (a)) / ticket 25 (resolved 2026-08-29, build)
REMEDY: Once fair.py gains the portfolio aggregate that composition.py:1894-1901 already plans, compare the summed retained residual to the band and record the headroom on the exposure section. Even before that, printing the sum of selected-tier residuals beside the band would make the breach visible.

## [major] PE-06 The one £ input NORTH-STAR names by name — a regulator's penalty applied to the adopter's own size — under-prices every adopter's statutory ceiling by about 5x, and the estate's own check certifies the wrong rule as correct
CLAIM: ico's converter scales the whole loss triple by (rate x turnover)/cap with neither a min nor a max (units/ico/schema/to_fair_scenario.py:68-71), while the decided rule (ticket 07 section 1, echoed at map.md:32) is hi = min(rate x turnover, cap) with the examples scaled by hi/cap. The converter's selfcheck at :167-172 asserts the uncapped ratio exactly, so a passing test locks the divergence in, and no open ticket owns it — the build shipped under ticket 25 (commit 9aa89df, not ticket 21 as first stated), and tickets 07, 21, 24 and 25 are all resolved. Two corrections to the impact. First, the code-vs-decision divergence bites only above turnover = cap/rate = 435,000,000; below it the min() i
EVIDENCE: units/ico/schema/to_fair_scenario.py:68-71 `cap, rate = float(f.get('cap_gbp') or 0), f.get('rate'); if turnover is not None and cap and rate: scale = (float(rate) * float(turnover)) / cap; lo, mode, hi = lo*scale, mode*scale, hi*scale` -- no min, no max. Ran against the shipped v3 schema: turnover unset -> (275,000, 3,913,900, 9,063,360); turnover 86,000,000 -> (54,368, 773,783, 1,791,837); turnover 1,000,000,000 -> (632,184, 8,997,471, 20,835,310) with cap_gbp = 8,700,000. Selfcheck at :167-16
OWNER: new tickets 07/21/24/25 (all resolved)
REMEDY: One line: apply the decided min() (or, if the owner prefers the statute, max(cap, rate*turnover)) and update the selfcheck assertion at :167-172 to the chosen rule. Record which of the two the owner wants in ticket 07's answer.

## [major] PE-07 The money half of the threat line is neither published nor proportionate: loss magnitude is an unsigned, adopter-keyed constant in the platform's repo, so that exposure is the platform's opinion about an adopter rather than a priced consequence of signed facts
CLAIM: The threat line's loss magnitude is a platform-held per-adopter fixture — the same pattern ADR-0021 retired — and the migration that is owned would leave it intact.

`platform/feeds/to_fair_scenario.py:34-38` hard-codes `THREAT_LM_GBP` keyed by adopter name (driftwood 1k/4k/9k, tuppence 5k/25k/90k, ludlow 20k/100k/400k) and `:44` reads it. The feeds publisher's own `threat-register/v2/feed.json` payload carries only `threat`, `flavour`, `lef` and `source` per institution — no magnitude — so the number that turns the threat line into money is written and signed by the platform, not by the feed's publisher, and is not derived from any adopter's signed `size` (driftwood declares turnover 86m GB
EVIDENCE: units/platform/feeds/to_fair_scenario.py:34-37 `THREAT_LM_GBP = {'driftwood': (1_000, 4_000, 9_000), 'tuppence': (5_000, 25_000, 90_000), 'ludlow': (20_000, 100_000, 400_000)}` and :44 `lm = THREAT_LM_GBP.get(institution, ...)`. units/feeds/threat-register/v2/feed.json payload.institutions carries only threat/flavour/lef/source per institution -- no magnitude. The feeds repo ships no to_fair_scenario.py (find across the clone returns only platform/feeds/to_fair_scenario.py), so composition.py:54
OWNER: new n/a
REMEDY: Move THREAT_LM_GBP into the feeds publisher's own feed payload (per institution, beside the lef it already ships) and move to_fair_scenario.py into the feeds repo, so composition's converter resolution finds the publisher's copy. That is what ticket 24 item 4 already decided.

## [major] PE-08 The £'s proportionality is exercised on one adopter of three: tuppence and ludlow publish no size, so NORTH-STAR §2's adopter description is untrue of two of the three named example consumers
CLAIM: Two of the three adopter parties declare no `size` block at all (only units/driftwood/party.yaml:33-38 does), so tuppence's and ludlow's uk-gdpr exposure is the publisher's unsized statutory-cap read — both exactly 9,039,791.01976426 GBP, hole for hole — against driftwood's 1,787,177.0751717847 (the same figure times its 1,720,000/8,700,000 turnover scale), and every per_customer restatement in both is null (composition.py:935-949; :1487-1506). Their other prices[] line, threat-register, does differ (tuppence 222,574.31 fintech, ludlow 318,229.78 healthcare), so the two are not identical overall — only on the regulator line. NORTH-STAR.md:43 step 2 scopes itself to "one adopter" and driftwoo
EVIDENCE: `grep -l '^size:'` across units/{driftwood,tuppence,ludlow}/party.yaml returns driftwood only. units/tuppence/composed/evidence.json and units/ludlow/composed/evidence.json both carry `ico feed penalty-schema 9039791.01976426`, identical to the last decimal, versus driftwood's 1787177.0751717847 (= 9,039,791.01976426 x 0.19770114942528735, the turnover scale factor). composition.py:1487-1506 `_sized_turnover` returns None with no size and the publisher then 'prices at its own statutory cap, the 
OWNER: new none
REMEDY: Add a signed size block to tuppence's and ludlow's party.yaml. It is a five-line change per repo and it converts the estate's central claim from one instance to three.

## [major] PE-10 A signed regulator feed publishes an editorial apportionment as if it were published regulatory fact, and the carrier cites it as authority for retaining 60% of the adopter's largest exposure line
CLAIM: ico's control_weights carry no stated derivation anywhere in the feed envelope, its changelog, its payload schema (which forbids one: weight items are `additionalProperties: false` over source/id/weight) or its verify script — unlike the fine schedule beside them, where every regime carries an authority and a statute and three of the four carry dated public penalty notices (pci-dss cites undated, unnamed acquirer bands and says so). The values are round multiples of 0.05, and unlike other admitted guesses in the same repo (the editorial LEF, the demo signing key, the carrier's rate and load) they carry no `ponytail:` honesty flag. They nonetheless fix all four of driftwood's uk-gdpr hole pri
EVIDENCE: units/ico/penalty-schema/v3/feed.json payload.control_weights['uk-gdpr']['lower-tier'] = pl-2 0.3, ra-3 0.3, ca-2 0.2, ir-8 0.2 (every regime's weights are round tenths). The changelog states only what the field IS, not where the numbers come from. units/ico/verify-penalty-feed.sh:170-206 checks sum-to-1, duplicates, range (0,1] and regime/violation-type existence -- never provenance. Downstream: units/driftwood/composed/evidence.json holes = 536,153.1225515354 / 536,153.1225515354 / 357,435.415
OWNER: new none
REMEDY: Add a `basis` string per weight entry in the payload schema, the way the fines already carry `source`, and have verify-penalty-feed.sh refuse a weight with no basis. Even 'editorial, unsourced' as an explicit value is better than silence.

## [major] PE-11 The transfer arm has no working clock: the insurer's quote is provably stale against the exposure it prices, and the only mechanism that could refresh it has refused on every run it has ever had
CLAIM: Every scheduled insurer requote job has failed on both of its runs to date (2026-09-01 33496526156, 2026-09-02 33615860064; all three matrix legs, at step "re-price and compute the bump") with "REFUSED: missing instrument: .adopters/<adopter>/composed/HEADER.yaml carries no `exposure` section". The cause: fetch.yml checks each adopter out at the tag insurer/party.yaml pins, v1.1.0, and v1.1.0's tree predates the exposure section — the section landed in driftwood at commit ceac84b on 2026-08-29, four days after tag v1.1.0 (eacae33, 2026-08-25); same for tuppence and ludlow, and no adopter has cut a tag above v1.1.0. party.yaml's own signed comment asserting v1.1.0 is "the one whose tree carri
EVIDENCE: `gh run list --repo policy-as-versioned-insurer/insurer` shows fetch runs 33496526156 (2026-09-01) and 33615860064 (2026-09-02), both conclusion failure. `gh run view 33615860064 --repo policy-as-versioned-insurer/insurer --log` shows all three jobs failing identically: 'requote (driftwood) REFUSED: missing instrument: .adopters/driftwood/composed/HEADER.yaml carries no `exposure` section -- there is no signed exposure to attach a layer to' and the same for tuppence and ludlow. Consequence recor
OWNER: new none
REMEDY: Either bump the insurer's exposure pin to a tag whose tree carries the exposure section, or have the requote job resolve the pin to the newest tag that does and record which. The verify script already detects the staleness; the fix is a pin, not code.

## [major] PE-12 The transfer arm is not priced on the same footing as the other three, so the board's Total Cost of Risk overstates ceding a risk and the "one currency, comparable moves" principle is not true where the estate books it
CLAIM: tcor.py:117-118 prices a transfer as premium = full do-nothing ALE x (1+load) with residual = the deductible's face amount, so the sub-deductible layer is charged to the carrier and retained again. The overstatement is exactly d + load x E[retained], strictly positive whenever a deductible exists, so the transfer arm is systematically dearer than a correct layer decomposition: for driftwood's third-party-fulfilment risk the shipped £21,150 should be ~£16,135 (annual-aggregate grain, the grain ticket 14 chose) or ~£15,005 (per-occurrence), inflating the captured board line from £42,923 to the £47,921 in run-21's .estate-clone_platform_tcor_verify-tcor.out, ~12%. Two corrections to the finding
EVIDENCE: units/platform/tcor/tcor.py:116-118 `premium = ale_warn * (1.0 + load); out['transfer'] = line(deductible, 0.0, premium)`. Correct decomposition is premium = (1+load) x E[loss above deductible] and residual = E[loss below deductible]; at ~2.17 events/yr the expected retention alone is roughly 2.17x the per-event deductible, not 1x. Run-21 capture .estate-clone_platform_tcor_verify-tcor.out: 'balance sheet £47921 = residual £27271 + controls £3500 + premium £17150' -- the 17,150 premium is the th
OWNER: resolved-ticket-but-still-true ticket 36 (graduated from ticket 14)
REMEDY: premium = (1+load) * (ALE_warn - E[retained below deductible]); residual = E[retained below deductible]. Both are one extra simulate() call away, since fair.py already returns the annual loss list.

OWNER QUESTIONS:
- What is the £ FOR: an ordinal decision instrument (rank control options, make a dependency bump visible as a number in a reviewed PR), or a balance-sheet quantity a CFO books and a carrier cedes against? | CHANGES: Whether the remedy is editorial (qualify exposure.total on HEADER.yaml and in the deck, publish the calibration verdict beside it) or structural (a grounded frequency, an aggregate loss distribution, 
- Is appetite.tolerance one economic quantity or three? Today the same signed 40,000 GBP is a threshold on the marginal benefit of a control (enforce.py), a threshold on retained mean annual loss (cage.py), and an aggregate insurance attachment (exposure_section, consumed by the insurer's layer). | CHANGES: Whether party.yaml gains two more signed fields, whether the cage ladder compares to a per-line or an aggregate figure, and whether the insurer's attachment is the same number at all. It changes drift
- Is the insurer a real second opinion whose quote must be defensible, or an illustrative counterparty demonstrating the shape of a signed cross-org transfer? | CHANGES: Whether quote.py's formula goes to 2.0.0 off an aggregate loss distribution (its own stated upgrade path), whether the rate/load are recalibrated, and whether verify-insurer-quote.sh gains a loss-rati
- Is the twin's forward-intel line a further consequence of the same breach the ico line prices (a lost-conversion loss alongside a regulatory fine), or a distinct event? Its own payload declares claim_scope.included = ['uk-gdpr']. | CHANGES: Either the twin's lm moves inside the ico entry's lm list under one shared frequency (which lowers the total and removes the 2.17-vs-4.5 inconsistency), or claim_scope is corrected and the exclusion k
- Should tuppence and ludlow declare size before the estate claims proportionality, and where should a loss FREQUENCY legitimately come from? | CHANGES: Adding size to two party.yaml files is a five-line change per repo and converts the estate's central proportionality claim from one instance to three. The frequency answer decides whether ico's conver
- Should the adopter's selection-policy package be allowed to differ from platform/graded/cage.py, and should platform's unevidenced reduce table or the adopter's measured curve price the residuals? | CHANGES: Whether the pound-seam check narrows (agree on ordering and fail-closed rung, not on every selection), whether composition prices twin residuals off the adopter's own published curve, and whether ADR-

# twin-validity

VERDICT: Not fit yet — for one fixable set of reasons and one that is a decision, not a fix. The twin is neither theatre nor intelligence: it is a genuinely strong, self-auditing apparatus for HOLDING A BELIEF TO ACCOUNT (real Brier/log-loss, a real git-tree rewind that withholds post-T facts by construction, three information regimes with no default, structural refusal to score Netflix/Intel, an AST-verified planter/detector split, a grading system that refuses hand-typed grades, 1550 tests and 70/1/3 invariants green on a live runner today) wrapped around a forecast that is a hand-typed constant. `twin/verbs.py:932-956` emits `"probability": float(beliefs[proposition_id])` — nothing anywhere infers a probability from a signal, and the package prints that fact in its own artefact at run time. The estate seam is wired but not load-bearing: the twin's £1,897,646 line is driftwood's largest price and does reach an enacted Namespace cage tier, but via a hand-typed label no check verifies, from a feed that sits outside every signed tag driftwood has cut (v1.1.0 contains zero paths under twin/), produced by a clock that has fired twice, failed twice, and never written an observation line. What would make it fit: (a) the owner decides whether the twin must derive a probability or whether the north-star row is restated to say it scores recorded beliefs; (b) require provenance (evidence_grade + basis) on a world model as the schema already does for a valuation; (c) close the four already-owned tickets — 72 (the `bash -e` sweep bug), 64 (tags, signatures, three adopters, step-5 hardening), 51 (the scored headline), 30 (the twin's own cage); (d) three small unowned fixes: refuse when a declared `publishes[]` feed is absent, one check binding composed `proposed_tier` to `posture.acme.io/tier`, and a non-saturated skill eval.

STRENGTHS:
- A forecast really has been scored against a real, dated, cited outcome and the answer is honestly red. I reproduced it: `PYTHONPATH=. python3 -P -m twin backtest --repo <fixture> --org royal-mail --scenario would-the-twin-have-flagged-it --regime as-consumed --at 2018-06-01` then `twin score --outco
- The subjects' facts are real, not authored to produce a verdict. `grep -o "https://[^ \"']*" twin/fixtures.py | sort -u | wc -l` → 81 distinct real citations (SEC EDGAR, FCA, commonslibrary.parliament.uk, the Royal Mail prospectus on data.parliament.uk, BaFin, trade press), plus 18 example.invalid U
- The engine refuses to score what it cannot honestly score: Netflix (too famous to distinguish anticipation from recitation) and Intel (unresolved until 2026H2/2027H1) carry no answer key and `twin score` structurally refuses; Kodak and Maersk carry no outcome by design. Invariant 37 in today's CI as
- The invariant harness is real, runs daily on a live runner, and is honestly red for a pre-registered reason. Hub twin.yml run 33615039125 (2026-09-02T09:36Z): `RESULT: 70 passed, 1 failed, 3 skipped (0 pending invariants, 3 skipped and not faked)` and `1 failed, 1550 passed in 115.13s`. The single f
- Capability grades are computed and non-fakeable, not self-declared. `PYTHONPATH=. python3 -P -m twin grade` → `aggregate: 73 of 73 across 13 capabilities, 13 at full`, reproduced locally; twin/grades.py:150-188 refuses a hand-typed `grade:` key and refuses a capability whose checklist text or count 
- Several anti-gaming mechanisms are structural rather than prose: twin/detector.py imports nothing naming `planter` (AST-checked on real source) and detect() returns byte-identical output with a decoy `plants` key spliced in; misuse.log_removal() accepts no float parameter so a constraint-removal num
- The self-disclosure is specific and, at the sharpest point, is exactly the finding an auditor would write. `twin regimes` printed live: 'model residual not computed: … A forecast here reads a world model's declared belief and nothing infers it from a signal, so the three probabilities are identical 
- Test discipline is high and behavioural: 33,872 lines of twin source against 18,623 lines of tests, 1,326 `def test_` functions, 1,550 collected tests — roughly one test per 22 lines — and the tests run the CLI end to end, AST-scan real source, and construct negative controls that must fail.

## [critical] TWIN-01 The twin — one of the eco-system's six named participants — does not forecast: its scored number is an authored constant transcribed from YAML, so the flagship "we can prove when we're wrong" beat exercises the scoring plumbing and never the twin's judgement
CLAIM: Every probability the twin scores under a proper scoring rule is a verbatim read of an authored `beliefs:` entry: `twin.verbs.run()` emits `"probability": float(beliefs[proposition_id])` (twin/verbs.py:948) and nothing in the forecast path derives that number from a signal, a fact, an edge or a component's position. On the flagship falsifiability beat the scored number is the 0.05 authored at twin/fixtures.py:4242, and the run's own stdout concedes it: "A forecast here reads a world model's declared belief and nothing infers it from a signal, so the three probabilities are identical by construction". The five real, cited, dated Royal Mail signals are loaded and admitted (they appear in the b
EVIDENCE: twin/verbs.py:932-956, the only probability expression in the package (grep for `probability` in twin/pricing.py returns nothing). Reproduced live: `PYTHONPATH=. python3 -P -m twin backtest … --at 2018-06-01` then `twin score …` printed `market-consensus-2013 p=0.05 brier=0.9025`; 0.05 is literally the `beliefs:` value in orgs/royal-mail/world_models/market-consensus-2013.yaml inside twin/fixtures.py. `twin regimes` on the same scenario printed `as-consumed 5 fact(s) admitted` and `model residua
OWNER: new none
REMEDY: An owner decision, not a patch. Either build a signal→probability path (which reopens twin 11 Q1's ordinal-arithmetic and grade-5 rulings), or amend the north-star twin row and the deck's beat-5 title to say the twin scores RECORDED beliefs under proper scoring rules — a smaller, fully true claim.

## [major] TWIN-02 Every flagship back-test scores a number the artefact attributes to a real third party ("Market consensus, pre-crisis") with nothing recording how that belief was established — so the twin's headline red is a claim about a market it never sourced
CLAIM: A `signal` document requires `source` and `provenance` (twin/schema.py:615-624), a `valuation` requires `evidence_grade` and `basis` (:232), a causal edge requires an `evidence_grade` (:857-860) — but a `world-model`, whose belief is the sole origin of every scored probability in the shipped pipeline (twin/verbs.py:935-948; `forecast_book.emit` has no production caller), requires only `id`, `name`, `beliefs` (:611-614). Stronger than first stated: the schema is closed (:461-485), so a world model cannot carry a source, provenance, evidence grade or calibration even if its author wanted to — `Schema.validate` refuses each with "The schema is closed — it accepts beliefs, credence, id, name, no
EVIDENCE: twin/schema.py:611-614 (world-model Schema: required {id, name, beliefs}; optional {credence, note}) vs twin/schema.py:615-624 (signal: required {id, date, steep, source, statement, provenance}) vs twin/schema.py:232 (valuation requires `basis`). `grep world_model twin/invariants/*.py` finds only forecast-shape checks. Measured over the fixture corpus: 20 world_models/*.yaml documents in twin/fixtures.py, exactly 1 contains any URL, and that one cites Cowgill & Zitzewitz in support of the narrat
OWNER: new none
REMEDY: Add `evidence_grade` and `basis` as required fields on the world-model schema, exactly as `valuation` already requires them, and require a `calibration` block whenever a causal edge declares a grade inside the pricing threshold. Both are small, closed-schema changes the loader already has machinery for.

## [major] TWIN-03 The twin is the one participant whose output nobody can verify — and it is the source of the adopter's single largest price, so "every actor is attestable" is false exactly where the money is
CLAIM: Everything driftwood's twin publishes sits outside every signed tag, and so does the record that declares it. driftwood's newest tag v1.1.0 (eacae33, 2026-08-25) contains 60 paths and zero under `twin/`; `twin/forward-intel/v1/feed.json` was added 2026-08-28 (055e340, ticket 25) and last changed 2026-08-29 (f75509a, ticket 29), so the feed, its `bump: none` rule, and the `publishes[]` entry that declares it all live only on untagged main — v1.1.0's party.yaml has no `publishes:` block at all. The hub has zero tags locally and on origin, so the `twin/v0.1.0` that PIN.yaml pins (`tag_cut: false`) does not exist either. Every other publisher's declared feed path IS inside its newest gitsign-sig
EVIDENCE: `git -C units/driftwood ls-tree -r --name-only v1.1.0 | grep -c '^twin/'` → 0. v1.1.0 is dated 2026-08-25 (`git log -1 --format='%ci' v1.1.0`); twin/forward-intel/v1/feed.json was added 2026-08-28 (commit 055e340, 'ecosystem ticket 25'). v1.1.0's composed/evidence.json carries prices from ['ico','platform'] only. driftwood/party.yaml:62-66: 'Signed by the same gitsign tag as everything else here (ADR-0012), so an untagged feed.json on a branch is never a signature.' twin/forward-intel/bump.yaml 
OWNER: owned-by-open-ticket ticket 64
REMEDY: Cut twin/v0.1.0 on the hub (needs the release workflow truth.yml lacks; the owner dispatches) and cut a driftwood release covering twin/forward-intel v1 by queueing its bump.yaml — exactly what ticket 64 already specifies. Until then, add a gate row that goes amber naming the absence.

## [major] TWIN-04 The twin participant has published nothing on its schedule: the twin's clause of principle 5 and step 5 of the demonstration rest on a sweep whose move path cannot execute, and the citable run cannot see it
CLAIM: driftwood's twin-sweep workflow has fired twice on its schedule (33508119299, 2026-09-01T12:31Z; 33627910027, 2026-09-02T12:04Z), failed both times, and has never written an observation or opened a proposal PR. The cause is exactly as stated: the sweep step runs under GitHub's default `shell: /usr/bin/bash -e {0}` while its body opens only `set -uo pipefail`, so `python3 twin/emit-forward-intel.py --check` exiting 1 — the very "the feed moved" signal the job exists to act on — aborts the step before `rc=$?` (twin-sweep.yml:88-91). Both downstream steps show as skipped in the run, proving `moved` was never set; `observations/` does not exist on main and no `twin/forward-intel-*` branch exists
EVIDENCE: `gh run list --repo policy-as-versioned-driftwood/driftwood --workflow twin-sweep.yml` returns exactly two runs, both `schedule`, both `failure`: 33508119299 (2026-09-01T12:31Z) and 33627910027 (2026-09-02T12:04Z, after run 21's TRUTH line). `gh run view 33627910027 --log-failed` shows `shell: /usr/bin/bash -e {0}` while the script body opens `set -uo pipefail`, then `FAIL: twin/forward-intel/v1/feed.json is not what the overlay renders` followed immediately by `##[error]Process completed with e
OWNER: owned-by-open-ticket ticket 72
REMEDY: `set +e` around the `--check` call, or `python3 twin/emit-forward-intel.py --check || rc=$?` — the same pattern verify-reconcile.sh already uses. Then re-derive the signal-lookup rows too, per ticket 72 finding 2.

## [major] TWIN-05 The twin's own participant definition — forecasts scored against reality — is true for no adopter, and the ratified ambition still names a clock the build deliberately replaced with a human
CLAIM: NORTH-STAR §4 step 5's named stimulus has never been played forward, and the truth surface grades step 5 green anyway on a presence check. Driftwood's own signal lookup declares niobium `unbound` by design (twin/signals.yaml:89-98), and the human-run classify-and-judge skill that replaces automated binding for it has never been executed (run-21 capture: "no adopter repo carries a claim file yet -- the skill has not been run for real"). But no scenario at all is on driftwood's clock: twin-sweep.yml runs only `emit-forward-intel.py --check`, which "loads neither scenarios nor world models" (twin/VENDORED.md:63-64), and signals.yaml's only consumer in the whole repo is verify-twin-scenarios.sh:
EVIDENCE: units/driftwood/twin/signals.yaml, `unbound_scenarios`: 'niobium-supply-shock-2026 … arrives as a dated headline a human classifies with the classify-and-judge skill (ticket 50). Binding it to a pin here would be the twin deciding on the clock, which is the one thing this table exists to prevent.' Run-21 capture talk/captures/.estate-clone_feeds_verify-news-headline-skill.out: 'ok no adopter repo carries a claim file yet -- the skill has not been run for real'. `find units/driftwood/twin -type d
OWNER: owned-by-open-ticket ticket 51 (open) + ticket 23 (resolved, Answer item 1)
REMEDY: Either restate NORTH-STAR §4 step 5 to describe the human-run classify-and-judge path (the decision ticket 23 actually took), or land ticket 51 and run the skill once for real so the beat has a citable instance.

## [major] TWIN-06 The citable number counts north-star step 5 as observed-true when the north star itself records that step as never having run, and the deck's twin beat rests on that verdict
CLAIM: verify-e2e-step5-twin-forecasts.sh grades six `[ -e ]` path-existence checks plus a count of six scenario YAMLs, then runs verify/twin-evals/verify-twin-evals.sh — whose seven skill metrics all score 1.000 against both threshold and prior, and whose feed assertion is over the hub's own published envelopes "with a placeholder tag/commit", never driftwood's rendered feed. Nothing it runs looks at the artefact's content. In TRUTH run 21 (verified in the CI log for run 33616685427, not only the captures) it printed `ok driftwood/twin/forward-intel/v1/feed.json` and PASSed, in the same run in which .estate-clone/driftwood/verify-twin-overlay.sh FAILed with "twin/forward-intel/v1/feed.json is not 
EVIDENCE: verify/e2e/verify-e2e-step5-twin-forecasts.sh header: 'This script does not pretend to look at a forecast; it looks at the five things step 5 needs'. Run-21 captures on origin/main: verify_e2e_verify-e2e-step5-twin-forecasts.out → 'ok driftwood/twin/forward-intel/v1/feed.json (the feed the estate consumes)' … 'PASS'; .estate-clone_driftwood_verify-twin-overlay.out → 'FAIL: twin/forward-intel/v1/feed.json is not what the overlay renders'; .estate-clone_driftwood_twin_verify-twin-scenarios.out → '
OWNER: owned-by-open-ticket ticket 64 (.scratch/ecosystem/issues/64-the-twin-is-three-adopters-tagged-and-si
REMEDY: Make step 5 run `emit-forward-intel.py --check` itself (or read verify-twin-overlay's verdict) so the beat cannot be green while the feed it names is stale, and change the beat title to what the script proves.

## [major] TWIN-07 The demo's own proof that the twin is "not decoration" is a number that cannot fall: the gate's only graded twin-quality signal scores a heuristic against the corpus it was fitted to, and in one case against its own lookup table
CLAIM: The truth surface carries exactly one twin quality metric (verify/twin-evals/verify-twin-evals.sh), and all seven of its metrics sit at the 1.000 ceiling against a baseline recorded once, on 2026-08-13, in a single commit (f91a41c) — so the metric can only ever move down, and its recorded bar has never been refreshed. At least one of the seven is tautological, and worse than the record admits: for evolution-judge, all FOUR corpus labels (Carillion 0.62, NMC 0.55, Wirecard 0.8, Enron 0.45) are bit-identical values in `_MATURITY_KEYWORDS` (twin/evolution_judge.py:71-85), and in every case the firing phrase sits in the component's own NAME (twin/evolution_judge.py:257-262), so the accumulated e
EVIDENCE: Run-21 capture talk/captures/verify_twin-evals_verify-twin-evals.out: seven lines all reading `score=1.000 … last=1.000`. twin/skill-scores.jsonl: exactly 7 lines, all `recorded_at 2026-08-13T00:00:00Z`, corpus totals 23, 4, 4, 4, 3, 3, 5 = 46 items; `git log --oneline -- twin/skill-scores.jsonl` → one commit, f91a41c. twin/evolution_judge.py:71-85 `_MATURITY_KEYWORDS` contains payment-acquiring/merchant acquiring (Wirecard), construction/support-services (Carillion), hospital operations/healthc
OWNER: owned-by-open-ticket .scratch/ecosystem/issues/23-the-news-feed-and-the-headline-skill.md (Facts, ref
REMEDY: Either hold out a corpus the heuristic was not fitted on (even 5 items from a fifth org would break the tautology), or reclassify the seven metrics on the surface as harness-mechanism checks rather than skill scores, so a reader of the TRUTH line is not told the twin's judgement was graded.

## [major] TWIN-09 Nothing ever checks that the cage a workload actually runs in is the cage the £ selected — the estate can enact a tier no price justifies and still grade green
CLAIM: The last mile from the £ to the cage is unbound: no check anywhere in the estate compares a party's composed `proposed_tier` to the `posture.acme.io/tier` label on its governed Namespace, so ADR-0022's ratified sentence — the tier is "declared on the Namespace manifest ... rendered from the signed composed artefact" (docs/adr/0022:15-16) — is satisfied today only by a hand-typed label and a hand-written comment (driftwood gitops/apps/namespace.yaml:22-30, last written by commit 348b97c, 2026-08-28). The estate is not inert here: driftwood's scheduled `propose-tier.yml` runs `platform/wargamer/tier_pr.py run` for real every day and succeeded on 2026-09-01 and 2026-09-02 (runs 33505428933, 336
EVIDENCE: units/driftwood/gitops/apps/namespace.yaml sets `posture.acme.io/tier: "isolated"` with a comment reading '"isolated" is the strictest proposed_tier across today's composed/evidence.json prices[] for this party'. `grep -rln "proposed_tier" units/driftwood --include='*.py' --include='*.sh' --include='*.yml'` → no matches. On the hub, only verify/e2e/step2_reprice.py and step3_band.py read the field, and both operate on a tempfile.TemporaryDirectory() copy that is not a git repository. platform/co
OWNER: new TWIN-09
REMEDY: One check, in the adopter's shift-left or in compose: read `proposed_tier` from composed/evidence.json, read `posture.acme.io/tier` from gitops/apps/namespace.yaml, and refuse when the label is looser than the strictest priced tier.

OWNER QUESTIONS:
- Must the twin DERIVE a probability from signals, or is 'a recorded belief, scored honestly against reality' the thing you actually want? Today `twin run` reads a YAML number and nothing infers one, and the package says so in its own output. | CHANGES: If derivation is required: a new capability, a new eval that is not corpus-circular, and probably a model call inside a reviewed-PR skill, plus TWIN-02's provenance requirement becomes urgent. If not:
- Should a world model's beliefs carry a required evidence grade and basis, the way a valuation and a causal edge already do? Today the schema will accept any probability with no source, and 19 of 20 world models in the corpus carry none. | CHANGES: Adding it forces a grade and a basis onto reference-map.yaml's 13 driftwood beliefs and Royal Mail's market-consensus 0.05 — which will make several of them visibly grade 4 or 5, and grade 5 may not p
- Is it acceptable for the twin's forward-intel feed to remain outside every signed tag and outside `inherits[]`, so that its absence is silent and its bytes carry no gitsign signature — while it produces driftwood's largest price line? | CHANGES: If not acceptable: cut twin/v0.1.0 and a driftwood forward-intel release (ticket 64), and make composition raise `Refused` when a declared `publishes[]` feed is absent. If acceptable as a deliberate e
- What cage does the twin itself run inside, on the ticket 09 ladder, at what price? Ticket 30 is open and frontier, and today there is no twin cage in any adopter — the Article 22 floor exists only as `enact_guard.py`'s command-pattern net and the absence of a merge function. | CHANGES: It determines whether ticket 30 is a small artefact (a twin-self cage spec plus a price in the adopter's overlay, wired into verify-all.sh) or a real piece of infrastructure (a caged runtime for the a
- How do you want the twin's judgement quality graded, given that all seven metrics score 1.000 on 46 items drawn from the same subjects the heuristics were fitted to, and evolution-judge's eval is literally a lookup table being asked to reproduce its own lookup? | CHANGES: A held-out corpus (even five items from a fifth org) breaks the tautology and makes the metric able to fall — which is what ticket 29 promised. Alternatively, relabel the seven metrics on the surface 
- Should NORTH-STAR §4 step 5 be restated? The build deliberately reversed its mechanism — driftwood's signals.yaml declares niobium unbound precisely so the twin cannot decide on the clock — but the ambition document still describes the twin playing the niobium headline forward on its schedule, and t | CHANGES: If restated: step 5 becomes 'a human runs the classify-and-judge skill and the reviewed PR lands a claim', ticket 51's scope narrows, and verify-e2e-step5 can be rewritten to check that a real claim f

# security-spine

VERDICT: The artefact-signing spine is genuinely fit and is the strongest thing in the estate: 24 real keyless tags with Rekor entries, per-repo anchored identity pins that are negatively tested and enforced at four independent places (release.yml, the adopter gate, five-facts.py, the cluster boundary), no key material committed, and a cluster-side verifier whose design decisions — verify at the signed tagger time, refuse an unanchored regexp before using it, never turn could-not-look into a verdict, prove by AST walk that it cannot sign — are better than most production systems. Against NORTH-STAR §2's "consumed only through a pinned, signed dependency", the platform→adopter and nist→adopter legs are proven on the citable run. The enforcement spine is not fit yet, for three non-overlapping reasons. First, the one control that actually gates enactment is broken in a way its own proof cannot see: verify_gitsign.py evaluates the Fulcio chain at the tag's tagger second, which is systematically at or below the certificate's notBefore, so it rejects 5 of 24 genuine estate tags (including ico v3.0.0, driftwood v1.1.0 and ludlow v1.1.0, all live pins) while its own fixture happens to be delta-0 and passes. Second, everything about actor attestation — SPIRE, mTLS, SPIFFE authz, OpenBao, device SVIDs, human login — has never been observed running on any citable run (all six scripts SKIP on "kind cluster 'driftwood' is not listed"), and federation is one trust domain with no peer and the peers' anchors held as literals in the platform's own tree, which is the tenant shape §2 forbids. Third, the governance layer principles 4 and 5 rest on — "bump only by reviewed PR", "a human merges" — has no enforcement point anywhere: zero rulesets and zero branch protection on all nine repos, verified live, plus an accepted release/<M>.<m>.x signing path that touches main at all and that nothing covers. What would make it fit, in order: (1) fix the tagger/notBefore race and add a delta-−1 tag to the fixture corpus — a day's work that unblocks four of run-21's seven reds; (2) protect main and release/*.x on all nine repos with a required review, converting principles 4 and 5 from convention to mechanism; (3) pin the Flux install and the Renovate install like every other tool — hours; (4) sign the proposer's commit and delete the hardcoded signed:True before step 3 ever fires, so no fabricated provenance can enter a real proposal; (5) either land the party/schema.json patch so federation becomes a party-owned signed fact, or write down that federation is out of scope for this build. Items 2 and 5 are owner decisions; the rest are mechanical and the estate already does each of them correctly somewhere else in the same repository.

STRENGTHS:
- 24 of 24 estate tags are real keyless gitsign signatures with real Rekor entries under per-repo anchored identity pins. Re-derived on one: `cd units/ico && gitsign verify-tag v3.0.0 --certificate-identity-regexp='^https://github\.com/policy-as-versioned-ico/ico/\.github/workflows/cut-release\.yml@re
- The identity pins are correct, per-repo, and negatively tested, and all six identity-regexp checks are PASS on the citable run 21. Every units/<u>/.github/workflows/release.yml carries the identical anchored shape with only the org/repo substituted. units/driftwood/scripts/verify-identity-regexp.sh:
- verify_gitsign.py's trust decisions are principled: it evaluates the chain at the tagger timestamp inside the SIGNED payload rather than 'now' (lines 20-25, 191-196); it REFUSES an unanchored identity regexp before using it (lines 213-217, 'an unanchored pattern matches a substring of any identity a
- A pod cannot pick its own tier, and three named live defects in that area were found and closed. units/driftwood/composed/policies/v4.0.0/cage-tier.yaml:30-37 takes the tier from `namespaceObject` and CLOBBERS the pod's own posture.acme.io/tier label; the fallback is fail-closed (`variables.nsTier i
- The observation cage is real code that demonstrably bites. .github/workflows/truth.yml:108-162 does `git reset -q` FIRST (fixing a reproduced 2026-08-28 defect where staged-and-clean entries rode along), stages only OBSERVATION_LANE, asserts the STAGED SET against the same env list rather than a sec
- Tool supply chain is pinned by version AND sha256 nearly everywhere: gitsign 0.17.1/69213a8a…, kyverno 1.18.2/cb2feb83…, cosign 3.1.3/4629c757… (truth.yml:38-49); kind, flux, kyverno and flux-operator all version+digest in units/driftwood/.github/workflows/drift-sample.yml:47-61, whose header states
- No private key material or credential literal is committed anywhere. `grep -rln "BEGIN .*PRIVATE KEY"` over all eight unit clones and the hub (excluding .git) returns zero; units/ico/schema/keys/ and units/platform/feeds/keys/ hold only *.pub.pem; no ghp_/github_pat_/AKIA literal found.
- twin/enact_guard.py:94 now fails closed (`DEFAULT_MODE = "operations"`), flipped 2026-08-29 after discovering that thirteen tests asserting guard behaviour passed only because an autouse fixture forced a mode the shipped default did not use. The module names its own ceiling honestly rather than dres
- truth.yml:59-66 deliberately withholds the write credential from the untrusted scripts: `persist-credentials: false` with a written rationale, and GH_TOKEN scoped to the cage step's env: only. This is a real, deliberate mitigation and better than the GitHub default, even though its residual (SS-07) 

## [major] SS-02 Only the artefact half of "every actor is attestable" is proven on the clock; the actor half — workloads, humans, devices, the agent — has never been observed anywhere, and a resolved ticket rests on a check that has never been able to look
CLAIM: No citable TRUTH run has ever carried a green live identity tail: on run 21 (and on every capture back to run 9) five identity-plane scripts — platform/identity/verify-identity.sh, platform/identity/verify-federation.sh, platform/access/verify-access.sh, platform/eud/verify-eud.sh and platform/verify-source-verification.sh — grade SKIP with the same substrate reason, "kind cluster 'driftwood' is not listed by kind get clusters", so no SVID, mTLS handshake, SPIFFE-principal authorization, live OpenBao jwt config, Pomerium/Dex login, device SVID or gitsign-verifier pod has ever been observed on a source NORTH-STAR §5 permits a document to cite. The sixth script named in the original finding, b
EVIDENCE: Last lines from origin/main:talk/captures/, run 21: `.estate-clone_platform_identity_verify-identity.out` -> `SKIP: offline proof holds; live tail could not look: kind cluster 'driftwood' is not listed by kind get clusters — SPIRE is Istio's CA, mTLS STRICT, authz by SPIFFE principal…`; `.estate-clone_platform_identity_verify-federation.out` -> same SKIP; `.estate-clone_platform_verify-source-verification.out` -> same SKIP; `.estate-clone_platform_access_verify-access.out` -> same SKIP; `.estate
OWNER: new SS-02
REMEDY: Decide whether the identity plane is in scope for the citable surface at all. If yes, give it an ephemeral-cluster lane of the shape drift-sample.yml already proves works (fresh KinD per run, bounded waits, delete on always) so the live tails can grade for real. If no, record that decision in NORTH-STAR §6 and change the SKIP reason lines so they state the absence rather than restating the claim.

## [minor] SS-03 Anti-tenancy is proven where §2 makes it load-bearing (signed artefacts, pinned dependencies); the unbuilt workload-identity layer leaves the platform holding unsigned facts about three adopters — declared, refused-to-pass, and owned by ticket 68
CLAIM: Federation is declared, not live, and the peers' trust anchors are literals in the platform's own tree. There is one trust domain, `acme.internal` (units/platform/identity/spire/helmrelease.yaml:55), with no `federation` key in the SPIRE values at all, so no bundle endpoint is served. Only driftwood has ever run SPIRE: verify-federation.sh:21-23 records that platform runs no live cluster in this estate and tuppence/ludlow carry no SPIRE CRDs — three of the four "cluster-running" parties, not two. Because party/schema.json is `additionalProperties: false` and party_artefact.py:185-188 reads its allowed top-level keys straight from it, a party.yaml carrying the three decided fields is REFUSED 
EVIDENCE: units/platform/identity/README.md:74-86 and units/platform/identity/federation/driftwood.yaml:12-31 name three blockers: driftwood's live SPIRE still runs the estate-wide `acme.internal`; tuppence and ludlow run KinD with no SPIRE; `spire-server.federation.enabled` is off so no bundle endpoint is served. Confirmed in code that the two live checks assert mutually exclusive worlds: units/platform/identity/verify-identity.sh:50 asserts `td == "acme.internal"` (single domain) while verify-federation
OWNER: owned-by-open-ticket ticket 68
REMEDY: Either (a) patch party/schema.json to accept trust_domain, bundle_endpoint and federates_with[], migrate the four party.yaml files, and delete the literals from platform/identity/federation/ — small and named; or (b) record in NORTH-STAR §6 that federation is explicitly out for this build, and mark ADR/README accordingly so the aspiration stops reading as pending work. Which one is an owner call.

## [major] SS-04 "Reviewed PR" and "a human merges" are unverifiable assertions, not checkable properties — and ADR-0001's recorded risk acceptance rests on a forge-immutability leg that exists on none of the nine repos
CLAIM: The signature attests a workflow path, not a review — and there is zero branch protection, zero rulesets, zero environment protection and zero tag protection on all nine repositories, a regression from the 2022-thesis org which still has both.

Anyone with push access to a unit repo can rewrite cut-release.yml, push it to main unreviewed, dispatch it, and produce a tag whose certificate every consumer's identity pin accepts; nothing on GitHub prevents this in any of the nine repositories. Verified live: `rulesets` -> `[]` on all nine, including with `?includes_parents=true` (so no org-level ruleset applies either); `branches/main/protection` -> 404 "Branch not protected" on all nine; `enviro
EVIDENCE: Verified live, read-only, on all nine: `gh api repos/policy-as-versioned-<u>/<u>/rulesets` -> `[]` for platform, nist, ico, driftwood, tuppence, ludlow, feeds, insurer AND the hub; `gh api repos/.../branches/main/protection` -> `404 {"message":"Branch not protected"}` for all nine. The GitHub OIDC subject is the workflow FILE PATH at a ref (units/platform/.github/workflows/release.yml:54 pins `…/cut-release\.yml@refs/heads/(main|release/…)$`), not the workflow's content, so the certificate attes
OWNER: new none
REMEDY: Apply branch protection or a branch ruleset on main in all nine repos requiring a pull request and at least one approving review from someone other than the author, and cover release/*.x (see SS-05). If a single-operator estate cannot satisfy 'a reviewer other than the author', say so explicitly in NORTH-STAR §6 rather than leaving principle 5 reading as enforced.

## [minor] SS-05 The maintenance-branch signing path principle 4 deliberately admits is unexercised, stale and governed by nothing — fixing unprotected `main` without covering `release/*` would be half a fix
CLAIM: Every unit's EXPECTED_IDENTITY_REGEXP accepts a tag signed from a release/<major>.<minor>.x branch — in each unit's own release.yml and in the three adopters' cross-repo pins (driftwood/tuppence/ludlow platform-pin.yaml and shift-left.yml) — platform has such a branch today (release/2.0.x, carrying a working workflow_dispatch cut-release.yml with no ref guard), and no ruleset or branch protection exists on any ref of any of the eight repos. A dispatch on that branch still runs the publisher gate but needs no review and touches main not at all, producing a tag whose signature every consumer accepts. Two things the gate does not do: it never checks who may write to or dispatch from that ref (v
EVIDENCE: All eight release.yml pins end `@refs/heads/(main|release/[0-9]+\.[0-9]+\.x)$` (units/platform/.github/workflows/release.yml:54 and the seven siblings). Live: `gh api repos/policy-as-versioned-platform/platform/branches --jq '[.[].name]|join(",")'` -> `ecosystem/thin-slice,main,observations,policy-composition/tickets-09-16-wip,release/2.0.x,renovate/configure,repair/…` — release/2.0.x exists. cut-release.yml is workflow_dispatch, which accepts any branch. SS-04's ruleset query returns [] for eve
OWNER: new none
REMEDY: Extend whatever protection SS-04 lands to the release/*.x ref pattern, and add an assertion to the existing verify-certificate-identity-regexp.sh family that every branch matching the accepted pattern in the live repo is protected — so the pin and the protection cannot drift apart.

## [major] SS-06 The gate breaks the estate's own "everything the gate observes with is pinned" rule inside the very identity that signs the only citable number
CLAIM: truth.yml:91 pipes an unpinned remote installer to root — `curl -s https://fluxcd.io/install.sh | sudo bash` — inside the single `gate` job that, eight steps later, gitsign-signs and pushes talk/truth.log with `github.token` (truth.yml:153-163). It is the only curl-to-shell in the hub or in any of the eight unit repos, and it sits directly under its own step comment (truth.yml:79-81) that "Every tool the gate observes with is pinned by version AND checksum", between two steps that do verify sha256sums (84-89). Three unit workflows state the opposite rule and implement it: driftwood/.github/workflows/drift-sample.yml:44-46 ("A `curl | bash` installer would put an unreviewed script inside the 
EVIDENCE: .github/workflows/truth.yml:80-92. The step's own comment reads 'Every tool the gate observes with is pinned by version AND checksum, like gitsign above. An unpinned tool makes the number unreproducible: the same estate grades differently on two days for a reason no TRUTH line records.' The step then does `echo "${KYVERNO_SHA256}  kyverno.tgz" | sha256sum -c -`, `echo "${COSIGN_SHA256}  cosign" | sha256sum -c -`, and finally `curl -s https://fluxcd.io/install.sh | sudo bash`. The estate's own ru
OWNER: new issues/54-the-gate-observes-with-the-estate-s-own-toolchain.md (adjacent, does n
REMEDY: Copy drift-sample.yml's pinned tarball form verbatim — the version and digest are already known in this estate. This is a five-line change and removes both the reproducibility gap and the root-code-execution step.

## [major] SS-07 The only citable number is signed and pushed from the same process that just ran eight other orgs' unpinned code, while the clock records a separation guarantee it does not have
CLAIM: The hub's write credential is separated from the gate's untrusted foreign scripts by process convention, not by a boundary, and the comment asserts a property the design does not have. truth.yml runs one job: the gate step (.github/workflows/truth.yml:92-98) runs `bash talk/verify-all.sh`, which discovers 84 verify*.sh scripts — 68 of them cloned unpinned off eight other orgs' default branches into .estate-clone/ (clone-estate.sh:22,28,58), 16 the hub's own under verify/ — and runs each as plain `bash` (talk/verify-all.sh:47,60), unsandboxed, as the runner user, in a job whose earlier steps use passwordless sudo (truth.yml:75,86,90,91). A later step in that same job holds `GH_TOKEN: ${{ gith
EVIDENCE: .github/workflows/truth.yml:158-160: `# The credential is handed to THIS command only; nothing the gate ran could reach it (see persist-credentials above).` followed by `git -c http.extraheader="AUTHORIZATION: basic $(printf 'x-access-token:%s' "${GH_TOKEN}" | base64 -w0)" push origin HEAD:"${GITHUB_REF_NAME}"`. The gate step (truth.yml:93-100) runs `bash talk/verify-all.sh`, which discovers and runs every verify*.sh across .estate-clone (full clones of eight orgs' default branches, per clone-es
OWNER: owned-by-open-ticket ticket 56 (read-credential half only); write-credential half + overclaiming comm
REMEDY: Either move the cage/commit/push into a separate job that does not run foreign scripts (artifact-passing the transcript), or soften the comment to state what persist-credentials:false actually buys. Widen ticket 56 to cover the write credential, not only the read one.

## [major] SS-08 Step 3's signing half is unbuilt while the proposer's own record asserts it: the one artefact authored by an agent rather than a human carries a manufactured Rekor claim, and a discovered verify script grades that literal as a pass
CLAIM: The proposer's commit is unsigned while four places in the estate assert a hardcoded gitsign/Rekor claim about it, and the derived word "signed" is already printed to humans in the citable run's committed captures.

Mechanics: units/driftwood/.github/workflows/propose-tier.yml:184-185 is the only git config in the file and sets user.email/user.name alone — the file contains no gitsign install and none of `gpg.format x509`, `gpg.x509.program gitsign`, `commit.gpgsign true`, unlike twin-sweep.yml:211-213, drift-sample.yml:251-253 and the hub's .github/workflows/truth.yml:153-157 (the finding cited 145-149). Ludlow (:233) and tuppence (:223) are identical. units/platform/wargamer/tier_pr.py con
EVIDENCE: units/driftwood/.github/workflows/propose-tier.yml:184-185 is the ONLY git config in the file: `git -C driftwood config user.email "wargamer-proposer@policy-as-versioned-driftwood.invalid"` / `git -C driftwood config user.name "wargamer proposer"` — no `gpg.format x509`, no `gpg.x509.program gitsign`, no `commit.gpgsign true`, unlike truth.yml:145-149, drift-sample.yml's cage step and twin-sweep.yml, all three of which set exactly those three lines with a written explanation. units/platform/warg
OWNER: owned-by-open-ticket ticket 12 (identity-as-spine), owner decision re-grill 16 / P142
REMEDY: Add the three gitsign config lines to propose-tier.yml alongside the existing two, and derive wargamer.py's `signed`/`identity` from the actual commit (`git log --format=%G?` / the certificate SAN) rather than a literal — or delete both fields. Do it BEFORE ticket 74 fires step 3.

OWNER QUESTIONS:
- Should a gitsign tag be trusted when the Fulcio certificate's validity window opens one second AFTER the tagger timestamp the tag itself signs? Put differently: is the correct trust instant the tagger's claimed time, the certificate's notBefore, or the Rekor signed entry timestamp? | CHANGES: SS-01's remedy and severity, SS-12's remedy, the fixture corpus in platform/distribution/verify/testdata/, and at least four of run-21's seven fails (three adopter verify-reconcile scripts and verify-
- Is this estate meant to demonstrate that policy-as-a-versioned-dependency is enforceable against a repository's own writer, or only against foreign parties? Concretely: should main and release/*.x be protected with a required review across all nine repositories, given you are the only human in the e | CHANGES: SS-04 and SS-05's severity and remedy. If protection is applied, several tickets that stand resolved on 'bumped by reviewed PR' become genuinely proven rather than asserted; if the answer is 'this is 
- Is the SPIRE / Istio / OpenBao / Pomerium / EUD identity substrate in scope for the citable truth surface, or is it a designed-and-shelved package? And if in scope, is federation (four trust domains, twelve objects) part of the ambition for this build or explicitly later? | CHANGES: SS-02 and SS-03's severity and whether they are shortfalls or out-of-scope. If in scope, it adds an identity lane to the truth surface and a party/schema.json build ticket; if out, NORTH-STAR §6 gains
- Should the proposer's commit be gitsign-signed, and should wargamer.py's `signed`/`identity` fields be derived from the actual commit or deleted? Is this a blocker for ticket 74 firing step 3 for real? | CHANGES: SS-08's severity (latent vs discharged) and whether ticket 74 should be blocked on a signing fix. If the answer is 'sign it', the three gitsign config lines go into propose-tier.yml and the two litera
- Is the hub's truth workflow allowed to execute 84 unpinned scripts from eight other organisations' default branches in the same job that holds a contents:write token and runs an unpinned installer as root — or should the gate and the commit be separated into different jobs? | CHANGES: SS-06 and SS-07's remedy and whether ticket 56 should be widened. If the answer is 'separate them', the truth workflow becomes two jobs with an artifact handoff and the overclaiming comment can be rep
- Given that both of ADR-0023's retirement triggers have now fired, do the two ed25519 signing paths get deleted, or does 'one signature' become 'one signature per artefact class'? | CHANGES: SS-13's remedy and whether the ADR's amendment paragraph is now stale documentation or still live doctrine. Deleting also changes what platform/feeds/ can serve, which interacts with the feeds-migrati

# engineering-quality

VERDICT: Fit for demonstrating the thesis on a citable clock; not yet fit for the adoptable eco-system NORTH-STAR §2 implies. The measured estate is 122,479 lines of code and test (66,100 python, 13,732 shell, 24,024 yaml, 18,623 test-python) plus ~53,500 lines of tracked prose. Its agent-legibility is genuinely excellent — 0.37-0.82 prose-to-code across the core engines, 180 dated in-code incident notes, 48 READMEs, zero dead modules found in either the hub or the eight units, 88% return-type annotation in the hub, zero bare except handlers in 261 handlers, and verified determinism (fair.py byte-identical across runs; the three-architecture determinism matrix green in the same CI run where three other jobs failed). The hub's twin suite is 1550/1551 green locally and in CI, failing only on one pre-registered owned invariant. Against that, two structural blockers stop a human team picking this up: all eight unit repos (28,490 python + 11,246 shell) have zero tests in any framework, zero type checking and zero lint, standing on 7,000 lines of monolithic in-module selfcheck() functions (gate.py's is 53% of the file) that a test runner cannot address; and the three adopters are copy-pasted, with only 15 of 39 shared code files identical after party-name normalisation and 24 genuinely diverged — including drift/five-facts.py, the engine that decides the estate's central drift claim, where driftwood has a WINDOW_CLUSTER guard tuppence/ludlow lack and tuppence/ludlow have an _org() fix driftwood lacks, so neither copy has both. Two instrument faults sit on top: six computed-semver scripts print SKIP and exit 0 (graded PASS, latent only because kyverno is pinned and installed on the runner), and the clock never provisions a cluster so 12 of run 21's 18 SKIPs can never be graded there. Making it fit for a human team is a short, specific list: wire pytest+mypy over the existing selfcheck seams in platform, publish the shared adopter logic as a pinned artefact the way the estate already publishes policies, change six exit 0 to exit 3, and put build/test/run instructions in a README that currently mentions none of them.

STRENGTHS:
- 1550 of 1551 pytest tests pass, and the identical single failure appears locally (`.venv/bin/python -m pytest -q` -> `1 failed, 1550 passed in 693.95s`) and in CI (`gh run view 33615039125 --log-failed` -> `1 failed, 1550 passed in 115.13s`). The failure is `flux_coverage_floor_is_still_reachable`, 
- Determinism is real and proven across architectures. Every stochastic path is explicitly seeded (platform/fair/fair.py:132,324; feeds/fetch/market-moves.py:62; twin/substrate.py:125; twin/pert.py:225; twin/benchmark.py:205), the one os.urandom (twin/substrate.py:137) is the deliberately non-determin
- No dead code. I checked every python module in the hub's twin/ and in all eight unit clones for any reference outside its own file (.py/.sh/.yml/.yaml/.md): zero orphans in either. For a 66k-line estate built in two months by one person that is unusual.
- No bare exception handlers anywhere: 261 `except` clauses across hub and units, zero `except:`. Of the 45 that swallow, 29 are the negative-test idiom inside twin/invariants/; I read the four production ones individually and only two are worth reporting (both minor).
- compose() is a genuinely deep module, not a shallow wrapper: platform/compose/composition.py:2005 takes two arguments (adopter_dir, parent_trees), returns (evidence dict, path->content mapping), and its docstring ends 'Writes nothing to disk -- that is the CLI's job.' Reading the body confirms it: t
- fair.py is the best-shaped module in the estate: 390 lines, pure stdlib, three CLI verbs (summary/compare/selfcheck), one file read, no writes, fully deterministic, and its selfcheck asserts real properties including that a sum crossing a perspective or a currency REFUSES rather than adding ('no num
- Guards are structural, not name-matching. wargamer/tier_pr.py's disposing_calls() does an AST walk, adopted after a real planted regression (a rewritten _open_or_update_pr calling `gh pr merge --squash --admin`) passed the previous attribute-name-only check — recorded in the module's own docstring a
- The generated artefacts really are generated, not copy-pasted: all three adopters' composed/policies/v4.0.0/*.yaml are byte-identical to platform/distribution/policies/v4.0.0/ modulo the composed-for label and two provenance annotations (inherited-from: platform@2.0.1, source-path: distribution/poli
- No code vendoring into adopters: driftwood/twin/ is 62 files / 1,446 lines of data and config only — no hub twin/*.py module appears in it (checked by basename against the hub's twin/) — with the pin declared in PIN.yaml and VENDORED.md.
- Comment density is high and load-bearing: measured prose:code ratios of 0.82 (gate.py), 0.80 (verbs.py), 0.73 (composition.py), 0.63 (tier_pr.py). 180 comment lines name a specific past defect, usually with a date and an incident reference; 48 READMEs across the estate. This is what makes the codeba
- Type discipline: 882/994 hub functions (88%) and 610/873 unit functions (69%) carry return annotations; mypy runs over 159 source files in CI and is one stale `type: ignore` from clean.
- The tool supply chain is pinned by checksum, not just version: .github/workflows/truth.yml:39-49 pins gitsign 0.17.1, kyverno 1.18.2 and cosign 3.1.3 each with an explicit SHA256. That discipline is currently the only reason EQ-02's latent false-pass is not live.
- The honesty check is correctly scoped and says out loud what it cannot do: verify-e2e-step7-honesty.sh PASSed in run 21 while step 4 FAILed, which is right — its claim is 'every claim above is reported honestly', not 'every claim above is true' (lines 12-15) — and its own comment states that nothing

## [major] EQ-01 The platform's policies carry pass/fail tests but the platform's own 19,066-line machinery carries none, is type-checked nowhere, and has no CI on a change at all — so the "adopter organisation operates it" role in NORTH-STAR §2 is not honestly on offer
CLAIM: The eight unit repos' 28,490 lines of python and 11,246 of shell, plus the 3,738 lines of python in the hub's verify/ and talk/, are covered by no python test framework, no type checker and no linter. Verified: `def test_` appears in zero files across all eight clones; no pyproject/ruff/mypy/flake8/setup.cfg/tox/pre-commit/pytest.ini/conftest.py exists anywhere in any unit tree; no unit workflow mentions pytest, mypy, ruff, shellcheck, flake8, pylint or black. The hub's pytest.ini pins `testpaths = tests`, all 1,551 collected tests import twin.* only, and .github/workflows/twin.yml:64 runs `mypy twin tests conftest.py`, so the gate's own code is unchecked by the one type checker the estate o
EVIDENCE: `grep -rl 'def test_' --include='*.py'` across the eight fresh clones returns 0 files (no pytest, no unittest). `ls -a <unit> | grep -icE 'ruff|mypy|flake8|pyproject|pre-commit'` = 0 for all eight; `grep -rl 'pytest|mypy|ruff|shellcheck' <unit>/.github/workflows/` = 0 for all eight. In the hub, `grep -rl 'verify/|talk/|build_deck|pound_seam' tests/*.py` returns no matches — all 1,551 collected tests import twin.* only — and /Users/cns/httpdocs/controlplane/policy-as-versioned-flux/.github/workfl
OWNER: recorded-owner-decision REGRILL-ANSWERS.md row 30 (P179 "Test seams"), cited at issues/18-the-publisher-
REMEDY: Each selfcheck() is already a sequence of independent assert/print('ok ...') blocks, so the cheapest honest fix is a one-line `test_selfcheck.py` per module that invokes it, plus pytest+mypy jobs in platform's CI — a day's work that buys a runner and failure isolation without rewriting logic. Then add `verify talk` to the hub typecheck job's existing mypy argument list.

## [major] EQ-02 The gate's own "a green that could not look is a red" rule is unenforced in six of its checks: with kyverno absent, they report proof instead of could-not-look
CLAIM: Six verify scripts in platform/computed-semver/ exit 0 after printing a SKIP line when the kyverno CLI is absent, and talk/verify-all.sh:62 grades exit 0 as PASS ("observed true"), so on any runner without kyverno all six are counted as observed-true — the defect class the estate names as its cardinal sin (NORTH-STAR.md:35 "A green that could not look is a red"; :57 the three-outcomes rule). The six, at the cited lines: verify-gate.sh:11-13, verify-cage-engine.sh:12-14, verify-comparison-window.sh:11-13, verify-rederive-bumps.sh:9-11, verify-generator-standing-check.sh:18-20 (each `echo "SKIP: …"; exit 0`), and verify-corpus-generator.sh:34-36 (`SKIP (step 3):`, after two steps of real offli
EVIDENCE: talk/verify-all.sh:61-62 reads `case $rc in 0) printf '%-70s PASS\n' "$s"; pass=$((pass+1));;`, and the same file's header lines 6-13 state 'Nothing is faked: a live tail that cannot see its cluster must exit 3, not 0.' The six: platform/computed-semver/verify-gate.sh:11-13, verify-cage-engine.sh:12-14, verify-comparison-window.sh:11-13, verify-rederive-bumps.sh:9-11, verify-generator-standing-check.sh:18-20, verify-corpus-generator.sh:34-36, each `if ! command -v kyverno …; then echo "SKIP: …";
OWNER: new none — orphan of ticket 55
REMEDY: Change `exit 0` to `exit 3` in six files, and make verify-witness-set.sh exit 3 when its step 5 could not look. Seven one-line edits.

## [major] EQ-04 The estate's only correctness instrument now returns "red" regardless of the code's state, and a real regression has already slipped through it unseen — the thesis's "silent failure on main" reproduced in the project's own CI
CLAIM: twin.yml is the estate's only CI gate that runs pytest or mypy — no unit repo, no hub verify*.sh, no truth.yml and no legacy org repo invokes either — and all 25 runs the Actions API reports for it (2026-08-16 to 2026-09-02) are `failure`, i.e. it has never been green in the recorded window. On the exact hub commit named in TRUTH run 21 (7b92990, run 33615039125) three of its eight jobs are red: `tests` and `invariants` both fail on invariant 44 `flux_coverage_floor_is_still_reachable`, which is one honestly-owned finding (build ticket `.scratch/twin/build/70-full-system-coherence-confirmatory.md`, finding 1 — not the ecosystem ticket of the same number), and `typecheck` fails on a single de
EVIDENCE: `gh run list --workflow=twin.yml --limit 3` returns conclusion `failure` for head SHAs 7b92990, ce4be49, e7981a5. `gh run view 33615039125 --json jobs` shows `tests failure`, `invariants failure`, `typecheck failure`, with `demo`, `reproduce-elsewhere` and all three `determinism` legs `success`. `gh run view 33615039125 --log-failed` gives `twin/feed_signal.py:232: error: Unused "type: ignore" comment [unused-ignore]` / `Found 1 error in 1 file (checked 159 source files)`. Reproduced locally: `.
OWNER: new EQ-04
REMEDY: Delete the stale `# type: ignore` on twin/feed_signal.py:232, and either mark flux_coverage_floor_is_still_reachable as expected-red or split it so the job's colour tracks new information rather than a standing finding.

## [major] EQ-06 The enactment half of the thesis — signed policy actually in force on a running cluster — is permanently could-not-look on the only citable instrument, and the designed fix is built for one adopter beat and unowned for platform's ten
CLAIM: Eleven of run 21's eighteen SKIPs (not twelve) are caused by the hub clock having no cluster substrate: `.github/workflows/truth.yml` installs gitsign, kyverno, cosign and flux but stands up no cluster, and platform/lib.sh:19-35's `substrate_ok` needs a kind cluster NAMED `driftwood` with every flux-system Kustomization Ready — so installing kind on the runner would not move any of them. That is 13% of the 84 checks, not a sixth; the twelfth SKIP the auditor bucketed here (platform/policy/verify-conditional.sh) is estate state, the retirement of require-nonroot-2.0.1. Nine of the eleven still grade their offline half on every run and would red on a regression there (verify-graded's run-21 ca
EVIDENCE: Run-21 grades extracted from `gh run view 33616685427 --log`: PASS=57 FAIL=7 SKIP=18, matching the TRUTH line exactly. Bucketing the 18 SKIP reasons: 12 cite `kind cluster 'driftwood' is not listed by kind get clusters` or `kind cluster 'driftwood' does not exist`; 3 cite `distribution/versions.yaml declares one version (4.0.0)`; 1 cites `GitHub unreachable (Command '['gh', 'auth', 'status']' returned non-zero exit status 1.)`; the remaining 2 are verify-publisher-gate part c and verify-insurer-
OWNER: new none
REMEDY: Either install kind and stand up one ephemeral cluster in truth.yml (verify/e2e/lib.sh already knows how to create and tear one down), or state in NORTH-STAR §5 that the clock grades offline only and live tails are graded on a separate named lane — so a permanent SKIP becomes a declared property rather than an unexplained gap.

## [minor] EQ-07 The semver and composition mechanisms are verified all-or-nothing by an undocumented convention, so the estate's most load-bearing code is harder to change safely than the thesis's "ordinary software a team owns" promise — no claim it makes is untrue today
CLAIM: In the ~32,000 Python lines that have no test suite — the eight unit repos (28,490 L) plus the hub's own gate instrument, `verify/` and `talk/` (3,738 L) — the substitute for unit tests is a single in-module `selfcheck()` function, one per module, 43 of them across the units and 6 more in hub `verify/`+`talk/`. None of them can be run selectively (`--selfcheck` is a bare flag with no name selector anywhere), and all of them abort at the first failing assert, so the assertions after it are never exercised. The pattern is at its worst in `platform/computed-semver/gate.py`, where `selfcheck` is 597 of 1,134 lines (53%), packs 106 asserts behind exactly one `print`, takes 15s wall (63s CPU) to r
EVIDENCE: AST measurement of the longest function per module: platform/computed-semver/gate.py selfcheck = 597 of 1,134 lines (53%); platform/compose/composition.py selfcheck = 1,009 of 3,654 (28%) against compose() itself at 298 lines; computed-semver/coverage.py selfcheck = 374 of 976 (38%); cage_engine.py selfcheck = 343 of 1,492; wargamer/tier_pr.py selfcheck = 248 of 792; party/party_artefact.py selfcheck = 237 of 793; verify/schedules/schedules.py selfcheck = 213 of 890; twin/invariants/harness.py's
OWNER: new none
REMEDY: Each selfcheck is already a sequence of independent assert / print('ok ...') blocks, so splitting it into def check_<name>() functions with a driver loop is mechanical and yields per-check reporting for free. Start with gate.py (highest ratio).

## [major] EQ-08 The human governance layer the mea culpa names as the thing v1 missed has never had more than one party in it: proposer, reviewer, merger and signer are one identity in all nine repos
CLAIM: Bus factor 1: no second person has ever committed to, reviewed, or been a member of this estate. All nine live repositories have exactly one human author (hub 408 commits by Chris Nesbitt-Smith, 17 by the truth-surface bot; platform 98, driftwood 89, tuppence 58, ludlow 51, nist 14, ico 14, feeds 8, insurer 7 human commits, every other identity a bot), as do all 15 legacy 2022-thesis org repos. Every one of the nine orgs has exactly one member, chrisns. No branch protection and no rulesets anywhere: `branches/main/protection` 404s and `rulesets?includes_parents=true` returns [] on all nine (org-level rulesets could not be read directly for want of admin:org scope, but an inherited ruleset wo
EVIDENCE: `git shortlog -sne --all` on the hub: 408 commits by Chris Nesbitt-Smith <chris@cns.me.uk>, 17 by the 'truth surface' bot, no other human. Per-unit author counts: platform 98 human + 4 release bot; driftwood 89 + 3 sampler + 1 github-actions; tuppence 58 + 2; ludlow 51 + 2; nist 14; ico 14; feeds 8; insurer 7 — one human identity in every repo. Corroborated by the github-live map: `repos/O/R/rulesets` returns [] and `branches/main/protection` returns 404 on all nine repos, and every one of 46 ex
OWNER: new none
REMEDY: No code change. The owner's decision: either test the 'a second engineer can pick this up' claim by having someone actually try, or stop making it in the pitch material.

OWNER QUESTIONS:
- Is the eco-system meant to be operated by someone other than you — a client platform team taking these nine repos and running them — or is it a reference implementation whose job is to be read and cited, not adopted? | CHANGES: If it is read-only, EQ-01 and EQ-03 drop from critical to minor and the remaining work is EQ-02, EQ-05 and EQ-13. If it is adopted, EQ-01 and EQ-03 are the two things that must be built before anythin
- Is 'three adopters' meant to be three genuinely independent organisations that each own their own tooling, or three instances of one shared, pinned adopter package? | CHANGES: If they are meant to be independent, the divergence is a feature and EQ-03 becomes a documentation task (say so, and stop presenting the three reconcile FAILs as comparable evidence). If they are mean
- Should the daily clock stand up an ephemeral Kubernetes cluster, or should the TRUTH line explicitly declare itself an offline-only grade? | CHANGES: Provisioning a cluster converts about 14% of the surface from permanently-SKIP to genuinely graded, and probably moves the pass/fail counts materially in both directions. Declaring the clock offline-o
- Is 'a second engineer could pick this up' a claim the estate makes, or one it should stop making? | CHANGES: If the claim stands, the cheapest test is to have one engineer attempt to add a check and cut a release from the README alone, and to fix what they hit (which today would start at EQ-13, since the REA
- Should the twin CI workflow's colour track new information, or is a permanently red gate the intended honesty signal? | CHANGES: If red-means-red, the standing finding needs an expected-red channel (amber) so a new regression is visible, and the same treatment probably applies to the TRUTH line's seven standing fails. If a perm
- Is the in-module selfcheck() function the intended permanent test strategy, or a scaffold that a real suite was meant to replace? | CHANGES: If it is the intended strategy, then EQ-01 and EQ-07 are design decisions to be recorded in an ADR and defended, and the fix is limited to making the captures readable. If it is a scaffold, then a one

# operability-adoptability

VERDICT: Four audiences, four different answers. (a) Conference: PARTLY FIT. The seven-beat, grade-carrying, generated-not-authored deck is the estate's single best idea and beat 1 opens in 0.15s with zero dependencies — but the committed deck.md on origin/main reads seven "could not look" slides while run 21's own captures grade five of those checks PASS, the thesis's headline runtime claim (>=3 versions coexisting) is undemonstrable by construction because distribution/versions.yaml declares exactly one version, and the only documented live bring-up reconciles a locally-fabricated in-cluster git server while the runbook tells the presenter to say it is the real signed GitHub remote. (b) Adopter organisation: NOT FIT. Nine GitHub orgs, 37 workflows, 84 verify scripts, nine controllers on one KinD cluster, ~2,300 lines of your own Python, three divergent forks of the adopter gate (1087/661/1213 lines), zero onboarding documents anywhere in eight repos, and no measured or stated time-to-first-cage. (c) Regulator or intelligence publisher: PARTLY FIT — the best-designed adopter surface in the estate. The feed envelope is seven closed fields with a well-argued no-in-band-signature rule, the publisher contract is one readable page, and a publisher's own verify passes offline in seconds on a stock laptop (I ran ico/verify-penalty-feed.sh: PASS). But the contract has no independently fetchable home (feeds' own verify SKIPs without a sibling platform checkout), there is no SDK or starter repo, and ico's own README still teaches the retired ed25519 mechanism rather than the envelope adopters pin. (d) ControlPlane asset: NOT FIT as reusable, fit as an argument — the hub, which holds the thesis, PRD, 24 ADRs, NORTH-STAR, the truth surface and 56,233 lines of Python, carries no licence at all (gh api .../license -> 404) while all eight units carry Apache-2.0. What would make it fit: a LICENSE file, a second declared policy line (an owner decision, ticket 58), a rebuilt deck, an onboarding page per audience, and honesty in the runbook about what up.sh actually reconciles from.

STRENGTHS:
- The seven-beat deck discipline is genuinely novel and works: build_deck.py generates deck.md from one capture per check and verify-demo.sh refuses the deck if a figure is not in the capture behind it, if a beat status disagrees with the run, or if a TRUTH line is quoted from another commit. It caugh
- Beat 1 is instantly demonstrable on any laptop with no dependencies: `python3 platform/fair/fair.py summary platform/fair/scenarios/driftwood-cart-pii.json` returned ale=19558.5, var95=30947.9, tvar=34086.7, carried=34958.4 in 0.155s wall clock, run directly in this review.
- The publisher contract is small, closed and defensible: platform/feeds/schema.json is 7 required fields with additionalProperties:false and an explicit argument for why there is no in-band signature field ('a signature cannot cover itself'). feeds/README.md:20-70 documents file layout, the bump ladd
- Real live-cluster evidence does exist on a clock, in the adopters' own lanes: driftwood/drift/samples.jsonl's newest line (run 33624104359, 2026-09-02) records an ephemeral KinD cluster kind-dsample-33624104359 in which fact_1_ready_at_the_pin_on_the_real_remote and fact_2_tag_signature_verified_at_
- The gate's toolchain is pinned by version AND SHA256 (gitsign 0.17.1, kyverno 1.18.2, cosign 3.1.3 at .github/workflows/truth.yml:39-49) with the kyverno-1.19 false-red incident written into the comment — REVIEW-2026-08-31's critical finding C1 is fixed and none of run 21's seven fails is a cage-tie
- The platform engines are more reusable than the demo suggests: only three hard-coded adopter sets exist estate-wide (platform/identity/verify-federation.sh:47, computed-semver/gate.py:967, comparison_window.py:347, the last two inside selfchecks), and the 77 adopter-name mentions in the 3,654-line c
- clone-estate.sh:40-56 documents, with the observed failure for each, why both --depth 1 and --filter=blob:none were rejected ('a gate that grades differently depending on how its inputs were fetched is not a gate') — an example of the estate reasoning about its own instrument honestly.
- Ticket 60's reorder made the three verify-reconcile.sh scripts grade the five-fact sample BEFORE checking for a local cluster, which is why they moved SKIP->FAIL in run 21. That is a better outcome, not a regression: the gate now reports a real red where it used to report a comfortable grey. It fixe
- REVIEW-2026-08-31's M1 is substantially fixed: feeds and insurer now have registered workflows and real signed tags (git -C units/feeds tag -l -> threat-register/v1.0.0, threat-register/v2.0.0; units/insurer -> v1.0.0).

## [major] O3 The one instrument that proves "every actor is attestable" is blind to the estate's only multi-feed publisher, and reports that blindness as an honest queue note
CLAIM: verify-e2e-step6-provenance.sh:88 selects a unit's newest tag with `git tag -l 'v*.*.*'`, a glob that cannot match the feeds repo's tag convention `<feed>/vX.Y.Z` — documented at feeds/README.md:33-37 and enforced unconditionally by feeds/.github/workflows/cut-release.yml:99,111,114, which builds every tag as `${{ inputs.feed }}/v${{ inputs.version }}`. feeds' two real tags (threat-register/v1.0.0, threat-register/v2.0.0) do verify: gitsign verify-tag against release.yml:38-39's own anchored regexp and issuer returns "Good signature from [.../cut-release.yml@refs/heads/main]" and "Validated Rekor entry: true". Step 6 nevertheless prints "ok   no signed tag yet, honestly queued for cut-releas
EVIDENCE: verify/e2e/verify-e2e-step6-provenance.sh:88 `tag="$(git -C "$ESTATE/$u" tag -l 'v*.*.*' 2>/dev/null | sort -V | tail -1)"` followed by `[ -n "$tag" ] || { queued+=("$u"); continue; }`. feeds/README.md:38-41: 'a release tag names the feed: `<feed>/vX.Y.Z`, e.g. `cve/v2.1.0`. A single-feed publisher (`ico`, `nist`) stays on a plain `vX.Y.Z`.' `git -C units/feeds tag -l` -> threat-register/v1.0.0, threat-register/v2.0.0. Run 21 capture (origin/main:talk/captures/verify_e2e_verify-e2e-step6-provena
OWNER: new none
REMEDY: Widen the glob to `'*v[0-9]*.[0-9]*.[0-9]*'`, or better, resolve each unit's expected tag shape from its own party.yaml `publishes[]`, and re-grade.

## [major] O4 The presenter's bring-up page would have the stage narrate a signed-GitHub reconcile the cluster never performs — it reconciles an unsigned tag it created seconds earlier from a git server inside itself, and the runbook's own honesty banner is where the claim is repeated
CLAIM: talk/RUNBOOK.md:74-77 tells the presenter that `.estate-clone/driftwood/scripts/up.sh` points Flux at the real `policy-as-versioned-driftwood` GitHub repo and that "mo-09 retired the in-cluster git-server this used to seed". At the default-branch HEAD a presenter actually clones (clone-estate.sh:57, run by talk/up.sh:60), that script does the opposite: it git-inits a throwaway seed from the local gitops/ tree and tags it with an unsigned annotated tag (up.sh:30,36), builds and loads the git-server image (up.sh:74-75), applies and restarts `deploy/git-server` (up.sh:78,84-87), and applies a GitRepository at `url: ${GIT_URL_IN_CLUSTER}` (up.sh:97) = `http://git-server.flux-system.svc.cluster.l
EVIDENCE: talk/RUNBOOK.md:57-60: '`.estate-clone/driftwood/scripts/up.sh` — KinD `driftwood` + Flux, pointed at the real `policy-as-versioned-driftwood` GitHub repo (mo-09 retired the in-cluster git-server this used to seed) + reconcile.' Against that: driftwood/scripts/up.sh:32-38 git-inits a throwaway seed from the local gitops/ tree and tags it v1.0.0; :74 `docker build -q -t "$IMAGE" "$WORK/ctx"`; :78 `kubectl apply -f "$GITSERVER_DIR/deployment.yaml"`; :93-101 apply a GitRepository with `url: ${GIT_U
OWNER: resolved-ticket-but-still-true ticket 42 (Question: "re-land the mo-09 git-server removal in all three up.sh");
REMEDY: Either correct RUNBOOK section 1 to state what up.sh does, or add a `--remote` mode that points the GitRepository at the real GitHub URL and signed tag and lets the gitsign-verifier gate it — which is the beat the talk actually wants.

## [minor] O5 The eco-system's own repository is legally un-reusable: the hub's docs, ADRs, truth surface and twin are all-rights-reserved, while every artefact a consumer actually pins is Apache-2.0
CLAIM: The hub repo `policy-as-versioned-flux/policy-as-versioned-flux` is public and carries no licence — no LICENSE/COPYING file has ever existed on any ref in its history (`git log --all --diff-filter=A -- 'LICENSE*' 'COPYING*'` → empty), `gh api .../license` returns 404, and the repo's `license` field is `null` — while all eight unit repos (each in its own org, e.g. `policy-as-versioned-platform/platform`) are public and carry a 202-line Apache-2.0 LICENSE that GitHub recognises as `Apache-2.0`. The unlicensed hub holds NORTH-STAR.md, docs/PRD.md (472 lines), all 24 ADRs, CONTEXT.md, verify/, talk/, twin/ (33,872 Python lines) and tests/ (18,623 Python lines). The gap is wider than the hub alon
EVIDENCE: `git ls-files | grep -i license` in the hub returns no output. `gh api repos/policy-as-versioned-flux/policy-as-versioned-flux/license` -> `{"message":"Not Found", ... "status":"404"}`. `gh api repos/policy-as-versioned-flux/policy-as-versioned-flux --jq .visibility` -> `public`. `ls units/*/LICENSE | wc -l` -> 8. The unlicensed hub holds NORTH-STAR.md, docs/PRD.md, all 24 ADRs, CONTEXT.md, verify/, talk/, twin/ (33,872 lines of Python) and tests/ (18,623 lines).
OWNER: new none
REMEDY: Add LICENSE (Apache-2.0, matching the units) at the hub root and a one-line note in README.md.

## [major] O7 The estate's own README claims a shared apparatus it does not have — the thing that is genuinely inherited is the policy, and saying otherwise is the overclaim this project exists to avoid
CLAIM: The estate publishes no shared adopter apparatus: the same responsibility (cs-28's adopter gate) exists as three independently written implementations — driftwood/.github/scripts/adopter-gate.py 1087 lines, tuppence/.github/scripts/adopter-gate.py 661, ludlow/.github/scripts/adopter_gate.py 1213 (renamed), sharing only 260-340 identical lines — over differing workflow sets (8/6/6) and 2,270-2,422 lines of adopter-side Python each, with no shared action, reusable workflow, template repo or onboarding guide anywhere (the only `uses:` across all 20 adopter workflows are actions/checkout@v4 and actions/setup-node@v4). What is genuinely inherited is the policy layer: composed/policies/v4.0.0, kin
EVIDENCE: platform/README.md:11-14 states the config-base promise. `wc -l` on the gates: driftwood/.github/scripts/adopter-gate.py 1087, tuppence/.github/scripts/adopter-gate.py 661, ludlow/.github/scripts/adopter_gate.py 1213 (note the third even renames the file). Adopter-side Python totals (excluding twin/): driftwood 2,321, tuppence 2,422, ludlow 2,270. Workflow counts: 8/6/6, with twin-sweep.yml and verify-identity-regexp.yml only in driftwood. `grep -n 'path:' */gitops/flux-system/gotk-sync.yaml` ->
OWNER: new none
REMEDY: Decide whether the adopter gate is platform-published code — in which case publish it as a versioned artefact adopters pin, which is the estate's own doctrine applied to itself — or genuinely per-adopter, in which case stop calling it the config-base pattern. Either answer is defensible; the current state is neither.

OWNER QUESTIONS:
- Who is the primary audience you are actually optimising for right now — the conference room, a named prospective adopter, ControlPlane's sales motion, or the argument itself as a written artefact? The estate currently spends effort as if all four are equally weighted. | CHANGES: It reorders the frontier. If the answer is the conference room, O1 (the second declared version) and O2 (rebuild the deck) dominate and O5/O10/O12 can wait. If it is adopters or ControlPlane, the lice
- Is a fourth adopter — one you did not author — a goal, or is the estate deliberately a closed demonstration of three example consumers? | CHANGES: It decides whether the adopter gate becomes a platform-published, pinned artefact (a real build with a real version), whether a template/scaffold repo gets created, and whether O7/O10/O12 are findings
- Do you want the truth surface's clock to stand up a real cluster, or is 'the offline half plus the adopters' sampled facts' the honest and intended scope of pass=57? | CHANGES: Either the gate grows a cluster-capable job (which would convert ~12 permanent SKIPs into real PASS/FAIL and materially change every headline number), or RUNBOOK section 5 and the deck gain one senten
- Is the estate meant to be reusable by others — forkable, quotable, liftable into a client engagement — or is it a private demonstration you show and narrate? | CHANGES: A LICENSE file at the hub root, and a short 'what is reusable, what is bespoke' section in README that draws the seam explicitly (the envelope, the truth-surface discipline, fair.py and the compose re
- The RUNBOOK says driftwood's bring-up reconciles the real signed GitHub remote; the script reconciles a git server it builds on the laptop seconds earlier. Which of those is the demo you want to give? | CHANGES: Either RUNBOOK section 1 is corrected (cheap, and the demo stays a pinning demo), or up.sh grows a --remote mode and the live provenance beat becomes real (a build, and it makes O6's verifier packagin
- Ticket 58 decision (1) — declare a second policy line — has been waiting since 2026-08-31. Is there a reason not to take it, or has it simply not been in front of you? | CHANGES: Answering it unblocks tickets 58 and 63, converts three permanent SKIPs into gradeable checks, and restores the talk's second beat. Declining it, with a dated reason, would mean the estate deliberatel

# process-and-record

VERDICT: Not yet fit, and the unfitness is concentrated in one place: the estate's architecture rests on ~84 decision items the owner is recorded as never having reasoned about, the durable record (docs/adr/) does not say so, and nothing converts them. The record's honesty is genuinely excellent — every bare-agree ticket quotes the owner verbatim and states in its own text that a bare agree does not ratify; ticket 08 self-reports its own budget breach on the day; ticket 60 cites a real numbered TRUTH line and discloses the caveat that limits it; the truth number has never been gamed (18 lines, none with fail=0, every red owned by a named ticket). But honesty about a debt is not the same as servicing it. The one rule written specifically to prevent this — GAPS.md:93 rule 1, "No recommendation attached to an architectural question" — was silently dropped when the rules were copied into map.md:16, and within three hours ~85 items with recommendations attached went to the owner in one batch and came back on one line. Fifteen resolved build tickets cite as proof a TRUTH line (run=7, 2026-08-29) whose hub tree contains none of the checks they claim; only one has been corrected and no ticket owns the other fourteen. Two ADRs superseded by decided owner positions carry no banner; four of the six ADRs born of the bare-agree batch do not record their own provisionality; one architectural reversal (Audit→Deny, ADR-0022's 2026-08-28 22:15 addendum) was taken inside an implementation run with no round at all and CONTEXT.md still contradicts it. What would make it fit: mark provisionality in the ADRs themselves, make Status: resolved illegal while items are PROVISIONAL (or record the owner's explicit acceptance that the recommendations ARE the architecture and retire the vocabulary), restore rule 1 to map.md, correct the fourteen ticket Answers and widen ticket 67(d)'s check to cover them, and put the fifteen assistant-resolved cross-ticket conflicts to the owner in the one-page panel-verdict shape that is the only format that has ever produced a reasoned reply.

STRENGTHS:
- The record never claims a ratification it does not have: all seventeen bare-agree tickets quote the owner verbatim and state in their own text that a bare agree does not ratify (.scratch/ecosystem/issues/04-the-feed-contract.md:17, 09-the-cage-ladder-v2.md:141, 15-price-everything-that-was-counted.m
- Breaches are self-reported on the day they happen: 08-the-pound-seam.md:17 records its own overrun ('The daily budget of five decisions was already spent by tickets 04 and 07 before this round; the owner answered on the day anyway, and that is recorded here'), and map.md:18 records the five-per-day 
- Every one of the 24 ADRs carries reconstructible rationale: a section inventory across docs/adr/*.md shows each has an '## Alternatives' / '## Considered options' / '## Why' block plus '## Consequences', so a newcomer can reconstruct why any ADR exists from the ADR alone. Five supersessions (0011, 0
- Ticket 61's promise was kept and is now citable: talk/captures/verify_renovate_verify-renovate-merged-feed-pr.out reads 'SKIP: no merged Renovate feed-pin PR exists yet...' at run 17 (commit 93c2d79) and 'PASS: driftwood #20: Renovate raised threat-register v1 -> v2, Chris Nesbitt-Smith merged it, a
- Ticket 60 is the standard the rest should meet: a genuinely numbered TRUTH line (run 20, verifiable in talk/truth.log), plus a self-disclosed caveat at 60-...:186 ('today's graded samples came from workflow_dispatch firings... not from the cron event'), plus a successor ticket (74) for the part that
- The truth number has never been gamed: talk/truth.log's eighteen lines show fail counts 16,11,11,11,11,11,14,16,16,6,7,3,1,1,3,3,7,7 -- fail=0 has never occurred -- and each of run 21's seven reds maps onto a named open ticket (72, 73, 62, 66, 74). Process rule 4 ('done is the truth surface, never t
- The provisional/decided distinction is applied with real discipline: the same 'five questions in one batch' shape produced DECIDED once (D1-D5, on a reasoned reply) and PROVISIONAL the second time (ticket 58, bare 'Agree'). The record graded two owner replies differently on their actual wording rath
- The enact guard's fifteen disable/re-enable cycles (2026-08-31 14:51 to 2026-09-01 09:59) are each a separate dated commit on main with a stated reason, the mode is a checked-in file visible in git blame, and twin/ENACT_MODE currently reads 'operations'. Two of the fifteen record the guard doing its

## [major] P1 The eco-system's architecture is the assistant's, decided in the asking: the drift review's own corrective against that was dropped when its rules were copied into the map, with no decision recorded
CLAIM: GAPS.md:93 process rule 1 ("No recommendation attached to an architectural question. State the trade, or make the call and record it as the assistant's.") was dropped when the drift review's process rules were carried into the live map: map.md:16 lists five rules "from the drift review" -- GAPS rules 2, 3, 4, 5 plus a new substitute about bare agrees -- and rule 1 (along with rule 6, which unlike rule 1 has an owning ticket) is absent; no wording of rule 1 appears anywhere in .scratch/ecosystem/, and no ticket owns it. The rule was the review's own headline remedy for its headline finding (REPORT.md:26: 482 decisions each carrying a recommendation, 157 bare architectural answers, 147 of them
EVIDENCE: .scratch/drift-review-2026-08-27/GAPS.md:93 carries rule 1 verbatim. .scratch/ecosystem/map.md:16 lists five process rules 'from the drift review' and rule 1 is not among them; `grep -rn "recommendation attached" .scratch/ecosystem/` returns no hits. The rule exists because .scratch/drift-review-2026-08-27/REPORT.md:26 measured the pathology: '482 decisions were put to you. Every one carried my recommendation. 306 (63%) got a bare "agree"... Of the 254 architectural decisions, 157 were bare, and
OWNER: new none
REMEDY: Restore rule 1 to map.md:16, or delete it from GAPS.md with a dated reason. A rule the owner never rescinded and the operating document silently omits is worse than either. For future rounds: state the trade with no recommendation, or make the call and record it as the assistant's.

## [major] P4 The honesty claim fails inside the tracker: fifteen NORTH-STAR §4 build tickets are marked resolved on a TRUTH line that arithmetically proves their checks were not yet in the gate, and fourteen remain uncorrected and unowned
CLAIM: Fifteen resolved tickets (21, 25, 26, 28, 29, 32, 36, 40, 41, 42, 43, 47, 49, 50, 52) each close with "Definition of done: its check is in `talk/verify-all.sh`. The run that recorded it is the TRUTH line of 2026-08-29." That line exists and is unique — talk/truth.log:5, `TRUTH 2026-08-29T12:03Z run=7 hub=918022b units=[driftwood=eacae33 ico=8902b66 ludlow=a800a58 nist=33a05df platform=58ef9c5 tuppence=751522b] pass=43 fail=11 skip=0 excluded=2 total=56` — and it grades the pre-build estate, so it is proof of the opposite of what the tickets cite it for. All six unit SHAs it names are commits dated 2026-08-22 to 2026-08-25, four to seven days before the "Built 2026-08-29 by the /implement run
EVIDENCE: `grep -l "TRUTH line of 2026-08-29" .scratch/ecosystem/issues/*.md` returns exactly 15 files. talk/truth.log's only 2026-08-29 line is `TRUTH 2026-08-29T12:03Z run=7 hub=918022b units=[driftwood=eacae33 ico=8902b66 ludlow=a800a58 nist=33a05df platform=58ef9c5 tuppence=751522b] pass=43 fail=11 skip=0 excluded=2 total=56`. `git ls-tree --name-only 918022b:verify/` returns only `_estate.py party proportionality provenance`; `git ls-tree --name-only origin/main:verify/` returns `_estate.py demo e2e 
OWNER: new P4
REMEDY: Append the same dated note ticket 40 carries to the other fourteen Answers, and widen ticket 67(d)'s gate check from map.md to `.scratch/ecosystem/issues/*.md` so any TRUTH figure quoted anywhere in the record must resolve to a real line whose tree contains the named check.

## [major] P5 The estate's headline differentiator — "price and cage, never refuse" — is false in shipped code, and the glossary, the ADR and the north star give three different answers with no owner ruling
CLAIM: Commit 318052d (author date 2026-08-28 22:15, inside the 16:22->06:40 implementation run 3e83a16..6058943) added two dated addenda to ADR-0022, one of which promotes `governed-namespace-requires-claim` from `Audit` to `Deny` -- 'the one refusal the doctrine allows' -- with no owner round and no marking of its own. It reverses an explicitly owner-confirmed reversal: REGRILL-ANSWERS.md:80 records reversals 11-12 as 'Confirm: a MutatingPolicy defaults the strictest cage at CREATE; replaces the governed-namespace deny gate.' The shipped code followed one minute earlier (platform f5f3023, 2026-08-28 22:14; versions.yaml:152 and render-governed-namespace-guard.py:75 both `Deny`). Four records stil
EVIDENCE: `git show 318052d -- docs/adr/` : '+ **Added 2026-08-28 (review):** a pod created in a governed Namespace with NO `policy-as-versioned.dev/policy-version` claim is REFUSED by `governed-namespace-requires-claim`, promoted that day from `Audit` to `Deny`... This is the one refusal the doctrine allows'. map.md:13 states the estate's vocabulary rule: 'Price and cage; never count, refuse or file.' CONTEXT.md:240-241 reads 'There is no `CREATE` deny any more: a pod that claims nothing gets the namespa
OWNER: new none
REMEDY: Fix CONTEXT.md's Governed-namespace entry to match ADR-0022's addendum, or put the Audit->Deny promotion to the owner as a real decision. Then add a process rule that names what the build does when a discovered fact forces an architectural call mid-run: record it as the assistant's (GAPS rule 1's second clause) and list it for the next round.

## [minor] P7 The canonical decision record states the feed envelope and the refuse-vs-price doctrine flatly as accepted, while the fact that neither was ratified with a reason lives only in the ticket the ADR links to
CLAIM: Three of the six ADRs born of the 2026-08-28 batch — ADR-0019 (the feed envelope), ADR-0020 (refuse-vs-price) and ADR-0021 (the twin/estate seam) — carry `status: accepted` and no signal whatever that the decision rests on a bare agree the owner gave no reason for: not the word "provisional", not the owner's words, not a distinguishing status. ADR-0022:10-11 and ADR-0024:81-85 do disclose it. ADR-0023 is not a fourth omission: every bullet of its decision section is one of D1-D5, the five cross-ticket decisions the tickets record as DECIDED on the three-lens-panel line (10-schedules-and-skills.md:80, 13-...:83, 16-...:78, 19-...:84, 20-...:77, map.md:18), and its lines 9-10 already quote the
EVIDENCE: `grep -c -i provisional docs/adr/0019*.md docs/adr/0020*.md docs/adr/0021*.md docs/adr/0023*.md` returns 0 for all four. docs/adr/0022-...:10-11: 'Decided 2026-08-28 in `.scratch/ecosystem/issues/09`. Provisional: the owner agreed without a reason.' docs/adr/0024-...:81-85 records the same for the rejection-ledger items ('are PROVISIONAL on a bare agree (`.scratch/ecosystem/issues/10-schedules-and-skills.md:80`)'). The originating tickets are unambiguous: 04-the-feed-contract.md:17 (ADR-0019), 0
OWNER: new none
REMEDY: Copy ADR-0022's one-sentence provisionality line, with the originating ticket path, into ADR-0019, 0020, 0021 and 0023. One commit, no code change.

## [major] P8 The audit trail still sells two retired mechanisms as live architecture, and the ticket that owed the correction has closed
CLAIM: Two ADRs carry clauses that decided owner positions have superseded, and neither carries the dated banner the estate's own rule (`.scratch/ecosystem/map.md:123`, "Superseded documents get banners") requires, while six such banners sit on five sibling ADRs (0011:7, 0014:5, 0015:5, 0015:7, 0016:5, 0018:5). No ADR uses a `status:` frontmatter for supersession — all 24 say `status: accepted` — so the banner is the only mechanism, and its absence is the whole record.

ADR-0010 (the strong half). Its consumer-side `sunset:` field (0010:9-11, restated 0010:33-34 and consequence 0010:76-77) was dropped by ticket 13's Answer item 5, "Consumer-side `sunset:` is not carried ... Decided", recorded on th
EVIDENCE: ADR-0010: `.scratch/ecosystem/issues/13-lift-or-retire-the-original-mechanisms.md:86` 'ADR-0010:5-9 (consumer-side `sunset:`) is superseded by the ADR ticket 10 writes'; ticket 13:73 records Q5 as DECIDED (D5) on 'I agree with you're more advanced reasoning'. `grep -n "ADR-0010|supersede" docs/adr/0024*.md` shows ADR-0024 supersedes 'ADR-0015 point 5' only. `head docs/adr/0010-sunset-scheduled-proposals-not-application.md` shows `status: accepted`, no banner. Ticket 02's Answer flagged the debt 
OWNER: new none
REMEDY: Add dated superseded-in-part banners to ADR-0010 (naming ticket 13 Q5/D5 and ADR-0024) and ADR-0008 (naming the 2026-07-20 rejection and NORTH-STAR §5). Fold into ticket 67 or ticket 39's scope.

OWNER QUESTIONS:
- Do you want to be asked fewer, bigger architectural questions with a recommendation attached, or do you want the assistant to make the call and record it as its own (GAPS.md rule 1's second clause)? The evidence says the panel-verdict shape -- five conflicts, three lenses each, one page -- is the on | CHANGES: If you want to decide: 84 items need to be re-put to you in ~17 panel-shaped rounds, and Status: resolved becomes illegal while any item is PROVISIONAL (17 tickets reopen). If you want the assistant t
- Is `Status: resolved` on a ticket whose decisions are all PROVISIONAL acceptable to you? Your own map (map.md:16) says an unratified decision 'stays open'. | CHANGES: If not acceptable, 17 of the 41 resolved tickets return to open, the frontier list roughly doubles, and ticket 59(b)'s derived-Status checker gains a rule that reads the PROVISIONAL markers. If accept
- Is there a date or an event -- a talk, a client, a /to-spec handoff -- before which the architecture must be ratified, and who ratifies it if not you? | CHANGES: A date turns the ratification backlog into a schedulable ticket with a check (n items ratified by date d). No date means the honest thing is to say so in map.md and stop calling the items provisional,
- When the build discovers a fact mid-run that forces an architectural decision -- as it did on 2026-08-28 at 22:15, promoting governed-namespace-requires-claim from Audit to Deny, 'the one refusal the doctrine allows' -- do you want to be interrupted, or do you accept the build deciding and recording | CHANGES: If you want to be interrupted, implementation runs need a hold-and-ask mechanism and the Audit->Deny promotion needs a retrospective round. If you accept the build deciding, the process rules need a s
- Do you consider ADR-0008 (the four-panel Grafana dashboard) and ADR-0010 (the consumer-side `sunset:` field) dead, and do you want them bannered as superseded? | CHANGES: Two one-commit banners plus the CONTEXT.md sunset cleanup (P10). If instead you consider either still live, then ticket 13's D5 needs reopening and the publisher-side supersede mechanism the estate bu
- Fifteen of the twenty recorded cross-ticket conflicts (GRILL-WALK.md C1-C20) were resolved by the assistant rather than put to you -- including whether an adopter may declare its own floor tier (C9), how prices[] is shaped when four tickets reshape it independently (C11), and retiring the currency c | CHANGES: If yes, one one-page panel round in the format that produced D1-D5, and the fifteen C-items graduate from 'amendment' to either DECIDED or reopened. If no, the record should say plainly that cross-tic

# scope-and-coherence

VERDICT: The ambition is coherent — more so than the probe list assumed. Four of the six suspected rule conflicts dissolve on contact with code: composition provably cannot read a clock (its selfcheck asserts it cannot import datetime), the publisher gate really does publish degraded-at-quarantine rather than refuse (gate.py:82,256-264), "no gate" is a vocabulary collision not a rule conflict, and the rejection ledger is derived from closed PRs with no committed register. The estate has built the hard, distinctive parts: a real cross-org signature spine (24 tags, all verifying against Rekor), one adopter's signed composed artefact carrying four parties' priced output in one currency under its own perspective, and an honesty machine that made its own headline number worse (skip 22→18, fail 3→7 at run 20) in order to be true. It is nonetheless NOT fit for the purpose NORTH-STAR states, for one arithmetic reason and one shape reason. The arithmetic: §4 step 3 — the pivot of the whole demonstration — is foreclosed. Applying platform/graded/cage.py's own reduce table to the adopters' own signed evidence.json and party.yaml tolerances, all three sit at the ladder's bottom rung `isolated`, driftwood with 5–12% headroom, and tuppence and ludlow past the end of the ladder entirely (£180,796 residual at the tightest cage against £15,000 and £5,000 bands). The only mechanical mover the estate has pushes the number up, and up is already the floor. The shape: 54% of the 84 graded checks are platform's own and 54% of the ~97,600 hand-written lines are the twin and its tests; between them they own one of the seven §4 steps, while the three adopters whose consumption is the entire point carry 13% of the instrument. What would make it fit, in order: (1) an owner decision on £ calibration so a band can be crossed at all; (2) `size` blocks on tuppence and ludlow so principle 3 operates for more than one party; (3) a clock that can see a cluster, or every live claim moved to a sample lane, so the estate's central runtime claim becomes citable; (4) a choice between the three candidate purposes, because at 4 tickets closed in 3 days against 31 open the estate cannot afford all three.

STRENGTHS:
- The eco-system's central artefact claim is real and re-derivable: driftwood/composed/evidence.json carries four priced lines from four distinct parties — ico £1,787,177; feeds £19,559; insurer premium £113,403; twin forward-intel £1,897,646 — all GBP, all under `perspective: driftwood`, in one artef
- The signature spine is complete: 24 tags across the 8 unit repos all verify with gitsign against Rekor, including feeds' and insurer's first tags cut 2026-09-01 (ticket 57). Spot-verified independently: `git tag -l` in the fresh feeds clone at 69c89b0 shows threat-register/v1.0.0 and v2.0.0.
- "No gate" survives contact with the release path in code, not just in a ticket: computed-semver/gate.py:82 `DEGRADED_TIER = "quarantine"`, gate.py:256-264 records `degraded` rather than refusing when the gate could read everything it needed, and cut-release-gate.py:153 `degraded_tag()` emits the pre
- The gate has no override at any scope: cut-release-gate.py:41-54 documents that CUT_RELEASE_TEST_MODE wraps only the cosign subprocess and never touches run_gate()/comparison_window.evaluate()/release_integrity.refusal(), and that the string appears exactly once in the file.
- ADR-0006 is enforced, not merely stated: compose/composition.py:3566-3571 asserts its own source contains no `import datetime`, `from datetime`, `import time`, `import sched` or `import croniter` — the module never gained the capability to read a clock.
- The rejection ledger is genuinely not an exemption ledger: wargamer/rejection_ledger.py:1-31 derives suppression at run time from closed-unmerged PRs, the old committed fixture is deleted with the reason recorded, offline it is empty and says so, and a different curve hash or £ is a new question tha
- The retirement of policy 2.0.0/2.0.1/3.0.0 used the estate's own mechanism with two independently observed live reasons recorded in distribution/versions.yaml:31-59 (the Priority admission triple mismatch; a pod forging its own tier label reaching the API server from an `isolated` namespace on 2026-
- The number got worse because the instrument got better: run 19→20 moved skip 22→18 and fail 3→7 because ticket 60 reordered the three verify-reconcile scripts and step 4 to grade the real lane sample before checking for a cluster. Four could-not-looks became honest observed-falses — exactly the beha
- Illustrative parameters are disclosed rather than laundered: twin/severity-anchors.yaml:13,56,67 marks the GPD `xi` and `beta` `anchored: false` with the reasoning, even though the line they feed (forward-intel, £1,897,646) is the largest number on driftwood's balance sheet.
- Publisher bumps are checked, not merely declared: ico's and nist's .github/scripts/declared-bump-gate.py recomputes the bump from the feed's own rule.yaml before cut-release.yml tags and refuses on disagreement — it refused for real the first time it ran (ticket 18 Answer 5). Principle 4's "never de
- truth.yml's red is honest and correctly attributed: the final step `fail if the gate failed` (.github/workflows/truth.yml:163-165) fires because verify-all.sh exited nonzero with 7 reds, while the observation-lane cage still commits and pushes the TRUTH line cleanly (run 33616685427 log shows `7b929

## [major] F2 Proportional pricing is demonstrated by one adopter only: tuppence and ludlow carry no size, so the eco-system prices a £5k-appetite firm and a £15k-appetite firm at the identical £9,039,791 with per-customer null
CLAIM: Only driftwood declares a `size` block (driftwood/party.yaml:33-38, turnover GBP 86,000,000, 240,000 customers, as_of 2026-06-30); tuppence and ludlow declare none. Because a missing size is treated exactly like a stale one — ico/schema/to_fair_scenario.py:61-72 leaves the loss triple at the statutory cap when turnover is None, and platform/compose/composition.py's `_sized_turnover` returns None for a party that signs no size ("a missing SIZE is a missing behaviour") — tuppence and ludlow both carry a byte-identical GBP 9,039,791.02 uk-gdpr exposure, which is 5.06x driftwood's GBP 1,787,177.08 for the same regime at the same pinned ico v3 (the multiple is exactly cap / (rate x turnover) = 8,
EVIDENCE: `grep -c '^size:'` over the three party.yaml files returns 1 (driftwood), 0 (tuppence), 0 (ludlow). driftwood/party.yaml:33-38 declares turnover £86,000,000, 240,000 customers, as_of 2026-06-30. composed/evidence.json penalty-schema amount: driftwood 1,787,177.075; tuppence 9,039,791; ludlow 9,039,791 (identical). Every `per_customer` field in tuppence's and ludlow's prices[] is null. The widen-to-cap rule is map.md:32 (ticket 07): "stale size widens to the cap, never refuses". ico/penalty-schem
OWNER: new none
REMEDY: Add a signed `size` block to tuppence/party.yaml and ludlow/party.yaml (four fields plus as_of, the shape driftwood already ships), and add a gate check that an adopter without `size` is graded amber rather than silently capped. Small, cheap, and it unblocks the calibration question in F1.

## [major] F3 Falling behind a published version costs nothing: the decided supersede price is unbuilt, unowned and unreachable from composition, so the map and two resolved tickets assert a mechanism the estate does not have
CLAIM: The decided supersede mechanism has no consumer anywhere in the estate — not in composition, and not in the proposer the decision actually names. Ticket 13 D5 (issues/13:83), ticket 19 D5 (issues/19:84) and ADR-0023's Decision (docs/adr/0023:55-56) all decide that a pin behind a newer published version is priced by the existing EOL ramp from the newer version's publish date, with the adopter's scheduled proposer reading pins and opening a retirement PR. `grep -rn "eol_ramp|eol_scenario"` across all eight unit clones and the hub finds zero callers outside platform/feeds/to_fair_scenario.py itself: composition.py contains no `ramp` at all, no adopter's party.yaml subscribes to the `eol` feed (
EVIDENCE: The ramp exists at platform/feeds/to_fair_scenario.py:74-101 (eol_ramp(eol_date, as_of), +1x per year past EOL) and requires an explicit --as-of. compose/composition.py:272-275 defines FEED_CONVERTERS with exactly two rows (threat-register, penalty-schema) plus the QUOTE_PREFIX at line 300 — no `eol` row. composition.py:3560-3561 states it directly: "an 'eol' parent kind does not exist in the party artefact schema at all". `grep -n 'eol' {driftwood,tuppence,ludlow}/party.yaml` returns nothing, t
OWNER: new none
REMEDY: Either wire it (add an `eol` row to FEED_CONVERTERS, subscribe one adopter to feeds/eol, and pass the composition date through as --as-of — noting that this reintroduces a date-dependent price and needs an ADR-0006 note), or record in ticket 74 that the ramp is not a live mover and strike it from the three.

## [major] F5 The honesty check prints a could-not-look as a green and tells the audience the intelligence publisher is unsigned, when its tags are Rekor-validated — the truth surface states something false about the estate in the one check that proves attestability
CLAIM: verify-e2e-step6-provenance.sh:88 selects each publisher's tag with `git tag -l 'v*.*.*'`, which cannot match feeds' documented per-feed tag shape `<feed-name>/vX.Y.Z` (cut-release.yml:6-7, ADR-0019). feeds therefore falls into the `queued` bucket at line 89 despite holding two gitsign-signed, Rekor-validated tags (threat-register/v1.0.0 tlog 2672987434 and threat-register/v2.0.0 tlog 2673492733, both re-verified in session against feeds' own EXPECTED_IDENTITY_REGEXP and issuer). The false absence is printed as a green note at line 104 and repeated in the PASS sentence at lines 117-119; run-21's capture ends "ok   no signed tag yet, honestly queued for cut-release.yml: feeds" / "PASS: ... 7 
EVIDENCE: verify/e2e/verify-e2e-step6-provenance.sh:87-88: `tag="$(git -C "$ESTATE/$u" tag -l 'v*.*.*' ... | tail -1)"; [ -n "$tag" ] || { queued+=("$u"); continue; }`; line 104 prints `ok   no signed tag yet, honestly queued for cut-release.yml: ${queued[*]}` and line 119 folds it into the PASS sentence. `git tag -l` in the fresh feeds clone at 69c89b0 returns threat-register/v1.0.0 and threat-register/v2.0.0; the publishers map re-verified both against Rekor. Run-21 capture origin/main:talk/captures/ver
OWNER: new none
REMEDY: Change the glob to match both schemes (e.g. `git tag -l | grep -E '(^|/)v[0-9]+\.[0-9]+\.[0-9]+$'`) and re-grade; add a case to the script's own selfcheck using a per-feed tag name so the regression cannot recur.

## [major] F8 The estate cannot demonstrate its most distinctive principle on itself: the AI's only restraint is a file the AI writes, and the interim proposer-merger split has no enforcement and no owner
CLAIM: REGRILL 29 (.scratch/drift-review-2026-08-27/REGRILL-ANSWERS.md:35, restated :55) records "author and merger must be different identities for now" as the interim step toward re-grill 37's end state, an AI disposing inside a priced cage. Nothing enforces it anywhere: `gh api repos/<owner>/<repo>/rules/branches/main` — which includes inherited org rulesets — returns `[]` for the hub and all eight unit repos, repo rulesets are 0, branch protection 404s on every one, no CODEOWNERS exists in any repo, and no workflow or script in the estate references merged_by or any four-eyes shape. All 36 merged pull requests across the nine repos have author == mergedBy == chrisns, with no exceptions (driftwo
EVIDENCE: Live read-only today: `gh api repos/policy-as-versioned-driftwood/driftwood/rulesets` -> 0; `gh api repos/policy-as-versioned-flux/policy-as-versioned-flux/branches/main/protection` -> 404 "Branch not protected". `gh pr list --repo policy-as-versioned-driftwood/driftwood --state merged` -> PRs 22,21,20,19,18,16,15,14,12,11 all `author=chrisns mergedBy=chrisns`. Even the flagship step-2 event: PR #20's commit is ea1c8db by github-actions[bot] on branch renovate/feeds-threat-register-2.x, but auth
OWNER: new REGRILL-29 (decision) / ticket-30 (nearest open, not a match)
REMEDY: Two small builds and one decision: (a) enable a ruleset requiring a review from a different identity on the hub and the three adopters, or record explicitly that the rule is deferred with a reason; (b) replace ENACT_MODE with a tier the agent cannot select for itself (a signed declaration in a repo the agent cannot push to), with a £ attached; (c) the owner says whether the AI-disposal end state i

## [major] F10 The estate's only post-admission re-caging mechanism was retired on a word collision (posture currency vs money FX), with no ticket owning the removal or the re-decision, and that reason is queued for the /to-spec handoff
CLAIM: Ticket 13 recorded a PROVISIONAL decision (bare agree, not ratified) that the currency-controller **CronJob** is retired, with a reason that does not apply to it ("ticket 07's fx feed replaces it" — ticket 07 is money FX; this controller is about version currency). No open ticket owns executing it: every ticket naming it (10, 12, 13, 32) is resolved, and open ticket 35 covers scanner, notification spine, OSCAL CronJob, api and datastore, not this. Nothing in the estate changed: at run-21's platform SHA 46cd775 the whole module still ships (416 lines of .py+.sh plus manifests), currency-controller/README.md:3-7 still says "This controller makes posture live ... This closes the single biggest 
EVIDENCE: map.md:40 (ticket 13 line: "currency-controller retired") and map.md:113 ("The currency controller is retired (ticket 13)"). At platform 46cd775 the directory contains currency.py, manifests/, up.sh, verify-currency.sh and README.md (416 lines of .py+.sh by `find | wc -l`). currency-controller/README.md's opening lines still read "This controller makes posture live... This closes the single biggest gap research 16 flagged." verify-currency.sh is discovered by the gate and grades SKIP in run 21. 
OWNER: new ticket 13 item 2 (resolved answer, provisional)
REMEDY: Delete the module and its verify script in one commit citing ticket 13, or reopen ticket 13 round 2 and record that retirement is deferred with a reason. Either way, correct map.md:113 with a dated line.

OWNER QUESTIONS:
- Are driftwood, tuppence and ludlow meant to be plausible firms whose real balance sheets happen to land where they land, or fixtures whose numbers exist so that the ladder visibly moves? Right now their signed numbers put all three at the ladder's bottom rung and two past its end. | CHANGES: Either §4 step 3 and step 4's wording change and ticket 74 closes as unreachable-by-design, or a new calibration ticket blocks ticket 74 and touches all three party.yaml files (appetite.tolerance, siz
- Is the AI-disposal end state you described in re-grill 29 and 37 — an AI allowed to dispose inside a priced cage, with an Article 22 floor — the actual purpose of this estate, with the Kubernetes cage ladder as its substrate and vocabulary? Or is it a distant aspiration and the Kubernetes policy sto | CHANGES: If yes: the twin, enact_guard, the identity spine and the £ engine become the critical path; the multi-version distribution question, the publisher marketplace and the deck can freeze; three or four n
- Is the truth surface allowed to bring up a cluster, or is "offline only" a deliberate constraint on it? | CHANGES: If a cluster is allowed: one change to truth.yml converts up to eleven SKIPs and makes principle 2 citable for the first time. If not: those eleven checks need the ticket-16 sample-lane treatment (eac
- When is this due, and to whom? Is there a talk, a client conversation or a funder date the estate is aimed at? | CHANGES: A near date forces candidate purpose A (one adopter, one worked example, recalibrate so a band crosses, park the twin and two adopters — the estate shrinks to roughly 30%). A distant or absent date ma
- Should NORTH-STAR be a living document with dated update lines, or a frozen ratification that documents cite as-of 2026-08-27? | CHANGES: If living: one dated update block on §2 and §4, ticket 67's Question widened, and a standing rule that the file is refreshed each time a participant row becomes true. If frozen: a short preamble sayin
- Does an adopter get to set its own forgetting curve for rejected proposals, the way it now sets its own appetite? | CHANGES: If yes: half_life_days and reject_suppress move onto party.yaml beside appetite.tolerance, rejection_ledger.derive() takes the party's values, and the platform file becomes a documented default. If no

# legacy-and-lift

VERDICT: Not fit yet, for one structural reason and one procedural one. Structurally: NORTH-STAR §6 asks for a lift-or-retire decision per working part and §4 asks for a demonstration in which a feed re-prices something real and a workload keeps running caged tighter. Ticket 13 produced the decisions genuinely and with evidence, but the three decided *to lift* (three real apps, the Crossplane cloud plane, the handbook) are all unstarted five days on, all absent from map.md's own order of attack (54→67), and the first of them cannot deliver its stated purpose as scoped: composition.py can price exactly two feed names (FEED_CONVERTERS at composition.py:271-274) and reads no workload, image, SBOM or dependency at all. The eco-system's only application workload is an unpinned `nginx` Pod, and driftwood's composed price would be byte-identical if it were deleted. Procedurally: all three ADR consequences ticket 13 recorded for itself were never written (git log since 2026-08-27 on ADRs 0004/0007/0010 is empty), one mechanism (the currency controller) was retired on a reason that is a category error and is still present, still the eco-system's only CronJob, and still graded by the gate every run, and the round-2 decisions (scanner, Flux notification spine, OSCAL CronJob) sit in ticket 35 behind a blocker (33) nobody is working. Meanwhile the superseded org has greener CI than the successor (14 legacy repos, zero red; hub truth/twin 12 consecutive failures), runs the only clock in either org that ever produced a merged retirement PR (fleet#69, machine-authored, human-merged 2026-08-15), and is the only place either the cloud plane or multi-version coexistence was ever proven at runtime. What would make it fit, in order: invert 33/35 so the CVE/EOL converter lands before or with the app lift and restate ticket 33's definition of done honestly; write the three ADR banners and withdraw the currency-controller retirement; put a dated banner on each of the 14 legacy READMEs plus an org description and profile README naming the two implementations (this removes most reader confusion at almost no cost and pre-empts no decision); run ticket 13 round 2; and decide explicitly what happens to the thesis's ≥3-coexisting-versions claim now that fleet declares two versions and platform declares one.

STRENGTHS:
- The eco-system's isolation from the original org is real and I re-derived it rather than trusting the map: for each of the 13 non-hub legacy repos, `grep -rl "policy-as-versioned-flux/<repo>"` over all eight fresh unit clones returned 0 files, and the same grep over the hub's talk/ and verify/ retur
- The original org is not rotting red: `gh run list` across all 14 non-hub repos shows zero failures on any current workflow. fleet's sunset escalator ran green today (2026-09-02T12:33:55Z, schedule) and the weekly governance nag ran green 2026-08-31.
- The legacy org holds the only real instance in either org of NORTH-STAR §4 step 3's shape: `gh pr view 69 --repo policy-as-versioned-flux/fleet` returns `#69 Sunset: retire policy 1.0.0 (scheduled 2026-08-15) | author=app/github-actions mergedBy=chrisns at 2026-08-15T17:20:11Z state=MERGED` — a mach
- Three retirements in ticket 13 are honest and well-reasoned rather than silent drops: the Grafana/CIO dashboards (owner rejected 2026-07-20, quoted verbatim), the readiness collector's counts (explicitly replaced by the adopter gate's priced impact, not deleted), and consumer-side `sunset:` in princ
- Ticket 13's fact-finding is exemplary: twenty dated facts with per-clause citations gathered before any question was put, including facts that undercut its own recommendation (the H6-06 refuter noting the 'CIO dashboard' framing was the assistant's paraphrase, not the owner's words). Two of its fact
- The archive rule is the right rule and its current state is correct, not a failure: 'archive each original repo once its lift or retirement is recorded and graded green by the truth surface, fleet last' means nothing is destroyed before its replacement is proven; `gh repo list --json isArchived` sho
- The eco-system's clock discipline is materially stronger than the mechanism it would replace. fleet's escalator holds contents:write + pull-requests:write + issues:write and pushes branches; driftwood's propose-tier.yml declares an OBSERVATION_LANE, re-composes into RUNNER_TEMP, and ends with a cage
- docs/ARCHIVE.md's 2026-08-28 banner is the model for closing the two-org confusion: dated, specific, names what reversed it, and says the checklist below is 'not to be executed' rather than deleting it. Fourteen more of those would close finding L8.

## [major] L2 The intelligence publisher's signed catalogue over-promises: two of its six feeds (cve, eol) are advertised as consumable but composition refuses any adopter who pins them, so the CVE and EOL halves of "intelligence re-prices the adopter" are decorative
CLAIM: The cve and eol feeds are published and discoverable but cannot be pinned by any adopter, because composition has no wiring for them — not because no converter exists.

feeds/party.yaml:18-27 advertises cve and eol in publishes[], which ADR-0019 point 5 makes the whole discovery contract. A working, verified converter for both already exists (platform/feeds/to_fair_scenario.py:56 cve_scenario, :74 eol_ramp, :92 eol_scenario, exercised on the real signed payloads by platform/feeds/verify-feeds.sh:46-48,63,68-70, and pointed at by feeds/converters/README.md:29). What is missing is composition's side: the names are absent from FEED_CONVERTERS (composition.py:271-274) and price_parent's non-pena
EVIDENCE: feeds/party.yaml:12-35 lists `- kind: feed, name: cve, path: cve, payload_schema: cve/payload.schema.json` and the same for eol. `ls feeds/cve feeds/eol` → each has bump.yaml, payload.schema.json, rule.yaml, v1, v2. `ls feeds/converters/` → only fx.py and README.md. platform/compose/composition.py:1540-1543 raises Refused for any name not in FEED_CONVERTERS. driftwood/composed/HEADER.yaml:6-27 pins platform, nist, ico/penalty-schema, feeds/threat-register, insurer/quote-driftwood — no cve, no eo
OWNER: new none
REMEDY: One ticket: add cve and eol to FEED_CONVERTERS (platform/feeds/to_fair_scenario.py already implements and selfchecks cve_scenario and eol_scenario), or make an unconverted pin a priced hole under ADR-0020 on ticket 69's pattern rather than a refusal.

## [major] L3 The thesis's non-negotiable retirement window has no actor: an old policy version can only leave an adopter by a hand-written PR, and the owner's own decided replacement has no builder
CLAIM: D5 — the only DECIDED item in ticket 13, one of the five decisions the owner ratified with a reason on 2026-08-28 — specifies that the adopter's scheduled proposer reads pins against the feed and opens a retirement PR keyed to ticket 10's dedupe ledger. Only the ledger half was built. Ticket 13:87 assigned the retirement PR to ticket 10 as a proposal type; ticket 10 resolved the same day accepting only the key (10:90), and its build ticket 28 landed that key — platform/wargamer/rejection_ledger.py:81-84 and platform/honesty/proposer_bounds.py:70-74 both name "a retirement proposal" as the reason the `kind` slot exists — while ticket 13:90 explicitly says "New build tickets carry items 1, 3 a
EVIDENCE: `grep -rn "retirement PR|retirement pull" .scratch/ecosystem/issues/ map.md` returns hits in ticket 13 and ticket 10 only. .scratch/ecosystem/issues/13-*.md:83 records the decision. .scratch/ecosystem/issues/10-*.md:4 `Status: resolved`, :94 `Graduated: "Daily clocks, caged observation lane and derived ledger"` — the retirement-PR proposal type is not in it; :75 (note C16) flags the hole in advance: "A retirement proposal has no dedupe key or half-life under Q5(a)". driftwood/.github/workflows/p
OWNER: new issues/13 D5 / issues/10 D5
REMEDY: Open one build ticket carrying D5's second half (the proposer reads pins against the EOL feed and opens a retirement PR, dedupe key <org>/<kind>/<slug>), blocked on L2's converter.

## [minor] L4 One decided supersession lost its owner: the ADR set still teaches a consumer-side sunset field the estate voted out, and two lesser ADR notes stayed unwritten
CLAIM: Ticket 13 (resolved 2026-08-28) recorded three ADR edits for itself at .scratch/ecosystem/issues/13-lift-or-retire-the-original-mechanisms.md:86, and all three are unwritten: docs/adr/0004-cloud-plane-fork-collie.md was last touched 2026-07-14 (867c50d), 0007-agent-assisted-editorial-governance.md and 0010-sunset-scheduled-proposals-not-application.md on 2026-07-18 (d4b8c28, 63a0cc8), while docs/adr/ has been edited five times since 2026-08-28. The three differ in weight and only one is overdue now. ADR-0010's supersession is the live defect: it flows from D5, recorded DECIDED, and its vehicle already shipped without it — ticket 13:36 and ticket 02:26 both assign it to "the ADR ticket 10 wri
EVIDENCE: `git log --oneline --since=2026-08-27 -- docs/adr/0004-cloud-plane-fork-collie.md docs/adr/0007-agent-assisted-editorial-governance.md docs/adr/0010-sunset-scheduled-proposals.md` returns nothing. docs/adr/0010-*.md heads `status: accepted` with no banner (six other ADRs — 0013, 0014, 0015, 0016, 0018, 0024 — carry supersession text); the only later reference, docs/adr/0023-*.md:70, sharpens ADR-0010's timing clause and says nothing about the consumer-side placement D5 superseded. docs/adr/0004-
OWNER: new ticket 13 (resolved 2026-08-28)
REMEDY: Three dated banners, roughly ten lines total — the cheapest correction available in the estate.

## [major] L5 The map retires the only mechanism that keeps a running pod's posture true after admission, on a confusion between two meanings of "currency" — and the gate grades it every run as still alive
CLAIM: Ticket 13 item 2 (issues/13-...:77) records the currency controller as retired on two clauses that both fail on the primary sources, and map.md:113 restates that retirement flatly, under "Not yet specified", as a closed question — while the module is present, installed and graded. Clause 1, "ticket 07's fx feed replaces it", is a homonym confusion: the controller's currency is version currency ("posture re-evaluated after admission ... when a running workload's admitted version is retired from the platform array", README.md:1-7), ticket 07's is money and FX (07:22, "the FX rate is a signed feed, kind: feed, name: fx"); the owner's own GAPS register keeps them as separate rows 2.8 and 3.18. C
EVIDENCE: platform/currency-controller/README.md:1-8 — 'posture re-evaluated after admission ... when a running workload's admitted version is retired from the platform array, a bounded reconcile pass re-patches its posture (default) or evicts it, and the SVID follows'. map.md ticket 07 line — 'FX is a signed `fx` feed'. `ls platform/currency-controller/` at platform=46cd775 → currency.py, manifests/, up.sh, README.md, verify-currency.sh; manifests/cronjob.yaml:19 `schedule: "* * * * *"`. `grep -rln "kind
OWNER: owned-by-open-ticket ticket 13 (Answer item 2, provisional; round 2 not closed) with contradiction ag
REMEDY: Withdraw the retirement or re-retire it with a reason grounded in what the module does; correct map.md:113; if it stays, give it an owner and a plan for its live tail.

## [minor] L6 The superseded org's daily clock still runs and can quietly eat the audit-trail exhibit — real, but the truth surface never claimed that namespace, and ticket 13 keeps it live on purpose
CLAIM: Three scheduled workflows still run green in the project's original GitHub org and none of them is visible to the truth surface: fleet's sunset escalator (`cron: "0 8 * * *"`, `contents: write`, last success 2026-09-02T12:33:55Z), fleet's weekly governance nag and policy's weekly governance nag (both `cron: "0 9 * * 1"`, last successes 2026-08-31T16:44:53Z and 16:28:37Z). The gate globs `verify*.sh` from `.estate-clone` and `verify/` only (talk/verify-all.sh:46), `clone-estate.sh:23` clones only the eight units, and schedules.py builds its targets from the ESTATE directory listing plus the hub (schedules.py:137-145, 85-86), so no gate script anywhere in the hub or the eight units references 
EVIDENCE: clone-estate.sh:23 `UNITS=(platform driftwood tuppence ludlow nist ico feeds insurer)` — no legacy repo. verify/schedules/schedules.py:85-86 `REMOTE = "policy-as-versioned-{unit}/{unit}"` and `HUB_REMOTE = "policy-as-versioned-flux/policy-as-versioned-flux"`; :138 '(unit, checkout root, remote) for every unit plus the hub'. `gh run list --repo policy-as-versioned-flux/fleet --limit 5` → sunset escalator schedule success 2026-09-02T12:33:55Z; weekly governance nag schedule success 2026-08-31T16:4
OWNER: new issues/56 (adjacent, not owning); issues/13 (adjacent, not owning)
REMEDY: Either disable fleet's workflows (deferred under 13 item 2, fleet-last, so not yet), or add one honest sentence to the TRUTH-line documentation recording that two clocks in the reference org sit deliberately outside the surface.

## [major] L7 Principle 4's "older lines are patchable" is not true today: the platform runs a single version because the older lines could not be patched, so the thesis's headline multi-version mechanism is undemonstrated and unowned
CLAIM: The 2022 Medium post's requirement — "the runtime must support multiple policy versions simultaneously — at least three semver versions" (research/03-blogs-thesis.md:40, :172, :207; glossed as a count and called "the crux of the original implementation" at CONTEXT.md:153-155) — is met by neither org's declared version set today. Fleet's array declares two (clusters/cluster1/policy-versions.yaml, elements 2.0.0/tag 2.0.3 and 2.2.0) since fleet#69 removed the 1.0.0 element on 2026-08-15 — proposed by the sunset cron, merged by the owner, exactly as ADR-0010 requires. Platform's declares one (distribution/versions.yaml:77, 4.0.0) since the dated RETIRED 2026-08-29 note dropped 2.0.0/2.0.1/3.0.0
EVIDENCE: `gh api repos/policy-as-versioned-flux/fleet/contents/clusters/cluster1/policy-versions.yaml` decoded: inputs[0].versions holds exactly two elements, `version: "2.0.0"` (tag 2.0.3) and `version: "2.2.0"` — the third, 1.0.0, was retired by the sunset cron via fleet#69 on 2026-08-15. platform/distribution/versions.yaml declares one element, 4.0.0, under a dated 2026-08-29 comment: 'RETIRED 2026-08-29 -- 2.0.0, 2.0.1, 3.0.0 and the two patch backports 2.0.2 and 3.0.1 were all here' for two observed
OWNER: owned-by-open-ticket ticket 63 (open, unblocked by resolved grilling ticket 58)
REMEDY: Decide explicitly whether the >=3-version claim is retained, narrowed (the mechanism supports N, demonstrated at N=1 because the older bodies were unsafe) or retired, and correct CONTEXT.md:153-155 where a reader meets it.

OWNER QUESTIONS:
- Is the point of lifting ledger, storefront and reports to make the £ move when a feed moves — or is it enough that a real, recognisable application runs caged, with the price still coming only from feeds and declared size? | CHANGES: If the answer is the first, ticket 35's CVE/EOL converter must come before or with ticket 33, ticket 33's DoD becomes a price check, and finding L1 is a real blocker. If the second, ticket 33 stands a
- The thesis's headline mechanism is three or more policy versions coexisting at runtime. fleet declares two today and platform declares one, for good reasons in both cases. Do you still want that claim demonstrated, and if so by which org? | CHANGES: If it is retained, something has to re-establish a 3-version array — most likely re-authoring 2.x/3.x bodies under ADR-0022 in platform, which is real work behind ticket 71's Kyverno question, and fle
- Do you want the original org's 14 repos to keep reading as a live system on GitHub until each one is lifted, or should they carry a dated 'superseded reference implementation' banner now, ahead of any archiving decision? | CHANGES: If banners now: about 40 lines of README edits plus one org profile page closes most of finding L8 this week, independent of tickets 33/34/35. If not: L8 stays open until the last lift lands, and ever
- The currency controller was recorded retired on the grounds that ticket 07's fx feed replaces it. Reading its README (post-admission posture reconciliation, not money), was that the mechanism you meant to retire — and if it stays, who owns it? | CHANGES: If it was a mistake, the retirement is withdrawn, map.md:113 is corrected, and the module needs an owner and a plan for its live tail. If you did mean to retire a posture-currency mechanism, the reaso
- fleet's sunset escalator is the only clock in either org that has ever opened a retirement PR a human then merged (fleet#69). D5 replaced it with a publisher-side supersede that no ticket builds. Is running that lifted mechanism once, for real, part of what 'the eco-system has run end to end' means  | CHANGES: If yes, a new build ticket carries D5's second half (blocked on the CVE/EOL converter of L2), fleet's escalator stays alive as the fallback until it grades green, and NORTH-STAR §4's step list arguabl
- ADR-0004 still asserts the Crossplane cloud plane is an integral, non-deferred, KiND-proven second plane, and that proof exists only in the legacy fleet repo. Is the eco-system's claim today 'two planes' or 'one plane, with the second sequenced after the Pod slice'? | CHANGES: If 'one plane for now', three documents get a dated sentence and the front-door overclaim in L8 and L4 closes cheaply. If 'two planes', ticket 13's sequencing is wrong and the cloud-plane lift into tu


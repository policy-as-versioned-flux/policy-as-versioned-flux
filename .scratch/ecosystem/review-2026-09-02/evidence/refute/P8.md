# P8 skeptic pass -- REFUTED (headline), partially survives

feeds @ 69c89b0 (matches TRUTH run=21 units[feeds]).

## Refuted: "three of the four provenance URLs do not resolve"
All four return 200 anonymously, no redirect, each rendering a real GitHub page:

    200 | <title>Release v1.0.0 . policy-as-versioned-ico/ico . GitHub</title>
    200 | <title>Release policy/v3.0.0 . policy-as-versioned-platform/platform . GitHub</title>
    200 | <title>Release v1.1.0 . policy-as-versioned-nist/nist . GitHub</title>
    200 | <title>Release v1.1.1 . policy-as-versioned-platform/platform . GitHub</title>

Control: .../nist/releases/tag/v9.9.9-nonexistent -> 404. So 200 is not a soft-404.
Repos are PUBLIC (gh repo view ... --json visibility).

The auditor's instrument was `gh release view`, which queries the Releases API and
404s for a tag carrying no Release object. The feed cites `/releases/tag/<tag>`,
which GitHub serves for any tag. Instrument fault, not estate fault.
Underlying refs confirmed: gh api .../git/refs/tags lists refs/tags/v1.1.0 (nist),
refs/tags/policy/v3.0.0 and refs/tags/v1.1.1 (platform).

## Survives: fixture reads
fetch/lib.py:44-52 read_upstream() opens fetch/source/<feed>.json; docstring
lib.py:10-14 says so. grep for urlopen/requests/httpx across feeds returns only
three comments (lib.py:13, market-moves.py:26, news.py:26). Every UPSTREAM const
is passed to lib.main() and only printed (lib.py:117). market-moves fixture
_comment: "ILLUSTRATIVE ... this is not a fetch of Polymarket". Disclosed in-tree.

## Survives, narrowed: prose vs check
news/rule.yaml:10-11 "an event with no resolvable provenance URL is not admitted".
fetch/news.py:39-41 checks only .startswith(("http://","https://")).
verify-market-and-news.sh:190 does the same. So "resolvable" is unenforced --
but no admitted event exercises the gap today: all four resolve.

## Lesser true fact (not the finding as written)
Only ico v1.0.0 has a Release object; the other three are tag pages.
gh release list --repo .../platform shows only v0.1.1, v0.1.0. Did not verify why.

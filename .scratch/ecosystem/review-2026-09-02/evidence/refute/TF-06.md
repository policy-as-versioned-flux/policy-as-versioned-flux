# TF-06 skeptic pass — REFUTED (overstated), corrected claim supplied

## What re-derives cleanly
- ADR-0007:25-26, ADR-0006:19, ADR-0001:59-60, CONTEXT.md:127-129, docs/PRD.md:332 all state the carrier
  (created / lastReviewed / rationale / risk / ethos, as annotations + a versioned rationale.md).
- All three ADRs are `status: accepted`; `grep -l -i superseded docs/adr/*.md` lists only 0013,0014,0015,0016,0018,0024.
- In the eight unit clones: `find -name 'rationale*'` -> 0; `grep -rE 'lastReviewed|last_reviewed|last-reviewed'` -> 0
  (hyphen variant included, which the auditor's pattern omitted); `grep 'annotations:' platform/distribution/policies/` -> 0;
  `grep -i purposeless` -> 0; `grep -E '^\s*created:'` -> 0.
- driftwood/composed/policies/v4.0.0/require-nonroot.yaml:10-12 carries only inherited-from / source-path.
- evidence.json members[] keys = family,name,kind,version,source_party,source_sha,action. No date, no why.
- No .scratch/ecosystem/ ticket mentions advisory metadata / rationale.md / lastReviewed.

## Where it breaks
1. NOT "none of it exists anywhere". A complete implementation exists in the reference org repo
   `policy-as-versioned-flux/policy`: workloads/kyverno/require-department-label/policy.yaml carries
   `mycompany.com/created: "2026-07-14"` and `mycompany.com/last-reviewed: "2026-07-14"`; rationale/ holds five
   per-policy rationale.md files with Risk mitigated / Intent / Ethos and a closing "Dated: 2026-07-14. Reviewed: 2026-07-14.";
   ADVISORY-METADATA.md, EDITORIAL-REVIEW.md and a runnable demo-removal/ exist. Owned and marked done by
   .scratch/faithful-floor/issues/22-advisory-metadata-editorial-review.md. NORTH-STAR.md §6 requires the original org's
   working parts be "lifted into the eco-system or explicitly retired, one by one, with a decision each" — so the defect is
   a missing lift-or-retire decision, not an absence.
2. NOT "'Purposeless policy...' has no artefact in the estate". It is quoted verbatim in
   policy/rationale/require-department-label/rationale.md (Ethos), and at CONTEXT.md:54 and docs/PRD.md:40.
3. FALSE evidence item: prices[] does not have a null `why`. Across driftwood (4), ludlow (2), tuppence (2) entries no
   `why` key exists at all; priced entries instead carry `lef_basis`, a prose rationale
   ("lm sourced from ICO ... real public fines ... warn/deny lef are editorial").
4. NOT "all three verbs absent". Removal is implemented and exercised in the eco-system:
   platform/distribution/versions.yaml retires by deleting an array element (Flux prunes, orphan-guard denies),
   platform/distribution/verify-retirement.sh proves it, and 2.0.0/2.0.1/3.0.0 were really retired 2026-08-29
   (platform commit 3dab34d) with an in-tree, observed, dated defence.
5. Rationale is not wholly absent from the units: platform/distribution/policies/v4.0.0/require-nonroot.yaml:1-15 is a
   versioned prose "why" header. It is a YAML comment, so composition strips it — the composed artefact starts at
   `apiVersion:` with no header. That is the honest, narrower defect.

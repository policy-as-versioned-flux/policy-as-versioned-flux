# TF-04 skeptic pass — REFUTED (overstated; core counterexample exists)

## Verified as stated
- platform/distribution/policies/v4.0.0/ inventory exact: require-nonroot.yaml:24 `validationActions: [Audit]`;
  posture-trust-boundary.yaml:15 Deny, :38 `variables.posture == variables.claimed`;
  cage-tier MutatingPolicy; cage-netpol GeneratingPolicy; stamp-posture MutatingPolicy; priorityclasses.yaml (4 PriorityClass).
- versions.yaml:117 (policy-version-orphan-guard, Deny) and :157 (governed-namespace-requires-claim, Deny) — both version-claim meta-rules.
- 55 policy objects across the eight units (28 Validating + 18 Mutating + 9 Generating). Count exact.
- `grep -rlE 'encrypt|Encrypt'` over unit yaml -> exactly 2 files, both driftwood twin world-model.
- identity/README.md:74 "None of it is live, and verify-federation.sh says so and exits 3 rather than passing."
- No Kyverno policy in the eight units carries an encryption/key-management/data-classification rule.

## Falsified
1. "no policy body anywhere in the estate" — FALSE.
   verify/proportionality/policies/encrypt-at-rest-ludlow.yaml:27 `validationActions: [Deny]`,
   body lines 42-52: every pod mounting a PVC must carry `storage-encrypted=true`.
   Audit/Deny split stamped from the FAIR £ by render.py via platform/risk/enforce.py.
   The auditor's own assess/thesis-fidelity.md:262-269 describes this file — the extracted
   finding contradicts its own assessment.
2. "Claimed ownership: none found" — FALSE. Four owners:
   - NORTH-STAR.md:31 (RATIFIED 2026-08-27) principle 2: "There is no gate. (Owner: 2026-08-20, 2026-08-22.)"
   - CONTEXT.md:75-82: "Cage (supersedes the mea-culpa's lane-keeping vs. gate; rewritten 2026-08-28,
     ticket 09) — There is no gate... A 'locked door' is the bottom rung reached by the £."
   - .scratch/ecosystem/map.md:13 "Vocabulary: there is no gate... Price and cage; never count, refuse or file."
   - .scratch/ecosystem/issues/09-the-cage-ladder-v2.md — Status: resolved.
3. "Every Deny ... is a meta-rule" — imprecise. 3 of the 55 counted objects are department-label Deny bodies:
   platform/computed-semver/corpus/{department-label-2.0.0,known-department-label-2.0.1,known-department-label-2.1.1}.yaml
   (fixed test input copied from the legacy `policy` repo, README.md:11-13 — not shipped).
4. Title overstates on access control / data protection:
   - platform/identity/istio/peerauthentication-strict.yaml (PeerAuthentication mtls STRICT) SHIPS —
     identity/kustomization.yaml:31. Governs transport encryption.
   - identity/demo-mtls/authorizationpolicy.yaml admits by SPIFFE principal (excluded from package).
   - cage-netpol IS versioned+distributed and generates NetworkPolicies; `isolated` gets ingress:[]/egress:[]
     (graded/tests/cage-netpol/generated-all.yaml:80-81). Auditor's own §3.4 calls this
     "a defensible equivalent and in one respect stronger... a genuine contribution".

## Residual that does hold
`grep -rn encrypt-at-rest` across all 8 units -> 0 hits. The one real gate-domain policy body is
hub-only bench rig: not in any party's publishes[], no composed/ tree, no signed tag, not Flux-fanned.
That is a DISTRIBUTION gap in a demo rig, not an absence of policy.

## Note
driftwood clone HEAD 67bfc7a is one drift-sample commit ahead of truth-line 6cf0671;
`git diff 6cf0671 HEAD -- composed/` is empty. Evidence unaffected.

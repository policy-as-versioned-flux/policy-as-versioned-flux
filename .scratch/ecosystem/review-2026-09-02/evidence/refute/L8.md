# L8 skeptic re-derivation — REFUTED (overstates on four of six load-bearing components)

## What held (re-derived, not taken from the auditor)

- `gh api orgs/policy-as-versioned-flux` → `{"login":"policy-as-versioned-flux","name":null,"description":null,"blog":null,"public_repos":16,...}`. No name, no description, no blog. Confirmed.
- `gh api repos/policy-as-versioned-flux/.github` → 404. The `.github` repo does not exist at all, so there is no profile README. Confirmed (stronger than the auditor's contents-path 404).
- 16 public repos: hub + `apps` (archived:true) + 14 live non-hub. Confirmed by `gh api orgs/policy-as-versioned-flux/repos --paginate`.
- All 14 live legacy READMEs read in full over the API. None carries a superseded/archived banner; none contains the strings `eco-system`, `ecosystem`, `NORTH-STAR` or `north star`. The two grep hits (fleet:80 "app1/2/3 -- archived, not deleted", policy:79 "not an archive/deprecate flag") are unrelated. Confirmed.
- fleet README verbatim: "The config repo Flux reconciles (PRD §5.1)... **three policy versions coexisting side by side**". policy README verbatim: "Tagged semver releases of this repo *are* the dependency consumers pin — see the hub repo's [CONTEXT.md].../[ADR-0001]/[ADR-0002]". Confirmed.
- `wc -l README.md` = 38. README.md:4-5 "governed by one Kyverno engine across a Kubernetes workload plane and a Crossplane cloud plane". README.md:**18** (auditor said 19) "docs/adr/ | The **decisions** and why (ADR-0001…0010)" against `ls docs/adr | wc -l` = 24. Confirmed.
- `grep -nE "policy-as-versioned-(platform|nist|ico|driftwood|tuppence|ludlow|feeds|insurer)" README.md` → exit 1, no matches. Confirmed.
- Deck carries no legacy-repo reference (the two grep hits on `git show origin/main:talk/deck.md` are the English word "reports" at lines 186, 208). Confirmed.

## What failed

**1. "Two orgs" is wrong: there are nine.** Each unit lives in its own org, by design.
`git -C .../units/<u> remote get-url origin` for all eight → `policy-as-versioned-platform/platform`, `policy-as-versioned-nist/nist`, `.../ico`, `.../driftwood`, `.../tuppence`, `.../ludlow`, `.../feeds`, `.../insurer`. NORTH-STAR.md:26 mandates it: "each participant lives in its own GitHub organisation". And all eight eco-system orgs are *also* bare — `gh api orgs/policy-as-versioned-<u> -q .description` → NULL for every one, none has a `.github` repo. The auditor checked only the legacy org, so the finding both mis-frames the topology and misses that the org-level gap is nine-wide, not one-sided.

**2. "no signpost anywhere a reader lands" is false for the hub README and for every unit README.**
README.md:13 is row 2 of the Start-here table: `| [NORTH-STAR.md](NORTH-STAR.md) | **The north star** — the eco-system, ratified 2026-08-27; every map serves a part of it |`, and :14 is the twin row. `git blame -L 12,20 README.md` → lines 13, 14, 15 are commit `2326f319` (2026-08-28), i.e. added deliberately. All eight unit READMEs link back to the hub at line 9 or 10 (`[policy-as-versioned-flux](https://github.com/policy-as-versioned-flux/policy-as-versioned-flux)`).

**3. "linking into ... CONTEXT.md and ADRs as authority for claims those documents no longer make" is refuted.**
Only 7 of 14 legacy READMEs cite a hub doc at all (fleet, policy, cloud, governance-agent, handbook-generator, c2p-collector, renovate-config). Every ADR they cite — 0001, 0002, 0003, 0004, 0005, 0006, 0007, 0009, 0010 — is `status: accepted` (checked all 24 frontmatters). For the auditor's own example, CONTEXT.md:529-536 still reads "**Transport = signed git tags, keyless (gitsign).** Policy is distributed as semver **git tags** ... See [ADR-0001]", and :542-544 "**Adoption cadence = pinned everywhere + Renovate PR.** ... See [ADR-0002]" — exactly the claims policy's README cites them for. ADR-0001's body still states it verbatim. CONTEXT.md:12-14 additionally carries its own rebaseline note pointing at NORTH-STAR.md.

**4. "ticket 02 ... did not reach ... the hub README" is refuted.**
`.scratch/ecosystem/issues/02-supersede-and-rebaseline-the-documents.md` Answer: "`README.md`: rows for the north star and the twin; 'Deny = gate' becomes 'rungs on the cage ladder; there is no gate'." `git log --oneline -- README.md` → most recent is `2326f31 ecosystem: resolve ticket 02`. It reached the hub README; it did not reach the legacy repos.

**5. The ambition ref misreads §7.** NORTH-STAR.md:69-76 §7 is an enumerated list of exactly five documents (`.scratch/twin/map.md`, `.scratch/twin/spec.md`, `docs/ARCHIVE.md`, `CONTEXT.md`, `.scratch/talk-spec/the-whole-model.md`, plus one rename). Legacy READMEs and the hub README are not on it; REVIEW-2026-08-31.md:34 records "All five §7 supersessions carry dated banners". §6's actual instruction for the original org is lift-or-retire "one by one, with a decision each" — not banners.

**6. Ownership is misattributed.** Ticket 67's four items are (a) NORTH-STAR copies, (b) ico bump.yaml, (c) OBSERVATION_LANE paths, (d) a gate check on map figures — none touches legacy READMEs or org front doors. The actual owner is ticket 13 (resolved 2026-08-28), item 2, PROVISIONAL: "Each original repo is archived on GitHub once its lift or retirement is recorded and graded green by the truth surface. fleet goes last, after ticket 16's fan-out reconciles in an adopter. The hub stays live. ... Reason: **two live implementations is the finding (H9-11)**." Ticket 33 (open, blocked by 09) and ticket 35 (open, blocked by 16/21/33) carry the lifts. The concern was already named in the drift review at REPORT.md:171 (H9-11). This is a dated, sequenced plan with a stated gate, not an unowned gap.

## Surviving core

Nine org front doors have no description and no profile README, and the 14 live legacy repo READMEs carry no dated pointer to the eco-system. Real, but minor-grade hygiene, already owned by ticket 13 item 2's archive-on-lift rule, and it does not stand where the finding puts it: the hub README and every unit README already signpost.

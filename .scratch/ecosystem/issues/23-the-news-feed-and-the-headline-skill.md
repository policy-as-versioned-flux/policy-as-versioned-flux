# 23 — The news feed and the headline skill

Type: grilling (HITL)
Status: resolved
Blocked by: 21, 10

## Question

Ship dated news events as a signed feed under the contract, `kind: feed`, `name: news`, starting with the niobium headline as the first committed entry. Decide the payload (event id, date, headline, source URL, claimed scope). Decide the headline-to-coordinate step: a Claude Code skill a human runs that turns one headline into a Wardley map coordinate and a supply-constraint actor path, with its circularity disclosed. Decide how the twin consumes it in NORTH-STAR §4 step 5.

## Notes

GAPS 1.12. Findings H5-02, H4-12, H5-05.

## Facts found (2026-08-28, AFK, before any question to the owner)

Source of truth is `.estate-clone/` for the estate and `twin/` for the twin. Paths below are relative to the repo root.

- The niobium headline is mocked. `quantum-niobium-analysis.md:3` names it "Headline under test (mocked)", dated 2026-08-19; its does-not-claim list ends "It is a test stimulus" (`:220`). The demo image is "a composed illustration, not a real news report" (`HONESTY-niobium-beat.md` §"The mocked headline"). There is no source URL because there is no source. (`.scratch/talk-spec/pitch-v4/research/`)
- The only committed niobium row is `nb-refining-capacity`, actor `supply-constraint`, evolution 0.62, velocity 0.05, `links_risk: pq-harvest-now-decrypt-later`, whose own source string calls it "The deliberate LOUD, NON-FIRING entry" (`.estate-clone/platform/wardley/intel/market-intel.json:129-136`). `forward_signal()` branches only on `attacker-capability` and `defensive-capability` (`.estate-clone/platform/wardley/wardley.py:228-232`), so the row emits nothing. H4-12: the `links_risk` edge to the capability it constrains "exists and is simply not traversed" (`G-drift-findings.md:559-560`).
- The full play-forward (velocity 0.05 to 0.06 from the headline, evolution 0.30 to 0.34 from a separate algorithmic result) was reproduced only against an uncommitted `/tmp/intel-B-niobium-plus-gidney.json` (`HONESTY-niobium-beat.md:26`). The honest finding is that the headline alone does not cross a stage boundary (`quantum-niobium-analysis.md:139-145`). The named gap is "a headline->intel-diff classifier" that should "propose a coordinate change as a reviewable signed diff rather than auto-apply it" (`HONESTY-niobium-beat.md:38-40`).
- The twin's dated-signal document: required `id, date, steep, source, statement, provenance`, optional `substrate` (`twin/schema.py:595-604`). `provenance` is `free_mapping`; `observed_by`, `url`, `legal_status` are a fixture-author convention (`twin/fixtures.py:4493-4510`), enforced nowhere. `substrate` is not an origin marker: `_substrate_ref` parses it as a `BlobRef` content hash and refuses anything else (`twin/verbs.py:165-172`). The `Schema` type is closed by construction; unknown fields are refused (`twin/schema.py:442-458`).
- `steep` is the output of the `signal-classify` skill, not an input: `classify()` returns `{"steep", "claim"}` and ingest tags the signal with it (`twin/signal_classify.py:98-120`, `twin/ingest.py:65-72`). A feed that carries `steep` carries a classification.
- Binding is automated at volume with no human gate, by construction (`twin/ingest.py:1-25`). Its output is grade 5 and "grade 5 cannot price a scored forecast (`grade_5_only_path_never_prices`)" (`twin/ingest.py:17-19`). `sense()` refuses any hand-authored binding claim not at grade 5 (`twin/verbs.py:221-228`). The pricing path is graded inside a threshold; an ungraded or under-graded path "produces an unpriced structural blast radius" (`twin/pricing.py:22-28`).
- The claim schema: `kind`, `evidence_grade`, `claimed_by`, `evidence` required; `component`, `signal`, `evolution_position`, `confidence` optional (`twin/schema.py:617-633`). `_refine_claim`: a `binding` must name a signal and may not carry `evolution_position`; a `position` or `override` must carry an absolute `evolution_position` and may not name a `signal`; an `override` must be attributable to a registered role (`twin/schema.py:920-930, 985-995`). There is no delta, velocity or reason field. A grade moves only through a `regrade` record with `by_role` and `reason` (`twin/schema.py:638-650`).
- Unbound signals are retained with decay in a published parameter file, never deleted, reported via `twin unbound-pool` (`twin/unbound_pool.py:1-30`, `twin/decay.yaml`); a later model change can rebind them (`twin/retrospective_sweep.py:1-3`).
- The twin's definition of a skill: judgement not reproducible from pins, emitting a grade-5 assertion, packaged as one module with a `SKILL` constant, a fixture corpus and a threshold entry (`twin/skills.py:43-51`). All six twin skills are Python heuristics evaluated by `twin/skills.py`, not Claude Code skills a human runs. The only Claude Code skill in the hub is `.claude/skills/demo-deck`. The inventory already holds `signal-classify` (STEEP-tag and bind) and `evolution-judge` (infer position) (`.scratch/twin/issues/20-skill-inventory.md:101-103`).
- The planted-ground-truth machinery is sealed: the planter holds the plant list in a sealed artefact, the detector runs with no access to it, and a harness guard proves the split structurally (`twin/planter.py:1-12`; `twin/substrate.py:83` `planted_signals`). A feed entry labelled "planted" is visible to the detector.
- The evolution-judge 1.0 score is circular: three of four corpus labels are literal keyword-table values (`G-drift-findings.md:679-684`, H5-08). GAPS 3.22 asks to "disclose the circularity in the skill card" (`GAPS.md:81`). The forecast book carries a `CLAIM_SCOPE` in the body of every result naming what a score does and does not evidence (`twin/forecast_book.py:37-43, 68-82`).
- Two disjoint Wardley engines: the twin has the value chain (`needs` edges, D/K/R) and no velocity, horizon or money (`twin/wardley.py:1-30`; `grep velocity twin/*.py` returns nothing); the platform has velocity, projection and £ and no edges (`.estate-clone/platform/wardley/wardley.py:68-72`). H5-06's publisher/consumer recommendation (`G-drift-findings.md:661-666`) predates re-grills 31 and 39 and is not a decision.
- The platform's forward layer prices under `../risk/appetite.json` (retired by ticket 08 #5) and stamps `author: ai-generated` with no model call anywhere (`.estate-clone/platform/wardley/wardley.py:190, 207`; H5-04 `G-drift-findings.md:646-651`). `market-intel.json:5` declares itself "Editorial ... not a live subscription". The owner's rule that a demo shows the real thing and simulations are narrated as simulations (2026-07-31) is cited at `G-drift-findings.md:651`.
- Signing today: committed feed `.sig` files are legacy ed25519 with no live re-sign path (`sign.sh` is gone, ticket cs-27); release-gate evidence is `cosign sign-blob` keyless (`.estate-clone/platform/feeds/README.md:24-31`). ADR-0019 replaces both for feeds with the gitsign tag and no cosign bundle (ticket 04 #3). Ticket 21 (open, this ticket's blocker) does the migration and moves market intel into the `feeds` repo (`21-build-the-feed-contract.md:9`).
- `.estate-clone/` holds driftwood, ico, ludlow, nist, platform, tuppence. There is no clone of `feeds` or `insurer`, so the news feed's publisher repo is not inspectable locally (ticket 01 says both exist and are empty, `map.md:26`).
- No platform workflow carries `schedule:`. The hub has two: `.github/workflows/truth.yml:8` and `twin.yml:12`. The truth line: `TRUTH 2026-08-28T05:08Z run=5 ... pass=43 fail=11 skip=0 excluded=2 total=56` (`talk/truth.log`, last line).
- Adopters' `party.yaml` still pins `pricing` and `threat` as kinds with no `name` or `since` (`.estate-clone/driftwood/party.yaml:23-27`); nothing news-shaped is subscribed. Ticket 11 (open) already places "the niobium headline" in each org twin's standing scenario library (`11-an-org-twin-each.md:9`).
- The forward-intel payload is a fixed field list, `{perspective, shock, horizon, lef, lm, currency, curve}`, with no grade and no provenance field (ticket 08 #2; ADR-0021 "The decision"). A `payload_schema` change is always major (ticket 04 #3).
- NORTH-STAR §4 step 5: the twin "on its schedule, plays a dated external signal forward (the niobium headline) ... emits a scored forecast, and publishes forward intelligence the platform consumes" (`NORTH-STAR.md:46`). §2: the intelligence publisher consumes "public sources on a clock" (`:18`). Principle 6: "every artefact carries a signature that says what it does and does not assert" (`:35`). §6: substrates are synthetic with planted ground truth (`:67`).

Already decided, not re-asked: the envelope `kind, name, version, published_by, published_at, payload_schema, payload`, signed by the gitsign tag only; `kind: feed` with a free `name`; subscription is `inherits[]` + `name` + `since`; discovery is `publishes[]`; a `payload_schema` change is major; revocation is a new version (ticket 04, ADR-0019), so `news` needs no platform PR to exist; the feeds org is the intel publisher and threat/CVE/EOL/market intel move there (tickets 01, 21); the twin emits a `forward-intel` feed, scenario under a perspective, no recommended action ever, from the adopter's own repo, fixed payload fields, `source: twin` into `prices[]`, the estate annualises and selects (ticket 08, ADR-0021), so the headline's £ enters only through forward-intel; news gathering runs on a clock, LLM-free, and a human merges (NORTH-STAR §2, principle 5, ADR-0010, reversals 7/14/15 in the owner's shape), so "a human commits headlines by PR, no schedule" is not an option; a skill is grade-5 judgement not reproducible from pins (twin 20 Q1, `twin/skills.py:43`); binding is fully automated at volume, with spot-checks, trusted downstream, grade 5 cannot price a scored forecast on its own (twin 11 Q2, `twin/ingest.py`); unbound signals are retained with decay (twin 11 Q3); position is inferred first, a human correction is an `override` by a registered role scored later, and no arithmetic on ordinal scales (twin 11 Q1); the skill inventory holds `signal-classify` and `evolution-judge` (twin 20 Q3); planted ground truth is a sealed planter/detector split (twin 12 Q2); twin subjects are driftwood, tuppence, ludlow and the real-firm beats are evals (re-grills 31, 39); the forecast book is built out with a claim scope that travels with every result (reversal 22, twin 21 Q5); never an exemption, price and cage, instrument faults refuse and behaviours are priced (ADR-0020, re-grill 20, reversal 5); every package self-versions (re-grill 2, ticket 06); every ticket wires its own check into the gate (map process rule); a disclosure that lives only in a SKILL.md violates principle 6, so it is not an option.

## Grilling round 1 — drafted 2026-08-28, HELD

Drafted AFK on the owner's instruction. Adversarially checked by two reviewers. Nothing below has been put to the owner.

❓ **Q1** - **Does a planted headline belong in the feeds org's news feed at all**: the ticket says niobium is the first committed entry. It is a mocked stimulus with no source. `substrate` on a signal is a content hash, not an origin marker (`twin/verbs.py:165-172`), and the planter split is sealed, so a self-labelled planted entry is not an eval plant either. Ticket 11 already puts the niobium headline in each org twin's standing scenario library. Reversal 22 scores publishers per feed; a publisher that signs a fabricated headline into `news` contaminates its own reliability record. Options: (a) the news payload gains a required `origin: observed | planted`, niobium ships as `origin: planted` with no URL, and the publisher's score excludes planted rows; (b) replace niobium with a real, dated, URL-bearing headline on the same supply chain and retire the mock to twin fixtures; (c) the news feed carries observed entries only; niobium lives in the org twin's scenario library (ticket 11) as the twin's own dated signal, never in the feeds org's feed; step 5's demo plays it from there.

➡️ (c). It keeps the feed worthless to fake (no field that says "ignore this one"), keeps the per-publisher reliability record clean, matches ticket 11's existing shape, and needs no new field. NORTH-STAR §4 step 5 names the headline as the twin's stimulus, not as the publisher's feed content; "first committed entry is niobium" is this ticket's own framing, not an owner decision. (a) costs a schema field and a scoring exclusion for one row. (b) throws away the one worked play-forward.

❓ **Q2** - **The news payload shape**: the ticket lists event id, date, headline, source URL, claimed scope. The twin's signal document is `id, date, steep, source, statement, provenance`, but `steep` is the output of `signal-classify`, so a verbatim reuse makes the publisher assert a classification. Options: (a) `payload.events[]` carries `id, date, source, statement, provenance{url}` only, `url` made required by the news `payload_schema`, and the twin's signal schema makes `steep` optional until bound (one-line change at `twin/schema.py:598`), so the feed ingests with no translation and carries no judgement; (a') the same, with `steep` kept required and tagged by the publisher; (b) the ticket's five fields as a new schema, `claimed_scope` free text the publisher writes.

➡️ (a). The feed is LLM-free data gathering (reversal 7) and asserts only that something was said, when, and where it was read. Everything judged (STEEP, component, coordinate, scope) is a skill output on the twin side, graded 5 by construction and contestable. (a') puts a classification in a feed that is supposed to be worthless to fake. (b) invents a second signal shape the twin then translates, and `claimed_scope` on a raw event is a judgement with no grade.

❓ **Q3** - **What the headline skill is and what it writes**: the ticket asks for a Claude Code skill a human runs that turns one headline into a coordinate and an actor path. The twin already has the two halves as heuristics: `signal-classify` (bind) and `evolution-judge` (infer position). A `position` claim must carry an absolute `evolution_position` and may not name a signal; a human's judged coordinate is an `override` by a registered role (twin 11 Q1); there is no delta or velocity claim kind. Options: (a) the headline skill is `signal-classify` plus `evolution-judge` packaged as one Claude Code skill a human runs over the unbound pool; it opens a PR to the adopter's overlay carrying a grade-5 `binding` claim and, if the human judges a move, an `override` by his registered role with the absolute `evolution_position` and the headline cited in `evidence`; the actor path is a twin `needs` edge; (b) a new claim kind carrying a velocity delta, so the platform's `proj = ev + velocity x horizon` has something to consume (schema change; arithmetic on the ordinal axis twin 11 Q1 guards against); (c) the skill proposes a signed diff to `market-intel.json` in the feeds repo (the HONESTY note's shape) and the platform's forward layer stays the coordinate author.

➡️ (a). It is the twin's decided model with a human at the keyboard, not a seventh skill; every record it writes already exists in the schema; the reviewed PR is the unit of adoption; the override is attributable and scored later, which is exactly the disclosure the ticket asks for. (b) reopens the ordinal guard. (c) keeps two coordinate authors alive (H5-06) in a layer that prices under a retired appetite file. Cost of (a), named: velocity and horizon do not exist in the twin, so "plays forward" is ticket 11's territory until the twin's map carries movement.

❓ **Q4** - **What makes a headline-derived claim price-eligible**: (a) writes a grade-5 binding and forward-intel carries the consequence into `prices[]`, but grade 5 "cannot price a scored forecast" (`twin/ingest.py:17-19`) and the pricing path is graded inside a threshold (`twin/pricing.py:22-28`). Nothing decided says how a headline reaches the £. Options: (a) only the human's `override` (or a `regrade` by a registered role, `twin/schema.py:638-650`) in the same reviewed PR is price-eligible; the grade-5 binding alone informs, ranks and sits in the pool; (b) corroboration by a second independent signal or channel lifts the grade mechanically, the way `twin/corroboration.py` grades an enactment; (c) forward-intel carries the grade of its weakest path and `fair.py` discounts or ignores grade-5-only scenarios (amends the ADR-0021 payload, major bump).

➡️ (a). It is what twin 11 Q1 and Q2 already say, read together: the machine proposes, the role disposes, the role is scored. It needs no schema change and no ADR amendment. (b) is a real mechanism but there is no corroboration table for signals today, only for enactments. (c) moves a twin gate into the estate's `fair.py`, which then has to know the twin's ladder.

❓ **Q5** - **Where the circularity disclosure travels**: the coordinate is the judgement; the engine only projects it. GAPS 3.22 says "in the skill card"; principle 6 says every artefact says what it does and does not assert; the forecast book's `CLAIM_SCOPE` travels in the body of every result. A gate check is mandated by the map's process rule whatever is chosen. Options: (a) forward-intel gains `claim_scope` (what a headline-derived scenario asserts: a judged position by a named role; what it does not: that the engine found it independently) and `derived_from` (the claim ids), amending ADR-0021 and bumping the payload schema major, so the `prices[]` entry shows it; (b) the scope goes inside the existing `shock` text, no schema change, not machine-checkable; (c) the scope lives on the twin `override` claim's `evidence` only and never reaches the estate.

➡️ (a). A disclosure the balance sheet cannot see is a deck line. The payload is one version old and has no consumer yet (ticket 25 is not built), so the major bump costs nothing now and is the honest moment to do it; `derived_from` is what the gate check discriminates on. (b) is unverifiable; (c) stops at the seam ADR-0021 exists to cross. This closes H5-04 without renaming: the "AI" step becomes a human-run skill whose output says what it is.

Later rounds, blocked on the above:

- The ticket's Question asks for the supply-constraint actor path and the scored forecast; both are deferred to round 2 because they depend on Q3 and Q4. The path: whether `nb-refining-capacity -> pq-cryptanalysis` becomes a twin `needs` edge whose propagation moves the linked capability, or a third branch in platform `forward_signal()`; and velocity as arithmetic on the evolution axis against twin 11 Q1's ordinal guard (ticket 11 territory).
- Who runs the binding: reversal 7 (a human runs the reasoning skill) and twin 11 Q2 (binding fully automated at volume, no human gate) pull opposite ways for this one step, and NORTH-STAR §4 step 5 says "on its schedule". Ticket 10 owns the boundary, the cadence and the rejection half-life; recommend adding 10 to this ticket's `Blocked by:` and putting the reversal-7-versus-11-Q2 question there, not here.
- Scoring the headline forecast in the forecast book: pre-registration date is the signal's `date`; what the resolution question is for a scenario-library entry (Q1) and an override (Q4), under reversal 22's build.
- Who authors the shared commoditisation map once there are three twins, and whether `market-intel.json` and the platform's detached-ed25519 map signing retire (H5-06; tickets 11, 21, 25).
- Whether `author: ai-generated` is renamed or earned by the new skill (H5-04), decidable after Q3 and Q5.
- The demo beat for step 5 (ticket 20): what the truth surface captures, given Q5(a)'s gate check.


## Cross-ticket notes (2026-08-28)

- C4 (10, 11): the later-round "reversal 7 versus twin 11 Q2" question is already answered by 10 Q1(a): a heuristic stand-in never runs on the clock, classification is a skill a human runs. 11's "already decided" still carries automated binding. Add 10 to Blocked by, as this ticket itself recommends.
- C10 (15): Q5(a) adds `claim_scope` and `derived_from` to the forward-intel payload as a major; 15 Q4(a) adds `register[]` as another major. One ADR-0021 amendment in ticket 25, not two.
- C13 (20): 20 lists "niobium is the first entry of the signed news feed" as already decided; Q1(c) here says it never enters the feed. Tell 20 the line is open.
- C14 (10, 11): Q3(a) lands the skill's PR on "the adopter's overlay", which assumes 11 Q1(a) (overlay in the adopter repo) and pre-empts 10's open item on where a skill's claim file lands.

## Answer

Resolved 2026-08-28. On 2026-08-28 the owner read every held round and wrote: "ive already read the recommendations and I can't find fault with a single one. Well done. Get everything ready for me to then to-spec". A bare agree does not ratify. Each item below is PROVISIONAL, with that line as the owner record and the recommendation's own reason as the rationale, as tickets 04, 07 and 08 were. The five-per-day rule was overridden by the owner's instruction. Five cross-ticket conflicts were then put to the owner with a three-lens panel verdict. The owner wrote: "I agree with you're more advanced reasoning". D1 to D5 are DECIDED. D2, C4, C10, C13, C14 and C19 apply here.

1. The news feed carries observed entries only. Niobium never enters it. It lives in each org twin's scenario library (ticket 11) as the twin's own dated signal, and step 5 plays it from there. Rationale: the feed stays worthless to fake, the publisher's reliability record stays clean, and no new field is needed. C13 corrects ticket 20's "first entry" line. C19: niobium is one of six standing scenarios per adopter.

2. The news payload is `payload.events[]` with `id, date, source, statement, provenance{url}`. The news `payload_schema` requires `url`. The twin's signal schema makes `steep` optional until bound. Rationale: the feed asserts only that something was said, when, and where it was read. Every judgement is a skill output on the twin side, grade 5 and contestable. D2: the feed defines "changed" in its own versioned rule file beside the feed, under ticket 10's one rule.

3. The headline skill is `signal-classify` plus `evolution-judge` packaged as one Claude Code skill a human runs over the unbound pool. It opens a PR with a grade-5 `binding` claim. If the human judges a move, the same PR carries an `override` by his registered role with an absolute `evolution_position` and the headline in `evidence`. The actor path is a twin `needs` edge. Rationale: it is the twin's decided model with a human at the keyboard. Every record already exists in the schema. The reviewed PR is the unit of adoption. C14: the PR lands on the adopter's overlay in the adopter's repo. C4: classification never runs on the clock; twin 11 Q2's automated binding is superseded for it. Ticket 10 is added to Blocked by. Named cost: the twin has no velocity or horizon, so "plays forward" is ticket 11's territory.

4. Only the human's `override`, or a `regrade` by a registered role, in the same reviewed PR is price-eligible. The grade-5 binding alone informs, ranks and sits in the pool. Rationale: the machine proposes, the role disposes, the role is scored. No schema or ADR change.

5. The forward-intel payload gains `claim_scope` (a judged position by a named role, not an engine finding) and `derived_from` (the claim ids). Rationale: a disclosure the balance sheet cannot see is a deck line. The payload has no consumer yet, so the major bump is free now. `derived_from` is what the gate check reads. This closes H5-04. C10: one ADR-0021 amendment in ticket 25, with `register[]` (15 Q4a), as one major.

Consequences:
- ADR-0021's payload is amended once, in ticket 25: `claim_scope`, `derived_from`, `register[]`.
- Ticket 20's niobium line and twin 11 Q2's automated classification are superseded. Lookup binding stays on the clock.
- Ticket 10's open item on where a skill's claim file lands is closed.
- GAPS 3.22 closes in the payload, not in a skill card. `author: ai-generated` retires when the skill lands.
- Ticket 21 builds the news envelope, schema and changed-rule file. Ticket 25 carries the payload amendment and the `derived_from` gate check. A new build ticket carries the skill, the `steep` change and its gate check. Ticket 11 carries the actor path and play-forward.
- Revisit triggers: a real, dated, URL-bearing headline on the same supply chain arrives (Q1b); the twin's map gains velocity or horizon (actor path, Q3b); a corroboration table for signals exists (Q4b); a forward-intel consumer appears before ticket 25 lands.

Graduated:
- Build the news feed and the headline skill (task).
- The supply-constraint actor path and the scored headline forecast (grilling).

# 39 — Supersede ADR-0013, ADR-0017 and ADR-0018 point 3

Type: task (AFK)
Status: resolved
Blocked by: 15

## Question

Write the ADR that supersedes the bare-id key, new-hole refusal and widening refusal (ADR-0013), the self-created-hole refusal (ADR-0017) and the new-ungoverned refusal (ADR-0018 point 3) with priced holes and priced widening; add supersession banners; ADR-0015's deny-to-issue is superseded with ticket 09.

## Notes

Graduated 2026-08-28 from ticket 15's resolution. Definition of done includes wiring its check into `talk/verify-all.sh`.

## Comments

- 2026-09-04 (build, wave 2): built on hub branch `ticket-39-supersede-adr-0013-0017-0018` from that day's `main` (ticket 38 merged, PR #16). Hub only, no unit change. ADR-0026 written; banners on ADR-0013, ADR-0017 and ADR-0018 (point 3); CONTEXT.md's Baseline, Control id, Hole and Delta entries; `verify/adr-supersession/verify-adr-supersession.sh` in the gate. The removed-control question ticket 38 left is settled: priced, not refused. Answer below.
- 2026-09-04 (build, wave 2, resumed): the first builder was cut off after its selfcheck went green and before committing. A second builder inspected the uncommitted worktree, re-ran both modes of the script (PASS, exit 0 each) and `verify/record/verify-record-states-the-purpose.sh` (PASS), and checked every source ADR-0026 cites against the file it names rather than trusting the draft. Five citations were wrong and are corrected in the ADR, none changing a decision: the removal assertion lives in `composition.py --selfcheck`'s `run2-removed` case (run by `verify-composition.sh` step 1), not in `verify-composition.sh` itself; `check_source` lives in `verify/priced-holes/priced_holes.py`, whose own selfcheck plants `removed-control` in the source it must pass; ADR-0018 §2's words are "a mirror of one into the other is duplicated state", not the paraphrase the draft quoted; ticket 14's lever is quote `conditions` keyed on `(source, id)` with a `consequence` of void or uplift (Answer 3); and ADR-0019 does not say a weights feed bumps with the catalogue, so the withdrawn-control consequence now says only what ADR-0019 and ticket 15 item 1 say. The schema sentence the platform build must rewrite is now quoted verbatim. `origin/main` moved two commits during the run (the ticket 38 map line and a truth run); neither touches a file this branch touches, and `0026` is still the free number there (`0027` exists on `main`, minted by another ticket).
- 2026-09-04 (review fixes, PR #17): the spec reviewer found one blocking defect and four minors, all fixed on the branch. Blocking: ADR-0026 point 6 said the refusals composition keeps "are all instrument faults" and named four, while `compose/composition.py` on the platform integration branch emits eleven refusal kinds (`verify-composition.sh` step 1b prints nine of them; run 2026-09-04: dangling-claim, missing-baseline-file, missing-instrument, no-controls-parent, removed-control, restatement-of-non-validating, split-diamond, unknown-control-id, unpriceable-inability; its 400-character scan window misses `claim-against-another-partys-policy` and `rule-conflict`, which the same scan at 800 characters finds). Point 6 now classifies all eleven, one line and one reason each: ten instrument faults and one behaviour, `removed-control`, which point 5 retires and the platform build must price; `unpriceable-inability` is named as the kind nearest the line. The Consequences honesty list and point 1 now say the unknown-id refusal's code kind is `unknown-control-id`, not `missing-instrument`. `verify-adr-supersession.sh` carries the eleven as a fixture (`REFUSAL_KINDS`, offline, never SKIP), grades the classification in a new step 2b, and its selfcheck gains two legs (a copy without the `removed-control` line fails; a copy without the classification's lead sentence fails). Minors: point 1 and Answer D1 cited "ticket 38 D2" for the wire form; it is ticket 38's Answer item 2 (D11 for the baseline-source rule; D2 is the ungoverned denominator). Consequences said ADR-0019 makes a `payload_schema` change major; the rule is ticket 04's Answer item 3. Context and point 5 quoted ADR-0017 as "Removing a self-added control: never"; ADR-0017 has that as a heading and "Never (chosen)" as the option, quoted so now, and the ADR-0017 banner too. Context names the standing line it retires, ticket 15 line 38 "an adopter may add a control and never remove one" under "Already decided, not re-asked" (2026-08-28), why ADR-0025 lets this ADR retire it (no owner quote on removal exists; ticket 38 handed the question here) and that it is consistent with NORTH-STAR principle 1. D7 cites ticket 89 item 4 instead of "the wave note".

## Answer

**What was built** (hub only):

1. `docs/adr/0026-a-hole-is-priced-never-refused-the-claim-keys-on-source-and-id.md`, with Context, Decision, Options considered and Consequences. It records ticket 15's items 1 to 3 and 5, ADR-0020, ADR-0022 (its tighten-only cage, its "lowering the floor is priced, never refused", and its 2026-09-02 note carrying the owner's mutating-admission-controller reason) and ticket 38's D1 to D12 as the reasons. Six decision points: `(source, id)` as a refinement of ADR-0013's bare-id rule; a hole priced by regulator weights as a partition of the regime exposure; a widening as a priced delta; an ungoverned namespace as a ramped priced delta; a removal priced, never refused; and every refusal kind composition emits today, eleven, each classified as an instrument fault or a behaviour with a reason (ten instrument faults; `removed-control` the one behaviour, retired by point 5).
2. Dated "Superseded in part, 2026-09-04" banners on ADR-0013 (new-hole, widening, removal, one-catalogue key), ADR-0017 (self-created hole, removal of a self-added control) and ADR-0018 (point 3 only), each linking `[ADR-0026](0026-...md)` in the blockquote style ADR-0014/0015/0016 use for ADR-0022. ADR-0018's ADR-0022 banner for §4 stands beside it, untouched. The ticket-38 notes of 2026-09-03 ("ticket 39's superseding ADR, which takes its number when it is written") are replaced by these banners; their build date is carried inside the banner text.
3. CONTEXT.md: **Baseline** no longer says "may never remove"; it says a removal is priced and why, and cites ADR-0026. **Control id** gains the `(source, id)` key, the wire form, and the classification of the unknown-id hard failure as a missing instrument. **Hole** no longer says "A removal still refuses"; it says a removal prints as a `removed-control` delta. **Delta** cites ADR-0026 and names the two delta kinds still to be built.
4. `verify/adr-supersession/verify-adr-supersession.sh` (mode 100755; discovered by `talk/verify-all.sh`'s `find .estate-clone verify -name 'verify*.sh'`). Offline, never SKIP, selfcheck-first like `verify/record/verify-record-states-the-purpose.sh`. Grades the record only: ADR-0026 exists once, is accepted, carries the four sections and the delegated ADR-0025 line, names the three superseded rules, the `(source, id)` key, ticket 15, ADR-0020, ADR-0022, ticket 38, `removed-control`, "removal is priced", "missing instrument" and the `check_selected_set` honesty line; the three banners are dated 2026-09-04, link an existing ADR-0026 file, sit above the title and name their rules; ADR-0018 keeps its ADR-0022 banner; no ADR still says "takes its number when it is written"; the four CONTEXT entries cite ADR-0026 and no longer carry the refusals. Selfcheck legs: a copy of ADR-0013 with its banner stripped fails; a copy of ADR-0026 with `## Options considered` removed fails; a CONTEXT.md whose ADR-0026 citations are rewritten to ADR-0013 fails.

**Which check grades it:** `verify/adr-supersession/verify-adr-supersession.sh` (new). `verify/record/verify-record-states-the-purpose.sh` re-run after the CONTEXT.md edits, still PASS (the Cage and Multi-version entries it reads were not touched). The code side stays graded by `verify/priced-holes/verify-priced-holes.sh` (ticket 38).

**Test-first:** the script was written before the ADR. First run: `FAIL: selfcheck: no docs/adr/0026-*.md to plant defects in` (exit 1). With the selfcheck bypassed by the env override the grading legs read 16 FAIL lines against the unchanged record. After the ADR, banners and CONTEXT edits: one FAIL, check 4's forward-reference pattern catching ADR-0026's own Context quoting ticket 38's note; the ADR was reworded rather than the check loosened. Then 0 FAIL, exit 0; `selfcheck` mode PASS, exit 0.

**Decisions** (all delegated, ADR-0025):

- D1 `(source, id)` is a refinement of ADR-0013's bare-id rule, not a replacement. Reason: ticket 15 re-grill 19 says the wire value stays the bare catalogue id and the claim key becomes `(source, id)`; ADR-0013 already named the catalogue once by the enclosing href, so the source was always half the key, and ticket 38's Answer item 2 (with D11 for which parent is the baseline's catalogue) keeps the three real adopters' headers byte-stable. What is superseded is the one-catalogue assumption, which ticket 15 item 5's adopter self-catalogue breaks.
- D2 The removal refusal is retired: a removal is priced, never refused, as a `removed-control` delta with the amount the hole carried, status `unselected` on the regime line with its amount unchanged, and one `baseline-narrowing` summary delta for a named-baseline change that drops controls, mirroring the widening delta. Reasons, in the ADR's order: the doctrine admits one refusal and it is a missing instrument, and a removed control is a missing behaviour (ticket 38's own framing); ADR-0013's reason ("an exemption by another name") described a count a removal could hide a hole from, and under pricing the regulator's weight prices a control whether or not the adopter selected it (`_decorate_regime_holes`: `unselected` keeps its amount, the partition still sums to the entry, graded by pound-seam), so a removal hides nothing from the pound; ADR-0022 already settled the same shape one layer down ("lowering the floor is priced, never refused"); the one pound a removal withdraws is a bespoke control's own scenario, an obligation the adopter invented and signs the withdrawal of, and the insurer's quote conditions (ticket 14) are where that has consequences. The ratchet alternative (union with the last signed set) was rejected as duplicated state under ADR-0018 §2 and as a count that never falls. Ticket 38 D6 is superseded by this.
- D3 The verifier grades the record only. Reason: `verify-priced-holes.sh` grades the platform source for refusal absence already; a second grader over the same source drifts from the first, and the record grader can never SKIP because every file it reads is in the hub. The header says so, as `verify-record-states-the-purpose.sh` does.
- D4 Written now, with the code lag named. Reason: ADR-0025 favours writing the decision when it is made; ticket 38 has landed on hub `main` and the platform integration branch, so the three refusals the ticket names are gone in code; the fourth, `check_selected_set`'s `removed-control`, still refuses on the platform branch and ADR-0026's Consequences say so by function name, with the list of what its build must change (`compute_deltas` kinds, the TINY-baseline selfcheck case, the schema description, `check_source`). The verifier requires that honesty line to be present.
- D5 Slug and title: `0026-a-hole-is-priced-never-refused-the-claim-keys-on-source-and-id.md`, "A hole is priced, never refused: the claim keys on (source, id), and a widening, a removal and an ungoverned namespace print as priced deltas". Banners dated 2026-09-04, the day the superseding ADR exists; ticket 15's 2026-08-28 decision date and ticket 38's 2026-09-03 build date are stated inside the ADR's Context and the banner text respectively. Reason: a banner dates the supersession, and the supersession is the ADR, which did not exist before today.
- D6 One banner per superseding ADR per file: the ticket-38 notes are replaced, not stacked under the new banners. Reason: ADR-0014/0015/0016 carry one line per superseding ADR; the 38 note's only content the new banner does not carry was the forward reference the 2026-09-04 review already objected to. ADR-0018 keeps two banners because they record two different supersessions (ADR-0022 for §4, ADR-0026 for point 3).
- D7 The ADR-0018 banner is confined to point 3. Reason: ticket 89 item 4 has ADR-0018 in its own scope ("check ADR-0014, ADR-0018 and ADR-0022 carry dated notes that say the same"), so the deny-is-not-a-rung note on ADR-0018 is ticket 89's to write, and this banner names only what this ADR supersedes.
- D8 The unknown-id hard failure is classified a missing instrument (ADR-0020), not retired; its code kind stays `unknown-control-id`. Reason: an id no pinned catalogue defines cannot be priced by anything, which is ADR-0020's instrument line exactly; the record classifies, the code keeps its name, and ADR-0026 point 6 says so.
- D10 (2026-09-04, review fixes) Every refusal kind the source emits is classified, not only the hole-shaped ones. Reason: the reviewer offered narrowing point 6 to the hole-shaped refusals or classifying the full list; the doctrine's sentence "the only refusal is a missing instrument" is a claim about every refusal, so a list that names four of eleven cannot support it. Ten are instrument faults; `removed-control` is the one behaviour and point 5 already retires it. `unpriceable-inability` is classified an instrument fault (the price instrument is what is missing, ADR-0020's line) and named as the kind nearest the line, with the twin's appetite default (ticket 15 re-grills 32, 35) as the shape a later build could price it by; that later build is not charted here. The verifier's list is a fixture of the eleven, so the record check stays offline; a twelfth kind in the source is the next ADR's fact.
- D9 The platform build that deletes `check_selected_set`'s refusal is named in ADR-0026's Consequences without a ticket number. Reason: this ticket is hub-only and wave builders minting ticket numbers in parallel collide; the integrator opens it at merge time from the ADR's list.

**Map line:** Ticket 39: ADR-0026 supersedes ADR-0013, ADR-0017 and ADR-0018 point 3; a hole, a widening, an ungoverned namespace and now a removal are priced deltas keyed (source, id), the eleven refusal kinds composition emits are classified (ten instrument faults, `removed-control` the one behaviour), banners and CONTEXT.md agree, `verify-adr-supersession.sh` grades the record; the removal's platform build is the one thing the record leads the code on.

## Waits on the owner

Nothing new from this ticket. Ticket 38's list stands and is unchanged by it: the push of the platform integration branch `ecosystem/build-2026-09-03` and its merge as `pavc-other-hand`, a platform signed tag, and the re-composition and push of driftwood, tuppence and ludlow so their evidence carries `deltas[]`.

# 18 — The publisher release under cages

Type: grilling (HITL)
Status: open
Blocked by: 06

## Question

The publisher's own release, without a gate. A release the gate would block is published at a degraded (quarantined) tier; instrument faults still refuse. The declared bump lives in a versioned file in the repo. The gate runs the full combination set, enumerates the cage dial's value space, includes the cage tier in the witness shape, publishes the tool and the corpus so an adopter can re-derive the number, and attaches the £ of every downward move to the evidence. Cut one release whose number the gate actually determines; pass institution_pins so the matrix fills.

## Notes

Re-grills 5, 6, 7, 11, 12, 13, 15; findings H9-04, H6-12. Blocked by the ESLint versioning research because the bump rules follow it.

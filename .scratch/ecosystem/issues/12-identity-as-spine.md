# 12 — Identity as spine

Type: grilling (HITL)
Status: open
Blocked by: 09

## Question

Posture is the attestation the cage reads. Settle: a trust domain per institution with federation (bundle endpoint, federatesWith); the tier and the version both ride in the SVID path; fix spire-agent CrashLoopBackOff and the OIDC provider; Pomerium wired to break-glass.py as external authz; OpenBao login proven live once; the identity substrate itself caged (governed namespaces, platform-machinery claims); the flagship reach gate derived from versions.yaml; EUD as a later ticket with one real Linux VM and swtpm as its minimum. Two ADRs: the carrier decision and the actor-class decision.

## Notes

Charting Q2: spine. Findings H8-01..H8-16. Blocked by the cage ladder because the SVID carries the tier.

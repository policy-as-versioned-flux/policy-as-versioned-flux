# 11 — An org twin each

Type: grilling (HITL)
Status: resolved
Blocked by: 08

## Question

What a twin of driftwood, tuppence or ludlow contains at minimum: the org overlay (components on the Wardley value chain, people as roles, typed causal edges, perspective, appetite), its subscribed feeds as sensed signals, a standing scenario library (the niobium headline, an EOL date passing, a fine published, a key-person event), and what it emits to the estate. The real-firm subjects become model and tooling evals: which evals, run when, with what pass threshold. Also: the substrate generator becomes a real model call or is dropped.

## Notes

Re-grills 31, 39; findings H1-02, H5-02, H5-08, H5-09. Blocked by the £ seam because the emitted artefact is decided there.

## Facts found (2026-08-28, AFK, before any question to the owner)

Code truth is `twin/` in the hub and `.estate-clone/` for the org repos.

- No twin overlay names an adopter. `twin/fixtures.py` authors 15 org overlays (netflix, intel, pocket, twin-self, carillion, royal-mail, wirecard, nmc, astrazeneca, enron, library, sanofi, backtest, kodak, maersk); driftwood, tuppence, ludlow: 0 (H1-02).
- No model repository is committed anywhere. Every twin repo is built at run time by `twin fixture --out /tmp/model` (`.github/workflows/twin.yml:112-121`); no adopter repo has a twin directory (`ls .estate-clone/driftwood`: composed, deploy, drift, git-server, gitops, kind, scripts).
- An overlay is 14 collections (`twin/model.py:35-39`); a directory nothing loads is refused (`:64-77`). An overlay pins `world_ref` and `Overlay.load` resolves it with `World.load(repo, at_commit=world_ref)` on the **same** `ModelRepo` (`twin/model.py:157-170`). So an overlay committed in an adopter repo must carry the world layer (components, propositions, world_models, priors) in the same git tree, or the loader must learn a second repo.
- A component resolves overlay-first then falls through to the world layer (`twin/model.py:296-311`); the directional rule forbids world→overlay only (`twin/model.py:3-8`). The library's nine scenarios are authored against world-layer components ("all nine world-layer so any org overlay could reuse them", `twin/fixtures.py:4005-4012`; e.g. `world/components/cryptographic-key-material.yaml` at `:3709`). `reference-map` is a world_model (`:1052`), not a component set.
- A person is `{id, role?}` (`twin/schema.py:766`); signatures bind to a role in a versioned register (`twin/roles.yaml:10-36`), never to an individual.
- A perspective requires `party` (one of seven flat PARTIES, `twin/schema.py:76-78`), `pays`, `values` (per-component graded valuations, `:195-207`), `ruin: mapping_of(text)` (prose red lines, not an amount) and `cash_flow` (`:654-672`). Neither the perspective nor a valuation carries a `currency` field. No adopter `party.yaml` carries `size`, `appetite` or `reporting_currency` today and the party schema forbids unknown keys (`grep` over `.estate-clone/*/party.yaml` and `platform/party/schema.json`: nothing); tickets 07 and 08 decided them, tickets 24 and 25 build them.
- Pricing has three gates: path graded inside `pricing_threshold: 2`, valuation graded inside it, and admission to a declared cash flow (`twin/pricing.py:20-33`; `twin/evidence-ladder.yaml:25,40`). An overlay whose edges are all grade 3+ prices nothing; it emits register entries with no number.
- A signal requires `date, steep, source, statement, provenance` (`twin/schema.py:595-604`). Twin decision 11 separated `date-observed` and `date-published` on the signal object (`.scratch/twin/issues/11-sense-move-loop.md:125`). `ingest.ingest_run` classifies at volume with no human gate, output grade 5, by construction (`twin/ingest.py:12-24`); the heuristic is proven only on political and economic signals and names "swap the body for a model call" as the upgrade path (`twin/signal_classify.py:14-22`).
- The adopters declare four parents: platform implementations 1.1.1, nist controls 1.1.0, ico pricing v1, platform threat v1 (`.estate-clone/driftwood/party.yaml:17-27`). Only the first two have a Flux pin; pricing and threat are unchecked declarations (`party.yaml:11-16` says so). `platform/feeds` holds cve, eol and threat-register (v1, v2, v1.0.0); no adopter pins cve or eol.
- The standing library is one shared `library` org with nine committed classes (`twin/schema.py:58-61`). No class exists for an EOL date passing or a fine being published. The class enum is closed (`twin/schema.py:736`) and an invariant requires the library org to carry an executable scenario per committed class (`twin/invariants/checks.py:1310-1322`), so a new class is a three-place change: enum, library fixture, adopter overlay.
- The niobium headline row `nb-refining-capacity` is a non-firing entry: `forward_signal()` branches only on attacker- and defensive-capability actors (`.estate-clone/platform/wardley/wardley.py:222-236`, H5-02).
- `schedule.sweep()` runs every scenario in every org it is handed, unconditionally, no staleness check, by design; it builds no cron (`twin/schedule.py:26-38, 60-75`). `twin.yml` runs invariants, tests, typecheck, the three beats and the determinism matrix daily at 05:17 UTC (`:13-14`); `twin sweep` is invoked only inside `twin/beat-intel.sh:93` against the intel fixture. No per-adopter sweep runs anywhere.
- The truth surface discovers `verify*.sh` only under `.estate-clone/` and `verify/` (`talk/verify-all.sh:41`); no twin script is discovered or excluded, so nothing the twin does appears in the TRUTH line (last: `pass=43 fail=11 total=56`, `talk/truth.log`). `truth.yml` commits `talk/truth.log` with `[skip ci]` (`.github/workflows/truth.yml:43-44`); any second committed record needs the same step.
- Six real skills, eight threshold rows (a second `causal-claims-grade-accuracy` metric and a `toy-classifier` fixture), thresholds 0.65-0.8 (`twin/skill-thresholds.yaml:8-11, 14-70`); every heuristic scores 1.0 on its own corpus (H5-08); lowering needs a cited `authorised_by` (`:3-6`). `skill-scores.jsonl` was last written 2026-08-13 at `heuristic-0.1.0`; `record_skill_scores.py` is called by no workflow.
- The three real-firm beats run daily in a declared order (`twin/beat-sequence.sh`; `twin.yml:82-86`); `twin/demo_slice.py:37-80` records the subject rationale.
- The substrate generator is a deterministic reference implementation, "no provider is reachable from this suite" (`twin/substrate_generator.py:1-15`); model-touched substrate is `authored`, never `derived` (`twin/substrate.py:30-36`, twin decision 14); the capability inventory still lists it as a skill (`twin/honest_build.py:173-176`); no anthropic/claude client exists in `twin/*.py`.
- The twin's estate reads are three narrow ones: `enact.dependency_pins()` globs GitRepository pins under `*/gitops/**` (`twin/enact.py:13-31`); `drift.py:52` and `verdict.py:55` read `driftwood/drift/`. "forward intel" appears in no twin code (ticket 08).
- The hub repo has zero git tags (`git tag | wc -l` = 0); platform is pinned by a Flux GitRepository `spec.ref.tag` (`.estate-clone/driftwood/gitops/platform/platform-pin.yaml`). Nothing an adopter could pin `twin` to exists today.
- The twin already models itself as a subject (`twin-self` overlay: components, a bus-factor-1 maintainer, an adoption-risk scenario, two priced controls; `twin/capabilities/twin-inside-twin.yaml:1-30`), the shape a twin-of-the-twin cage price can reuse.

Already decided, not re-asked: adopters are the twin's primary subjects, the real firms are the backtest corpus and model/tooling evals, not the demo (re-grills 31, 39); the overlay is part of the adopter's own signed composed artefact and the twin consumes "the adopter's own overlay", no participant reaches into another (NORTH-STAR §2), so a hub-built adopter fixture is out; the twin emits a `forward-intel` feed under the ADR-0019 envelope, from the adopter's own repo, signed by the adopter's twin agent identity, payload `{perspective, shock, horizon, lef|null, lm, currency, curve}`, never a recommended action; the estate annualises via `source: twin` in `prices[]` and a versioned selection-policy picks the tier (ticket 08 Q1-4, ADR-0021); appetite on `party.yaml`, signed by the adopter alone, is the one authority for the money boundary; every price and payload carries `perspective` and `currency`; no sum crosses perspectives (ticket 08 Q5-6, re-grill 33, ADR-0021); size, obligations and `reporting_currency` on `party.yaml`, GBP for the three adopters, stale size widens, a missing instrument refuses (ticket 07, ADR-0020); the tail via `twin/severity.py` (ticket 08 Q7); the twin acts inside a priced cage, propose-only outermost, Article 22 floor, the tool-call guard is a cage with a spec and a price (re-grills 37, 41); agent signatures attest the absence of a human, so the overlay (human tag) and the feed (twin agent) are two identities (NORTH-STAR principle 6, re-grill 29); schedules gather data LLM-free, reasoning is a Claude Code skill a human runs, the reviewed PR is the unit of adoption (reversals 7, 14, 15); full twin depth, the narrowing rejected (reversals 3-4); covert sensing excluded, substrates synthetic with planted ground truth (NORTH-STAR §6, twin tickets 12, 15); feed envelope, `inherits[]` with `name` and `since`, `publishes[]`, revocation as a priced hole (ticket 04, ADR-0019), so the twin's subscribed feeds are the adopter's `inherits[]`; the news feed, the niobium headline and the headline skill are ticket 23; the prediction-market feed is ticket 22; the forecast book waits on the feed contract (reversal 22); twin evals live in the gate because the truth surface is the only citable source and every ticket wires its check in (NORTH-STAR §5, map process rules, ticket 03); a threshold is lowered only with a cited authorisation (`skill-thresholds.yaml:3-6`, twin decision 20); the substrate generator is neither dropped nor a clock step: twin decision 12 Q4 chose seeded model generation from a versioned recipe, reversals 7/14/15 make any model call a skill a human runs, decision 14 marks its output `authored`, reversals 3-4 reject dropping depth; so it becomes a Claude Code skill, output `authored`, the deterministic generator kept as its eval, and `honest_build.py:173-176` is corrected to say so. That last item is a derived consequence and goes to the owner only if one of those four decisions is wrong; no exemption, no gate, price and cage, never count/refuse/file (NORTH-STAR principles 1-2).

## Grilling round 1 — drafted 2026-08-28, HELD

Drafted AFK on the owner's instruction. Adversarially checked by two reviewers. Nothing below has been put to the owner. Five questions.

❓ **Q1** - **Where each adopter's twin overlay lives**: NORTH-STAR §2 makes the overlay the adopter's own signed artefact and ticket 08 puts the emitted feed in the adopter's repo. The grain is open. Options: (a) an `orgs/<adopter>/` overlay inside the adopter's own repo (e.g. `driftwood/twin/`), signed under the adopter's ordinary gitsign tag; (b) a separate `<adopter>-twin` repository in the adopter's GitHub org with its own tags. Either way two prerequisites are facts, not options: the world layer must be readable from the same git tree (`model.py:157-170`), so it is vendored at `world_ref` as a pinned copy or the loader gains a second repo; and `twin` must self-version (a signed semver tag; the hub has none) before an adopter can pin it, which is the ESLint rule and belongs to the build ticket this graduates into.

➡️ (a). The overlay is signed by the adopter's human tag and the forward-intel feed by the twin agent; two identities, one repo, so a verifier re-renders the feed from the overlay at the pinned commit in one checkout (ADR-0012, one mechanism). (b) separates the overlay from the feed ticket 08 already placed, and adds a repo nothing pins. Recommended shape for the world layer: a vendored copy under `twin/world/` pinned by the same tag, because a loader that spans two repos is a second mechanism for one directional rule.

❓ **Q2** - **The overlay floor, and how the perspective binds to `party.yaml`**: `party.yaml` is the authority for appetite and currency (settled). The perspective holds no money boundary: `ruin` is prose and neither it nor a valuation carries a currency. Options: (a) authored floor: the caged workload, the pinned policy line and the regulated data as components with Wardley positions; people as roles only; at least one causal edge graded inside the pricing threshold to a declared cash flow; at least one perspective, `party: employer`, whose valuations carry a `currency` (schema bump) that the verify script checks equals `party.yaml`'s `reporting_currency`, and whose id the emitted payload's `perspective` names; appetite is never copied into the overlay; (b) derived skeleton: a generator reads `party.yaml`, `deploy/pod.yaml` and the gitops pins into components and copies currency and appetite in at run time, the adopter authors only edges and the perspective; (c) free-form: any non-empty overlay, the script checks only that a forecast can be emitted.

➡️ (a). Below this floor `pricing.py`'s three gates emit register entries and no curve, so the forward-intel feed carries nothing the estate can annualise. Component valuations cannot be derived from turnover, so (b) still needs authored numbers and adds a generator whose output is `derived` with nobody accountable behind it. Binding by check keeps `party.yaml` the one signed authority and adds one field, not a second copy of the boundary. Sequenced after ticket 25 lands `reporting_currency`; more perspectives (union, regulator) are ordinary additions, none privileged (re-grill 33).

❓ **Q3** - **How a subscribed feed becomes a sensed signal**: the twin-side representation only; whether it runs on the clock or as a skill is ticket 10's, which inherits this answer. Options: (a) every new pinned version of a subscribed feed maps by lookup to one signal: `date` = the payload's event date where its schema carries one (EOL date, fine date), `published_at` in provenance with the tag and commit; `source` = the signed tag; `steep` from a fixed table keyed by feed name (penalty-schema political; eol, cve, threat-register technological; fx and market-moves economic); grade 5, contestable. Headlines are excluded from the lookup; their path is ticket 23's. Feeds with no pin (ico pricing, platform threat today) cannot fire until ticket 21 pins them; (b) only feeds a standing scenario names become signals, the rest are fetched and not sensed; (c) every feed version is bound by a human running a skill.

➡️ (a). The mapping has no judgement in it, so nothing here is a reasoning step; the judgement (what a headline means on the map) is already ticket 23's skill. (b) lets the scenario library gate what the twin can see. (c) reintroduces the human-at-entry gate twin decision 11 Q2 rejected in favour of downstream trust. Mis-tagging is priced downstream, never a refusal.

❓ **Q4** - **The standing scenario library per adopter**: a scenario object lives in one overlay and may name components in that overlay or the world layer. Two of the ticket's four named events have no class. A `penalty-published` scenario risks pricing the fine twice: the estate already re-prices a fine schema bump through composition and `prices[]` with `lef: null` (NORTH-STAR §4 steps 1-3, ADR-0021 keeps two engines only where each answers a different question). Options: (a) four standing scenarios per adopter against the adopter's own components: niobium headline (`supply-shock`), EOL date passing (new class `eol-date-passes`), fine published (new class `penalty-published`, whose shock is the post-publication consequence on the value chain, e.g. customer attrition under the adopter's perspective, never the fine amount, which stays the estate's), key-person event (`bus-factor-key-person`); each new class is the three-place change (enum, library fixture, adopter overlay); (b) three per adopter, the fine left wholly to the estate; (c) all nine committed classes replicated into each adopter overlay.

➡️ (a). It is the thin slice NORTH-STAR §4 step 5 needs, with the seam kept: the estate prices the fine, the twin prices what the fine does to the value chain, and `lef: null` lets the pricing feed supply frequency. (b) loses the one scenario that exercises the seam end to end. (c) adds classes (climate-event, m-and-a) no feed the adopters subscribe to can move yet. World-layer components may be reused where they fit; the adopter's own workload, policy line and data are overlay components by Q2.

❓ **Q5** - **The evals: which, and when**: gate placement and threshold policy are settled. Options: (a) one `verify/verify-twin-evals.sh` in the hub, discovered by `verify-all`, runs the six skill evals at the existing thresholds, the three real-firm beats and `cross_architecture_determinism`; PASS/FAIL by exit code; scores are printed into the gate output and `truth.log` is the record (no second committed file); a fall in any score against the last recorded value is a FAIL; (b) skill evals daily in the gate, the beats and determinism weekly in `twin.yml`; (c) evals run only on a `model_version` change.

➡️ (a). `twin.yml`'s daily run is a second clock whose result no document may cite (NORTH-STAR §5). (b) leaves the beats outside the truth line, which is where re-grill 39 put them. (c) loses the score-over-time record the harness exists for, and the corpus digest already makes a no-change day visible. `twin.yml` keeps running the suite on push; the gate is what is cited.

Later rounds, blocked on the above: the twin's own cage, spec and price per adopter twin, reusing the `twin-self` shape (re-grills 37, 41), decidable once Q1 fixes the repo and identity and ticket 09 fixes the ladder; the substrate-generator skill's recipe and where its `authored` output lands, after Q5 wires the deterministic generator in as its eval; whether the adopter twin also publishes a score-card feed beside forward-intel (reversal 22), after Q5 and ticket 21; sensor admission for the key-person scenario in a real adopter (ethics gate, roles register, DPIA), after Q4 and ticket 19; cadence of the per-adopter sweep and which steps are skills, ticket 10, after Q3; the loader-versus-vendored world layer and the `twin` release tag, in the build ticket Q1 graduates into.


## Cross-ticket notes (2026-08-28)

- C4 (10, 23): "already decided" carries twin 11 Q2 (binding automated at volume, no human gate); 10 Q1(a) says a heuristic never runs on the clock and classification is a human-run skill; 23 Q3(a) packages signal-classify as that skill. Q3(a) here (lookup, no judgement) survives either way; the ingest rule does not.
- C5 (12): Q1(a) rests on "two identities, one repo: the overlay under the adopter's human tag, the feed under the twin agent". 12's facts show every tag in the estate is cut by a GitHub Actions workflow subject (a machine identity no human can log in with), and 12 Q4(b) roots the twin agent as the workflow OIDC identity of the twin schedule. Both signers are then Actions subjects differing by workflow path; "human tag" is not a real category here.
- C14 (10, 23): 23 Q3(a) opens the headline skill's PR against "the adopter's overlay", assuming Q1(a) before the owner chooses it; 10's later round still lists the landing place as open.
- C19 (19): Q4(a) fixes four standing scenarios per adopter; 19 Q3(a) adds "a rival reads my holes" and 19 Q4(c) "publisher X withdraws". The library is being composed in two tickets.

## Answer

Resolved 2026-08-28. On 2026-08-28 the owner read every held round and wrote: "ive already read the recommendations and I can't find fault with a single one. Well done. Get everything ready for me to then to-spec". A bare agree does not ratify, so the five items below are PROVISIONAL, each with its recommendation's own reason as the rationale, as tickets 04, 07 and 08 were. The five-per-day rule was overridden by that instruction; recorded. The cross-ticket conflicts then went to the owner as five decisions with a panel verdict. The owner wrote: "I agree with you're more advanced reasoning". D1 to D5 are DECIDED. None changes a question here. C4, C5, C13, C14 and C19 amend this ticket.

1. **Where the overlay lives.** Each adopter's twin overlay lives inside the adopter's own repo (`driftwood/twin/` and siblings), signed under the adopter's ordinary release tag. The world layer is vendored under `twin/world/`, pinned by the same tag. The loader does not learn a second repo. Rationale: one checkout lets a verifier re-render the feed from the overlay at the pinned commit (ADR-0012). A separate twin repo is a repo nothing pins. Amendment C5: "human tag" means the overlay release workflow identity; the twin agent is the twin schedule's workflow identity. `twin` must self-version with a signed semver tag first; the hub has none.

2. **The overlay floor.** The floor is authored. It holds the caged workload, the pinned policy line and the regulated data as components with Wardley positions. People are roles only. At least one causal edge is graded inside the pricing threshold to a declared cash flow. At least one perspective has `party: employer`. Its valuations carry a `currency` field (schema bump) that the verify script checks equals `party.yaml`'s `reporting_currency`. The payload's `perspective` names that perspective. Appetite is never copied into the overlay. Rationale: below this floor the pricing gates emit no curve, so the feed carries nothing the estate can annualise. A generator cannot derive valuations from turnover. Lands after ticket 25 delivers `reporting_currency`.

3. **A subscribed feed becomes a signal by lookup.** Each new pinned version of a subscribed feed maps to one signal with no judgement. `date` is the payload's event date where its schema carries one. Provenance carries `published_at`, tag and commit. `source` is the signed tag. `steep` comes from a fixed table keyed by feed name. Grade 5, contestable. Headlines are ticket 23's skill. Unpinned feeds cannot fire until ticket 21 pins them. Rationale: nothing here is a reasoning step; mis-tagging is priced downstream. Amendment C4: twin decision 11 Q2 ("automated binding at volume, no human gate") is superseded by the map's Note. Lookup binding stays on the clock because it is mechanical.

4. **The standing scenario library.** This ticket owns it. Each adopter carries six standing scenarios against its own components: niobium headline (`supply-shock`), EOL date passing (new class `eol-date-passes`), fine published (new class `penalty-published`), key-person event (`bus-factor-key-person`), a rival reads my holes, and publisher X withdraws (both from ticket 19). Each new class is the three-place change: enum, library fixture, adopter overlay. The `penalty-published` shock is the consequence on the value chain, never the fine amount. The estate prices the fine; the twin prices what the fine does. Rationale: the thin slice NORTH-STAR §4 step 5 needs, seam kept. Amendments C19 and C13: ticket 19's two scenarios join here; niobium never enters the news feed.

5. **The evals.** One `verify/verify-twin-evals.sh` in the hub, discovered by `verify-all`, runs the six skill evals at existing thresholds, the three real-firm beats and `cross_architecture_determinism`. PASS or FAIL by exit code. Scores print into the gate output; `truth.log` is the record. A fall in any score against the last recorded value is a FAIL. `twin.yml` keeps running on push but is never cited. Rationale: only the truth surface is citable (NORTH-STAR §5).

Consequences: twin decision 11 Q2 is superseded by item 3. Ticket 20's niobium line is corrected. Ticket 10's open item closes: a skill's claim file lands as a PR on the adopter's overlay (C14). Ticket 21 pins ico pricing and platform threat. Ticket 25 lands `reporting_currency` and the perspective `currency` bump. A new build ticket carries items 1, 2, 4 and 5. The substrate generator becomes a Claude Code skill with `authored` output, the deterministic generator kept as its eval, `honest_build.py:173-176` corrected. Revisit triggers: a loader forced to span two repos; a scenario class no subscribed feed can move; the D5 dissent if the EOL ramp misprices.

Graduated: "Build: adopter twin overlay, twin release tag and twin evals" (task). "The twin's cage, spec and price per adopter" (grilling). "Sensor admission for the key-person scenario" (grilling).

# 16 — Flux rescoped and verified at the cluster

Type: grilling (HITL)
Status: resolved
Blocked by: none

## Question

Two linked questions. The Flux test, re-scoped: can a publisher's signed, pinned policy be proven in force inside a consumer org, continuously, across an org boundary? Design the measurement and its pre-registration. Cluster-side verification: a verifying step at the cluster boundary, either a controller or the mo-07 OpenPGP dual-signing bridge so `GitRepository.spec.verify` bites, time-boxed until Flux ships gitsign. Also: wire `gitops/platform` into each adopter's reconcile so the ResourceSet fan-out reconciles for the first time, and make the composed set the thing it installs.

## Notes

Re-grills 1, 18; findings H6-01, H6-02, H9-01, H9-09, H9-10.

## Facts found (2026-08-28, AFK, before any question to the owner)

Source of truth is `.estate-clone/` and the three live KinD clusters (read 2026-08-28). Paths below are relative to `.estate-clone/`.

- Live sources differ per cluster. kind-driftwood reconciles `driftwood` and `nist` from `http://git-server.flux-system.svc.cluster.local` (GitRepository 27d old; the `git-server` Deployment was re-created 2026-08-27T13:23:36Z). kind-tuppence and kind-ludlow reconcile from the real remotes (`https://github.com/policy-as-versioned-{tuppence,ludlow,nist}/...`) at `v1.0.0`, Ready=True, no `spec.ref.commit`, no `git-server` Deployment. An org-boundary reconcile therefore already exists live on two clusters, unpinned by commit (GAPS 2.7).
- The mo-09 repoint was applied live on 2026-08-20 to all three clusters, then regressed in the committed tree: `driftwood/scripts/up.sh` (last touched by `3f885a9`, the 2026-08-25 estate build) still seeds a local repo, tags it unsigned, bakes `driftwood-git:local` and points Flux at the git-server (`up.sh:26-40,56-70,98`; `lib.sh:9-12`); tuppence and ludlow `up.sh` carry the same two `git-server` lines. driftwood was re-brought-up from that tree on 2026-08-27. `gotk-sync.yaml:20-23` pins `tag: v1.0.0` with the commit line commented out while `v1.1.0` (the composed release) exists in all three adopters. driftwood PR 11 (open, ticket 03) adds `commit:` and makes `verify-reconcile.sh` read the pin from the tree.
- No cluster has a ResourceSet. `gitops/platform/platform-pin.yaml:6-11` is opt-in and never applied; the adopter Kustomization reconciles `./apps` only (`gotk-sync.yaml:37`; `apps/kustomization.yaml:3-6`: namespace plus two Phase-0 ConfigMaps). Only kind-driftwood has the engine: flux-operator v0.58.1, source-controller v1.9.3 (Flux 2.9 line), Kyverno admission/background/cleanup controllers. kind-tuppence and kind-ludlow have neither the ResourceSet nor the ValidatingPolicy CRD. `platform/engine/up.sh:19-31` installs Kyverno and flux-operator against driftwood only.
- kind-driftwood already carries five kubectl-applied ValidatingPolicies with fan-out names (`posture-trust-boundary`, `-1-0-0`, `-2-0-0`, `require-nonroot-1-0-0`, `-2-0-0`, aged 5-7d; H6-01). A sample that only checks "the rendered object is present" passes against hand-applied objects unless it also checks the Flux Kustomization inventory.
- The fan-out installs platform's own tree. `platform/distribution/versions.yaml:31-33,44,61` renders one GitRepository at `https://github.com/policy-as-versioned-platform/platform` per array element (`policy/v2.0.0`, `v2.0.1`, `v3.0.0`) plus a Kustomization at `./distribution/policies/v<version>`, plus one orphan guard. It never references an adopter's `composed/` tree. Every array commit is an ancestor of its tag, on purpose (`cut-release.yml:170-178`; `cut-release-update-array-commit.sh`): `policy/v2.0.0` and `v3.0.0` resolve to `e34ae7f` but pin `fa862b7`; `v2.0.1` resolves to `ebc4ff5` but pins `a1072d9` (H6-12, GAPS 3.27). Flux's `spec.ref.commit` takes precedence over `tag`, so those sources check out the ancestor; whether `spec.verify` mode `Tag` still verifies the tag object when `commit` is set and differs is unverified.
- Each adopter's `composed/policies/v3.0.0` differs from platform's in 5 of 7 files (advisory `composed-for`/`inherited-from` annotations) and lacks two files platform's dir carries (`kustomization.yaml`, `priorityclasses.yaml`), so it cannot be `kustomize build`-ed as-is. The three adopters' composed trees differ from each other only by the `composed-for` label (H9-02). Nothing under `gitops/` references `composed/` (one comment, `apps/namespace.yaml:12`).
- `driftwood/composed/HEADER.yaml:6-22` pins four parents by SHA: platform `1.1.1` `58ef9c5` (equals `platform-pin.yaml:24-25`), nist `1.1.0` `33a05df` (equals `gotk-sync-nist.yaml:28-29`), ico `pricing v1` `5d1e76fa` (a real ico commit, not ico's only tag `v1.0.0` = `0918623a`), platform `threat v1` `7a36bf7`. ico has no GitRepository and no Renovate manager anywhere; ticket 04 renames `pricing` to `feed`/`penalty-schema` and ticket 21 owns the migration.
- Flux `GitRepository.spec.verify` (2.9) accepts modes `HEAD`, `Tag`, `TagAndHEAD` and two key types in one Secret: OpenPGP (`.asc`) and SSH (`.sshpub`). gitsign is unsupported; fluxcd/source-controller#1068 is open, opened 2023-04-09, no comments, no milestone, no linked PR (`docs/upstream/fluxcd-source-controller-1068-gitsign.md`; ADR-0001 Consequences). A git tag object carries exactly one signature block. `grep -rn 'verify:\|secretRef' */gitops` finds nothing. `gotk-sync.yaml:5-8` ("only speaks OpenPGP, so we do NOT fake a PGP block") and `gotk-sync-nist.yaml:8-11` ("only speaks OpenPGP") are both stale against Flux 2.9's SSH support (H9-09).
- Every tag in the estate is cut keyless by the workflow's own Actions identity via pinned gitsign 0.17.1 (`platform/.github/workflows/cut-release.yml:19-29`) and verified identity-pinned with `GITSIGN_REKOR_MODE=offline` in each repo's `release.yml` on its own tag push only (`driftwood/.github/workflows/release.yml:43-44,76-88`). The one cross-party verification is `adopter-gate.py:258-266`, `cosign verify-blob` of platform's evidence bundle, never of a policy tag.
- The old window is fully pre-registered: `driftwood/drift/window.yaml` `window:` 22 (2026-08-07 to 2026-11-06), `cadence:` 32 (hourly), `subjects:` 38 (three Phase-0 ConfigMap/namespace subjects), `drift_event:` 62 with `deploy_marker` = the Kustomization's `lastAppliedRevision` (68), falsifiers 77, `scope_limit:` 89 (state only; the action branch is "a second declared window", 104-106), `operation:` 111-114 (operator cron, because a hosted runner cannot reach local KinD), `intervention_policy:` 125 (observation-only). `verdict.yaml:56-58` needs 90% coverage and a closed window. `samples.jsonl` holds 3 samples, last 2026-08-13; `crontab -l` has no probe entry. `twin/verdict.py:250-290` closes the continuous-state branch unmeasured. `drift/forced-campaign.yaml` (build ticket 78) is the precedent for a planted mechanism instrument: 464 samples in its own log, excluded from the organic tally by construction.
- The truth surface: `talk/verify-all.sh:4-16` globs `verify*.sh`, grades 0 PASS / 3 SKIP / other FAIL; `truth.yml:23` runs on `ubuntu-latest` with no cluster; last line `TRUTH 2026-08-28T05:08Z run=5 ... pass=43 fail=11 skip=0 excluded=2 total=56`. `driftwood/verify-reconcile.sh:14-38` asserts GitRepository Ready at `v1.0.0` with a 40-char commit, the two ConfigMaps, and nist at `v1.0.0` with a 40-char commit, which contradicts the committed nist pin `v1.1.0`. It has no `exit 3` path, so on the hosted runner and on tuppence/ludlow it FAILs where verify-all's own rule says SKIP. It asserts nothing about the URL, a signature, or any policy object.
- Renovate maintains the `{tag, commit}` pair for exactly two files per adopter, `gotk-sync-nist.yaml` and `gitops/platform/platform-pin.yaml` (`renovate.json` customManagers, git-refs). The adopter's own `gotk-sync.yaml` is unmanaged.
- `verify-orphan-guard.sh` has no live tail; `verify-retirement.sh` does. `render-orphan-guard.py` is the offline twin of the ResourceSet Go template (`versions.yaml:12-15`) and the two have never been compared against a live render (H6-02, with refuter correction).

Already decided, not re-asked: re-scope the Flux test to "a publisher's signed policy proven in force inside a consumer org, continuously, across an org boundary", drop the drift-floor test (re-grill 1); cluster-side verification is real work, a controller or the mo-07 bridge so `spec.verify` bites (re-grill 18); the bridge is time-boxed until Flux ships gitsign, keys are per-org Actions org secrets backed up to 1Password, the implementing ticket links #1068 and marks the path temporary in-repo, and every tag the cluster fetches carries the bridge signature (mo-07 Answer §3, owner 2026-08-20); regulators are pinned directly by each institution, never through platform, and ico is not a GitRepository (mo-07 §4; ticket 04 §2: ico's penalty schema is a compose-time `feed`, ticket 21 owns its pin); each org self-verifies with the pinned harness and the hub aggregates (re-grill 4); Flux is the distribution arm, held integral unless disproven, the falsification test stays open (NORTH-STAR principle 7; reversal 1); transport is gitsign-signed tags pinned by `{tag, commit}`, OCI+cosign rejected for the floor, live semver ranges rejected, Renovate customManager maintains the pair (ADR-0001, ADR-0002); the fan-out is a ResourceSet over one version array and the policy mechanism stays vanilla-Flux-expressible (ADR-0005); a composed artefact is self-signed as one gitsign tag with parents by pinned SHA, reproducible byte-for-byte, a new package with its own semver (ADR-0012; re-grill 2; ticket 06); the old window's continuous-state branch closes unmeasured, its amendment does not fire, three samples are never quoted as a result, and the operator cron was declined and is not offered again (owner 2026-08-15); the action-continuity branch is its own pre-registered window (`window.yaml:104-106`); done is the truth surface and every ticket wires its check into `verify-all.sh`, driftwood before widening (map Notes; ticket 03); schedules gather data LLM-free, nothing timed changes a verdict, the reviewed PR is the unit of adoption (principle 5; reversals 7, 14, 15); everything is caged, price and cage, never count/refuse/file (principle 2; reversal 5). The composed set being what the fan-out installs is the ticket's own scope (GAPS 1.3; H9-01), so "composed stays merge-time" is not offered below.

## Grilling round 1 — drafted 2026-08-28, HELD

Drafted AFK on the owner's instruction. Adversarially checked by two reviewers. Nothing below has been put to the owner. Five questions.

❓ **Q1** - **What one sample observes**: the old window's subjects are Phase-0 ConfigMap markers and its instrument is state-only; the re-scope needs subjects that make each word observable. Options: (a) five facts per sample: the adopter's GitRepository for each publisher is Ready at the pinned `{tag, commit}` and its URL is that publisher's real GitHub remote; `spec.verify` reports the tag signature verified; the Kustomization's `lastAppliedRevision` equals that commit (the old window's `deploy_marker`, reused); every policy object the signed tag renders is present on the cluster byte-equal to an offline render; and every such object is in that Kustomization's Flux inventory, so a kubectl-applied twin does not pass. State continuity only, said so in `scope_limit`; (b) the existing three ConfigMap subjects plus the Flux Ready condition, no policy objects.

➡️ (a). It is the only shape that makes "signed" (`spec.verify`), "across an org boundary" (real remote, publisher's commit), "in force" (rendered objects live and Flux-owned) and "continuously" (sampled on a cadence with coverage) observable. (b) proves a marker, which H6-01 says was never enough. Build cost inside (a), stated now: there is no offline renderer for a per-adopter `composed/` tree (its version dirs lack `kustomization.yaml`), so one must exist before sample one; and driftwood PR 11 must merge first so the commit pin is real.

❓ **Q2** - **What would falsify "Flux is the distribution arm"**: principle 7 holds Flux integral unless disproven, and the old window pre-registered its falsifiers before sampling (`window.yaml:77`). The re-scoped window needs its own, declared before sample one, or it is a demonstration. Options, declared together or singly: (a) a sample where a signed, pinned source is Ready and verified yet a rendered policy object is absent or unequal on the cluster and is not healed within N reconcile intervals; (b) `spec.verify` passes on a tag whose gitsign identity fails the identity-pinned CI verification, so the bridge attests what gitsign does not; (c) coverage below the floor at window close, recorded unmeasured, not as a pass.

➡️ All three, declared in the new `window.yaml` before the first sample. (a) is the "in force, continuously" claim failing; (b) is the bridge lying, which is the one way cluster-side verification can be worse than none; (c) is what killed the first window, and naming it keeps a stopped instrument from reading as a stable estate. N is set in the pre-registration from the Kustomization interval (5m today), not chosen after data.

❓ **Q3** - **Where the sample runs, so coverage is honest**: the first window died on coverage (hourly operator cron, never installed, 3 samples); `window.yaml:107-110` records that a hosted runner cannot reach local KinD; re-grill 4 says each org self-verifies. Options: (a) a scheduled workflow in the adopter's own repo brings up an ephemeral KinD, installs the engine, reconciles from the real remotes with `spec.verify` on, records the Q1 sample and commits it to `drift/samples.jsonl` as the old probe did; `verify-all.sh` discovers the adopter's verify script, which grades the latest sample and exits 3 when none is fresh; the hub's daily `truth.yml` stays the aggregate; (b) a self-hosted runner on the owner's machine samples the long-lived kind-driftwood on the same schedule.

➡️ (a). It keeps the measurement inside the consumer org, runs on a clock the org owns, needs no machine that stays on, and writes could-not-look honestly. This is new build: a KinD-in-Actions job with engine install (several minutes per run), and `verify-reconcile.sh` gains the `exit 3` path it lacks today. The cost, stated in the pre-registration's `scope_limit`: a fresh cluster proves reconcile-from-signed-source across the boundary each run; it does not observe between-deploy drift on a long-lived cluster, which is the claim re-grill 1 dropped. Cadence and the coverage floor are numbers ticket 10 owns; they are written into the pre-registration under its schedule rule before sample one, not chosen here. A planted heal-on-drift beat, if wanted, is a separate mechanism instrument on the `forced-campaign.yaml` precedent, never in the organic tally.

❓ **Q4** - **Controller or bridge, and if bridge, which key**: re-grill 18 names both. A tag carries one signature, so "dual-signed" means the tag signed by the bridge key and the tagged commit signed by gitsign, verified with mode `Tag`. Options: (a) the mo-07 bridge, time-boxed, with the key type as a sub-choice: (a1) OpenPGP as mo-07 wrote it, `gpg` in every release workflow; (a2) SSH, `git -c gpg.format=ssh`, one `ssh-keygen` key per org as the org secret, `.sshpub` in the Flux Secret, which Flux 2.9 accepts and mo-07 could not know; (b) a gitsign-verifying step at the source boundary, a controller that runs identity-pinned `gitsign verify` against the fetched revision and gates the Kustomization, the shape `docs/upstream/...1068-gitsign.md` describes; (c) contribute the #1068 provider upstream and wait.

➡️ (a2). It satisfies re-grill 18 with the least tooling and the same custody mo-07 decided; the Rekor provenance stays on the commit where `release.yml` already signs and verifies. The key type is an amendment to mo-07 on a fact that changed, not a reopening. (b) is a second controller to build and run in six orgs for the same time-box; (c) is the removal trigger, not the bridge. Whichever is chosen, the build must first test whether mode `Tag` verifies when `spec.ref.commit` is set to a commit that is not the tag's target, because that is what platform's fan-out sources do today (H6-12); if it does not, the ancestor-pin design (GAPS 3.27) must change before the bridge can bite on platform's sources.

❓ **Q5** - **What the fan-out installs on the adopter cluster**: today the opt-in `platform-distribution` Kustomization would install platform's own version dirs, byte-identical for every adopter, and `composed/` has no consumer. Under ADR-0012 the composed set is one tag in the adopter's repo, so a ResourceSet ranging it has one source; the array becomes `{version, path}` over that source and the orphan guard still ranges the array (an ADR-0005 consequence to record). Options: (b) a ResourceSet in the adopter's repo ranging `composed/policies/v<version>` from the adopter's own signed tag, with `composed/orphan-guard.yaml` as the guard; no platform source object on the cluster; (c) (b) plus the platform and nist GitRepository objects kept on the cluster as verified sources only, with the sample asserting each one's resolved commit equals the matching parent SHA in `composed/HEADER.yaml` (platform `58ef9c5`, nist `33a05df`; the `policy/v*` fan-out sources are platform-internal and are not compared).

➡️ (c). (b) is what "a composed set is a new package" means on a cluster and makes Q4's verify bite on the artefact the adopter risk-bears on, but alone it moves the org boundary off the cluster: the cluster would fetch only the adopter's own repo. (c) keeps the boundary observable at the cluster with no second policy install, and the HEADER equality is cheap for exactly those two sources. Facts the build absorbs: composed version dirs need `kustomization.yaml` and `priorityclasses.yaml` or an equivalent; the Go template and `render-orphan-guard.py` finally get compared live (H6-02); the bridge key set under (c) is adopter, platform and nist, the three publishers a GitRepository fetches, so ico, feeds and insurer stay gitsign-only at compose (mo-07 §3 read with ticket 04).

Later rounds, blocked on the above: the new window's numbers (cadence, coverage floor, N, open and close dates) under ticket 10's schedule rule, before sample one (Q2, Q3); widening from driftwood to tuppence and ludlow (engine install, ResourceSet CRD, git-server removal re-landed in all three `up.sh`, which is mo-09 done twice) once the driftwood slice is green in `verify-all` (Q3, Q5); the bridge removal trigger in code, a verify script that goes red when #1068 closes or `spec.verify` grows a sigstore type, plus key rotation (Q4); the ancestor-pin decision, GAPS 3.27, which the composed tag inherits from `cut-release-update-array-commit.sh` (Q4, Q5); live tails for `verify-orphan-guard.sh` and the byte-for-byte Go-template versus Python-render assertion (Q5); ico pinned at a tag rather than SHA `5d1e76fa`, owned by ticket 21 as an `inherits[]` subscription (Q5); the stale `gotk-sync*.yaml` comments and `verify-reconcile.sh`'s nist `v1.0.0` assertion (GAPS 2.7), fixed in the same PR that adds `exit 3` (Q3); the action-continuity window as its own pre-registration (settled as separate; not this ticket).


## Cross-ticket notes (2026-08-28)

- C2 (10, 20): Q3(a) commits `drift/samples.jsonl` from a scheduled workflow; 10 Q3 rejects machine commits to main from a clock and 10's proposed `verify-schedules.sh` asserts no scheduled run merged anything. Ticket 03's `truth.log` commit is the precedent; 10 must rule before this lands. Q3 also defers cadence and coverage floor to 10; 10 Q2(a)'s daily floor already answers it.
- C6 (12, 18): Q4(a2) signs the tag object with an SSH key, so adopter, platform and nist tags are no longer gitsign-signed (one signature block per tag). 12 and 18 list "one signing mechanism, the gitsign tag, never a second" as settled, and 18 relies on `release.yml` verifying the tag with gitsign. Re-grill 18 said "controller or the bridge"; picking the bridge reverses what 12 and 18 treat as closed. Say so in the question.
- C12 (20): Q3(a) makes the §4 step-4 live tail observable inside a CI run (ephemeral KinD in Actions). 20 Q2 rejects its option (a) on the ground that step 4 is "permanently could-not-look" in CI and picks a presenter-run local `--live` number instead. If Q3(a) lands, 20 Q2(a) is the honest option and NORTH-STAR §5 (one command in CI is the only citable source) is kept.

## Answer

Resolved 2026-08-28. Owner record: on 2026-08-28 the owner read the held round and wrote: "ive already read the recommendations and I can't find fault with a single one. Well done. Get everything ready for me to then to-spec". Under the map's process rule a bare agree does not ratify. Q1, Q2, Q3 and Q5 are PROVISIONAL, each recommendation's own reason as rationale, as tickets 04, 07 and 08 were. The five-per-day rule was overridden by that instruction; recorded. Five conflicts then went to the owner with a three-lens panel verdict. The owner wrote: "I agree with you're more advanced reasoning". D1, D3 and D4 touch this ticket. They are DECIDED with that line and the panel reasoning as rationale. Q4 is decided and amended.

1. **Q1, provisional.** One sample records five facts. Each publisher's GitRepository is Ready at the pinned `{tag, commit}` and its URL is the publisher's real remote. The tag signature is verified at the source boundary. The Kustomization's `lastAppliedRevision` equals that commit. Every rendered policy object is live and byte-equal to an offline render. Every such object is in the Flux inventory. State continuity only. Rationale: the only shape that makes every word observable. Build cost: an offline renderer for `composed/`, and driftwood PR 11 merged first.

2. **Q2, provisional.** Three falsifiers are declared in the new `window.yaml` before sample one: a verified source Ready but a rendered object absent or unequal and not healed within N intervals; cluster-side verification passing a tag that identity-pinned CI rejects; coverage below the floor at close, recorded unmeasured. N comes from the Kustomization interval.

3. **Q3, provisional, stands under D1.** A scheduled workflow in the adopter's own repo brings up an ephemeral KinD, installs the engine, reconciles from the real remotes with verification on, and appends the sample to `drift/samples.jsonl`. The adopter's verify script grades the latest sample, exit 3 when none is fresh. Rationale: measurement stays inside the consumer org, on its own clock. D1 amendment: the sample is an OBSERVATION, which a clock may append. The lane is caged: a repo ruleset limits the scheduled identity to observation paths, bot commits are signed, and `verify-schedules.sh` asserts no scheduled run changed a signed artefact. Cadence and coverage floor come from ticket 10's schedule rule (10 Q2(a) daily floor).

4. **Q4, decided, D3.** The held recommendation (SSH bridge) is rejected. One signing mechanism exists, the gitsign tag, on every artefact. Cluster-side verification is an identity-pinned gitsign-verifying controller at the Flux source boundary. It gates the Kustomization, time-boxed until fluxcd/source-controller#1068 lands. No SSH or OpenPGP key re-signs any ref. Rationale: the bridge is a second signer under another name. Precondition kept: test Flux mode `Tag` with `spec.ref.commit` set to a non-target commit first (H6-12).

5. **Q5, provisional.** A ResourceSet in the adopter's repo ranges `composed/policies/v<version>` from the adopter's own signed tag, with `composed/orphan-guard.yaml` as the guard. The platform and nist GitRepository objects stay on the cluster as verified sources only. The sample asserts each resolved commit equals the matching parent SHA in `composed/HEADER.yaml`. Rationale: the boundary stays observable with no second policy install. The controller identity-pins adopter, platform and nist; ico, feeds and insurer stay compose-time (tickets 04, 21).

**Consequences:** D3 supersedes mo-07 Answer §3 (bridge, per-org keys) and the bridge wording in this Question. ADR-0001 Consequences gain the controller as the interim; ADR-0005 gains the `{version, path}` array over one source.  D4: ticket 20 Q2 flips to (a); NORTH-STAR §4 step 4 cites only the scheduled CI number and renders could-not-look until Q3 lands. D1: ticket 10 Q3 wording is amended; `verify-schedules.sh` is ticket 10's build. The ico pin at a tag lands in ticket 21. Q1, Q2, Q3 and Q5 land in a new driftwood build ticket; Q4 in a new controller ticket; widening to tuppence and ludlow in a third, after driftwood is green in `verify-all`. Revisit triggers: #1068 closes or `spec.verify` grows a sigstore type (a verify script goes red, controller removed); the mode `Tag` test fails (GAPS 3.27 ancestor-pin design changes); coverage below floor (unmeasured, never a pass).

**Graduated:** Driftwood proves the composed set in force from signed sources in CI; The gitsign-verifying source controller; Widen the Flux slice to tuppence and ludlow.

# 24 — Size beyond turnover

Type: grilling (HITL)
Status: resolved
Blocked by: 07

## Question

Ticket 07 scales `pct_of_global_turnover` by turnover. Three regimes do not use turnover: PCI prices per month, HIPAA per violation, FCA on relevant revenue plus discretion. Settle how each reads the size facts (owner, 2026-08-19: "cost per customer"): does HIPAA scale by `data_subjects`, PCI by card-holding `customers`, FCA by a declared `relevant_revenue`? Does the party artefact need per-obligation counts, or do the four size facts suffice? Also: the `fx` feed's fetch cadence and source, which ticket 10 owns unless it is settled here.

## Notes

Findings H3-02, H3-15, H9-06, H9-07. GAPS 1.10, 3.18. Graduated from ticket 07. ADR-0020, ADR-0019.

## Facts found (2026-08-28, AFK, before any question to the owner)

Source of truth is `.estate-clone/`. Paths below are relative to it.

- The one pricing schema, `ico/schema/v2/penalty-schema.json` (`published_by: ico`), carries four regimes: `uk-gdpr` (`pct_of_global_turnover`, rate and `cap_gbp`), `pci-dss` (`per_month_escalating`, three monthly bands, GBP, contractual not statutory), `hipaa` (`per_violation_tier`, `min_usd 68928`, `max_usd 2067813`, `annual_cap_usd 2067813`, USD), `fca` (`pct_of_relevant_revenue_plus_discretion`, no rate, no cap, GBP). One publisher, `ico`, publishes FCA and HHS penalties.
- `ico/schema/to_fair_scenario.py:47-75` is the only reader. It reads no size fact. `per_violation_tier` (`:64-68`) uses per-violation min/max only; `annual_cap_usd` is never read. The HIPAA examples (Anthem $16m, Premera $6.85m) exceed the per-violation max, so the mode clips to it: HIPAA prices `[68928, 2067813, 2067813]` for every org (H3-15). `per_month_escalating` (`:70-73`) annualises the 7+ month band: `[600000, 900000, 1200000]`; no cardholder or transaction term exists. FCA falls into the turnover branch (`:53-62`): no cap, so `hi = 1.2 × largest example = £122.6m` (Standard Chartered £102m) for any org.
- Ticket 07's rule `hi = min(rate × turnover, cap)`, examples scaled by `hi / cap`, is defined only where a rate and a cap exist. FCA has neither in the schema; HIPAA has an annual cap but no rate; PCI has no size term at all. So three of four regimes have no rule for reading size, which is this ticket.
- Of ticket 07's four size facts, only `turnover` has a decided reader. `customers`, `data_subjects` and `headcount` are read by nothing decided or built.
- Nothing from ticket 07 has landed: `platform/party/schema.json:8` is `additionalProperties: false` with no `size`, `obligations`, `appetite` or `reporting_currency`; all three `party.yaml` files carry `overlay: {add: [], restate: []}` and nothing else; `compose/composition.py:953-954` still hard-codes `uk-gdpr / lower-tier` and never passes `--also`.
- `fair.py:60-88` prices several obligation sources additively under one shared frequency draw; currency is not read (`fair.py` has no currency branch; only `graded/cage.py:224` prints `GBP`).
- The only per-org differentiation today is editorial: `platform/feeds/threat-register/v2/register.json:7-19` keys a LEF triple by org name. No size mechanism exists anywhere.
- No `fx` feed exists. The `policy-as-versioned-feeds/feeds` repo is empty (ticket 01). Existing feeds are hand-versioned `v1`, `v2` directories; no platform workflow runs on `schedule:`. Ticket 10 owns the clock; ticket 21 moves the feeds into the `feeds` repo.
- Statute, for the owner's reading: HIPAA's annual cap is per identical provision per calendar year, so at tier-4 minimum the cap binds at 30 violations; FCA DEPP 6.5A step 3 sets seriousness at 0, 5, 10, 15 or 20% of relevant revenue, which is the revenue of the business area in breach, not global turnover; PCI non-compliance assessments are acquirer-contractual and scale with months, while cardholder-proportionate costs (reissuance, forensics) are not in the schema.

Already decided, not re-asked: the four size facts, `as_of`, self-signed, stale widens to the cap (ticket 07 §1); `obligations` as a list of regime names and a missing regime price as an instrument fault (ticket 07 §2, ADR-0020); every amount `{amount, currency}`, `reporting_currency` default USD, adopters GBP, FX as a signed `kind: feed, name: fx` from the feeds org, no rate for the date refuses (ticket 07 §3, ADR-0019); one currency, many perspectives (re-grill 33); several regimes on one breach priced additively and correlated (mo-18, `fair.py`); tuppence carries `fca`, ludlow carries `hipaa` (ticket 07 §2).

## Grilling round 1, first draft — SUPERSEDED by the revised round below

Nothing below has been put to the owner. Five questions.

❓ **Q1** - **How HIPAA reads size**: `per_violation_tier` has a per-violation band and an unread `annual_cap_usd`. Options: (a) `hi = min(min_usd × data_subjects, annual_cap_usd)`, examples scaled by `hi / annual_cap`, the ticket 07 shape with `data_subjects` in place of turnover; (b) scale by `data_subjects` with no cap, the examples as the floor; (c) leave HIPAA size-blind at the per-violation max, as today.

➡️ (a). It is ticket 07's one rule with one substitution, it reads a schema field that exists and is unread, and stale size widens to the same cap. The cap binds at 30 subjects, so ludlow at any real size prices the annual cap; that is honest to the statute, not a defect. (b) invents a number the statute does not permit. (c) keeps H9-06's flat figure for the one adopter whose regime is HIPAA.

❓ **Q2** - **How PCI reads size**: the schema has no cardholder term; its driver is months non-compliant. Options: (a) PCI stays size-blind: the annualised steady-state band is the price and `customers` enters nowhere, until a publisher ships a per-card line; (b) the converter multiplies a per-card reissuance cost by `customers` as card-holders, with the cost as a platform constant; (c) a fifth size fact, annual card transactions, selects the PCI merchant level and the level selects the band.

➡️ (a). The schema carries no per-customer term and ADR-0020 rejected "invents a number with no source". "Cost per customer" (owner, 2026-08-19) is then a publisher's line in a later schema version, consumed the ordinary way, not a converter constant. (c) adds a fact only one regime reads.

❓ **Q3** - **How FCA reads size**: `pct_of_relevant_revenue_plus_discretion` has no rate and no cap; relevant revenue is the business area's, not global turnover. Options: (a) tuppence declares a fifth size fact `relevant_revenue`, the schema gains the step-3 seriousness bands as `rate` (0.05..0.20), `hi = rate_max × relevant_revenue`, uncapped, mode from the examples scaled to it; (b) as (a) but relevant revenue defaults to `turnover` unless the adopter declares `relevant_revenue`; (c) FCA stays example-based, size-blind, as today.

➡️ (b). A single-line fintech's relevant revenue is its turnover, so the thin slice needs no new fact; the optional override exists for a multi-line firm; a mandatory separate fact invites understatement. Either (a) or (b) needs the publisher to ship a rate, which is a schema major (ticket 04 §3). (c) leaves tuppence at Standard Chartered's £122m.

❓ **Q4** - **Where the binding from formula to size fact lives**: Options: (a) in the converter, dispatched on the formula `type` as `lm_triple` does today (turnover-type reads `turnover`, violation-type reads `data_subjects`); (b) in the schema, a `scales_by: <size fact>` on each formula, with the party schema's `size` keys as the vocabulary, so a regime names its fact without a platform PR.

➡️ (a). A new formula type needs converter code regardless, so `scales_by` is a second place to say the same thing, and it can disagree with the type. The party artefact keeps ticket 07's four facts and `obligations` stays a list of names; no per-obligation counts. Reopen when a publisher ships a formula that two size facts could reasonably serve.

❓ **Q5** - **The `fx` feed's source and cadence**: ticket 07 fixed shape and signer; ticket 10 owns the clock in general; this ticket's text asks for fx specifically. Options: (a) HMRC monthly exchange rates (GBP base, official, one signed tag a month, the rate a UK finance team reports under); (b) Bank of England daily spot (GBP base, one tag per business day, ~250 Renovate PRs a year per adopter); (c) ECB daily reference rates (EUR base; GBP and USD are cross rates).

➡️ (a). It is the reporting rate, GBP-based like the adopters, official, keyless, and twelve tags a year is the lowest Renovate churn. "No rate for the date" then means no rate for the month, stated in the payload. Daily can be a second feed later if a sum ever needs it.

Later, blocked on the above: HIPAA multi-provision counts (one cap per provision) once Q1 fixes the base; whether `ico` keeps publishing `fca`, `hipaa` and `pci-dss` or each regulator becomes its own publisher party (ticket 21's move, Q3's schema major forces the question); PCI per-card and merchant level when a publisher ships them (Q2); the twin's own size read (ticket 11).

## Facts found, second pass (2026-08-28, AFK, after two adversarial reviewers)

Corrections and additions to the first pass. Paths relative to `.estate-clone/`.

- The live instrument is `ico/schema/v1/penalty-schema.json`, not v2: all three adopters pin `{ party: ico, kind: pricing, version: "v1" }` (`driftwood/party.yaml:26`, `tuppence/party.yaml:15`, `ludlow/party.yaml:15`) and `compose/composition.py:964-973` (`_ico_scenario`) builds from `ico_root/schema/<pinned>/penalty-schema.json`. `diff v1 v2` is one line: v2 adds Doorstep Dispensaree £275,000 to `uk-gdpr.lower-tier` examples (`v2/penalty-schema.json:4`). Every regime formula is identical across v1 and v2, so the first-pass numbers hold for both.
- `driftwood/party.yaml:13` says "ico ships no git tags". Stale: `git -C ico tag` returns `v1.0.0`.
- `ico/schema/to_fair_scenario.py:47-75` is the only parser of the penalty schema. It is ico's code, in ico's repo, and composition runs it as a subprocess (`composition.py:964-973`). A second file with the same name, `platform/feeds/to_fair_scenario.py:2-17`, converts the threat, CVE and EOL feeds and never reads the penalty schema. There is no single converter to change for all feeds.
- `to_fair_scenario.py:128` and `:141` assert `lo <= mode <= hi` for every (schema version, regime, violation type). Any size rule must keep that invariant.
- HIPAA arithmetic: `min_usd 68928 × 30 = 2067840 > annual_cap_usd 2067813`, so `hi = min(min_usd × data_subjects, annual_cap)` equals the cap for any adopter with 30 or more data subjects, the scale factor `hi / annual_cap` is 1, Anthem $16m and Premera $6.85m still exceed `hi` and clip. The first draft's Q1(a) yields exactly today's `[68928, 2067813, 2067813]` for ludlow. The first draft attributed the HIPAA flat triple to H9-06; H9-06 is the uk-gdpr £16,901,471.55 (`G-drift-findings.md:950-953`). The HIPAA flat triple is H3-15 and H9-07.
- FCA arithmetic: the first draft's Q3 "hi = rate_max × relevant_revenue, examples scaled to it" has no scale factor (ticket 07 scales by `hi / cap`; FCA has no cap) and inverts the triple for any firm smaller than a bank: `lo = min(examples) = £48.65m (TSB)`, and tuppence at £50m turnover gets `hi = 0.2 × 50m = £10m < lo`, which `:128` asserts against. The examples cannot stay as the floor under a size rule; H3-02's recommendation is that published fines "sanity-check the fit rather than be the fit".
- ADR-0020's stale rule ("stale size facts widen the loss triple back to the statutory cap", `docs/adr/0020:14`) has no target for FCA: the schema carries no cap and DEPP 6.5A sets none. A widening target has to come from somewhere.
- The owner's 2026-08-19 words, verbatim (`G-drift-findings.md` H9-06 evidence, H3-02 claim): "the financial risk of 21707 doesn't seem sane to the same on every org, its proporiate to the org right? maybe think about describing it as a cost per customer?" and "maybe that is a GDPR style percentage of global revenue style fine once the org is over a certain threshold?". Read plainly, "cost per customer" is a presentation of the org's £ (price divided by customers), not a PCI card-holder pricing term. The first draft's Q2 turned it into the latter and then rejected it. Nothing in ticket 07, ticket 08, ADR-0020 or CONTEXT.md decides whether a price is also stated per customer. `customers` has no reader anywhere.
- ADR-0020 `## Alternatives` (`docs/adr/0020:20`) rejected "price the unknown regime at the largest known cap" as "invents a number with no source". A converter constant for per-card cost is analogous, not the same decision.
- ADR-0019 §3 (`docs/adr/0019:18-21`): "Under `feed`, `name` is free, so a new publisher ships a new feed with no platform change." A size rule that lives in platform code per formula type is in tension with that sentence; a rule that lives in the publisher's converter or in the formula itself is not.
- Ticket 04 §3 makes a `payload_schema` change always major and re-grill 13 puts the declared bump in a versioned file in the publisher's repo. Whether adding `rate` to the fca formula is a payload-schema change depends on ticket 21's not-yet-written payload schema. The first draft's "schema major" was an inference.
- `.estate-clone/` holds `driftwood ico ludlow nist platform tuppence`. No local clone of `feeds` or `insurer` exists; "the feeds repo is empty" rests on ticket 01's text (`01-create-feeds-and-insurer-orgs.md:20`), not a clone. The `schedule:` grep over `*/.github/workflows` hits only the comment "Nothing timed, ever -- no `schedule:` anywhere" in the three `propose-tier.yml` files.
- Ticket 21 (`21-build-the-feed-contract.md:9`) moves threat, CVE, EOL and market intel out of platform into `feeds`. ico stays its own repo, migrates its schema into the envelope and gains a publisher `party.yaml`. The first draft's "ticket 21's move forces" the split-regulator question was wrong; only Q3's schema change forces it.
- HMRC monthly exchange rates are published for customs, VAT and import valuation, not statutory reporting (FRS 102 s30 / IAS 21 use spot or average rates from a market source). The first draft's "the rate a UK finance team reports under" is dropped; cadence, official source, GBP base and no API key remain true.
- Under ADR-0019 §5 and ticket 04 §4 the feeds org names and describes its own feed in `publishes[]`; ticket 10 owns cadence and rejection half-life generally. Q5 is therefore the feeds org's first fx publish, not a platform rule.

Already decided, not re-asked (adds to the first pass): instrument faults still refuse under no-gate (re-grill 11); the publisher declares the bump in a versioned file reviewed in the PR, a `payload_schema` change is always major (ticket 04 §3, re-grill 13); `appetite` sits next to `size` on `party.yaml`, signed by the adopter alone (ticket 08 §5); every `prices[]` entry carries `perspective` and `currency` and no sum crosses perspectives (ticket 08 §6); driftwood and tuppence carry `pci-dss`, ludlow carries `hipaa` and `uk-gdpr`, tuppence carries `fca` (ticket 07 §2); a missing instrument refuses, a missing behaviour is priced, stale size widens to the cap (ADR-0020); the feeds org publishes `fx` under its own `publishes[]` (ticket 04 §4, ADR-0019 §5).

## Grilling round 1 — revised 2026-08-28, HELD

Drafted AFK on the owner's instruction. Adversarially checked by two reviewers. Nothing below has been put to the owner. Five questions. Q3 and Q4 are coupled: Q4 is the one place that decides the size vocabulary, Q3 only asks whether FCA needs an entry in it.

❓ **Q1** - **What a HIPAA violation counts**: `per_violation_tier` carries a per-violation band and an `annual_cap_usd` the converter never reads (`to_fair_scenario.py:64-68`). The cap is per identical provision per calendar year and binds at 30 tier-4 violations, so any rule of the form `min(min_usd × data_subjects, annual_cap)` prices the cap for every real adopter: the same `[68928, 2067813, 2067813]` ludlow gets today. Only what `data_subjects` multiplies against moves the figure. Options: (a) per affected individual, one provision: `hi = min(min_usd × data_subjects, annual_cap_usd)`, examples scaled by `hi / annual_cap`; ticket 07's shape, reads `data_subjects`, numerically a no-op for ludlow; (b) per affected individual, per provision: the publisher ships a `provisions` count on the violation type (Anthem's $16m is about eight provision-years), `hi = min(min_usd × data_subjects, annual_cap_usd × provisions)`, `provisions` defaults to 1 until shipped so it degrades to (a); the examples become a fit check, not the fit; (c) HIPAA stays size-blind at the per-violation max, as today.

➡️ (b). It is (a) plus one publisher-shipped integer, so the base lands now with no number invented, and the figure moves the day the publisher ships a count with a source. (a) alone changes the mechanism and not one evidence.json. (c) keeps H3-15/H9-07's flat triple for the one HIPAA adopter. Stale `data_subjects` widens to `annual_cap × provisions`, which is a cap, so ADR-0020 applies unchanged.

❓ **Q2** - **Cost per customer**: the owner's words on 2026-08-19 were "its proporiate to the org right? maybe think about describing it as a cost per customer?". That is a denominator on a price, not a PCI size term; `customers` is a decided size fact with no reader. Separately, PCI's schema driver is months non-compliant with no card-holder or transaction term, so PCI stays size-blind under ADR-0020's reasoning (a per-card constant in the converter invents a number with no source) and driftwood and tuppence keep an identical PCI band, the flat shape H9-06 criticised, until a publisher ships a per-card line. Options: (a) every `prices[]` entry gains `per_customer: {amount, currency}` = amount divided by the adopter's `customers`, under the entry's perspective and currency, never summed, absent when `customers` is undeclared; (b) per-customer is a display the reasoning skill renders from `prices[]` and `size`, nothing in evidence changes; (c) not built; the owner meant something else and says what.

➡️ (a). It reads a decided fact, adds no formula, and puts the proportionate figure where the reviewed PR can see it. (b) hides it from the PR that is the unit of adoption. The PCI size term is not asked: no source exists for one, and it goes to Later with the question of who the PCI publisher party is.

❓ **Q3** - **How FCA reads size, and what stale widens to**: `pct_of_relevant_revenue_plus_discretion` has no rate and no cap; relevant revenue under DEPP 6.5A is the business area's, not global turnover; today tuppence prices at 1.2 × Standard Chartered, about £122.6m. The whole triple has to come from size, because keeping the examples as the floor inverts the triple for any firm smaller than a bank (£10m hi under £48.65m lo at £50m turnover; `:128` asserts). Options for the triple: (a) the publisher ships the step-3 seriousness bands as `rate_lo 0.05`, `rate_mode 0.10`, `rate_hi 0.20`; `lo, mode, hi = rate × relevant_revenue`; the published fines become a fit check the converter prints and never uses; relevant revenue defaults to `turnover` unless the adopter declares an optional `relevant_revenue`; (b) as (a) but `relevant_revenue` is mandatory for an adopter declaring `fca`; (c) FCA stays example-based and size-blind, as today. Options for stale size, which has no cap to widen to: (i) 1.2 × largest published example, as the converter does today; (ii) a publisher-shipped `widen_to` on the formula, sourced to the largest final notice under the framework; (iii) `rate_hi × the largest turnover declared in the estate`.

➡️ (a) with (ii). A single-line fintech's relevant revenue is its turnover, so the thin slice needs no new fact; the optional override serves a multi-line firm; a mandatory separate fact invites understatement. (ii) keeps the widening number sourced; (i) is the same number under a publisher's name only by accident; (iii) leaks one adopter's size into another's price. Either (a) or (b) needs the publisher to ship rates: the publisher declares the bump; major if ticket 21's payload schema is closed. If (a) is chosen, `relevant_revenue` amends ticket 07 §1 and the CONTEXT.md `Size facts` entry, and Q4 records it.

❓ **Q4** - **Who owns the size read, and the size vocabulary**: the converter that reads the schema is ico's code, run by platform composition as a subprocess (`composition.py:964-973`), and it reads no size fact. Something has to receive the adopter's signed `size` and apply the regime's rule. Options: (a) platform composition holds one converter over all formula types, dispatched on `type` as `lm_triple` does; ico's converter retires; a new formula type is a platform PR, against ADR-0019 §3's "no platform change"; (b) each formula self-describes with `scales_by: <size fact>` and rate fields, and one platform reader applies it generically; a new regime is a schema change only; (c) each publisher ships its converter beside its feed, as ico does today, and composition calls it with the adopter's `size` as an argument; a new regime is the publisher's code, signed under the publisher's tag. Vocabulary in every option: the four ticket 07 facts plus `relevant_revenue` as optional if Q3 picks (a); `obligations` stays a list of names; no per-obligation counts on the party artefact.

➡️ (c). It is what exists, it keeps ADR-0019 §3 true, and the publisher's converter is signed by the same tag as the schema it reads, so the trust boundary does not move. The `size` argument is the whole diff. (b) is a second vocabulary that can disagree with `type` and cannot express Q1(b)'s cap-times-provisions. (a) makes every regulator's arithmetic a platform PR. Reopen when a publisher ships a formula two size facts could reasonably serve.

❓ **Q5** - **The feeds org's first `fx` publish: source and tag cadence**: ticket 07 fixed the shape (`kind: feed`, `name: fx`, feeds org, dated, no rate for the date refuses); ticket 10 owns the clock generally; this ticket's text asks for fx specifically. Options: (a) HMRC monthly exchange rates: GBP base, official, no API key, one signed tag a month; (b) Bank of England daily spot: GBP base, one tag per business day, about 250 Renovate PRs a year per adopter; (c) ECB daily reference rates: EUR base, GBP and USD as cross rates.

➡️ (a). GBP base like the adopters, official, keyless, and twelve tags a year is the lowest Renovate churn. "No rate for the date" then means no rate for the month, stated in the payload. It is not the statutory reporting rate; the feeds org says so in the payload note and a daily feed can be a second `publishes[]` entry later if a sum ever needs it.

Later rounds, blocked on the above: who the PCI publisher party is (PCI is acquirer-contractual and ships under ico's schema today; Q2 leaves it size-blind until a publisher ships a per-card or merchant-level line); whether ico keeps publishing `fca` and `hipaa` or each regulator becomes its own publisher party, forced by Q3's schema change once ticket 21 gives ico a publisher `party.yaml`; the fit-check output of Q1(b) and Q3(a) as a line in evidence, decidable once Q4 fixes who prints it; the twin's own read of the adopter's `size` (ticket 11); general schedule cadence and rejection half-life beyond fx (ticket 10).


## Cross-ticket notes (2026-08-28)

- C3 (10, 22): Q5(a)'s monthly fx tag is one of three per-feed clock answers (10 Q4(a) PR per fetch, 22 Q5(b) PR on threshold) after 10 Q2(a) rejected a period per step. Let 10 carry the rule; this ticket keeps only the source.
- C11 (14, 15, 19): Q2(a)'s `per_customer` on every `prices[]` entry is one of five independent reshapes of `prices[]` (14 premium, 15 per-hole, 19 reliability and switching). Ticket 25 builds the shape once.
- C20 (19): Q4(c) ships converter code with each publisher; 19 Q5(a) vendors only feed payloads for offline re-derivation, so 19's guarantee needs the converter vendored too. Q3's "no cap to widen to" for FCA also lands on 19 Q1(b), which widens "toward the cap".

## Answer

Resolved 2026-08-28.

Owner record. On 2026-08-28 the owner read the held round and wrote: "ive already read the recommendations and I can't find fault with a single one. Well done. Get everything ready for me to then to-spec". A bare agree does not ratify (map process rule). Items 1 to 5 are PROVISIONAL, each with its recommendation's reason as the rationale. The five-per-day rule was overridden by that instruction. One conflict touching this ticket, D2, was then put to the owner with a three-lens panel verdict. The owner wrote: "I agree with you're more advanced reasoning". D2 is DECIDED and amends item 5.

1. A HIPAA violation counts per affected individual, per provision. `hi = min(min_usd × data_subjects, annual_cap_usd × provisions)`. The publisher ships `provisions` on the violation type. It defaults to 1 until shipped. The published fines become a fit check the converter prints and never uses. Rationale: ticket 07's shape plus one publisher-shipped integer; no number is invented; the figure moves when a source ships a count. Stale `data_subjects` widens to `annual_cap × provisions`, a cap, so ADR-0020 applies unchanged. Provisional.

2. Every `prices[]` entry gains `per_customer: {amount, currency}`: the amount divided by the adopter's `customers`, under the entry's perspective and currency, never summed, absent when `customers` is undeclared. Rationale: it reads a decided fact, adds no formula, and the reviewed PR can see it. PCI stays size-blind; no per-card source exists and a converter constant invents a number (ADR-0020). Amendment C11: the field lands in ticket 25's one `prices[]` pass with 14, 15 and 19. Provisional.

3. FCA reads size from relevant revenue. The publisher ships `rate_lo 0.05`, `rate_mode 0.10`, `rate_hi 0.20`. `lo, mode, hi = rate × relevant_revenue`. `relevant_revenue` is an optional fifth size fact and defaults to `turnover`. The published fines are a fit check only. Stale size widens to a publisher-shipped `widen_to` on the formula, sourced to the largest final notice under the framework. Rationale: a single-line fintech's relevant revenue is its turnover; a mandatory fact invites understatement; `widen_to` keeps the widening number sourced. Amends ticket 07 §1 and the CONTEXT.md `Size facts` entry. Amendment C20: `widen_to` is the widening target ticket 19 Q1(b) inherits. Provisional.

4. Each publisher ships its converter beside its feed, as ico does today. Composition calls it with the adopter's signed `size`. A new regime is the publisher's code under the publisher's tag. Platform holds no per-regime arithmetic. Vocabulary: the four ticket 07 facts plus optional `relevant_revenue`; `obligations` stays a list of names; no per-obligation counts. Rationale: it is what exists and keeps ADR-0019 §3 true. Amendment C20: vendoring for departure (19 Q5a) includes each publisher's converter code. Provisional.

5. The feeds org's first `fx` publish uses HMRC monthly exchange rates: GBP base, official, no API key. The payload note says it is not the statutory reporting rate. "No rate for the date" means no rate for the month. Rationale: lowest Renovate churn, GBP base like the adopters. Amendment D2 (decided): the monthly source is an instance of ticket 10's one rule. A scheduled fetch opens a PR when the computed bump is not `none`; "changed" is a versioned rule file beside the feed; sub-threshold observations append to the observation branch. This ticket keeps only the source. Provisional; the D2 amendment is decided.

Consequences:
- Ticket 07 §1 and CONTEXT.md `Size facts` gain optional `relevant_revenue`. ADR-0020's stale rule gains `widen_to` as the cap for a capless formula. ADR-0019 §3 is confirmed.
- Ticket 21 builds: ico's converter takes `size`; `provisions`, `rate_*`, `widen_to` and the fit-check line ship in ico's schema under its envelope migration with a publisher-declared bump; composition passes `size` and drops the hard-coded `uk-gdpr / lower-tier`; the feeds org's first `fx` tag with its rule file.
- Ticket 25 builds `per_customer`. Ticket 10 carries the fetch rule. Ticket 19 inherits `widen_to` and vendors converters. Ticket 11 owns the twin's read of `size`.
- Revisit triggers: a publisher ships a formula two size facts could serve (item 4); a PCI per-card or merchant-level line ships (PCI size); ticket 21 gives ico a publisher `party.yaml` (one regulator party each, or ico for all); a sum needs daily fx (second `publishes[]` entry).

Graduated: none.

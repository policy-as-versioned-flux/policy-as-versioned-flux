# 19 — Misuse and portability

Type: grilling (HITL)
Status: resolved
Blocked by: 04

## Question

Re-open the misuse catalogue against the eco-system (a publisher gaming its feed price; an adopter buying intelligence on a rival; a regulator's fine data mispriced downstream with no recourse; the twin's valuation used against an org in negotiation). Make portability a priced cage: publish each adopter's switching cost in £, require feeds to be re-derivable from pins a departing adopter keeps, put exit cost on the balance sheet.

## Notes

Re-grills 36, 38. Blocked by the feed contract.

## Facts found (2026-08-28, AFK, before any question to the owner)

Source of truth is `.estate-clone/` for the estate and `twin/` for the twin. Paths below are relative to those.

- Two misuse catalogues exist, both twin-scoped. `misuse-catalogue.yaml` has 10 entries: 7 on misuse of the twin's governance/pricing/scoring machinery against some other subject, 3 (v2) on misuse of the twin itself by its own operator (`twin/misuse-catalogue.yaml:11-16`). `behavioural-misuse-catalogue.yaml` has 8 on misuse of behavioural sensing. The loader refuses an entry without a `mechanism` (`twin/misuse.py:44-72`).
- No catalogue row names a marketplace party: no publisher, regulator, feed price, subscriber or rival. Nearest is `weaponising-another-orgs-twin`, mechanism `model.check_direction` (`twin/behavioural-misuse-catalogue.yaml:113-126`).
- Neither catalogue is graded today. `talk/verify-all.sh:42` globs `verify*.sh` under `.estate-clone/` and `verify/` only; `twin verify` runs only in `.github/workflows/twin.yml`. No harness check calls `load_catalogue`; the three misuse checks exercise `compute_attractiveness`, `log_removal` and `verify_removals` only (`twin/invariants/harness.py:458-486`). A third catalogue file dropped beside the two would be graded by nothing. Wiring is new work.
- The twin's constraint set records `power-asymmetry` as a scope exclusion (`twin/constraints.yaml:90`), `no-power-layer` as a stated limit (`:137`) and `exit-cost-asymmetry` as `status: unsolved` (`:146`). That exit cost is about people who cannot afford to leave an employer, not adopters leaving a publisher. NORTH-STAR §6 reverses the no-power-layer limit for portability only.
- The catalogue's own pattern for "computed, never stated": `compute_attractiveness()` re-runs `options.prefilter()` with one constraint stripped and records the cost; `log_removal()` has no parameter through which a caller could type the number (`twin/misuse.py:81-146`).
- Affected-parties register: per scenario, required, published beside the constraint pin, named never priced (`twin/affected_parties.py:47-79`, `twin/schema.py:729`). Disparate-impact audit is a sealed channel to one registered role (`twin/disparate_impact.py:1-24`, `twin/roles.yaml:12-40`). Both twin-only.
- DPIA machinery is sensor admission only: `dpia_triage()` fires on the ICO monitoring channels and on `profiling` and `financial_loss_risk` flags (`twin/ethics_gate.py:232-242`); `admit()` blocks a sensor with a mandatory incomplete DPIA. Nothing applies a DPIA to a feed; a feed that profiles data subjects would trip the same triggers if it were ever routed there.
- Pins split two ways. `implementations` (platform) and `controls` (nist) are `{tag, commit}` pins maintained by the only two Renovate customManagers (`driftwood/renovate.json:11-38`). `pricing` (ico) and `threat` (platform) have no Flux pin and no Renovate manager: the composer reads `schema/<v>` and `feeds/threat-register/<v>` out of the party clone by directory (`platform/compose/composition.py:240-247`, `:964-970`). `driftwood/party.yaml:12-16` says so in its own comment; its "ico ships no git tags" is now stale (ico carries `v1.0.0`, but the composer ignores it).
- The recorded ico sha in evidence, `5d1e76fa` (`driftwood/composed/HEADER.yaml:15-18`), is neither the ico clone HEAD (`8902b661`) nor the commit `v1.0.0` points at (`0918623a`); it is whatever HEAD was when composition ran. Re-derivation from the pin is not possible today for exactly the two edges that carry the £.
- The adopter's composed tree vendors the rendered parent policies (`composed/policies/`) but no feed payload (`ls driftwood/composed`). Tags are never deleted (ADR-0019); nothing stops a repo being deleted or made private.
- Every `prices[]` entry carries `source, kind, old/new_version, old/new_price, old_tier, proposed_tier, changed, proposed_as` (`composition.py:1009-1017`). No converter version, payload digest, publisher score, perspective or currency (ticket 08 decided the last two; ticket 25 builds them). `price_parent` handles `pricing` and `threat` edges only; `implementations` and `controls` edges are not priced (`:988-1021`). driftwood's `prices[]` has two entries; H4-04's "four of five feeds" counts cve/eol/market-intel that no adopter prices yet.
- `holes[]` in evidence is `{control_id, status: recorded}`, 285 rows, no £; `HEADER.yaml` `holes:` is a bare id list (~320 lines of a 600-line file); `cages` is `[]`. Pricing holes is re-grill 20's decision and ticket 15's open work, not code.
- The ico converter ships in the regulator's own repo (`ico/schema/to_fair_scenario.py`, committed there) and is invoked as a subprocess by the platform's composer (`composition.py:960-970`); `platform/fair/fair.py` consumes its output. The triple is `lo = min(examples)`, `mode = median`, `hi = max(cap, 1.2 × largest example, mode)` (`to_fair_scenario.py:47-60`): one inflated example lifts the ceiling above the statutory cap as well as the mode. The identical £16,901,471.55 across all three adopters is the ticket 07 converter defect (ignores `rate` and turnover), decided and awaiting build; a regulator publishing wrong examples is a different, still-open event.
- The platform today publishes threat/cve/eol/market intel and authors the £ engine and the policies being priced; ico is the one independent publisher (H4-04). Ticket 21 moves the intel feeds to the `feeds` org; ticket 18 owns the publisher's own release cage (degraded tier, corpus published so an adopter re-derives the number).
- Every org repo is public: driftwood, platform, ico, nist (clones); feeds and insurer created public and empty (ticket 01, 2026-08-28; not in `.estate-clone/`). `HEADER.yaml` lists the adopter's holes by control id in that public repo. ADR-0021 decides (ticket 25 builds; nothing in `composition.py` knows `perspective` yet) that forward intel is published from the adopter's own repo and the PR carries the curve hash. A rival needs `git clone`, not a purchase.
- The twin's price is the perspective's own declared valuation times propagated influence (`twin/pricing.py:9-25`); it never values an org from outside its perspective. "The twin's valuation used against an org" can only be the org's own published number re-read under someone else's perspective.
- ADR-0012's verifier re-renders the composed artefact from pinned parent SHAs byte-for-byte and rejected a content-hash digest per parent (`docs/adr/0012:23`). It needs the parent tree reachable.
- The forecast book scores only the twin's own pinned forecasts (`twin/forecast_book.py:1-12`); `reliability_diagram()` exists (`twin/scoring.py:129`). No publisher's prices are scored against realised outcomes anywhere. The map lists the forecast book as marketplace credibility instrument under Not yet specified, waiting on the feed contract, with no owning ticket (`map.md:38`).
- `fair.py` prices multiple obligation sources additively on one shared frequency draw (`platform/fair/fair.py:60-70`); a wrong regime triple moves the whole tail.
- Adjacent owners: ticket 10 lists misuse review as a candidate skill; 14 the insurer quote and retained-versus-transferred on the balance sheet; 21 `inherits[]` and publisher `party.yaml`; 11 the standing scenario library per adopter twin; 15 prices every counted thing; 18 the publisher release. The map parks the eco-system re-cut of the affected-parties register and DPIA on this ticket (`map.md:40`).

Already decided, not re-asked: the four eco-system rows exist (re-grill 36); portability is a priced cage, switching cost in £, feeds re-derivable from kept pins, exit cost on the balance sheet (re-grill 38, NORTH-STAR §6); adopters re-derive a publisher's number from its published tool and corpus (re-grill 5, ticket 18); a bad publisher release ships at a degraded tier, instrument faults still refuse (re-grill 11, ticket 18); one currency many perspectives, none privileged, no sum crosses (re-grill 33, ticket 08, ADR-0021); a mechanism-naming catalogue is disclosure not a ledger (re-grill 10); a missing behaviour is priced, a missing instrument refuses (re-grills 20, 27, ADR-0020); an obligation no subscribed feed carries refuses (ticket 07 item 2); tags never deleted, revocation is a new version plus `revoked[]`, a pin to a revoked version is a priced hole, discovery is `publishes[]` (ADR-0019, ticket 04); forward intel from the adopter's repo, twin-signed, no recommended action, adopter's selection policy picks the tier (ticket 08, ADR-0021); the forecast book is the marketplace credibility instrument with reliability diagrams per publisher (reversal 22); the insurer quote and TVaR transfer (reversal 20, ticket 14); the twin acts inside a priced cage, Article 22 floor (re-grill 37); declined proposals decay and re-raise (re-grill 22); covert sensing excluded, role-not-person signatures, the people-level exit cost stays an unsolved harm (twin ticket 15, `constraints.yaml`); the hub is the eco-system's own repository (NORTH-STAR §7); done is a check the gate discovers (map process rules).

Defaults set without a decision, open to challenge (ticket 08 precedent): the eco-system rows live in a third file, `twin/ecosystem-misuse-catalogue.yaml`, same `twin.misuse-catalogue/v1` schema and loader, every entry naming an estate mechanism by path or a cage price; a new harness check loads all three catalogues and refuses any row without a mechanism, and a `verify-misuse.sh` under `verify/` runs it so `verify-all.sh` sees it. Prose-only was not offered: the map makes a gate-discovered check the definition of done. Per-party rows in `party.yaml` were not offered: a regulator mispricing an adopter is in nobody's file.

## Grilling round 1 — drafted 2026-08-28, HELD

Drafted AFK on the owner's instruction. Adversarially checked by two reviewers. Nothing below has been put to the owner.

❓ **Q1** - **What a bad publisher does to the subscriber's price**: two of the four rows (publisher games its own price; regulator's data mispriced downstream) are one event seen from two ends. Ticket 18 already cages the publisher's own release (degraded tier); reversal 22 already gives every publisher a reliability score. What is open is whether either touches the subscriber's `prices[]` entry. Who runs the scorer and from which repo is not this ticket's: it is the forecast-book fog item (`map.md:38`) and must be a party that publishes no scored feed, never the feeds org (H4-04 relocated). Options: (a) disclosure only: each `prices[]` entry gains publisher version, converter version, payload blob id and the publisher's current reliability score; the adopter's selection policy may read the score, the engine never weights by it; (b) priced: the score is a feed (`kind: feed`, `name: publisher-reliability`, from the scorer party) and a score below the adopter's declared threshold widens the adopter's triple toward the cap, the ticket 07 stale-size pattern, until the adopter re-pins or the score recovers; a release at a degraded tier (ticket 18) is priced the same way; (c) the publisher's degraded tier alone widens the subscriber's triple; the score is disclosure.

➡️ (b). It is the only option that is price-and-cage rather than a register: the widening mechanism exists (ticket 07), the priced-hole precedent exists (ADR-0019), the score has a home (reversal 22). It gives the publisher a £ reason not to game and the adopter a £ reason to switch, which Q4 then prices. (a) is a register with no consequence (re-grill 35). (c) ignores a publisher that ships clean releases of bad numbers. Draft mechanism string for the row `publisher-games-own-feed-price`: `composition.compute_prices widens to cap on publisher-reliability below appetite; ticket 18 degraded tier`. Dependency: a scorer party, to be named in the forecast-book ticket.

❓ **Q2** - **Recourse when the pin cannot be dropped**: a regulator's penalty schema is the only price for a declared obligation; un-pinning it makes the regime an instrument fault and the composition refuses (ticket 07 item 2, ADR-0020). "A £ reason to switch" is empty for the regulator row. Options: (a) disclosure plus a priced hole: the regulator's score is disclosed as in Q1, and a published correction (a new schema version) not yet pinned is a priced hole growing over time (re-grill 27 pattern); the adopter's own number stays the regulator's until the re-pin; (b) the adopter may pin a second pricing publisher for the same regime (the insurer's or the feeds org's actuarial view) and compose the regime's triple from an ensemble; the regulator's data is one member; (c) the mispricing is a claim routed to the insurer quote (ticket 14); the eco-system prices nothing.

➡️ (a) now, (b) recorded as the later shape. (a) uses only mechanisms already decided (score, priced hole over time) and keeps the regulator's number authoritative for its own regime, which is the honest position: the ICO decides what the ICO fines. (b) is the real recourse but needs a second pricing publisher that does not exist and an ensemble rule ticket 21 has not written. (c) makes recourse depend on a party with no artefact yet. Draft mechanism string for `regulator-data-mispriced-downstream`: `hi = max(cap, 1.2 × largest example) and ticket 07 rate × turnover; correction-not-pinned is a priced hole; score disclosed in prices[]`.

❓ **Q3** - **Whether an adopter's priced exposure is public by design**: every org repo is public; `HEADER.yaml` lists the adopter's holes; ADR-0021 puts forward intel (shock, curve) in the adopter's own repo. "An adopter buying intelligence on a rival" and "the twin's valuation used in negotiation" both reduce to: the org's own signed numbers are one clone away, and the ADR-0012 re-render check plus Rekor need them reachable. Options: (a) public by design, priced: "a rival reads my holes" is a standing forward-intel scenario under the adopter's own perspective (ticket 11's library), priced by `fair.py` as a `source: twin` entry in `prices[]`, so legibility is a priced behaviour, never a private carve-out; the catalogue mechanism is the perspective rule (`twin/pricing.py:9-25`, a rival's reading is another perspective's price) plus role-not-person signatures; (b) sealed payloads: curve and holes as digests in the public repo, payload in a private repo readable by adopter and platform, verification by digest; (c) private adopter repos outright.

➡️ (a). No new repo, no new mechanism, and it keeps NORTH-STAR §4 step 6 (every step verifiable in Rekor and sidecars) and the ADR-0012 re-render intact. It also says the honest thing: this eco-system makes exposure legible on purpose and the price of that goes on the adopter's own balance sheet. (b) breaks public verifiability for anyone but the two parties and makes the platform a privileged reader (re-grill 33). (c) breaks public verifiability and the demonstration's provenance step (Flux and Renovate can read private repos; the third-party check cannot). (b) is the recorded upgrade path when a real adopter appears. Draft mechanism strings: `adopter-buys-intel-on-rival`: `no purchase exists; rival's reading is its own perspective's price (pricing.py); adopter prices the read as a twin scenario`. `twin-valuation-used-in-negotiation`: `the twin values only under its own perspective; the number is the org's own, signed by role; no sum crosses perspectives (ADR-0021)`. Depends on ticket 11 for the scenario library.

❓ **Q4** - **What the switching cost is made of, for which parents, and how it sits on the balance sheet**: nothing computes one today. The catalogue's pattern for a figure that must never be typed is `compute_attractiveness()`: re-run with one thing removed and record the difference. But `price_parent` prices only `pricing` and `threat` edges, holes carry no £ until ticket 15, and dropping a sole-source obligation feed or the `controls` parent is an instrument fault that refuses (ADR-0020). Options on the figure: (a) computed by the adopter's composition: for each publisher in `inherits[]` that has a substitute or is not a declared obligation's sole source, re-compose with that publisher's edges dropped and record the £ of holes and price moves that open, as `switching[]` in evidence, never declared; for a sole-source obligation feed or the `controls` parent, the switching cost is "the cost of the next publisher of the same regime or catalogue" and, absent one, is recorded as the refusal itself, unpriced and named; (b) declared by the publisher in `publishes[]` as a price list; (c) forecast by the twin as a standing scenario "publisher X withdraws" (ticket 11), emitted through forward intel. Options on the shape: (i) annualised over the pin's expected life into `prices[]` as `source: <publisher>`, `kind: switching`, with perspective and currency, comparable under principle 3; (ii) a one-off `switching[]` line, never summed with annualised prices.

➡️ (a) with (i). Same discipline as attractiveness: computed by the engine that will price holes (ticket 15), so the party it measures cannot state it; and annualised so it is a Price by CONTEXT.md's definition and sits beside the rest of the balance sheet rather than in a second ledger. Until ticket 15 lands, `switching[]` is built from the two priced edges only and is near-empty; say so in the evidence rather than fake a number. (b) lets the publisher state the number that measures its own lock-in. (c) is the later forward view. Ticket 14 owns retained-versus-transferred presentation. Blocked by: 15.

❓ **Q5** - **How a departing adopter re-derives its feeds from pins it keeps**: for `pricing` and `threat` there is no pin, only a directory version and a HEAD-of-clone sha; the composed tree vendors rendered policies but no feed payload; ticket 18 publishes the tool and corpus so an adopter can re-run the publisher's number, which is re-derivation of the publisher's release, not of the adopter's own priced payload. Options: (a) vendor the payload: the composed artefact carries every feed payload it priced under `composed/feeds/<party>/<version>/`, signed with the adopter's own tag; re-derivation reads the local copy and checks its git blob id against the tree entry at the pinned commit (`git hash-object` equals the pinned tree's entry), so ADR-0012's no-second-digest rule holds; (b) mirror the publisher: each adopter forks every publisher repo it pins and pins the fork; (c) pin only, price the loss: an unreachable publisher is a priced hole growing over time and Q4's switching cost rises with it.

➡️ (a), with the ticket 21 `{tag, commit}` pin for pricing and threat as its precondition, and (c) kept as what happens when the vendored copy is missing too. Smallest honest change: `composed/` already vendors rendered parents and ADR-0012 already re-renders from pinned SHAs, so extending the tree to the small JSON payloads makes re-grill 38's sentence literally true and testable offline. Ticket 18's corpus is the publisher's; this is the adopter's. (b) doubles every Renovate surface and breaks "consumed only through a pinned, signed dependency" (NORTH-STAR principle 4). Blocked by: 21.

Later rounds, blocked on the above: what the portability cage does when it bites (a switching cost above appetite tightens the tier, opens a proposal PR, or only sits on the sheet; blocked on Q4 and ticket 09's ladder); the further eco-system rows (ensemble stuffing across publishers, revoke-to-force-repin, fork-and-republish under the ESLint model; blocked on Q1 and Q2); the eco-system re-cut of the affected-parties register and DPIA (whether a feed subscription or forward-intel publication names affected parties, whether a DPIA-shaped record attaches to a feed that prices data subjects; blocked on Q3 and the catalogue default); misuse review as a Claude Code skill over the catalogue and removal log, and its cadence (ticket 10; blocked on the catalogue default); the second pricing publisher and ensemble rule (Q2 option b; blocked on ticket 21); recourse against the platform while it is both engine author and feed publisher (H4-04; blocked on ticket 21 and Q1); the verify script's shape (a `verify-portability.sh` that re-derives one adopter's prices from its vendored copies with the publisher clone absent, plus the `verify-misuse.sh` default above; blocked on Q4 and Q5).


## Cross-ticket notes (2026-08-28)

- C11 (14, 15, 24): Q1(b)'s reliability fields and Q4(i)'s `kind: switching` entry are two of five independent reshapes of `prices[]` (14 premium, 15 per-hole breakdown, 24 `per_customer`); ticket 25 builds the shape once.
- C16 (13): Q2(a) prices "a published correction not yet pinned" as a hole growing over time; 13 Q5(a) puts a dated supersede into ADR-0019 `revoked[]` and prices it by the EOL ramp. Two formulas for pinning a superseded version.
- C19 (11): Q3(a) adds "a rival reads my holes" and Q4(c) "publisher X withdraws" to the adopter twin's standing library; 11 Q4(a) fixes that library at four scenarios. Compose it in one place.
- C20 (24): Q5(a) vendors feed payloads so a departing adopter re-derives prices offline; 24 Q4(c) has each publisher ship its converter code beside the feed and composition call it. Re-derivation then needs the converter vendored too, not only the JSON. And Q1(b) "widens the triple toward the cap": 24 Q3 shows FCA has no cap and needs a publisher-shipped `widen_to`; the widening mechanism here inherits 24's answer.

## Answer

Resolved 2026-08-28. Owner record: the owner read every held round and wrote: "ive already read the recommendations and I can't find fault with a single one. Well done. Get everything ready for me to then to-spec". A bare agree does not ratify. Each item below is PROVISIONAL, its own reason as rationale, as tickets 04, 07 and 08 were. That instruction overrode the five-per-day rule. Five cross-ticket conflicts then went to the owner with a panel verdict. The owner wrote: "I agree with you're more advanced reasoning". D5 is DECIDED on that line and amends item 2.

1. A publisher's reliability score is a feed (`kind: feed`, `name: publisher-reliability`) from a scorer party that publishes no scored feed. A score below the adopter's declared threshold widens the adopter's triple, the ticket 07 stale-size pattern, until the adopter re-pins or the score recovers. A degraded-tier release (ticket 18) is priced the same way. Rationale: price and cage, not a register; every mechanism exists. C20: the widening target is the publisher-shipped `widen_to` from ticket 24 Q3, since FCA has no cap. C11: the reliability fields on `prices[]` land in ticket 25's one schema pass.

2. Recourse for a mispriced regulator feed is disclosure plus a priced hole. The score is disclosed in `prices[]`. The regulator's number stays authoritative until the adopter re-pins. D5 (decided): a pin behind a newer published version is priced by the existing EOL ramp (`to_fair_scenario.py eol_ramp`) from the newer version's publish date. The draft's "hole growing over time" is replaced by that one formula. `revoked[]` stays withdrawal only. An ensemble of pricing publishers is the later shape, blocked on ticket 21.

3. An adopter's priced exposure is public by design and priced. "A rival reads my holes" is a standing scenario under the adopter's own perspective, priced by `fair.py` as a `source: twin` entry in `prices[]`. The mechanism is the perspective rule plus role-not-person signatures. Rationale: no new repo or mechanism; Rekor and the ADR-0012 re-render stay intact. Sealed payloads are the upgrade path for a real adopter. C19: ticket 11 owns the scenario library and this row joins it.

4. The switching cost is computed by the adopter's composition, never declared. For each publisher in `inherits[]` that has a substitute, or is not the sole source of a declared obligation, re-compose with its edges dropped and record the £ of holes and price moves that open. For a sole-source feed or the `controls` parent, record the refusal, named and unpriced. The figure is annualised over the pin's expected life into `prices[]` as `kind: switching` with perspective and currency. Rationale: the party measured cannot state the number; annualised makes it a Price. Until ticket 15 lands it covers two priced edges; the evidence says so. C11: `kind: switching` lands in ticket 25. C19: "publisher X withdraws" joins ticket 11's library. C17: ticket 21 builds `inherits[]` on any party.

5. The composed artefact vendors every feed payload it priced under `composed/feeds/<party>/<version>/`, signed by the adopter's own tag. Re-derivation reads the local copy and checks its git blob id against the pinned tree entry (ADR-0012 holds). Precondition: ticket 21's `{tag, commit}` pin for pricing and threat. An unreachable publisher with no vendored copy is a priced hole. C20: vendoring includes each publisher's converter code (24 Q4c).

Consequences:
- D5 supersedes the draft Q2 formula and ticket 13 Q5's dated `revoked[]` supersede. ADR-0019 stands. ADR-0021's `prices[]` shape is amended once, in ticket 25.
- `exit-cost-asymmetry` stays unsolved for people; adopter exit cost is priced (NORTH-STAR §6).
- Ticket 25: reliability fields and `kind: switching` on `prices[]`. Ticket 21: `inherits[]` on any party, `{tag, commit}` pins for pricing and threat, vendored payloads and converters. Ticket 11: two new standing scenarios.
- New tickets carry the catalogue default (third file, harness check, `verify-misuse.sh`), the switching computation with `verify-portability.sh`, and the scorer party.
- Revisit triggers: the EOL ramp misprices a supersede (add explicit `supersedes: {version, eol_date}`); a real adopter appears (sealed payloads); a second pricing publisher exists (ensemble); ticket 09's ladder lands (what the portability cage does when it bites).

Graduated: Eco-system misuse catalogue graded by the gate; Switching cost computed in composition; The forecast book and the scorer party.

# 20 — One demo of the eco system

Type: grilling (HITL)
Status: resolved
Blocked by: 03

## Question

One demo artefact around the eco-system spine (NORTH-STAR §4 order). Retire the Marp deck and video split: the deck is rebuilt from the truth surface's captures and a human drives it; the video is a by-product. Every beat is a read of the truth surface. Monte Carlo and continuous refresh have beats. The niobium headline is a committed scenario. 'Gate' vocabulary is replaced by 'cage'. Nothing is narrated as live that the gate marks red.

## Notes

Findings H10-01, H10-02, H2-10, H4-06, H5-15. Blocked by the truth surface.

## Facts found (2026-08-28, AFK, before any question to the owner)

Hub paths are relative to the repo root; estate paths to `.estate-clone/`.

- The truth surface discards every script's output. Each verify script runs into one reused `mktemp` log, only its last line is kept for the table, and the log is removed after the loop (`talk/verify-all.sh:44,49,59`). Nothing a deck could quote survives a run except the TRUTH line (`:64`).
- The gate's current number is pass=43 fail=11 skip=0 excluded=2 total=56 (run 5, hub d4fcdb2; `talk/truth.log:3`). `truth.yml` runs on a daily cron, on dispatch, and on push to the gate's own files (`.github/workflows/truth.yml:7-12`); runs 4 and 5 were push-triggered. It runs with no cluster (`truth.yml:20-46` installs CLIs only), so every live tail is FAIL today and SKIP once ticket 03's five PRs land. It commits only `talk/truth.log` (`truth.yml:41-45`). `--live` already exists: `REQUIRE_LIVE=1`, SKIP counts as FAIL (`verify-all.sh:16,24-25,52-53`), and `talk/RUNBOOK.md:50-53` already names it as the stage pre-flight.
- `talk/deck.md` is hand-authored Marp: six beats in eleven slides (8 `[LIVE]`, one of them `[LIVE / narrated-virtual]` at `:249`, 3 `[NARRATED]`), plus the hourglass and the honesty slide. Its beats were written 2026-07-31 (`26770f8`); the only later content change is `e732650` (2026-08-20, four lines swapping the exemption ledger for cage wording), then a path move (`c9d0f20`). No slide covers composition, computed semver or the twin (H10-02). After that hand edit it still says "Deny is the *bottom* rung" (`:149`), titles a slide "The whole model — one hourglass" (`:297`; reversal 1 removed the neck) with a mermaid fence Marp cannot render natively (`:299-307`), and says "gate" for the PR check, the release check and the honesty check (`:179-188,316-319`).
- No `marp` or `marp-cli` is installed and there is no `package.json` at the repo root; nothing in the hub builds `deck.md`.
- pitch-v6 is untracked (`?? .scratch/talk-spec/pitch-v6/`), 261M: 166M audio, 3.3M captures (87 files), three mp4s at 41M, 26M, 13M (`du -sh`). `plan.md:10-12` still says "Subject: both north stars" and `:41` has an act "VII — the reframe: governance is one enactment arm, not the point". No file outside `.scratch/drift-review-2026-08-27/` references it (`git grep -l pitch-v6`).
- pitch-v6's `build_deck.py:1-7` reads every terminal slide's text from `captures/` at build time ("Nothing is retyped"); titles, eyebrows and narration are hand-authored in `narration.json`. The captures were hand-run on 2026-08-25, not produced by the gate; `captures/verify-all-tail.txt` shows the old gate at `pass=17 fail=8 skip-live=3`.
- pitch-v6's video pipeline needs a local TTS at `http://localhost:7693` (qwen3 clone, voice `andy3`; `gen_acts.py:33-52`), puppeteer under the owner's nvm tree (`render.js:8`) and ffmpeg (`assemble.sh:18,28`). None of it runs in CI.
- `narration.json` token counts: gate 17, cage 5, niobium 0, Monte 0, continuous 0. H2-10's refuter split the 17: several are the release/publisher/adopter/honesty checks the owner never asked to rename (`G-drift-findings.md:236`). Three segments are marked visual `mockup` (`:217,399,560`); `:217` is the only slide showing a cage tier table and quotes a selfcheck fixture while the real capture has `"cages": []` (`captures/compose-verify.txt:64`; H2-10, H4-06).
- `platform/fair/fair.py` is a seeded 10,000-trial stdlib Monte Carlo (`ITERATIONS = 10_000`, `SEED = 42`, `random.betavariate`, `:11-12,44-45`) with a determinism self-check (`:190-192`). A Monte Carlo beat has a reproducible capture available today at NORTH-STAR step 2.
- No adopter workflow runs on a clock: all three `propose-tier.yml:18` say "Nothing timed, ever -- no `schedule:` anywhere". The only schedules are the hub's `truth.yml` and `twin.yml:12-13`. A continuous-refresh beat has nothing to read until ticket 10 lands.
- The niobium headline exists only as the deliberately non-firing entry "Qubit/SRF-grade niobium refining + target-fab capacity" on the signed Wardley map and market intel (`platform/wardley/intel/market-intel.json:130-136`; `platform/wardley/map/wardley-map.json:98`). It appears in no twin file and no deck. Ticket 23 owns it as the first news-feed entry; ticket 11 as a standing scenario.
- The twin's demo sequence (`twin/beat-sequence.sh`, `twin/demo_slice.py:37-56`: Royal Mail, Netflix, Intel) is built around the real-firm subjects that re-grill 39 reassigns to evals. No adopter twin exists (ticket 11 open).
- `twin/ENACT_MODE` is `development`; `tests/test_enact.py` holds 21 tests of which 12 "assert a mode the checked-in `ENACT_MODE` does not select" (ticket 03 Answer:44); `twin grade` prints `full` inside a red suite (GAPS 2.13). A twin beat narrated as "acts inside a priced cage" has no green check behind it.
- `.estate-clone/` holds platform, driftwood, tuppence, ludlow, nist, ico. The feeds and insurer repos exist but are empty (ticket 01, resolved) and `clone-estate.sh:23` `UNITS=` does not list them, so no capture of a feeds-org publish can be taken yet (ticket 21).
- NORTH-STAR §4 (`NORTH-STAR.md:40-50`) fixes seven steps and their order and says none of steps 1-5 runs end to end today; §6 (`:64`) makes the talk and video reads of the truth surface, never the definition of done, never the clock. The gate has exactly four outcomes, PASS / SKIP (exit 3, with a reason) / FAIL / EXCLUDED (`verify-all.sh:47-56`); a step with no verify script has no grade at all.
- Owner constraints on the artefact (`appendices/A-ambition-timeline.md`): "first class citizen using marp style slides" (`:57`, 2026-07-23); "It'll tour" (`:94`); "LOTS of screenshots of the real thing" (`:353`, 2026-07-31); "Push. Always" (`:483`, 2026-08-16); "don't stress the video as scope" (`:623`, 2026-08-19); "there is no real gate anymore. Just cages" (`:660`, 2026-08-22); "Make sure that we're covering all of the features that we built in the work recently. the whole lot. the talk should fit within 20 minutes" (`:669`, 2026-08-25).

Already decided, not re-asked: one subject, the eco-system operating, orgs as example consumers; estate and twin are two instances in one story, not two talks (NORTH-STAR §1; H10-01). Beat order is NORTH-STAR §4 steps 1-7. The demo is a read of the truth surface, never the definition of done, never the clock (NORTH-STAR §6; process rule 4; ticket 03). Nothing is narrated as live that the gate marks red; live tails have three outcomes and SKIP prints its reason (ticket 03). The Marp-deck/video split is retired: one artefact rebuilt from captures, a human drives it, the video is a by-product (ticket 20 Question; H10-02). The deck must cover every recently built feature, in 20 minutes, 25 acceptable (A:669; pitch-v6 `plan.md:12`). Workload enforcement is a cage, never a gate; the publisher publishes at a degraded tier instead of refusing (A:660; re-grills 11, 14; CONTEXT.md Cage); the truth surface keeps the name "the gate" (ticket 03) and pipeline checks were never asked to be renamed (H2-10 refuter). Monte Carlo and continuous refresh each get a beat (A:623 context, 2026-08-19; H4-06); continuous refresh means LLM-free scheduled gathering plus human-run skills with the PR as the unit of adoption (reversals 7, 14, 15), and which steps run on the clock is ticket 10's. The niobium headline is a committed scenario: first entry of the signed news feed (ticket 23) and a standing scenario in each adopter twin (ticket 11). Real-firm twin beats are evals, the demo runs on an adopter twin (re-grill 39; ticket 11). The twin acts inside a priced cage, propose-only is the outermost setting (re-grill 37); the twin emits a scenario, the estate selects the tier (ADR-0021). The hand-drawn Wardley slide may not be captioned as engine output (H5-15). pitch-v6 is committed: `plan.md` with a dated superseded banner, `narration.json`, `acts.json`, `captures/`, `mermaid/*.mmd` and the pipeline scripts (reversal 8; "Push. Always"; GAPS 3.28; H10-02; ticket 02 banners-not-rewrites). Superseded artefacts get dated banners, never rewrites (map Out of scope; ticket 02).

## Grilling round 1 — drafted 2026-08-28, HELD

Drafted AFK on the owner's instruction. Adversarially checked by two reviewers. Nothing below has been put to the owner. Five questions.

❓ **Q1** - **What the one artefact is, physically**: the owner asked for "marp style slides" as a first-class citizen (A:57). pitch-v6 abandoned Marp for a generated `deck.html` read from `captures/`; `talk/deck.md` is hand-authored Marp with no coverage of the three newest epics and no Marp toolchain installed anywhere. Options: (a) a generator in `talk/` (lifted from `build_deck.py`) emits `talk/deck.md` as Marp markdown from the gate's captures, marp-cli renders it to HTML/PDF/PNG in CI, diagrams are pre-rendered images (Marp does not render mermaid; pitch-v6's `mermaid/*.mmd` already exist for this); (b) the generator emits self-contained HTML with speaker notes (pitch-v6's `deck.html` shape), Marp dropped because no toolchain exists and none is in CI; (c) pitch-v6's `deck.html` moves into `talk/` as-is and `talk/deck.md` is deleted.

➡️ (a). It keeps the 2026-07-23 constraint (Marp, tours, human-driven), keeps pitch-v6's one real discipline (no capture text is retyped; prose lives in one narration file the generator reads), and Markdown is reviewable in the PR that is the unit of adoption. (b) is the same generator with a worse review surface and drops a constraint the owner never withdrew; the toolchain cost is one `npx @marp-team/marp-cli` step. (c) freezes the 2026-08-25 captures. Hand editing is not an option: `e732650` touched `deck.md` after the cage pivot and left "Deny is the bottom rung" and the hourglass in place.

❓ **Q2** - **Which run feeds the deck, and how its captures survive**: "every beat is a read of the truth surface", but the gate deletes each script's output (`verify-all.sh:44,49,59`), and `truth.yml` runs with no cluster, so every live tail in the committed run is FAIL today and SKIP once ticket 03's PRs land. pitch-v6's captures were hand-run on one evening, which is how a mockup slide and a red "live" beat got in. Options: (a) the gate writes one file per script under `talk/captures/` beside the TRUTH line; the deck is built only from the daily offline CI run, so every `[LIVE]` beat (step 4, Flux reconciles the cage) renders as could-not-look in the committed deck; (b) same capture mechanism, but the deck is built from a `--live` run the presenter executes pre-flight on a host with clusters, its TRUTH line carries `live=1`, and the committed deck records which run it was built from; (c) captures come from a separate `talk/capture.sh` the presenter runs after the gate, recording the TRUTH run id it followed.

➡️ (b). One process produces the status and the text, so "nothing narrated as live that the gate marks red" is mechanical, and the deck carries the run id that proves it; the offline CI run still rebuilds the deck daily so the could-not-look cells are visible between rehearsals, and the `live=1` build is preferred when newer. (a) makes step 4 permanently could-not-look, which empties the rule. (c) is pitch-v6 again: a second clock that can disagree with the gate. Cost to name: the log is one reused mktemp overwritten per script (`:49`), so this is a per-script file plus a change to `truth.yml`'s commit step; the size of one run's stdout has not been measured and must be before choosing between committing captures and attaching them as a workflow artifact keyed by TRUTH run (gate output carries dates and run numbers, so "commit only when changed" does not help).

❓ **Q3** - **How the deck shows a step nobody has built yet**: NORTH-STAR §4 says none of steps 1-5 runs end to end; the feeds repo is empty, no adopter twin exists, nothing in the estate runs on a clock. The owner also said "covering all of the features that we built ... the whole lot" (A:669). Options: (a) generate all seven steps from day one; a step whose script exited 3 renders as could-not-look with the gate's SKIP reason; a step with no verify script yet renders as "no check yet — owned by ticket NN", a generator-side status distinct from any gate grade, and the deck never prints a gate grade for a script that does not exist; (b) generate only beats whose checks PASS, so the deck grows as the eco-system does; (c) do not build the deck until all seven steps have green checks.

➡️ (a). It is the three-outcome rule applied to slides, it gives the owner a live progress read of the destination (the map's "run end to end once" is visible as seven cells), and it stops the demo narrowing to whatever happens to work, which is the drift H10-01 and H10-02 record. Claiming a grade the gate never emitted is the H5-15 defect, so the two statuses stay distinct. (b) hides the spine and contradicts A:669; (c) makes the deck the clock, which NORTH-STAR §6 forbids.

❓ **Q4** - **What `verify-demo.sh` refuses**: process rule 5 says every ticket wires a check into the gate. pitch-v6's rules were: no figure without a capture, red shown as red, narration carries no digits. A context-sensitive "gate" lint is not implementable: a word lint cannot tell "the honesty gate" (ticket 03's kept name) from "the admission gate", and H2-10's refuter says most of pitch-v6's 17 uses are pipeline checks never asked to be renamed. Options: (a) the deck builds; every cited capture exists in the run the deck names; each beat's status tag equals that run's grade for its script; any £ amount, percentage or count in a beat's body appears verbatim in that beat's capture (headers, dates, tags and step numbers are outside the figure check; that ceiling is named in the script); beats are in §4 order; a phrase lint refuses only "exemption", "hourglass", "admission gate" and "deny gate", and every other "gate" is a human review item; (b) (a) but the lint refuses "gate" everywhere in slides and the truth surface is named "the truth surface" on slides; (c) the deck builds and every cited capture exists, nothing else.

➡️ (a). Each clause maps to a recorded defect: mockup slides (H2-10), figures without captures (pitch-v6 adversarial review), the stale hourglass and Deny rung (reversal 1), vocabulary drift (CONTEXT.md:75-76). (b) is cleaner but renames ticket 03's settled name and ADR-0011's "release gate" from a slide lint, which is ticket 18's and the vocabulary item's decision, not this one's. (c) would have passed pitch-v6. A human-drive requirement (a dated DRIVEN line after rehearsal) is a runbook step, not a check: the gate cannot observe a rehearsal and it would sit as a permanent SKIP.

❓ **Q5** - **Where the 80M of mp4 and 166M of audio go**: committing pitch-v6's text, captures and scripts is settled; the media is not. Options: (a) `pitch-v6.mp4` becomes a GitHub release asset on the hub, the audio is not kept, the deck's by-product video is a screen recording of a human drive from now on; (b) everything via git LFS in the hub; (c) media stays local only, the commit records its sha256 and size.

➡️ (a). The mp4 is the by-product the ticket demotes, so a release asset keeps it findable without loading every clone; the audio is regenerable from `narration.json` by the pipeline and is only the input to the mp4. (b) adds LFS to the hub for one video. (c) leaves the only rendered 2026-08-25 artefact one `rm -rf` from gone and contradicts "Push. Always".

Later rounds, blocked on the above: the video by-product, screen recording of a human drive versus keeping the local TTS/puppeteer/ffmpeg pipeline (`gen_acts.py`, `localhost:7693`, nvm puppeteer path) as an optional offline step, depends on Q1 and Q5; the Monte Carlo beat's placement, at §4 step 2 reading `fair.py`'s seeded simulate output or as its own beat before step 3, decidable now with no dependency and the round's first candidate once Q2 fixes where captures live; the continuous-refresh beat's placement (step 1 or 5) and what it reads, blocked on ticket 10's clock boundary; retiring `talk/RUNBOOK.md`'s hand-kept beat table in favour of the generated deck's citations, depends on Q1 and Q4; the vocabulary scope of "gate" outside workload enforcement, whether release-integrity and adopter checks are renamed on slides, depends on Q4's lint and ticket 18; the twin beat's subject and cage, which adopter twin and how "acts inside a priced cage" is shown while `ENACT_MODE` is `development` and 12 enact tests are red, depends on tickets 11 and 25.


## Cross-ticket notes (2026-08-28)

- C2 (10, 16): Q2 has `truth.yml` commit per-script captures on a schedule; 10 Q3 rules out machine commits to main from a clock. Ticket 03's `truth.log` commit is the precedent; 10 must say whether gate evidence is inside the rule.
- C12 (16): Q2 rejects (a) because step 4 is "permanently could-not-look" in CI; 16 Q3(a) runs an ephemeral KinD inside the adopter's Actions so step 4 is observable in the scheduled run. If 16 lands, (a) is viable and (b)'s presenter-run local number is the second clock NORTH-STAR §5 forbids.
- C13 (23): "already decided" says the niobium headline is "first entry of the signed news feed (ticket 23)"; 23 Q1(c) recommends it never enters the feeds org's feed and lives only in the twin's scenario library. Not settled; do not narrate it as feed content until 23 closes.

## Answer

Resolved 2026-08-28. The owner read the held round and wrote: "ive already read the recommendations and I can't find fault with a single one. Well done. Get everything ready for me to then to-spec". Under the map's process rule Q1, Q3, Q4 and Q5 are PROVISIONAL, with that line verbatim and each recommendation's reason as the rationale. The five-per-day rule was overridden by the owner's instruction. Q2 was then put to the owner as conflict D4 with a panel verdict. The owner wrote: "I agree with you're more advanced reasoning". Q2 is recorded as DECIDED with that line verbatim and the D4 reasoning as the rationale.

1. **The artefact.** A generator in `talk/`, lifted from pitch-v6's `build_deck.py`, emits `talk/deck.md` as Marp markdown from the gate's captures. `npx @marp-team/marp-cli` renders it to HTML, PDF and PNG in CI. Diagrams are pre-rendered from pitch-v6's `mermaid/*.mmd`. No capture text is retyped. Prose lives in one narration file. Rationale: it keeps the 2026-07-23 Marp constraint and pitch-v6's one real discipline, and Markdown is reviewable in the PR. Hand editing is not an option. Provisional.

2. **The feeding run.** The gate writes one file per script under `talk/captures/` beside the TRUTH line. The deck is built only from the scheduled offline CI run. Step 4's number comes only from that run, using the ephemeral KinD from ticket 16 Q3. Until 16 Q3 lands, step 4 renders could-not-look. That is the honest state. A presenter-run `--live` is rehearsal pre-flight only, never cited. Rationale (D4): a presenter-run local number is the second clock NORTH-STAR §5 forbids. Amendment (D1): the captures are observations. The lane is caged. A repo ruleset limits the scheduled workflow identity to the observation paths, bot commits are signed, and `verify-schedules.sh` asserts no scheduled run ever changed a signed artefact. Decided.

3. **Unbuilt steps.** All seven steps are generated from day one. A script that exited 3 renders as could-not-look with the gate's SKIP reason. A step with no verify script renders as "no check yet, owned by ticket NN". That is a generator-side status, never a gate grade. Rationale: the three-outcome rule applied to slides, and no narrowing to whatever works. Provisional.

4. **What `verify-demo.sh` refuses.** The deck must build. Every cited capture must exist in the run the deck names. Each beat's status tag must equal that run's grade for its script. Any £ amount, percentage or count in a beat's body must appear verbatim in that beat's capture. Headers, dates, tags and step numbers are outside the figure check; the script names that ceiling. Beats are in §4 order. A phrase lint refuses only "exemption", "hourglass", "admission gate" and "deny gate". Every other "gate" is a human review item. Rationale: each clause maps to a recorded defect. Renaming "release gate" is ticket 18's decision. Provisional.

5. **The media.** `pitch-v6.mp4` becomes a GitHub release asset on the hub. The audio is not kept. The by-product video is a screen recording of a human drive. Rationale: findable without loading every clone; the audio is regenerable. Provisional.

Amendment (C13): the "already decided" line is corrected. Niobium never enters the feeds org's news feed. It lives in the twin's scenario library (ticket 11, 23 Q1c). The deck never narrates it as feed content.

Consequences:
- Q2 supersedes the held round's (b) and cross-ticket note C12. Note C2 closes under D1. Ticket 03's `truth.log` commit is the first instance of the observation lane.
- `talk/deck.md` (hand-authored Marp) and `talk/RUNBOOK.md`'s beat table are superseded by the generated deck. Dated banners, never rewrites.
- pitch-v6 is committed as text, captures, mermaid and scripts; `plan.md` gets a dated banner.
- The generator, the capture lane, `verify-demo.sh` and the release asset land in a new build ticket (graduated below). The ruleset and `verify-schedules.sh` are ticket 10's build. Step 4's capture waits on ticket 16 Q3.
- Revisit triggers: the size of one run's stdout is measured before captures are committed rather than attached as a workflow artifact; ticket 18 or the vocabulary item renaming "gate" on slides widens the lint; Flux #1068 landing changes what step 4 reads.

Graduated:
- The generated deck (task).
- The demo's remaining beats (grilling): video pipeline, Monte Carlo, continuous refresh, twin beat.

## Comments

- 2026-08-31 (ambition review): The verify-demo race fix (2ade57f) is unproven by any run and its on-clock premise is still false — new ticket 66 owns the repair (grade the committed deck against the run its own TRUTH line names). If run 14 reds verify-demo after 66 lands, it returns here.

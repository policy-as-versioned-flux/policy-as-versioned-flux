# 13 — Lift or retire the original mechanisms

Type: grilling (HITL)
Status: resolved
Blocked by: none

## Question

One decision each for the mechanisms that worked in the original org and did not survive the rebuild: the handbook generator, the Flux notification spine, the CIO and estate dashboards, the readiness collector, the Crossplane cloud plane, the vulnerability scanner, the five real applications, the sunset-as-proposal cron, the OSCAL CronJob over live PolicyReports. For each: lift into the eco-system (where, owned by whom), or retire with a recorded reason. Also: does the original org become a sixth party (an incumbent estate) or is it archived once lifted?

## Notes

Findings H6-04..H6-13, H9-11; GAPS 3.23..3.26.

## Facts found (2026-08-28, AFK, before any question to the owner)
- The original org policy-as-versioned-flux has 16 repos, 1 archived (`apps`), 15 live including the hub, so 14 non-hub live repos: fleet, policy, storefront, ledger, api, reports, datastore, handbook-generator, readiness-collector, governance-agent, cloud, c2p-collector, pr-gate-action, renovate-config. Last pushes: fleet 2026-08-15, api 2026-08-22, c2p-collector 2026-08-23. [gh repo list policy-as-versioned-flux --json name,isArchived,pushedAt, 2026-08-28]
- fleet's `sunset escalator` runs daily on `cron: "0 8 * * *"` and has succeeded every day through 2026-08-27T18:50Z; a `weekly governance nag` also runs on schedule. The script opens a retirement PR and never merges (no `gh pr merge`; `allow_auto_merge: false` live). The one real retirement PR, fleet#69, was authored by app/github-actions and merged by a human on 2026-08-15T17:20Z. [fleet/.github/workflows/sunset-escalator.yml:26; governance-agent/sunset-escalator.sh:1-22; gh api repos/policy-as-versioned-flux/fleet -q .allow_auto_merge; gh pr view 69 -R policy-as-versioned-flux/fleet]
- The six unit repos have no `schedule:` trigger in any of their 22 workflows; their only CronJob is the currency controller (`* * * * *`), which 404s. The hub itself runs truth.yml daily (`47 5 * * *`) and twin.yml daily (`17 5 * * *`) since ticket 03, so the schedule gap is in the units, not the eco-system as a whole. No unit manifest installs any of the nine mechanisms; the only mentions are prose: Crossplane as a keying example in platform/oscal (README.md:32, fixtures/policyreports.yaml), trivy and sunset in the feed JSON, feeds/README.md and to_fair_scenario.py:12,17 comments, `readinessProbe` in the three git-server Deployments. [.estate-clone/platform/currency-controller/manifests/cronjob.yaml:20; .github/workflows/truth.yml:9; .github/workflows/twin.yml:13; grep -ril over .estate-clone, 2026-08-28; G-drift-findings.md H6-03, H6-10]
- fleet still carries every mechanism as manifests wired to a cluster by Flux Kustomizations: infrastructure/notifications/{alert,provider,receiver}.yaml (Alert scoped to the ResourceSet-generated GitRepositories), infrastructure/c2p/cronjob.yaml + oscal-file-server.yaml, infrastructure/readiness/cronjob.yaml (`*/15 * * * *`, CANDIDATE_TAG 2.2.0, image pinned by digest), infrastructure/monitoring/{kube-prometheus-stack,policy-reporter,trivy-operator}-helmrelease.yaml plus two Grafana dashboard JSONs, infrastructure/crossplane{,-providers,-sample}/, and verify-{notifications,crossplane,monitoring,flux-dashboard}.sh. Whether a cluster is running them today is could-not-look: the manifests prove intent, the run list proves Actions ran, nothing probed a cluster (H6-14's failure mode, not repeated here). [gh api repos/policy-as-versioned-flux/fleet/git/trees/HEAD?recursive=1; fleet/infrastructure/notifications/alert.yaml:1-19; fleet/infrastructure/readiness/cronjob.yaml:1-30; fleet/clusters/cluster1/bootstrap.yaml:1-47]
- The original apps are real stacks with real dependencies: ledger (Java, pom.xml, log4j2), storefront (package.json, old Angular static behind nginx), reports (Flask app.py + requirements.txt), api (Go, go.mod). Those four each carry k8s/deployment.yaml and renovate.json. datastore is README.md, claims.yaml, kustomization.yaml only: Crossplane v2 managed resources (s3.aws.m.upbound.io, RDS) under label `mycompany.com/policy-version`, no Deployment, no renovate.json. [gh api trees for ledger, storefront, api, reports, datastore; datastore/claims.yaml:1-30]
- The units' only application workload manifest is deploy/pod.yaml (`image: nginx`, readOnlyRootFilesystem true, runAsNonRoot true) in each adopter, used by the shift-left check; the only Deployments are the three git-server fixtures; driftwood/gitops/apps reconciles a namespace and two ConfigMaps. [.estate-clone/driftwood/deploy/pod.yaml:1-10; .estate-clone/driftwood/git-server/deployment.yaml:2; .estate-clone/driftwood/gitops/apps/kustomization.yaml:1-6]
- Labels and pins differ between the two implementations: original workloads label `mycompany.com/policy-version` and fleet pins `policy-as-versioned-flux/policy` (11 tags today, v1.0.0..v2.2.1; 10 verify with gitsign on this machine, v2.0.0 is SSH-signed; H9-11's "8 of 12 bad_cert" was a Flux-side read of a superseded snapshot and is not reproduced); the units label `policy-as-versioned.dev/policy-version` and pin `policy-as-versioned-platform/platform`. Lifting an app is a re-label and a re-pin, not a copy. [datastore/claims.yaml:16; .estate-clone/driftwood/deploy/pod.yaml:7; .estate-clone/platform/distribution/versions.yaml:43; gh api repos/policy-as-versioned-flux/policy/tags; git verify-tag with gitsign, 2026-08-28; G-drift-findings.md H9-11]
- The handbook generator is three scripts: generate.sh (`<checkout> <tag> [--with-summaries]`, reads the tag's tree via git plumbing; `--with-summaries` calls `claude -p` per policy with a content-hash cache), verify-fresh.sh (committed render equals render-at-tag) and verify.sh (end to end against a real signed tag). The units' composed artefact (driftwood/composed/) is machine-readable only; no human-readable rendering of any adopter's policy exists. ADR-0007's last-mile section that frames the handbook is marked "(proposed — confirm)" inside an accepted ADR. [handbook-generator tree; handbook-generator/README.md:1-20; docs/adr/0007-agent-assisted-editorial-governance.md:31-37; G-drift-findings.md H6-08]
- The readiness collector is `kyverno apply` of the candidate tag's policies (matchConditions stripped) against a dump of live workloads, publishing per-team pass/fail counts and a `ready` boolean to a ConfigMap. It never touches admission. Its output is a count. [readiness-collector/README.md:1-30]
- The owner rejected the Grafana dashboards on 2026-07-20 in his own words ("a bullshit grafana or other made up dashboard that could be not real data") and asked for real apps not nginx, one repo per app, on 2026-07-16. The readiness question as REPORT.md:38 records it is 'a CIO dashboard that answers "how ready am I to update"', the assistant's paraphrase of the 2026-07-16 steer, not an owner verbatim (H6-06 refuter). [.scratch/drift-review-2026-08-27/REPORT.md:38; G-drift-findings.md H6-04, H6-06]
- The units' OSCAL up-flow is fully offline against three hand-written PolicyReports in platform/oscal/fixtures/policyreports.yaml and is graded by verify-upflow.sh; the original ran result2oscal every 15 minutes on a cluster from the digest-pinned c2p-collector image and served the OSCAL doc over nginx. [.estate-clone/platform/oscal/verify-upflow.sh:1-11; c2p-collector/README.md:1-12; fleet/infrastructure/c2p/cronjob.yaml]
- The units price EOL already: platform/feeds/eol/v2 ramps loss-event frequency by years past eol_date ("priced like any other EOL risk, not a bespoke sunset"); distribution/versions.yaml carries no `sunset:`; the adopters' propose-tier.yml is triggered only by a merged pin bump or workflow_dispatch ("Nothing timed, ever -- no `schedule:` anywhere") and proposes a tier label change, never a retirement. Ticket 06 found supersede has no publisher-side form (ADR-0010's `sunset:` is consumer-side, on the fleet's array entry, "never on the immutable policy release itself"; CONTEXT.md:361-364 restates it). Ticket 10 (open, blocked by 04) owns cadence and the ADR that supersedes 0010. [.estate-clone/platform/feeds/eol/v2/eol-feed.json:5; .estate-clone/platform/feeds/README.md:52-54; .estate-clone/driftwood/.github/workflows/propose-tier.yml:18-21; docs/adr/0010:5-9; issues/06:40-42; map.md ticket 02 line; G-drift-findings.md H6-13]
- The units' CVE feed is fixture-authored in trivy shape ("mirrors `trivy image --format json`"); no scanner runs. The original ran trivy-operator on the cluster against ledger and storefront images, with 5 reproducible KiND quirks recorded (OOM at 500Mi on fat jars, scan-job concurrency lock, stuck reconciler, image-mode misses npm lockfiles, kyverno apply no-ops on kind: List). [.estate-clone/platform/feeds/cve/v2/cve-feed.json:5,27,35; fleet/infrastructure/monitoring/trivy-operator-helmrelease.yaml:1-8; memory feedback_trivy_operator_kind_quirks.md]
- Crossplane: ADR-0004 (accepted) makes the cloud plane "an integral second plane of the floor (not deferred)", proof KiND-only and spec-based (provider CRDs installed, no ProviderConfig, no reconcile; Kyverno judges the CR spec at admission). CONTEXT.md still carries "Two planes" and "the cloud plane is proven at the admission level". The units hold only the harvested OSCAL string in platform/oscal; the `cloud` repo holds the harvested catalogue and RDS/S3 component definitions; datastore's claims are the only cloud-plane workload. NORTH-STAR principle 2 names workload, human, device, model action and twin as cage subjects; a cloud resource is covered by "everything is always caged" rather than named. [docs/adr/0004-cloud-plane-fork-collie.md:52-53,66-67; CONTEXT.md:351-356,374-380; NORTH-STAR.md:31; G-drift-findings.md H6-10]
- Renovate: `dependencyDashboard: false` and `enabledManagers: ["custom.regex"]` in all three adopters, so a lifted app repo needs its own renovate.json enabling the stack's manager (maven, npm, gomod, pip) plus the dashboard, not a flag flip; fleet leaves the dashboard on and it runs. GAPS 3.25 names the fix as "turn it on" (S). [.estate-clone/{driftwood,ludlow,tuppence}/renovate.json:7-10; GAPS.md 3.25]
- The truth surface stands at 43 pass / 11 fail / 0 skip of 56, 2 excluded (run 5, 2026-08-28T05:08Z). None of the nine mechanisms is graded in its lifted form; the only adjacent script is verify-upflow.sh, which grades the offline OSCAL join over fixtures. [talk/truth.log last line; talk/verify-exclusions.txt; talk/verify-all.sh:42; grep over verify*.sh]
- docs/ARCHIVE.md carries the 2026-08-28 banner: the hub is not archived and not research-only; it is the eco-system's own repository; lift-or-retire of the working parts is decided by this ticket. The July checklist step "archive the hub on GitHub" is recorded as not to be executed. [docs/ARCHIVE.md:3-9]
- Twin code mentions none of the nine mechanisms; the only hits are the `euv-readiness` scenario id (fixtures.py, README.md:2051-2053, beat-intel.sh:31,123, capabilities/scenario-engine.yaml:81-84) and an FCA notification URL. [grep over twin/, 2026-08-28]
- CONTEXT.md already counts six parties (platform, nist, ico, driftwood, tuppence, ludlow) and ticket 01 added the feeds and insurer orgs; an incumbent original org would be a ninth org and a fourth adopter, not a "sixth party". [CONTEXT.md:20-24; map.md ticket 01 line]

Already decided, not re-asked: the five apps are lifted, real apps not nginx, one repo per app (owner 2026-07-16, REPORT.md:38, H6-04; GAPS 3.23); only placement is open (map: "placement is fog"). Sunset lift-in-principle: a machine opens a timed retirement PR and a human merges (ADR-0010; fleet#69 is the evidence); schedules run LLM-free steps (NORTH-STAR principle 5; reversals 7, 14, 15); ticket 10 owns cadence, the rejection half-life and the ADR superseding 0010 (map ticket 02 line). A publisher-side supersede exists in the eco-system (re-grill 2, copy ESLint's supersede; ticket 06). OSCAL: collection runs as a CronJob / Flux Kustomization (ADR-0009 consequences); each org self-verifies and the hub aggregates (re-grill 4); cage and procurement record emit the risk object on one schema (re-grill 3); placement and cadence stay open (later). The readiness question is answered by price, not a count: the adopter gate on a Renovate pin bump grades by priced impact and the incoming version lands in a cage (re-grill 14; map vocabulary rule; re-grills 20, 27, 32, 35; ticket 15 builds it), so the collector's counts are not lifted as counts. Grafana dashboards are retired: the owner rejected them on 2026-07-20 (REPORT.md:38) and NORTH-STAR §5 makes the truth surface the only citable read; no dashboard of any kind is re-asked. A cloud resource is a cage subject: ADR-0004 accepted, "integral second plane (not deferred)"; CONTEXT.md two planes; principle 2 everything is always caged; reversal 21 every non-machine-enforceable lever is a priced cage; only where it lands and when it is built are open. The original org is not archived as a whole and the hub is the eco-system's repository (NORTH-STAR §6, §7; ARCHIVE.md banner; ticket 02). Flux is the distribution arm, held integral unless disproven (principle 7); ticket 16 owns the Flux measurement and the fan-out reaching a cluster (re-grills 1, 18). The reviewed PR is the unit of adoption; author and merger differ (re-grill 29); Actions signs proposals (reversal 16); any lifted cron inherits these. Feeds: one envelope, signed by the tag, `publishes[]` discovery with `revoked[]`, revocation by new version (ticket 04, ADR-0019); a CVE or EOL feed the scanner or sunset consumes uses this contract. Renovate dependency dashboard: switch it on (GAPS 3.25), folded into whichever ticket first touches renovate.json. No exemptions, no refusal, price and cage (principles 1, 2; reversal 5): a lifted app that cannot meet a rule is caged and priced, never carved out.

## Grilling round 1 — drafted 2026-08-28, HELD

Drafted AFK on the owner's instruction. Adversarially checked by two reviewers. Nothing below has been put to the owner. Five questions.

❓ **Q1** - **Where the five real applications land**: lifting is decided; placement is not. Lifting an app repo is a re-label (`mycompany.com/policy-version` to `policy-as-versioned.dev/policy-version`), a re-pin (fleet/policy to the adopter's composed artefact) and a new renovate.json enabling the stack's manager plus the dashboard. Options: (a) one app repo per adopter by institutional shape: ledger to tuppence (bank), storefront to driftwood (retail), reports to ludlow (health); api and datastore placed in a later round; (b) all five into driftwood as one adopter's app repos, tuppence and ludlow keep the nginx fixture.

➡️ (a). NORTH-STAR §4 step 3 (the £ crosses a band, the tier moves, the workload keeps running caged tighter) is only honest with a real dependency a feed can move, and each adopter's twin (ticket 11) and ticket 09's ladder need a real pod per institution. Cost is three re-labels, three re-pins, three renovate.json files. (b) leaves two adopters demonstrating nothing. api has no natural home and datastore is the cloud-plane workload (Q3), so both wait.

❓ **Q2** - **The 14 non-hub original repos after lift**: fleet is the only repo whose manifests wire the version fan-out to a cluster (ResourceSet plus bootstrap Kustomizations) and the daily sunset cron ran green on 2026-08-27; it is the only place the thesis is wired end to end (H9-11). NORTH-STAR §6 keeps the hub live as the reference implementation and audit trail. Options: (a) archive each original repo on GitHub as its lift or retirement is recorded and graded green by the truth surface, fleet last, after ticket 16's fan-out reconciles in an adopter; the hub stays live; (b) keep the original org live as a fourth adopter (a ninth org, the ticket's "sixth party"), pinning the platform, caged as-is under its legacy labels, wrapped by the COTS shim; (c) leave all 14 live and undecided, as today.

➡️ (a). Two live implementations is the finding (H9-11): any demo of versions installing on a cluster is today a demo of the superseded one. Three adopters already demonstrate consumption; an incumbent with a different label family is a port dressed as a lift and a priced hole no party owns. (a) is staged so nothing is archived before its replacement is graded, which is the map's definition of done. (b) is the COTS shim (GAPS 3.14: decided four times, built zero times) and belongs to that gap if it is ever built.

❓ **Q3** - **Where the cloud plane lands and when it is built**: the subject is settled (ADR-0004, CONTEXT.md two planes, principle 2). What is open is placement and sequence. The original's admission-level proof is small: provider CRDs in KiND, two policies, one claim; the `cloud` repo holds the catalogue and RDS/S3 component definitions. Options: (a) datastore's claims become a workload in one adopter (tuppence, alongside ledger), the RDS/S3 policies become versioned members of the platform's published implementations (ADR-0017), the cage ladder's dials for a Crossplane CR are enumerated under ticket 09, and the truth surface grades it at admission in KiND, built after the Pod slice of §4 runs end to end once (sequencing, not deferral); (b) the same, built in the same pass as Q1's Pod lifts; (c) the cloud repo and datastore are lifted into the platform org as data and fixtures only, no adopter runs a claim, ADR-0004 gains a dated note that the runtime proof is not carried into the eco-system.

➡️ (a). The map's build order is thinnest slice end to end first, then widen; no §4 step needs a non-Pod resource and ticket 09's ladder is still being cut for Pods. (a) keeps ADR-0004 accepted with a dated sequencing note and costs nothing until Q1 has placed ledger. (b) doubles ticket 09's first cut. (c) contradicts "integral second plane (not deferred)" with no owner reason.

❓ **Q4** - **The handbook**: the composed artefact is each adopter's policy and no human can read it; ADR-0007's last-mile section (proposed, never confirmed) frames the handbook as the mechanical half. The generator already renders from a tag's tree. A scheduled job that commits a render to main is a machine write outside the reviewed PR (re-grill 29), so "on a schedule, signed with the artefact" cannot both hold. Options: (a) retire with reason: the TRUTH line, the Renovate PR's priced impact and the proposal PR are the reads; the last mile stays the open problem CONTEXT.md names; (b) lift as a compose-time render: a platform-published tool each adopter's compose step runs over its own composed artefact, output lands in the same PR and under the same gitsign tag as the artefact, verify-fresh.sh becomes the truth-surface script (render at tag equals committed render, gradable offline), verify.sh retired with it; the `claude -p` summaries become a Claude Code skill a human runs, whose output lands by PR (reversals 7, 14, 15).

➡️ (b). It cannot lie because it renders from the signed tag, which is the owner's Grafana objection in reverse, it is one script plus one check, it needs no schedule, and it confirms ADR-0007's proposed section with a real mechanism. (a) leaves the adopter's policy unreadable by the people the cage is priced for.

❓ **Q5** - **Does the consumer-side `sunset:` survive beside the publisher supersede**: re-grill 2 and ticket 06 settle that a publisher-side supersede exists; the EOL feed already prices past eol_date; ticket 10 must put the adopter's proposer on a clock anyway. ADR-0010 places `sunset:` on the fleet's own array entry, never on the release, with the reason that retirement is the adopting fleet's decision. Options: (a) publisher-side only: the platform declares a version superseded with an EOL date as a dated entry in ADR-0019's existing `revoked[]` (soft revocation, no new field on `publishes[]`), the EOL feed carries it, the adopter's proposer (propose-tier.yml, once ticket 10 schedules it) reads pins against EOL and opens the retirement PR; an adopter drops a version it no longer wants by an ordinary PR with no cron; ADR-0010's placement clause is superseded by the ADR ticket 10 writes; (b) both: publisher supersede as the signal, adopter `sunset:` kept as an earlier local override with the fleet cron ported to each adopter.

➡️ (a). It adds no mechanism beyond what ticket 10 must build, gives the eco-system the publisher-side supersede ESLint has and the units lack, and lets a regulator or publisher retire its own version across every adopter that pins it, which is the cross-org shape NORTH-STAR §2 describes. The adopter keeps its ADR-0010 sovereignty by the ordinary PR. (b) is (a) plus a second declaration path and three ported crons for a case no adopter has. Answering (a) is a recorded supersession of ADR-0010:5-9; it does not amend ticket 04's `publishes[]` shape.

Later rounds, blocked on the above: round 2 of this ticket decides the vulnerability scanner (adopter-side trivy-operator emitting a signed image inventory priced against the feeds org's CVE feed is the only shape consistent with re-grill 4; the five KiND quirks apply; needs Q1's real images and ticket 21's envelope), the Flux notification spine (Alert/Provider/Receiver as the observable that grades §4 step 4 inside ticket 16's measurement design, or drop the "six jobs" claim from spec.md and research/08) and the OSCAL CronJob placement and cadence on the adopter cluster (re-grill 4; whether the served assessment-results document is itself a signed feed; tickets 16 and 10); ticket 13 does not close before round 2. Also: api and datastore placement (Q1, Q3); archive sequencing per repo, each after its lift lands green on the truth surface (Q2); whether the readiness price (re-grill 14) runs on a clock ahead of any Renovate PR and where its signed output lands (ticket 10); the handbook rendering each workload's cage tier and price once ticket 09 renders the tier into the composed artefact (Q4, ticket 09).


## Cross-ticket notes (2026-08-28)

- C1 (09): Q3(a) says the Crossplane CR's dials "are enumerated under ticket 09"; 09's held round is pod-only and asks nothing about a cloud resource. Either 09 gains a later-round item or Q3(a) carries the work itself.
- C15 (10, 12): 10 Q2 and 12's later round both assign the currency-controller CronJob (404 every minute) to this ticket; the Question here lists nine mechanisms and not that one. Add it or send it back.
- C16 (10, 19, closed 04): Q5(a) reuses ADR-0019 `revoked[]` for a dated soft supersede. Ticket 04 A5 fixed `revoked[]` as withdrawal, where a pin to a revoked version is a priced hole now, not at an EOL date; 19 Q2(a) separately prices "a newer version not yet pinned" as a hole growing over time. Two formulas for the same state. And Q5(a) has the adopter's scheduled proposer open retirement PRs, a proposal type 10 Q3 (what the clock proposes) and 10 Q5 (dedupe and half-life) do not cover.

## Answer

Resolved 2026-08-28. The owner read the held round and wrote: "ive already read the recommendations and I can't find fault with a single one. Well done. Get everything ready for me to then to-spec". Under the map's process rule a bare agree does not ratify. Q1 to Q4 are recorded as PROVISIONAL with that line and each recommendation's own reason as the rationale. Q5 was put to the owner as decision D5 with a three-lens panel verdict. The owner wrote: "I agree with you're more advanced reasoning". Q5 is recorded as DECIDED with that line and the D5 reasoning. The five-per-day rule was overridden by the owner's instruction.

1. The three apps with an institutional home move now. ledger goes to tuppence, storefront to driftwood, reports to ludlow. A lift is a re-label to `policy-as-versioned.dev/policy-version`, a re-pin to the adopter's composed artefact, and a renovate.json that enables the stack's manager and the dependency dashboard. api and datastore wait. Reason: §4 step 3 needs a real dependency a feed can move. Provisional.

2. Each original repo is archived on GitHub once its lift or retirement is recorded and graded green by the truth surface. fleet goes last, after ticket 16's fan-out reconciles in an adopter. The hub stays live. The currency-controller CronJob joins the mechanism list (C15) and is retired under this rule now: it 404s and ticket 07's fx feed replaces it. Reason: two live implementations is the finding (H9-11). A fourth adopter is the COTS shim (GAPS 3.14). Provisional.

3. The cloud plane lands in tuppence beside ledger. datastore's claims are the workload. The RDS and S3 policies become versioned members of the platform's published implementations (ADR-0017). The truth surface grades them at admission in KinD. It is built after the Pod slice of §4 runs once. ADR-0004 stays accepted with a dated sequencing note. Amendment (C1): ticket 09's held round is pod-only, so the dials for a Crossplane CR are a later-round item on ticket 09, filed by this ticket. Reason: thinnest slice end to end first, then widen; no §4 step needs a non-Pod resource. Provisional.

4. The handbook is lifted as a compose-time render. A platform-published tool runs in each adopter's compose step over its composed artefact. The render lands in the same PR and under the same gitsign tag as the artefact. verify-fresh.sh becomes the truth-surface script. verify.sh is retired. The `claude -p` summaries become a Claude Code skill a human runs, landing by PR. Reason: a render from the signed tag cannot lie. Provisional.

5. Supersede is publisher-side only, in D5's form. The platform publishes the newer version. A pin behind a newer published version is priced by the existing EOL ramp (to_fair_scenario.py eol_ramp) from the newer version's publish date. `revoked[]` stays withdrawal only, priced now (ticket 04 A5, ADR-0019). The dated `revoked[]` entry is dropped. The adopter's scheduled proposer reads pins against the feed and opens a retirement PR (D1: a proposal, never a declaration). Retirement PRs use ticket 10's dedupe ledger, keyed `<org>/<kind>/<slug>` (D5). Consumer-side `sunset:` is not carried. An adopter drops a version by an ordinary PR. Decided.

Consequences:
- ADR-0010:5-9 (consumer-side `sunset:`) is superseded by the ADR ticket 10 writes. ADR-0004 gains a dated sequencing note. ADR-0007's last-mile section is confirmed by item 4.
- Ticket 10 Q3 and Q5 gain the retirement PR as a proposal type under D1 and the dedupe ledger.
- The map line "placement is fog" closes for three apps and the cloud plane.
- Tickets 21 and 25 gain nothing; the EOL ramp exists.
- New build tickets carry items 1, 3 and 4. Round 2 of this ticket carries the scanner, the Flux notification spine, the OSCAL CronJob, api and datastore placement, and per-repo archive sequencing. Ticket 13 does not close before round 2.
- Revisit triggers: the EOL ramp misprices a supersede (then the panel dissent, an explicit `supersedes: {version, eol_date}` on the newer release); a lifted app cannot run caged under the ladder (then it is priced, never carved out); Flux #1068 lands (D3 changes the verification shape item 2 waits on).

Graduated:
- Lift ledger, storefront and reports into their adopters
- Handbook as a compose-time render
- Ticket 13 round 2: scanner, notification spine, OSCAL CronJob, api and datastore

## Comments

**2026-09-02, review.** Item 2, the currency controller, was retired because 'ticket 07's fx feed replaces it'. The controller re-cages a running pod after its admitted version is retired; the fx feed is money. A homonym, not a reason. The module still ships at platform 46cd775, its CronJob is the eco-system's only one, and the gate grades it every run. Ticket 75 Q13 puts the re-decision to the owner; ticket 80 item 6 executes whichever way. Also: the three ADR notes this ticket assigned itself (0004, 0007, 0010) are unwritten; 80 item 7. Record: REVIEW-2026-09-02.md R10, legacy/L5, scope/F10.

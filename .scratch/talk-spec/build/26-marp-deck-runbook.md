# 26 — Marp deck + demo runbook

**What to build:** The first-class Marp deck authored *against the built estate* (every demo-live claim real) + an idempotent, offline-safe, resettable, audience-modular demo runbook.

**Blocked by:** 09, 13, 17, 19, 22, 24

**Status:** ready-for-agent

- [ ] Marp deck: breach-cost open → versioned dependency → proportionality (live) → living loop (live) → provenance (live) → balance-sheet close
- [ ] Runbook brings the estate up idempotently, offline-safe, resettable; audience-modular (re-foreground any institution)
- [ ] Every demo-live claim backed by a passing `verify-*.sh`
